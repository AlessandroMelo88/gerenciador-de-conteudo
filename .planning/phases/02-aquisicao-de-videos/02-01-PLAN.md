---
phase: 02-aquisicao-de-videos
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - clip-processor/pytest.ini
  - clip-processor/tests/__init__.py
  - clip-processor/tests/conftest.py
  - clip-processor/tests/test_rss_poller.py
  - clip-processor/tests/test_downloader.py
  - clip-processor/tests/test_dedup.py
  - clip-processor/tests/test_db.py
autonomous: true
requirements:
  - ACQU-01
  - ACQU-02
  - ACQU-03
  - ORC-02

must_haves:
  truths:
    - "pytest clip-processor/tests/ -x -q finaliza em menos de 10 segundos"
    - "Todos os testes unitários definidos existem e estão vermelhos (failing) antes da implementação"
    - "conftest.py fornece fixtures reutilizáveis: mock_redis, mock_db_conn, sample_rss_feed"
    - "Cada requirement (ACQU-01, ACQU-02, ACQU-03, ORC-02) tem pelo menos um teste mapeado"
  artifacts:
    - path: "clip-processor/pytest.ini"
      provides: "Configuração pytest com testpaths e marcadores"
    - path: "clip-processor/tests/conftest.py"
      provides: "Fixtures: mock_redis, mock_db_conn, sample_rss_xml"
    - path: "clip-processor/tests/test_rss_poller.py"
      provides: "Testes ACQU-01: detecção de vídeos novos via RSS"
    - path: "clip-processor/tests/test_downloader.py"
      provides: "Testes ACQU-02: download 720p, disk guard, partial cleanup"
    - path: "clip-processor/tests/test_dedup.py"
      provides: "Testes ACQU-03: Redis hit, Redis fallback para MySQL"
    - path: "clip-processor/tests/test_db.py"
      provides: "Testes ORC-02: update_status, recover_stuck_downloads"
  key_links:
    - from: "clip-processor/tests/conftest.py"
      to: "todos os test_*.py"
      via: "pytest fixtures"
      pattern: "@pytest.fixture"
---

<objective>
Criar a infraestrutura de testes pytest (Wave 0) com scaffolds de testes failing para toda a Phase 2.

Purpose: Nyquist compliance — nenhuma tarefa de implementação pode ser verificada sem testes automatizados existindo primeiro. Os testes definem o contrato de comportamento antes do código.

Output: pytest.ini, tests/__init__.py, conftest.py com fixtures, e 4 arquivos test_*.py com testes failing (RED state) cobrindo ACQU-01, ACQU-02, ACQU-03 e ORC-02.
</objective>

<execution_context>
@/Users/alessandrobm1/.claude/get-shit-done/workflows/execute-plan.md
@/Users/alessandrobm1/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-aquisicao-de-videos/02-VALIDATION.md
@.planning/phases/02-aquisicao-de-videos/02-RESEARCH.md

<interfaces>
<!-- Schema MySQL existente (clip-processor/src/ ainda é stub) -->
<!-- source_videos.status ENUM: 'pending','downloading','downloaded','transcribing','selecting','cutting','publishing','published','failed' -->
<!-- source_channels colunas: id, youtube_channel_id, channel_name, rss_url, active -->
<!-- source_videos colunas: id, youtube_video_id, channel_id, title, published_at, status, local_path -->

<!-- Módulos a implementar nos Planos 02 e 03 (os testes importam esses módulos) -->
<!-- src/db.py exports: get_db_connection(), update_status(conn, video_id, status, local_path=None), insert_video(conn, video_id, channel_id, title, published_at), recover_stuck_downloads(conn) -->
<!-- src/rss_poller.py exports: poll_all_channels(db_conn, redis_client) -->
<!-- src/downloader.py exports: download_video(video_id, output_path) -> bool -->
<!-- src/dedup.py exports: is_seen(video_id, redis_client, db_conn) -> bool, mark_failed_redis(video_id, redis_client) -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: pytest.ini e pacote de testes</name>
  <files>clip-processor/pytest.ini, clip-processor/tests/__init__.py</files>
  <action>
Criar `clip-processor/pytest.ini` com conteúdo:

```ini
[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts = -v --tb=short
```

Criar `clip-processor/tests/__init__.py` vazio (apenas torna o diretório um pacote Python).

Instalar pytest e pytest-mock dentro do ambiente Docker NÃO é necessário agora — eles serão adicionados ao requirements.txt no Plano 02. Para verificar localmente, usar:
```bash
cd /Users/alessandrobm1/develop/server/wordpress/canaldecortes/clip-processor && pip install pytest pytest-mock --quiet 2>/dev/null || true
```

Os testes rodarão dentro do container após o Plano 02 atualizar o requirements.txt e rebuildar a imagem.
  </action>
  <verify>
    <automated>ls /Users/alessandrobm1/develop/server/wordpress/canaldecortes/clip-processor/pytest.ini && ls /Users/alessandrobm1/develop/server/wordpress/canaldecortes/clip-processor/tests/__init__.py && echo "PASS: arquivos criados"</automated>
  </verify>
  <done>pytest.ini existe com testpaths=tests; tests/__init__.py existe (pode estar vazio)</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: conftest.py com fixtures compartilhadas</name>
  <files>clip-processor/tests/conftest.py</files>
  <behavior>
    - fixture `mock_redis`: retorna um objeto MagicMock com `.set()` retornando True (NX success) por padrão, e `.get()` retornando None
    - fixture `mock_db_conn`: retorna MagicMock simulando conexão pymysql com `.cursor()` como context manager retornando cursor com `.fetchone()` → None e `.execute()` → None
    - fixture `sample_rss_xml`: retorna string XML válida de feed RSS do YouTube com 2 entradas, cada uma com `<yt:videoId>`, `<title>` e `<published>` preenchidos
    - fixture `sample_video_id`: retorna string `'dQw4w9WgXcQ'` (ID de vídeo YouTube válido 11 chars)
  </behavior>
  <action>
Criar `clip-processor/tests/conftest.py` com as 4 fixtures pytest usando pytest-mock e unittest.mock.

O XML do `sample_rss_feed` deve ser um feed RSS Atom completo do YouTube com namespace `xmlns:yt="http://www.youtube.com/xml/schemas/2015"`. Incluir 2 entradas com videoIds `'abc123def456'` e `'xyz789uvw012'`.

Exemplo de estrutura XML:
```xml
<?xml version="1.0" encoding="UTF-8"?>
<feed xmlns:yt="http://www.youtube.com/xml/schemas/2015" xmlns="http://www.w3.org/2005/Atom">
  <entry>
    <yt:videoId>abc123def456</yt:videoId>
    <title>Gol incrível do Vini Jr</title>
    <published>2026-06-18T10:00:00+00:00</published>
    <link rel="alternate" href="https://www.youtube.com/watch?v=abc123def456"/>
  </entry>
  <entry>
    <yt:videoId>xyz789uvw012</yt:videoId>
    <title>Pênalti polêmico na Champions</title>
    <published>2026-06-17T15:30:00+00:00</published>
    <link rel="alternate" href="https://www.youtube.com/watch?v=xyz789uvw012"/>
  </entry>
</feed>
```

`mock_db_conn` deve suportar uso como `with conn.cursor() as cur:` — configurar `mock_db_conn.cursor.return_value.__enter__.return_value` como o cursor mockado.
  </action>
  <verify>
    <automated>cd /Users/alessandrobm1/develop/server/wordpress/canaldecortes/clip-processor && python -c "import tests.conftest; print('PASS: conftest importável')" 2>/dev/null || echo "INFO: importação direta não funciona para conftest — verificar via pytest na Task 3"</automated>
  </verify>
  <done>conftest.py existe com 4 fixtures: mock_redis, mock_db_conn, sample_rss_xml, sample_video_id</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Scaffolds de testes failing (RED state)</name>
  <files>
    clip-processor/tests/test_rss_poller.py,
    clip-processor/tests/test_downloader.py,
    clip-processor/tests/test_dedup.py,
    clip-processor/tests/test_db.py
  </files>
  <behavior>
    test_rss_poller.py (ACQU-01):
    - test_poll_detects_new_videos: dado feed RSS com 2 entradas nunca vistas, poll_all_channels() insere 2 registros no DB com status 'pending'
    - test_poll_skips_seen_videos: dado vídeo já no Redis (is_seen retorna True), poll não chama insert_video para esse vídeo
    - test_poll_handles_empty_feed: feed sem entradas não levanta exceção

    test_downloader.py (ACQU-02):
    - test_download_success: yt-dlp mockado sem erro → download_video() retorna True
    - test_disk_space_guard: shutil.disk_usage mockado com free=1GB → download_video() retorna False sem chamar yt-dlp
    - test_partial_cleanup: yt-dlp levanta DownloadError → arquivo .part é deletado via os.remove
    - test_permanent_error_no_retry: DownloadError com mensagem 'private' → retorna False após 1 tentativa (não 3)
    - test_retry_transient_error: DownloadError sem keyword permanente → tenta 3 vezes

    test_dedup.py (ACQU-03):
    - test_redis_hit: redis.set() retorna None (chave existia) → is_seen() retorna True
    - test_redis_miss_mysql_miss: redis.set() retorna True, MySQL fetchone() retorna None → is_seen() retorna False
    - test_redis_fallback: redis levanta RedisError → is_seen() consulta MySQL como fallback
    - test_mark_failed_redis: mark_failed_redis() chama redis.delete() com chave correta

    test_db.py (ORC-02):
    - test_status_update: update_status() executa SQL UPDATE com status correto
    - test_status_update_with_path: update_status() com local_path inclui local_path no SQL
    - test_recover_stuck_downloads: recover_stuck_downloads() executa UPDATE SET status='pending' WHERE status='downloading'
    - test_insert_video: insert_video() executa INSERT com todos os campos corretos

  Todos os testes devem usar `from src.rss_poller import poll_all_channels` etc. — importações que FALHAM agora (módulos não existem) = RED state correto.

  Usar `pytest.raises(ImportError)` NÃO — os testes devem falhar com ImportError na coleta, o que é o comportamento RED esperado. Ou alternativamente: estruturar os imports dentro dos testes (não no nível do módulo) para que o coletor não falhe — e usar `mocker.patch` para mockar os módulos ausentes.

  ABORDAGEM CORRETA: Usar `unittest.mock.patch` com `create=True` ou importar com try/except e marcar como xfail se o módulo não existir. Mais simples: criar os testes de forma que o importError em coleção seja esperado — pytest reporta como ERROR não FAILED, o que ainda indica RED.

  MELHOR ABORDAGEM: Criar os arquivos de teste com imports no topo SEM try/except. Quando o plano 02 criar os módulos, os imports passarão a funcionar. Isso é o RED correto — os testes existem mas falham na coleta por ImportError.
  </behavior>
  <action>
Criar os 4 arquivos de teste com imports no topo referenciando os módulos `src.rss_poller`, `src.downloader`, `src.dedup`, `src.db` que ainda não existem.

Cada arquivo deve ter:
1. Imports no topo (causam ImportError = RED state)
2. Funções de teste com nomes exatos listados no bloco behavior
3. Asserções específicas e verificáveis
4. Uso de fixtures do conftest.py

Para mocks de yt-dlp em test_downloader.py, usar `mocker.patch('yt_dlp.YoutubeDL')` como context manager.
Para mocks de shutil em test_downloader.py, usar `mocker.patch('src.downloader.shutil.disk_usage')`.
Para mocks de redis em test_dedup.py, usar a fixture `mock_redis`.
Para mocks de pymysql em test_db.py, usar a fixture `mock_db_conn`.

Verificar RED state: `pytest clip-processor/tests/ -x -q 2>&1 | head -20` deve mostrar erros de import ou falhas de asserção — NÃO deve mostrar todos passando.
  </action>
  <verify>
    <automated>cd /Users/alessandrobm1/develop/server/wordpress/canaldecortes && python -m pytest clip-processor/tests/ --collect-only -q 2>&1 | tail -10</automated>
  </verify>
  <done>
    4 arquivos test_*.py existem em clip-processor/tests/.
    pytest --collect-only mostra os testes coletados (ou erros de coleta por ImportError — ambos são RED state válido).
    Nenhum teste está PASSING (verde) — implementação vem nos Planos 02-04.
  </done>
</task>

</tasks>

<verification>
```bash
# Verificar todos os arquivos criados
ls /Users/alessandrobm1/develop/server/wordpress/canaldecortes/clip-processor/tests/
# Esperado: __init__.py conftest.py test_rss_poller.py test_downloader.py test_dedup.py test_db.py

# Verificar pytest.ini
cat /Users/alessandrobm1/develop/server/wordpress/canaldecortes/clip-processor/pytest.ini

# Verificar estado RED (testes existem mas falham por importação)
cd /Users/alessandrobm1/develop/server/wordpress/canaldecortes && python -m pytest clip-processor/tests/ --collect-only -q 2>&1 | head -30
```
</verification>

<success_criteria>
- pytest.ini existe com testpaths=tests
- tests/__init__.py existe
- conftest.py tem 4 fixtures funcionais
- 4 arquivos test_*.py existem com todos os testes definidos no behavior
- pytest --collect-only mostra os testes (podem estar em ERROR por ImportError — é o RED state correto)
- Nenhum teste está verde antes da implementação
</success_criteria>

<output>
Após conclusão, criar `.planning/phases/02-aquisicao-de-videos/02-01-SUMMARY.md`
</output>
