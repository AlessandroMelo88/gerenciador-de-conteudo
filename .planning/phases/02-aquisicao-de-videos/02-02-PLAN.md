---
phase: 02-aquisicao-de-videos
plan: "02"
type: execute
wave: 1
depends_on: []
files_modified:
  - docker-compose.yml
  - canaldecortes/clip-processor/requirements.txt
  - canaldecortes/mysql/init/02-seed-channels.sql
  - canaldecortes/clip-processor/src/db.py
autonomous: true
requirements:
  - ORC-02
  - ACQU-01

must_haves:
  truths:
    - "clip-processor tem volume ./canaldecortes/videos:/app/videos montado no docker-compose"
    - "REDIS_HOST e REDIS_PORT estão no bloco environment do clip-processor no docker-compose"
    - "requirements.txt inclui apscheduler, feedparser, redis, pytest, pytest-mock"
    - "02-seed-channels.sql insere canais de futebol reais com INSERT IGNORE (idempotente)"
    - "db.py exporta get_db_connection(), update_status(), insert_video(), recover_stuck_downloads()"
    - "pytest test_db.py passa verde após implementação de db.py"
  artifacts:
    - path: "docker-compose.yml"
      provides: "Volume mount e variáveis Redis para clip-processor"
      contains: "./canaldecortes/videos:/app/videos"
    - path: "canaldecortes/clip-processor/requirements.txt"
      provides: "Todas as dependências da Phase 2"
      contains: "apscheduler"
    - path: "canaldecortes/mysql/init/02-seed-channels.sql"
      provides: "Canais de futebol iniciais para monitoramento"
    - path: "canaldecortes/clip-processor/src/db.py"
      provides: "Funções de acesso ao MySQL para o daemon"
      exports:
        - get_db_connection
        - update_status
        - insert_video
        - recover_stuck_downloads
  key_links:
    - from: "docker-compose.yml"
      to: "./canaldecortes/videos"
      via: "volume mount"
      pattern: "canaldecortes/videos:/app/videos"
    - from: "canaldecortes/clip-processor/src/db.py"
      to: "MySQL clips_automation"
      via: "pymysql com env vars MYSQL_HOST/DATABASE/USER/PASSWORD"
      pattern: "pymysql.connect"
---

<objective>
Configurar a infraestrutura de suporte do daemon: docker-compose com volume e Redis, dependências Python, canais-seed e módulo de acesso ao banco de dados.

Purpose: Plano 01 criou os testes; este plano cria o contexto de execução (volumes Docker, dependências) e o primeiro módulo real (db.py) que faz os testes test_db.py passarem.

Output: docker-compose.yml atualizado, requirements.txt completo, seed SQL com canais reais, db.py implementado e testado.
</objective>

<execution_context>
@/Users/alessandrobm1/.claude/get-shit-done/workflows/execute-plan.md
@/Users/alessandrobm1/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/02-aquisicao-de-videos/02-CONTEXT.md
@.planning/phases/02-aquisicao-de-videos/02-RESEARCH.md

<interfaces>
<!-- docker-compose.yml atual (seção clip-processor) -->
<!--
  clip-processor:
    build: ./canaldecortes/clip-processor
    container_name: clip-processor
    restart: unless-stopped
    environment:
      - ANTHROPIC_API_KEY=${ANTHROPIC_API_KEY}
      - GROQ_API_KEY=${GROQ_API_KEY}
      - MYSQL_HOST=mysql
      - MYSQL_DATABASE=clips_automation
      - MYSQL_USER=clips_user
      - MYSQL_PASSWORD=${CLIPS_DB_PASSWORD}
    volumes:
      - ./canaldecortes/youtube/token.json:/app/token.json:ro
      - /tmp/clips:/tmp/clips   ← SUBSTITUIR por ./canaldecortes/videos:/app/videos
    networks:
      - internal
    depends_on:
      - mysql
-->

<!-- MySQL schema source_videos -->
<!--
  id INT AUTO_INCREMENT PRIMARY KEY
  youtube_video_id VARCHAR(64) NOT NULL UNIQUE
  channel_id INT NOT NULL (FK source_channels.id)
  title VARCHAR(500)
  published_at TIMESTAMP
  status ENUM('pending','downloading','downloaded','transcribing','selecting','cutting','publishing','published','failed') DEFAULT 'pending'
  local_path VARCHAR(1024)
  updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP
-->

<!-- Redis já existe no docker-compose como serviço 'redis' na rede 'internal' -->
<!-- Adicionar apenas as env vars REDIS_HOST=redis e REDIS_PORT=6379 ao clip-processor -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: docker-compose e requirements.txt</name>
  <files>
    /Users/alessandrobm1/develop/server/wordpress/docker-compose.yml,
    /Users/alessandrobm1/develop/server/wordpress/canaldecortes/clip-processor/requirements.txt
  </files>
  <action>
**docker-compose.yml** — Localizar o bloco `clip-processor:` e fazer 3 mudanças:

1. Substituir o volume `/tmp/clips:/tmp/clips` por `./canaldecortes/videos:/app/videos`
2. Adicionar ao bloco `environment:` as linhas:
   ```yaml
   - REDIS_HOST=redis
   - REDIS_PORT=6379
   ```
3. Adicionar `redis` à lista `depends_on:` do clip-processor

Também criar o diretório físico: `mkdir -p /Users/alessandrobm1/develop/server/wordpress/canaldecortes/videos` (necessário para o volume mount funcionar).

**requirements.txt** — Adicionar ao arquivo existente (que já tem yt-dlp, google-api-python-client, etc.):
```
apscheduler
feedparser
redis
pytest
pytest-mock
```

Usar exatamente esses nomes de pacote (sem pinning de versão — usar latest estável).

ATENÇÃO: Não remover nenhuma dependência existente do requirements.txt.
  </action>
  <verify>
    <automated>grep -n "videos:/app/videos" /Users/alessandrobm1/develop/server/wordpress/docker-compose.yml && grep -n "REDIS_HOST" /Users/alessandrobm1/develop/server/wordpress/docker-compose.yml && grep "apscheduler" /Users/alessandrobm1/develop/server/wordpress/canaldecortes/clip-processor/requirements.txt && grep "feedparser" /Users/alessandrobm1/develop/server/wordpress/canaldecortes/clip-processor/requirements.txt && echo "PASS: todas as mudanças verificadas"</automated>
  </verify>
  <done>
    docker-compose.yml tem volume ./canaldecortes/videos:/app/videos, REDIS_HOST=redis, REDIS_PORT=6379 e redis em depends_on.
    requirements.txt tem apscheduler, feedparser, redis, pytest, pytest-mock além das dependências existentes.
    Diretório canaldecortes/videos/ existe no filesystem.
  </done>
</task>

<task type="auto">
  <name>Task 2: Seed SQL de canais de futebol</name>
  <files>canaldecortes/mysql/init/02-seed-channels.sql</files>
  <action>
Criar `/Users/alessandrobm1/develop/server/wordpress/canaldecortes/mysql/init/02-seed-channels.sql` com canais de futebol brasileiros reais para monitoramento inicial.

Padrão idempotente com INSERT IGNORE (igual ao 01-clips-schema.sql).

Canais sugeridos (futebol PT-BR com grande audiência no YouTube):
- Sportv: UC_s__6CQ2hcRzDSGpMlIbDg
- GE — Globoesporte: UCtBQaGnKKEkMR8pNgJfJCxg
- ESPN Brasil: UCGgLrGi5EIhIlyNJGdHKzog
- Arquivo de Jogos: UCcxEFCDEUwXrMI6d_Wn3c6Q (exemplo; verificar IDs reais)

IMPORTANTE: O plano instrui o executor a pesquisar e usar IDs de canal REAIS do YouTube para canais de futebol PT-BR com alta visualização. Para cada canal, o `rss_url` segue o padrão `https://www.youtube.com/feeds/videos.xml?channel_id={youtube_channel_id}`.

Estrutura do arquivo:
```sql
-- Canal de Cortes — Seed de canais-fonte para monitoramento
-- Phase 2: Aquisição de Vídeos
-- Idempotente: INSERT IGNORE para re-execução segura
-- Executar: docker exec -i mysql mysql -uroot -p${MYSQL_ROOT_PASSWORD} < mysql/init/02-seed-channels.sql

USE clips_automation;

INSERT IGNORE INTO source_channels (youtube_channel_id, channel_name, rss_url, active)
VALUES
  ('{ID_REAL_1}', '{Nome Canal 1}',
   'https://www.youtube.com/feeds/videos.xml?channel_id={ID_REAL_1}', TRUE),
  ...
```

O executor deve buscar pelo menos 3-5 canais de futebol PT-BR reais. Usar a interface do YouTube ou ferramentas disponíveis para encontrar os channel IDs corretos (o ID fica na URL do canal: youtube.com/channel/{ID}).

Canais de alto volume recomendados para busca:
- "Canal do Nicola" (mercado de transferências)
- "Sportv" (transmissões ao vivo)
- "ESPN Brasil" (análises)
- "Casimiro" (reações ao vivo)
- "ge.globo" (cobertura geral)
  </action>
  <verify>
    <automated>ls /Users/alessandrobm1/develop/server/wordpress/canaldecortes/mysql/init/02-seed-channels.sql && grep -c "INSERT IGNORE" /Users/alessandrobm1/develop/server/wordpress/canaldecortes/mysql/init/02-seed-channels.sql && echo "PASS: seed SQL criado"</automated>
  </verify>
  <done>
    02-seed-channels.sql existe com pelo menos 3 canais de futebol PT-BR reais.
    Arquivo usa INSERT IGNORE (idempotente).
    Cada linha tem youtube_channel_id real (não placeholder), channel_name, rss_url correto e active=TRUE.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: db.py — módulo de acesso ao MySQL</name>
  <files>canaldecortes/clip-processor/src/db.py</files>
  <behavior>
    - get_db_connection(): conecta ao MySQL usando env vars MYSQL_HOST, MYSQL_DATABASE, MYSQL_USER, MYSQL_PASSWORD; charset utf8mb4; autocommit=False; connect_timeout=10. Retorna conexão pymysql.
    - update_status(conn, video_id, status, local_path=None): executa UPDATE source_videos SET status=%s [, local_path=%s] WHERE youtube_video_id=%s; faz commit.
    - insert_video(conn, channel_id, video_id, title, published_at): executa INSERT IGNORE INTO source_videos (youtube_video_id, channel_id, title, published_at, status) VALUES (%s, %s, %s, %s, 'pending'); faz commit.
    - recover_stuck_downloads(conn): executa UPDATE source_videos SET status='pending' WHERE status='downloading'; faz commit; loga quantas linhas afetadas.
    - test_status_update passa: mock cursor recebe execute() com SQL contendo 'UPDATE source_videos SET status'
    - test_recover_stuck_downloads passa: mock cursor recebe execute() com SQL contendo "status='downloading'"
    - test_insert_video passa: mock cursor recebe execute() com SQL contendo 'INSERT'
  </behavior>
  <action>
Criar `/Users/alessandrobm1/develop/server/wordpress/canaldecortes/clip-processor/src/db.py`.

Implementar as 4 funções exatamente como especificado no behavior.

Padrão de logging: usar `print(f'[{datetime.now().strftime("%Y-%m-%d %H:%M:%S")}] [DB] ...')` para todos os logs — sem biblioteca de logging (stdout simples, conforme decisão do CONTEXT.md).

Tratar `pymysql.OperationalError` em recover_stuck_downloads para robustez, mas não silenciar outros erros.

Cada função que faz query deve usar `with conn.cursor() as cur:` pattern.

Não fechar a conexão dentro das funções — quem chama é responsável por fechar (padrão do daemon).

Após criar db.py, rodar os testes test_db.py para verificar estado GREEN:
```bash
cd /Users/alessandrobm1/develop/server/wordpress/canaldecortes && python -m pytest clip-processor/tests/test_db.py -x -q
```
  </action>
  <verify>
    <automated>cd /Users/alessandrobm1/develop/server/wordpress/canaldecortes && python -m pytest clip-processor/tests/test_db.py -x -q 2>&1</automated>
  </verify>
  <done>
    db.py existe em clip-processor/src/db.py com as 4 funções exportadas.
    pytest clip-processor/tests/test_db.py -x -q passa verde (todos os testes GREEN).
    Funções usam `with conn.cursor() as cur:` e fazem commit explícito.
  </done>
</task>

</tasks>

<verification>
```bash
# 1. Verificar volume no docker-compose
grep "canaldecortes/videos" /Users/alessandrobm1/develop/server/wordpress/docker-compose.yml

# 2. Verificar env vars Redis
grep "REDIS_HOST" /Users/alessandrobm1/develop/server/wordpress/docker-compose.yml

# 3. Verificar requirements
grep -E "apscheduler|feedparser|redis|pytest" /Users/alessandrobm1/develop/server/wordpress/canaldecortes/clip-processor/requirements.txt

# 4. Verificar seed SQL
cat /Users/alessandrobm1/develop/server/wordpress/canaldecortes/mysql/init/02-seed-channels.sql

# 5. Testes db.py passando
cd /Users/alessandrobm1/develop/server/wordpress/canaldecortes && python -m pytest clip-processor/tests/test_db.py -v
```
</verification>

<success_criteria>
- docker-compose.yml: volume canaldecortes/videos:/app/videos + REDIS_HOST=redis + redis em depends_on
- requirements.txt tem apscheduler, feedparser, redis, pytest, pytest-mock
- 02-seed-channels.sql existe com 3+ canais reais de futebol PT-BR com INSERT IGNORE
- db.py implementado com 4 funções: get_db_connection, update_status, insert_video, recover_stuck_downloads
- pytest clip-processor/tests/test_db.py passa verde
</success_criteria>

<output>
Após conclusão, criar `.planning/phases/02-aquisicao-de-videos/02-02-SUMMARY.md`
</output>
