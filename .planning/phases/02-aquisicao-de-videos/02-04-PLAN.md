---
phase: 02-aquisicao-de-videos
plan: "04"
type: execute
wave: 3
depends_on:
  - "02-03"
files_modified:
  - canaldecortes/clip-processor/src/main.py
autonomous: false
requirements:
  - ACQU-01
  - ACQU-02
  - ACQU-03
  - ORC-02

must_haves:
  truths:
    - "docker logs clip-processor mostra '[ACQU] Daemon iniciado — poll RSS a cada 6 horas'"
    - "docker logs clip-processor mostra poll executando imediatamente na inicialização"
    - "docker restart clip-processor não causa crash — daemon reinicia e retoma polling"
    - "source_videos tem registros com status 'pending' após primeiro poll de um canal com vídeos"
    - "Suite de testes completa permanece verde após wiring do main.py"
  artifacts:
    - path: "canaldecortes/clip-processor/src/main.py"
      provides: "Daemon entry point com BlockingScheduler + signal handlers + recovery na boot"
      contains: "BlockingScheduler"
    - path: "canaldecortes/clip-processor/src/main.py"
      provides: "Chama recover_stuck_downloads na inicialização"
      contains: "recover_stuck_downloads"
  key_links:
    - from: "canaldecortes/clip-processor/src/main.py"
      to: "canaldecortes/clip-processor/src/rss_poller.py"
      via: "scheduler.add_job(poll_all_channels, 'interval', hours=6)"
      pattern: "poll_all_channels"
    - from: "canaldecortes/clip-processor/src/main.py"
      to: "canaldecortes/clip-processor/src/db.py"
      via: "recover_stuck_downloads() na boot"
      pattern: "recover_stuck_downloads"
---

<objective>
Montar o daemon completo em main.py substituindo o stub de Phase 1, e verificar o funcionamento end-to-end com o Docker.

Purpose: main.py é o ponto de entrada que une todos os módulos (db.py, rss_poller.py, dedup.py, downloader.py) num daemon funcional com agendamento a cada 6 horas.

Output: main.py com BlockingScheduler + signal handlers + recovery on startup. Checkpoint humano confirma que o container sobe e o daemon está ativo nos logs.
</objective>

<execution_context>
@/Users/alessandrobm1/.claude/get-shit-done/workflows/execute-plan.md
@/Users/alessandrobm1/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-aquisicao-de-videos/02-CONTEXT.md
@.planning/phases/02-aquisicao-de-videos/02-RESEARCH.md
@.planning/phases/02-aquisicao-de-videos/02-03-SUMMARY.md

<interfaces>
<!-- APScheduler BlockingScheduler pattern (RESEARCH.md Pattern 1) -->
<!--
from apscheduler.schedulers.blocking import BlockingScheduler
import signal

scheduler = BlockingScheduler(timezone='America/Sao_Paulo')
scheduler.add_job(
    poll_all_channels,
    'interval',
    hours=6,
    id='poll_rss',
    coalesce=True,
    max_instances=1,
    misfire_grace_time=300
)
signal.signal(signal.SIGTERM, shutdown)
signal.signal(signal.SIGINT, shutdown)
poll_all_channels()  # execução imediata na boot
scheduler.start()   # bloqueia o processo
-->

<!-- Pitfall documentado: container restart com status 'downloading' -->
<!-- recover_stuck_downloads() deve rodar ANTES de poll_all_channels() na boot -->

<!-- Módulos disponíveis após Plano 03: -->
<!-- from src.rss_poller import poll_all_channels -->
<!-- from src.db import get_db_connection, recover_stuck_downloads -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: main.py — daemon BlockingScheduler com recovery</name>
  <files>canaldecortes/clip-processor/src/main.py</files>
  <action>
Substituir o stub de Phase 1 em `/Users/alessandrobm1/develop/server/wordpress/canaldecortes/clip-processor/src/main.py` com a implementação do daemon.

Estrutura exata (baseada em RESEARCH.md Pattern 1):

```python
# Canal de Cortes — clip-processor daemon
# Phase 2: Aquisição de Vídeos
import signal
import os
from datetime import datetime
from apscheduler.schedulers.blocking import BlockingScheduler
from src.rss_poller import poll_all_channels
from src.db import get_db_connection, recover_stuck_downloads

def log(msg: str):
    print(f'[{datetime.now().strftime("%Y-%m-%d %H:%M:%S")}] {msg}', flush=True)

scheduler = BlockingScheduler(timezone='America/Sao_Paulo')

def shutdown(signum, frame):
    log('[ACQU] Recebendo sinal de shutdown — encerrando scheduler')
    scheduler.shutdown(wait=False)

scheduler.add_job(
    poll_all_channels,
    'interval',
    hours=6,
    id='poll_rss',
    coalesce=True,
    max_instances=1,
    misfire_grace_time=300
)

signal.signal(signal.SIGTERM, shutdown)
signal.signal(signal.SIGINT, shutdown)

if __name__ == '__main__':
    log('[ACQU] Daemon iniciado — poll RSS a cada 6 horas')
    log(f'[ACQU] MYSQL_HOST: {os.environ.get("MYSQL_HOST", "não configurado")}')
    log(f'[ACQU] REDIS_HOST: {os.environ.get("REDIS_HOST", "não configurado")}')

    # Recovery: vídeos presos em 'downloading' voltam para 'pending'
    try:
        conn = get_db_connection()
        recover_stuck_downloads(conn)
        conn.close()
    except Exception as e:
        log(f'[ACQU] Aviso: recovery on startup falhou — {e}')

    # Executar imediatamente na inicialização (não esperar 6h)
    log('[ACQU] Executando poll inicial...')
    poll_all_channels()

    log('[ACQU] Scheduler iniciado — próximo poll em 6 horas')
    scheduler.start()
```

ATENÇÃO: Manter o guard `if __name__ == '__main__':` para que a importação em testes não dispare o scheduler.

Após criar, rebuildar o container e verificar nos logs:
```bash
docker compose -f /Users/alessandrobm1/develop/server/wordpress/docker-compose.yml build clip-processor && docker compose -f /Users/alessandrobm1/develop/server/wordpress/docker-compose.yml up -d clip-processor
```

Aguardar 10 segundos e verificar logs:
```bash
docker logs clip-processor --tail=20
```
  </action>
  <verify>
    <automated>docker logs clip-processor --tail=30 2>&1 | grep -E "Daemon iniciado|poll RSS|ACQU" | head -10</automated>
  </verify>
  <done>
    main.py existe com BlockingScheduler, signal handlers SIGTERM/SIGINT, recover_stuck_downloads na boot e poll imediato.
    docker logs clip-processor mostra '[ACQU] Daemon iniciado — poll RSS a cada 6 horas' sem crash.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Daemon completo clip-processor com:
    - BlockingScheduler agendando poll RSS a cada 6 horas
    - Execução imediata do poll na boot
    - Recovery de jobs stuck em 'downloading' na inicialização
    - Logs em stdout: docker logs clip-processor -f
    - Canais de futebol seedados em source_channels (02-seed-channels.sql)
  </what-built>
  <how-to-verify>
    1. Verificar que o container está rodando sem crash:
       ```bash
       docker ps | grep clip-processor
       ```
       Esperado: container listado com status "Up X seconds"

    2. Verificar logs do daemon:
       ```bash
       docker logs clip-processor --tail=30
       ```
       Esperado: ver "[ACQU] Daemon iniciado — poll RSS a cada 6 horas" e "[ACQU] Executando poll inicial..."

    3. Aplicar seed de canais (se ainda não aplicado):
       ```bash
       export $(cat /Users/alessandrobm1/develop/server/wordpress/.env | grep -v '#' | xargs)
       docker exec -i mysql mysql -uroot -p${MYSQL_ROOT_PASSWORD} clips_automation < /Users/alessandrobm1/develop/server/wordpress/canaldecortes/mysql/init/02-seed-channels.sql
       ```

    4. Verificar que source_channels tem os canais seedados:
       ```bash
       docker exec mysql mysql -uclips_user -p${CLIPS_DB_PASSWORD} clips_automation -e "SELECT channel_name, rss_url FROM source_channels;"
       ```

    5. Verificar que source_videos tem registros após o poll inicial:
       ```bash
       docker exec mysql mysql -uclips_user -p${CLIPS_DB_PASSWORD} clips_automation -e "SELECT youtube_video_id, title, status FROM source_videos LIMIT 10;"
       ```
       Esperado: vídeos com status 'pending' (ou 'failed' se vídeos privados)

    6. Verificar que nenhum arquivo .part está acumulando:
       ```bash
       ls /Users/alessandrobm1/develop/server/wordpress/canaldecortes/videos/*.part 2>/dev/null || echo "OK: sem arquivos .part"
       ```

    7. Executar suite de testes para confirmar que wiring não quebrou nada:
       ```bash
       cd /Users/alessandrobm1/develop/server/wordpress/canaldecortes && python -m pytest clip-processor/tests/ -v
       ```
       Esperado: todos os testes GREEN
  </how-to-verify>
  <resume-signal>
    Digite "aprovado" se o daemon está ativo e os logs mostram o poll funcionando.
    Se houver problemas, descreva o erro observado para que possa ser corrigido.
  </resume-signal>
</task>

</tasks>

<verification>
```bash
# Container ativo
docker ps | grep clip-processor

# Logs mostram daemon ativo
docker logs clip-processor --tail=30

# Testes verdes
cd /Users/alessandrobm1/develop/server/wordpress/canaldecortes && python -m pytest clip-processor/tests/ -v

# Vídeos detectados no banco (após seed de canais)
docker exec mysql mysql -uclips_user -p${CLIPS_DB_PASSWORD} clips_automation -e "SELECT COUNT(*) as total_videos, status FROM source_videos GROUP BY status;"
```
</verification>

<success_criteria>
- main.py substitui o stub de Phase 1 com daemon BlockingScheduler completo
- Container clip-processor sobe sem crash e permanece ativo (docker ps mostra "Up")
- Logs mostram '[ACQU] Daemon iniciado — poll RSS a cada 6 horas'
- Suite completa pytest passa verde
- Checkpoint humano aprova o funcionamento end-to-end
- source_videos tem registros com status atualizado após o poll inicial
</success_criteria>

<output>
Após conclusão, criar `.planning/phases/02-aquisicao-de-videos/02-04-SUMMARY.md`
</output>
