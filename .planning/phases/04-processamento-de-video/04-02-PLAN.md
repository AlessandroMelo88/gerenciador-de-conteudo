---
phase: 04-processamento-de-video
plan: 02
type: tdd
wave: 1
depends_on:
  - "04-01"
files_modified:
  - clip-processor/src/video_processor.py
requirements:
  - VID-01
  - VID-02
  - VID-03
autonomous: true

must_haves:
  truths:
    - "cut_clip() usa FFmpeg com -ss e -to correspondendo aos timestamps da IA"
    - "cut_clip() gera video vertical 1080x1920 via scale/crop/setsar"
    - "generate_srt() filtra segmentos sobrepostos ao clip e desloca timestamps para comecar em 00:00:00,000"
    - "burn_subtitles() usa filtro subtitles com force_style legivel e borda/outline"
    - "extract_thumbnail() gera JPG a partir do clip"
    - "process_clip() atualiza generated_clips.clip_path, thumbnail_path e status pending quando o video fica pronto"
  artifacts:
    - path: "clip-processor/src/video_processor.py"
      provides: "Implementacao FFmpeg de corte, legendas, thumbnail e orquestracao por clip"
      exports:
        - cut_clip
        - generate_srt
        - burn_subtitles
        - extract_thumbnail
        - process_clip
  key_links:
    - from: "clip-processor/src/video_processor.py"
      to: "generated_clips"
      via: "SELECT clip + source_video, UPDATE clip_path/thumbnail_path/status"
      pattern: "pending_cut"
    - from: "clip-processor/src/video_processor.py"
      to: "ffmpeg"
      via: "subprocess.run([...], check=True, capture_output=True)"
      pattern: "subprocess.run"
---

<objective>
Implementar `video_processor.py` para transformar um momento selecionado em um arquivo Shorts completo: corte exato, 1080x1920, legenda queimada e thumbnail.

Purpose: Fechar VID-01, VID-02 e VID-03 com testes unitarios verdes antes de integrar metadata e poller.

Output: `video_processor.py` completo para processar uma linha de `generated_clips`.
</objective>

<execution_context>
@/Users/alessandrobm1/.claude/get-shit-done/workflows/execute-plan.md
@/Users/alessandrobm1/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-processamento-de-video/04-RESEARCH.md
@.planning/phases/04-processamento-de-video/04-VALIDATION.md
@.planning/phases/04-processamento-de-video/04-01-SUMMARY.md

<interfaces>
From `metadata_generator.py` after Plan 04-03:
```python
def generate_metadata(clip_context: dict, anthropic_client=None) -> dict:
    """Returns {'title', 'description', 'tags'}."""

def update_clip_metadata(conn, clip_id: int, metadata: dict) -> None:
    """Updates generated_clips title, description, tags."""
```

`process_clip()` may import these lazily inside the function so Plan 04-02 tests can pass before 04-03 is implemented.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: FFmpeg cut + vertical transform</name>
  <files>clip-processor/src/video_processor.py</files>
  <action>
Implementar `cut_clip(source_path, start_time, end_time, output_path)`:
- Criar diretorio de output.
- Rodar FFmpeg com `-ss str(start_time)` e `-to str(end_time)`.
- Usar filtro `scale=1080:1920:force_original_aspect_ratio=increase,crop=1080:1920,setsar=1`.
- Usar `libx264`, `aac`, `+faststart`, `-y`.
- Retornar `output_path`.
  </action>
  <verify>
    <automated>cd /Users/alessandrobm1/develop/server/wordpress/canaldecortes/clip-processor && python3 -m pytest tests/test_video_processor.py::TestVideoProcessor::test_cut_clip_uses_ffmpeg_with_exact_timestamps tests/test_video_processor.py::TestVideoProcessor::test_cut_clip_outputs_1080x1920_filter -v --tb=short</automated>
  </verify>
</task>

<task type="auto" tdd="true">
  <name>Task 2: SRT generation + subtitle burn-in</name>
  <files>clip-processor/src/video_processor.py</files>
  <action>
Implementar `generate_srt()`:
- Receber transcript dict com `segments`.
- Incluir segmentos que sobrepoem `[start_time, end_time]`.
- Clampar cue start/end dentro do clip.
- Shiftar tempos para relativo ao inicio do clip.
- Escrever SRT UTF-8.

Implementar `burn_subtitles()`:
- Rodar FFmpeg sobre o clip intermediario.
- Usar filtro `subtitles={srt_path}:force_style='Fontname=Arial,Fontsize=12,...,Outline=2,Shadow=1,Alignment=2,MarginV=160'`.
- Retornar output final.
  </action>
  <verify>
    <automated>cd /Users/alessandrobm1/develop/server/wordpress/canaldecortes/clip-processor && python3 -m pytest tests/test_video_processor.py::TestSubtitles -v --tb=short</automated>
  </verify>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Thumbnail + process_clip orchestration</name>
  <files>clip-processor/src/video_processor.py</files>
  <action>
Implementar `extract_thumbnail()`:
- Default `at_seconds` = meio do clip se disponivel pelo caller, ou 1.0.
- Rodar `ffmpeg -ss {at_seconds} -i clip_path -frames:v 1 -q:v 2 thumbnail_path -y`.
- Retornar thumbnail_path.

Implementar `process_clip(conn, clip_id, anthropic_client=None)`:
- Buscar clip pendente com join:
  `generated_clips gc JOIN source_videos sv ON sv.id = gc.source_video_id`
- Campos necessarios: `gc.id`, `gc.start_time`, `gc.end_time`, `gc.reason`, `gc.score`, `sv.youtube_video_id`, `sv.title source_title`, `sv.local_path`, `sv.transcript_path`.
- Setar status `cutting`.
- Carregar transcript JSON.
- Criar paths deterministicas:
  - raw clip temporario: `/app/clips/{clip_id}_raw.mp4`
  - srt: `/app/clips/{clip_id}.srt`
  - final clip: `/app/clips/{clip_id}.mp4`
  - thumbnail: `/app/thumbnails/{clip_id}.jpg`
- Chamar cut, generate_srt, burn_subtitles, extract_thumbnail.
- Atualizar `clip_path`, `thumbnail_path`.
- Se metadata_generator ja estiver implementado, gerar e persistir metadata.
- Setar status `pending` ao final.
- Em exception: logar, setar status `failed`, retornar False.
  </action>
  <verify>
    <automated>cd /Users/alessandrobm1/develop/server/wordpress/canaldecortes/clip-processor && python3 -m pytest tests/test_video_processor.py -v --tb=short</automated>
  </verify>
</task>

</tasks>

<verification>
```bash
cd /Users/alessandrobm1/develop/server/wordpress/canaldecortes/clip-processor
python3 -m pytest tests/test_video_processor.py -v --tb=short
python3 -m pytest tests/ -v --tb=short
```
</verification>

<success_criteria>
- `pytest tests/test_video_processor.py` passa completo
- Implementacao nao faz chamada real a FFmpeg nos testes unitarios
- `process_clip()` marca failed em erro isolado
- Criar `.planning/phases/04-processamento-de-video/04-02-SUMMARY.md` ao concluir
</success_criteria>
