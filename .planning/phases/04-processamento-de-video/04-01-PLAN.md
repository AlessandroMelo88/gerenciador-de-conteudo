---
phase: 04-processamento-de-video
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - clip-processor/src/video_processor.py
  - clip-processor/src/metadata_generator.py
  - clip-processor/tests/test_video_processor.py
  - clip-processor/tests/test_metadata_generator.py
  - clip-processor/tests/test_clip_pipeline.py
autonomous: true
requirements:
  - VID-01
  - VID-02
  - VID-03
  - VID-04

must_haves:
  truths:
    - "test_video_processor.py cobre corte por timestamps, filtro 1080x1920, SRT shiftado, burn-in e thumbnail em RED state"
    - "test_metadata_generator.py cobre structured output, limite de 100 chars no titulo e persistencia em generated_clips em RED state"
    - "test_clip_pipeline.py cobre processamento de clips pending_cut pelo poller em RED state"
    - "video_processor.py e metadata_generator.py existem com interfaces publicas definidas e sem implementacao real"
  artifacts:
    - path: "clip-processor/src/video_processor.py"
      provides: "Skeleton das interfaces de corte, legenda, thumbnail e process_clip"
      exports:
        - cut_clip
        - generate_srt
        - burn_subtitles
        - extract_thumbnail
        - process_clip
    - path: "clip-processor/src/metadata_generator.py"
      provides: "Skeleton das interfaces de metadata SEO"
      exports:
        - generate_metadata
        - update_clip_metadata
    - path: "clip-processor/tests/test_video_processor.py"
      provides: "Testes RED para VID-01, VID-02, VID-03"
      contains: "TestVideoProcessor"
    - path: "clip-processor/tests/test_metadata_generator.py"
      provides: "Testes RED para VID-04"
      contains: "TestMetadataGenerator"
    - path: "clip-processor/tests/test_clip_pipeline.py"
      provides: "Testes RED de integracao do pipeline Phase 4"
      contains: "TestClipPipelineIntegration"
  key_links:
    - from: "clip-processor/tests/test_video_processor.py"
      to: "clip-processor/src/video_processor.py"
      via: "from src.video_processor import"
      pattern: "from src.video_processor import"
    - from: "clip-processor/tests/test_metadata_generator.py"
      to: "clip-processor/src/metadata_generator.py"
      via: "from src.metadata_generator import"
      pattern: "from src.metadata_generator import"
---

<objective>
Wave 0: Criar contratos de interface e testes RED para toda a Phase 4 antes de implementar FFmpeg ou Claude metadata.

Purpose: Garantir que VID-01 a VID-04 tenham cobertura automatizada desde o inicio e que os planos seguintes trabalhem em ciclo TDD RED -> GREEN.

Output:
- `video_processor.py` skeleton
- `metadata_generator.py` skeleton
- `test_video_processor.py`, `test_metadata_generator.py`, `test_clip_pipeline.py` em RED state
</objective>

<execution_context>
@/Users/alessandrobm1/.claude/get-shit-done/workflows/execute-plan.md
@/Users/alessandrobm1/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-processamento-de-video/04-RESEARCH.md
@.planning/phases/04-processamento-de-video/04-VALIDATION.md

<interfaces>
Existing schema:
```sql
generated_clips(
  id INT,
  source_video_id INT,
  clip_path VARCHAR(1024),
  thumbnail_path VARCHAR(1024),
  title VARCHAR(200),
  description TEXT,
  tags TEXT,
  score TINYINT,
  reason TEXT,
  start_time FLOAT,
  end_time FLOAT,
  status ENUM('pending_cut','pending','cutting','published','failed')
)

source_videos(
  id INT,
  youtube_video_id VARCHAR(64),
  title VARCHAR(500),
  local_path VARCHAR(1024),
  transcript_path VARCHAR(500)
)
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Skeletons Phase 4</name>
  <files>clip-processor/src/video_processor.py, clip-processor/src/metadata_generator.py</files>
  <action>
Criar `clip-processor/src/video_processor.py` com:

```python
VIDEOS_DIR = '/app/videos'
CLIPS_DIR = '/app/clips'
THUMBNAILS_DIR = '/app/thumbnails'

def cut_clip(source_path: str, start_time: float, end_time: float, output_path: str) -> str:
    raise NotImplementedError

def generate_srt(transcript: dict, start_time: float, end_time: float, srt_path: str) -> str:
    raise NotImplementedError

def burn_subtitles(input_clip_path: str, srt_path: str, output_path: str) -> str:
    raise NotImplementedError

def extract_thumbnail(clip_path: str, thumbnail_path: str, at_seconds: float = None) -> str:
    raise NotImplementedError

def process_clip(conn, clip_id: int, anthropic_client=None) -> bool:
    raise NotImplementedError
```

Criar `clip-processor/src/metadata_generator.py` com:

```python
def generate_metadata(clip_context: dict, anthropic_client=None) -> dict:
    raise NotImplementedError

def update_clip_metadata(conn, clip_id: int, metadata: dict) -> None:
    raise NotImplementedError
```
  </action>
  <verify>
    <automated>cd /Users/alessandrobm1/develop/server/wordpress/canaldecortes/clip-processor && python3 -m pytest tests/test_video_processor.py tests/test_metadata_generator.py tests/test_clip_pipeline.py -v --tb=short</automated>
  </verify>
  <done>
- Imports dos novos modulos funcionam
- Testes falham por NotImplementedError, nao por ImportError
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Testes RED de video, metadata e integracao</name>
  <files>clip-processor/tests/test_video_processor.py, clip-processor/tests/test_metadata_generator.py, clip-processor/tests/test_clip_pipeline.py</files>
  <action>
Criar testes RED:

`test_video_processor.py`:
- `test_cut_clip_uses_ffmpeg_with_exact_timestamps`
- `test_cut_clip_outputs_1080x1920_filter`
- `test_srt_contains_shifted_segment_times`
- `test_burn_subtitles_uses_force_style`
- `test_thumbnail_extracted_from_clip`
- `test_process_clip_updates_paths_and_status`
- `test_process_clip_failure_marks_failed`

`test_metadata_generator.py`:
- `test_generate_metadata_returns_title_description_tags`
- `test_title_is_trimmed_to_100_chars`
- `test_update_clip_metadata_persists_fields`
- `test_generate_metadata_uses_structured_output`

`test_clip_pipeline.py`:
- `test_pending_cut_clips_trigger_processing`
- `test_clip_processing_failure_does_not_abort_poll`
  </action>
  <verify>
    <automated>cd /Users/alessandrobm1/develop/server/wordpress/canaldecortes/clip-processor && python3 -m pytest tests/test_video_processor.py tests/test_metadata_generator.py tests/test_clip_pipeline.py -v --tb=short</automated>
  </verify>
  <done>
- Testes existem e entram em RED state controlado
- Cobertura de VID-01, VID-02, VID-03, VID-04 esta representada nos nomes dos testes
  </done>
</task>

</tasks>

<verification>
```bash
cd /Users/alessandrobm1/develop/server/wordpress/canaldecortes/clip-processor
python3 -m pytest tests/test_video_processor.py tests/test_metadata_generator.py tests/test_clip_pipeline.py -v --tb=short
```

Esperado neste plano: RED state controlado por `NotImplementedError`.
</verification>

<success_criteria>
- Skeletons criados sem implementacao real
- Testes RED cobrem VID-01, VID-02, VID-03 e VID-04
- Nenhuma chamada real a FFmpeg ou Anthropic nos testes
- Criar `.planning/phases/04-processamento-de-video/04-01-SUMMARY.md` ao concluir
</success_criteria>
