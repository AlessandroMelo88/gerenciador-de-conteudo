---
phase: 04-processamento-de-video
plan: 04
type: execute
wave: 2
depends_on:
  - "04-02"
  - "04-03"
files_modified:
  - clip-processor/src/rss_poller.py
autonomous: false
requirements:
  - VID-01
  - VID-02
  - VID-03
  - VID-04

must_haves:
  truths:
    - "poll_all_channels() processa clips com status pending_cut depois do pipeline de IA"
    - "Falha em um clip nao aborta o processamento dos demais"
    - "Clip processado termina com clip_path, thumbnail_path, title, description, tags e status pending"
    - "Checkpoint humano valida pelo menos um MP4 real 1080x1920 com legenda queimada e thumbnail"
  artifacts:
    - path: "clip-processor/src/rss_poller.py"
      provides: "Integracao do processamento de clips no daemon existente"
      contains: "_process_pending_clips"
  key_links:
    - from: "clip-processor/src/rss_poller.py"
      to: "clip-processor/src/video_processor.py"
      via: "from src.video_processor import process_clip"
      pattern: "process_clip"
    - from: "generated_clips.status"
      to: "Phase 5 publishing"
      via: "pending_cut -> cutting -> pending"
      pattern: "pending_cut"
---

<objective>
Integrar Phase 4 ao daemon existente e verificar o pipeline end-to-end com checkpoint humano.

Purpose: Depois que Phase 3 insere momentos em `generated_clips(pending_cut)`, o mesmo ciclo do daemon deve cortar, legendar, gerar thumbnail e metadata, deixando o clip em `pending` para publicacao na Phase 5.

Output:
- `rss_poller.py` chama `process_clip()` para cada `generated_clips.status = 'pending_cut'`
- Checkpoint humano aprova um clip real renderizado
</objective>

<execution_context>
@/Users/alessandrobm1/.claude/get-shit-done/workflows/execute-plan.md
@/Users/alessandrobm1/.claude/get-shit-done/templates/summary.md
@/Users/alessandrobm1/.claude/get-shit-done/references/checkpoints.md
</execution_context>

<context>
@.planning/phases/04-processamento-de-video/04-RESEARCH.md
@.planning/phases/04-processamento-de-video/04-02-SUMMARY.md
@.planning/phases/04-processamento-de-video/04-03-SUMMARY.md

<interfaces>
From `video_processor.py`:
```python
def process_clip(conn, clip_id: int, anthropic_client=None) -> bool:
    """Processa generated_clips.id pendente. True = pronto, False = failed."""
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Integrar pending_cut clips no rss_poller.py</name>
  <files>clip-processor/src/rss_poller.py, clip-processor/tests/test_clip_pipeline.py</files>
  <action>
Adicionar import:
```python
from src.video_processor import process_clip
```

Adicionar helper:
```python
def _process_pending_clips(conn) -> None:
    with conn.cursor() as cur:
        cur.execute("SELECT id FROM generated_clips WHERE status = 'pending_cut'")
        rows = cur.fetchall()

    for row in rows:
        clip_id = row['id']
        _log(f'[VID] Iniciando processamento do clip {clip_id}')
        try:
            process_clip(conn, clip_id)
        except Exception as exc:
            _log(f'[VID] ERRO no processamento do clip {clip_id}: {exc}')
```

Chamar `_process_pending_clips(db_conn)` em `poll_all_channels()` depois do bloco de IA.

Nao alterar a assinatura de `poll_all_channels()` a menos que testes exijam; seguir o padrao de Phase 3.
  </action>
  <verify>
    <automated>cd /Users/alessandrobm1/develop/server/wordpress/canaldecortes/clip-processor && python3 -m pytest tests/test_clip_pipeline.py tests/test_rss_poller.py -v --tb=short</automated>
  </verify>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Checkpoint: Verificacao end-to-end Phase 4</name>
  <what-built>
Pipeline completo de processamento de video:
1. `generated_clips(pending_cut)` e consumido pelo daemon.
2. FFmpeg corta o momento em 1080x1920.
3. Legendas do transcript sao queimadas no video.
4. Thumbnail JPG e gerada.
5. Claude Haiku gera title/description/tags.
6. Clip fica `pending` para Phase 5.
  </what-built>
  <how-to-verify>
**Verificacao automatizada:**
```bash
cd /Users/alessandrobm1/develop/server/wordpress/canaldecortes/clip-processor
python3 -m pytest tests/ -v --tb=short
```

**Verificacao real com banco/container, se disponivel:**
```bash
docker exec mysql mysql -uclips_user -p${CLIPS_DB_PASSWORD} clips_automation \
  -e "SELECT id, status, clip_path, thumbnail_path, title FROM generated_clips ORDER BY id DESC LIMIT 5;"
```

**Verificacao visual:**
- Abrir o `clip_path` mais recente.
- Confirmar video vertical 9:16, legendas legiveis e audio presente.
- Abrir `thumbnail_path` e confirmar imagem valida.
  </how-to-verify>
  <resume-signal>
Digite "aprovado" se o clip real estiver valido. Se houver problema, descreva exatamente o que falhou: corte, proporcao, legenda, thumbnail ou metadata.
  </resume-signal>
</task>

</tasks>

<verification>
```bash
cd /Users/alessandrobm1/develop/server/wordpress/canaldecortes/clip-processor
python3 -m pytest tests/test_clip_pipeline.py tests/test_video_processor.py tests/test_metadata_generator.py -v --tb=short
python3 -m pytest tests/ -v --tb=short
```
</verification>

<success_criteria>
- `poll_all_channels()` chama processamento de clips `pending_cut`
- Falha isolada por clip nao aborta o poll
- Suite completa pytest verde
- Checkpoint humano aprovado com MP4 real, thumbnail e metadata
- Criar `.planning/phases/04-processamento-de-video/04-04-SUMMARY.md` ao concluir
</success_criteria>
