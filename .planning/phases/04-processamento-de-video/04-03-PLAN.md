---
phase: 04-processamento-de-video
plan: 03
type: tdd
wave: 1
depends_on:
  - "04-01"
files_modified:
  - clip-processor/src/metadata_generator.py
requirements:
  - VID-04
autonomous: true

must_haves:
  truths:
    - "generate_metadata() chama Claude Haiku com output_config json_schema"
    - "generate_metadata() retorna dict com title, description e tags"
    - "title e limitado a 100 caracteres antes de persistir"
    - "update_clip_metadata() grava title, description e tags em generated_clips"
    - "tags sao serializadas em TEXT de forma compativel com Phase 5"
  artifacts:
    - path: "clip-processor/src/metadata_generator.py"
      provides: "Geracao e persistencia de metadata SEO para YouTube"
      exports:
        - generate_metadata
        - update_clip_metadata
        - METADATA_OUTPUT_SCHEMA
  key_links:
    - from: "clip-processor/src/metadata_generator.py"
      to: "anthropic.Anthropic().messages.create()"
      via: "output_config=METADATA_OUTPUT_SCHEMA"
      pattern: "output_config"
    - from: "clip-processor/src/metadata_generator.py"
      to: "generated_clips.title/description/tags"
      via: "UPDATE generated_clips SET title=%s, description=%s, tags=%s WHERE id=%s"
      pattern: "UPDATE generated_clips"
---

<objective>
Implementar `metadata_generator.py` para gerar titulo, descricao e tags otimizados para YouTube usando Claude Haiku.

Purpose: Fechar VID-04 e preparar cada clip processado para a Phase 5 de publicacao.

Output: `metadata_generator.py` completo e testado.
</objective>

<execution_context>
@/Users/alessandrobm1/.claude/get-shit-done/workflows/execute-plan.md
@/Users/alessandrobm1/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-processamento-de-video/04-RESEARCH.md
@.planning/phases/04-processamento-de-video/04-VALIDATION.md
@.planning/phases/04-processamento-de-video/04-01-SUMMARY.md

<interfaces>
Input esperado:
```python
clip_context = {
    'source_title': str,
    'reason': str,
    'score': int,
    'start_time': float,
    'end_time': float,
    'transcript_excerpt': str,
}
```

Output:
```python
{
    'title': 'max 100 chars',
    'description': '...',
    'tags': ['futebol', 'cortes', ...],
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Claude Haiku structured metadata</name>
  <files>clip-processor/src/metadata_generator.py</files>
  <action>
Implementar:
- `SYSTEM_PROMPT` em PT-BR para canal de cortes de futebol.
- `METADATA_OUTPUT_SCHEMA` com raiz object e campos `title`, `description`, `tags`.
- `generate_metadata(clip_context, anthropic_client=None)`:
  - cria `anthropic.Anthropic()` se cliente None;
  - monta prompt com titulo original, reason, score, timestamps e excerpt;
  - chama `messages.create(model='claude-haiku-4-5', max_tokens=1024, output_config=METADATA_OUTPUT_SCHEMA)`;
  - parseia JSON;
  - garante `title[:100]`;
  - garante `tags` como lista de strings;
  - em erro, retorna fallback deterministico com titulo derivado de source_title/reason.
  </action>
  <verify>
    <automated>cd /Users/alessandrobm1/develop/server/wordpress/canaldecortes/clip-processor && python3 -m pytest tests/test_metadata_generator.py::TestMetadataGenerator -v --tb=short</automated>
  </verify>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Persistencia de metadata em generated_clips</name>
  <files>clip-processor/src/metadata_generator.py</files>
  <action>
Implementar `update_clip_metadata(conn, clip_id, metadata)`:
- Normalizar title max 100 chars.
- Serializar `tags` como string comma-separated.
- Executar:
  `UPDATE generated_clips SET title=%s, description=%s, tags=%s WHERE id=%s`
- Fazer `conn.commit()`.
  </action>
  <verify>
    <automated>cd /Users/alessandrobm1/develop/server/wordpress/canaldecortes/clip-processor && python3 -m pytest tests/test_metadata_generator.py::TestMetadataPersistence -v --tb=short</automated>
  </verify>
</task>

</tasks>

<verification>
```bash
cd /Users/alessandrobm1/develop/server/wordpress/canaldecortes/clip-processor
python3 -m pytest tests/test_metadata_generator.py -v --tb=short
python3 -m pytest tests/ -v --tb=short
```
</verification>

<success_criteria>
- `pytest tests/test_metadata_generator.py` passa completo
- Structured outputs usa `output_config`, sem prefill
- Nenhum titulo persistido passa de 100 caracteres
- Criar `.planning/phases/04-processamento-de-video/04-03-SUMMARY.md` ao concluir
</success_criteria>
