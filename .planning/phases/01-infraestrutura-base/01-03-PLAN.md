---
phase: 01-infraestrutura-base
plan: 03
type: execute
wave: 2
depends_on:
  - 01-01
  - 01-02
files_modified:
  - canaldecortes/.env
  - canaldecortes/youtube/token.json
autonomous: false
requirements:
  - INFRA-01
  - INFRA-02
  - INFRA-04

must_haves:
  truths:
    - "docker-compose up sobe n8n, clip-processor e whisper sem erros ao lado dos serviços existentes"
    - "n8n responde em http://localhost:5678/healthz"
    - "whisper responde em http://localhost:8000/health"
    - "banco clips_automation existe no MySQL com as 3 tabelas verificáveis"
    - ".env contém N8N_ENCRYPTION_KEY, ANTHROPIC_API_KEY e CLIPS_DB_PASSWORD com valores reais"
  artifacts:
    - path: "canaldecortes/.env"
      provides: "Secrets reais para todos os serviços"
      contains: "N8N_ENCRYPTION_KEY, ANTHROPIC_API_KEY, CLIPS_DB_PASSWORD"
  key_links:
    - from: "canaldecortes/.env"
      to: "docker-compose.yml (n8n)"
      via: "${N8N_ENCRYPTION_KEY} referência"
      pattern: "N8N_ENCRYPTION_KEY=.+"
    - from: "canaldecortes/mysql/init/01-clips-schema.sql"
      to: "MySQL container"
      via: "docker exec mysql mysql"
      pattern: "clips_automation"

user_setup:
  - service: anthropic
    why: "Claude Haiku para análise de momentos (Phases 3+)"
    env_vars:
      - name: ANTHROPIC_API_KEY
        source: "https://console.anthropic.com/settings/keys — criar nova chave API"
  - service: mysql-clips-user
    why: "Senha para o usuário clips_user no MySQL"
    env_vars:
      - name: CLIPS_DB_PASSWORD
        source: "Escolher uma senha segura (mínimo 12 caracteres); será criada no MySQL pelo script SQL"
---

<objective>
Configurar o arquivo .env com todos os secrets reais, subir os serviços Docker e aplicar o schema MySQL. Este plano é o momento em que a infraestrutura sai do papel e passa a rodar — conecta os arquivos criados nos Plans 01 e 02 ao mundo real.

Purpose: Nenhum serviço pode subir sem N8N_ENCRYPTION_KEY e ANTHROPIC_API_KEY. O schema MySQL precisa de CLIPS_DB_PASSWORD definido. Este plano garante que tudo está configurado antes do primeiro `docker-compose up`.

Output: .env com valores reais + todos os serviços rodando + banco clips_automation criado + validação verde
</objective>

<execution_context>
@/Users/alessandrobm1/.claude/get-shit-done/workflows/execute-plan.md
@/Users/alessandrobm1/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/alessandrobm1/develop/server/wordpress/canaldecortes/.planning/PROJECT.md
@/Users/alessandrobm1/develop/server/wordpress/canaldecortes/.planning/phases/01-infraestrutura-base/01-RESEARCH.md
@/Users/alessandrobm1/develop/server/wordpress/canaldecortes/.planning/phases/01-infraestrutura-base/01-01-SUMMARY.md
@/Users/alessandrobm1/develop/server/wordpress/canaldecortes/.planning/phases/01-infraestrutura-base/01-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Gerar .env com N8N_ENCRYPTION_KEY e verificar secrets do usuário</name>
  <files>
    canaldecortes/.env
  </files>
  <action>
ATENÇÃO: Este task requer valores do usuário. O executor deve:

1. Verificar se canaldecortes/.env já existe:
   ```bash
   ls /Users/alessandrobm1/develop/server/wordpress/canaldecortes/.env
   ```

2. Se NÃO existir: copiar de .env.example:
   ```bash
   cp /Users/alessandrobm1/develop/server/wordpress/canaldecortes/.env.example \
      /Users/alessandrobm1/develop/server/wordpress/canaldecortes/.env
   ```

3. Gerar N8N_ENCRYPTION_KEY automaticamente e inserir no .env:
   ```bash
   KEY=$(openssl rand -hex 32)
   # Substituir a linha vazia N8N_ENCRYPTION_KEY= pelo valor gerado
   sed -i '' "s/^N8N_ENCRYPTION_KEY=$/N8N_ENCRYPTION_KEY=${KEY}/" \
     /Users/alessandrobm1/develop/server/wordpress/canaldecortes/.env
   ```

4. Verificar se ANTHROPIC_API_KEY está preenchida. Se estiver vazia, pausar e pedir ao usuário:
   "Por favor, insira sua ANTHROPIC_API_KEY no arquivo /Users/alessandrobm1/develop/server/wordpress/canaldecortes/.env"
   (Obter em: https://console.anthropic.com/settings/keys)

5. Verificar se CLIPS_DB_PASSWORD está preenchida. Se estiver vazia, gerar uma senha segura:
   ```bash
   CLIPS_PWD=$(openssl rand -base64 16 | tr -d '=+/' | head -c 20)
   sed -i '' "s/^CLIPS_DB_PASSWORD=$/CLIPS_DB_PASSWORD=${CLIPS_PWD}/" \
     /Users/alessandrobm1/develop/server/wordpress/canaldecortes/.env
   echo "CLIPS_DB_PASSWORD gerada: ${CLIPS_PWD} — guarde em local seguro"
   ```

6. Confirmar que MYSQL_ROOT_PASSWORD=rootpassword está no .env (pode ser adicionado se não vier do .env.example, mas o valor já é conhecido do docker-compose existente).

7. Exibir confirmação final (SEM exibir os valores):
   ```bash
   echo "Variáveis configuradas:"
   grep -E "^(N8N_ENCRYPTION_KEY|ANTHROPIC_API_KEY|CLIPS_DB_PASSWORD|MYSQL_ROOT_PASSWORD)=" \
     /Users/alessandrobm1/develop/server/wordpress/canaldecortes/.env | \
     sed 's/=.*/=***CONFIGURADA***/'
   ```
  </action>
  <verify>
    <automated>grep -q "N8N_ENCRYPTION_KEY=.\{10\}" /Users/alessandrobm1/develop/server/wordpress/canaldecortes/.env && grep -q "CLIPS_DB_PASSWORD=.\{8\}" /Users/alessandrobm1/develop/server/wordpress/canaldecortes/.env && echo "Secrets OK"</automated>
  </verify>
  <done>.env existe em canaldecortes/ com N8N_ENCRYPTION_KEY (64 chars hex), ANTHROPIC_API_KEY e CLIPS_DB_PASSWORD preenchidos com valores reais; .env não está commitado no git (está no .gitignore)</done>
</task>

<task type="auto">
  <name>Task 2: Subir serviços Docker e aplicar schema MySQL</name>
  <files>
    canaldecortes/youtube/token.json
  </files>
  <action>
Executar a sequência de boot completa da infraestrutura. Todos os comandos rodam a partir de /Users/alessandrobm1/develop/server/wordpress/.

PASSO 1 — Criar arquivo token.json placeholder (evita erro de mount no clip-processor):
O arquivo canaldecortes/youtube/token.json deve existir. Se contiver apenas `{}` (placeholder do Plan 01), manter assim — será substituído no Plan 04.

PASSO 2 — Carregar variáveis do .env e subir os serviços:
```bash
cd /Users/alessandrobm1/develop/server/wordpress
set -a && source canaldecortes/.env && set +a
docker compose up -d n8n whisper clip-processor
```

Aguardar 30 segundos para os serviços inicializarem:
```bash
sleep 30
docker compose ps n8n whisper clip-processor
```

Se clip-processor falhar por causa do token.json vazio, confirmar que o arquivo existe e tem `{}` — é esperado neste estágio.

PASSO 3 — Verificar n8n e whisper:
```bash
curl -sf http://localhost:5678/healthz && echo "n8n OK"
curl -sf http://localhost:8000/health && echo "whisper OK"
```

Se whisper demorar (baixando modelo ~500MB na primeira vez), aguardar até 5 minutos e repetir o check.

PASSO 4 — Aplicar schema MySQL (com substituição da variável CLIPS_DB_PASSWORD):
```bash
cd /Users/alessandrobm1/develop/server/wordpress
source canaldecortes/.env
CLIPS_DB_PASSWORD_VALUE=$(grep "^CLIPS_DB_PASSWORD=" canaldecortes/.env | cut -d'=' -f2)

# Substituir placeholder e executar
sed "s/\${CLIPS_DB_PASSWORD}/${CLIPS_DB_PASSWORD_VALUE}/g" \
  canaldecortes/mysql/init/01-clips-schema.sql | \
  docker exec -i mysql mysql -uroot -prootpassword
```

PASSO 5 — Verificar schema criado:
```bash
docker exec mysql mysql -uroot -prootpassword -e "SHOW TABLES FROM clips_automation;"
# Deve listar: generated_clips, source_channels, source_videos
```

PASSO 6 — Rodar script de validação completo:
```bash
bash /Users/alessandrobm1/develop/server/wordpress/canaldecortes/scripts/validate-infra.sh
```
  </action>
  <verify>
    <automated>curl -sf http://localhost:5678/healthz && docker exec mysql mysql -uroot -prootpassword -e "SHOW TABLES FROM clips_automation;" 2>/dev/null | grep -c "source_channels\|source_videos\|generated_clips"</automated>
  </verify>
  <done>n8n em http://localhost:5678, whisper em http://localhost:8000, clip-processor rodando (ou em loop aguardando token.json real), banco clips_automation com 3 tabelas visíveis via SHOW TABLES, validate-infra.sh com 0 FAILs nos checks INFRA-01 e INFRA-02</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Infraestrutura Docker completa: n8n + clip-processor + whisper rodando ao lado dos serviços existentes (nginx, php, mysql, redis). Schema clips_automation criado no MySQL com as 3 tabelas.
  </what-built>
  <how-to-verify>
    1. Abrir http://localhost:5678 no browser — deve exibir a tela de setup/login do n8n
    2. Rodar: `bash /Users/alessandrobm1/develop/server/wordpress/canaldecortes/scripts/validate-infra.sh`
       - Deve exibir PASS para todos os checks de INFRA-01 e INFRA-02
       - O check de token.json pode mostrar SKIP (esperado — ainda não gerado)
    3. Verificar serviços existentes ainda rodando: `curl -sf http://localhost:80 && echo "nginx OK"`
  </how-to-verify>
  <resume-signal>
    Digite "aprovado" se n8n abriu no browser e validate-infra.sh retornou 0 FAILs nos checks Docker e MySQL.
    Descreva qualquer problema encontrado se algo falhou.
  </resume-signal>
</task>

</tasks>

<verification>
```bash
# Verificação completa Phase 1 Plans 01-03
bash /Users/alessandrobm1/develop/server/wordpress/canaldecortes/scripts/validate-infra.sh

# Verificação manual n8n
open http://localhost:5678

# Tabelas MySQL
docker exec mysql mysql -uroot -prootpassword -e "SHOW TABLES FROM clips_automation;"
```
</verification>

<success_criteria>
- `docker-compose ps` mostra n8n, whisper e clip-processor com status Up
- http://localhost:5678 retorna página do n8n (setup ou login)
- http://localhost:8000/health retorna 200
- `SHOW TABLES FROM clips_automation` lista exatamente 3 tabelas
- validate-infra.sh: 0 FAILs nos checks INFRA-01 e INFRA-02
- Serviços existentes (nginx, mysql, redis) continuam rodando sem interrupção
</success_criteria>

<output>
Após completar (incluindo aprovação no checkpoint), criar /Users/alessandrobm1/develop/server/wordpress/canaldecortes/.planning/phases/01-infraestrutura-base/01-03-SUMMARY.md
</output>
