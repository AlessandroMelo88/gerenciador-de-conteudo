---
phase: 01-infraestrutura-base
plan: 04
type: execute
wave: 3
depends_on:
  - 01-03
files_modified:
  - canaldecortes/youtube/generate_token.py
  - canaldecortes/youtube/token.json
autonomous: false
requirements:
  - INFRA-03
  - INFRA-04

must_haves:
  truths:
    - "Canal do YouTube existe com nome, banner e descrição preenchidos"
    - "Conta do Google associada ao canal está verificada via SMS"
    - "token.json existe em canaldecortes/youtube/ com refresh_token válido"
    - "App OAuth está em modo Production (não Testing) para evitar expiração de 7 dias"
    - "validate-infra.sh retorna PASS no check de token.json"
  artifacts:
    - path: "canaldecortes/youtube/generate_token.py"
      provides: "Script one-time para autorizar YouTube API via browser e salvar token.json"
      min_lines: 25
    - path: "canaldecortes/youtube/token.json"
      provides: "OAuth credentials com refresh_token permanente do YouTube"
      contains: "refresh_token"
  key_links:
    - from: "canaldecortes/youtube/token.json"
      to: "canaldecortes/clip-processor (volume mount)"
      via: "docker-compose volume :ro"
      pattern: "token.json"

user_setup:
  - service: google-cloud
    why: "Criar projeto OAuth para YouTube Data API v3"
    env_vars: []
    dashboard_config:
      - task: "Criar projeto no Google Cloud Console"
        location: "https://console.cloud.google.com/ → New Project"
      - task: "Ativar YouTube Data API v3"
        location: "APIs & Services → Library → search 'YouTube Data API v3' → Enable"
      - task: "Criar OAuth 2.0 credentials (Desktop App)"
        location: "APIs & Services → Credentials → Create Credentials → OAuth client ID → Desktop app → Download JSON como client_secret.json"
      - task: "Mudar OAuth app para modo Production"
        location: "APIs & Services → OAuth consent screen → Publishing status → Publish App"
  - service: youtube-channel
    why: "Canal do YouTube precisa existir e estar verificado antes do primeiro upload"
    dashboard_config:
      - task: "Criar canal do YouTube"
        location: "https://www.youtube.com → click no avatar → Create a channel"
      - task: "Verificar conta via SMS"
        location: "https://www.youtube.com/verify → usar número de celular real (não VOIP)"
      - task: "Preencher nome do canal, descrição e banner"
        location: "YouTube Studio → Customization → Basic info + Branding"
---

<objective>
Criar o script de geração de token OAuth do YouTube, executar o fluxo de autorização via browser uma única vez e verificar o canal do YouTube criado manualmente.

Purpose: INFRA-03 e INFRA-04 têm componentes 100% manuais que não podem ser automatizados (criação de canal, verificação por SMS, autorização OAuth via browser). Este plano automatiza tudo que é possível automatizar e guia o usuário pelos passos manuais inevitáveis em sequência.

Output: generate_token.py pronto para uso + token.json com refresh_token permanente + canal do YouTube verificado e configurado
</objective>

<execution_context>
@/Users/alessandrobm1/.claude/get-shit-done/workflows/execute-plan.md
@/Users/alessandrobm1/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/alessandrobm1/develop/server/wordpress/canaldecortes/.planning/PROJECT.md
@/Users/alessandrobm1/develop/server/wordpress/canaldecortes/.planning/phases/01-infraestrutura-base/01-RESEARCH.md
@/Users/alessandrobm1/develop/server/wordpress/canaldecortes/.planning/phases/01-infraestrutura-base/01-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Criar script de geração de token OAuth do YouTube</name>
  <files>
    canaldecortes/youtube/generate_token.py
  </files>
  <action>
Criar /Users/alessandrobm1/develop/server/wordpress/canaldecortes/youtube/generate_token.py.

O script realiza o fluxo OAuth 2.0 para aplicações desktop, salva o token em youtube/token.json e exibe instruções claras. Deve:
- Verificar que client_secret.json existe no mesmo diretório antes de rodar
- Usar acesso offline (`access_type=offline`) com `prompt=consent` para garantir refresh_token permanente
- Salvar token.json no diretório correto (youtube/token.json)
- Verificar que o refresh_token foi obtido após o fluxo
- Exibir aviso claro para NÃO commitar o arquivo

```python
#!/usr/bin/env python3
"""
Canal de Cortes — Geração de token OAuth do YouTube
Executar UMA VEZ para autorizar o acesso ao canal do YouTube.

Pré-requisitos:
1. client_secret.json no mesmo diretório (baixado do Google Cloud Console)
2. pip install google-auth-oauthlib google-api-python-client

Uso:
    cd /Users/alessandrobm1/develop/server/wordpress/canaldecortes/youtube
    python generate_token.py
"""

import json
import os
import sys

# Verificar dependências antes de importar
try:
    from google_auth_oauthlib.flow import InstalledAppFlow
    from google.oauth2.credentials import Credentials
except ImportError:
    print("ERRO: Dependências não instaladas.")
    print("Executar: pip install google-auth-oauthlib google-api-python-client")
    sys.exit(1)

SCOPES = [
    "https://www.googleapis.com/auth/youtube.upload",
    "https://www.googleapis.com/auth/youtube.readonly",
]

SCRIPT_DIR = os.path.dirname(os.path.abspath(__file__))
CLIENT_SECRET_FILE = os.path.join(SCRIPT_DIR, "client_secret.json")
TOKEN_FILE = os.path.join(SCRIPT_DIR, "token.json")


def main():
    # Verificar client_secret.json
    if not os.path.exists(CLIENT_SECRET_FILE):
        print("ERRO: client_secret.json não encontrado.")
        print(f"Esperado em: {CLIENT_SECRET_FILE}")
        print()
        print("Para obter o arquivo:")
        print("  1. https://console.cloud.google.com/")
        print("  2. APIs & Services → Credentials")
        print("  3. Create Credentials → OAuth client ID → Desktop app")
        print("  4. Download JSON → renomear para client_secret.json")
        sys.exit(1)

    print("Iniciando autorização OAuth do YouTube...")
    print("Um browser será aberto. Faça login com a conta do canal e autorize o acesso.")
    print()

    flow = InstalledAppFlow.from_client_secrets_file(
        CLIENT_SECRET_FILE,
        scopes=SCOPES,
    )

    # access_type=offline + prompt=consent garante refresh_token permanente
    # Sem isso, o token expira em 1h e não há como renovar automaticamente
    creds = flow.run_local_server(
        port=0,
        access_type="offline",
        prompt="consent",
    )

    # Verificar que refresh_token foi obtido
    creds_data = json.loads(creds.to_json())
    if not creds_data.get("refresh_token"):
        print()
        print("AVISO: refresh_token não obtido.")
        print("Isso pode acontecer se você já autorizou antes sem revogar.")
        print("Para resolver: https://myaccount.google.com/permissions → revogar acesso → rodar novamente")
        sys.exit(1)

    # Salvar token
    with open(TOKEN_FILE, "w") as f:
        f.write(creds.to_json())

    print()
    print(f"token.json salvo em: {TOKEN_FILE}")
    print(f"refresh_token: {'presente' if creds_data.get('refresh_token') else 'AUSENTE'}")
    print()
    print("IMPORTANTE: NÃO commitar token.json no git (já está no .gitignore)")
    print()
    print("Próximo passo:")
    print("  Mudar app para modo Production no Google Cloud Console para evitar")
    print("  expiração do token em 7 dias:")
    print("  APIs & Services → OAuth consent screen → Publish App")


if __name__ == "__main__":
    main()
```

Também criar /Users/alessandrobm1/develop/server/wordpress/canaldecortes/youtube/.gitignore:
```
token.json
client_secret.json
```
  </action>
  <verify>
    <automated>python3 -m py_compile /Users/alessandrobm1/develop/server/wordpress/canaldecortes/youtube/generate_token.py && echo "Syntax OK" && grep "refresh_token" /Users/alessandrobm1/develop/server/wordpress/canaldecortes/youtube/generate_token.py</automated>
  </verify>
  <done>generate_token.py existe, passa em syntax check Python, contém access_type=offline e prompt=consent, verifica presença de refresh_token após o fluxo, e salva em youtube/token.json</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <action>
    PASSOS MANUAIS OBRIGATÓRIOS — executar na ordem abaixo.

    PARTE A — Canal do YouTube (INFRA-03):

    1. Criar canal do YouTube:
       - Acesse https://www.youtube.com
       - Clique no avatar → "Create a channel"
       - Escolha um nome relacionado a futebol/cortes (ex: "CortesFutebol", "MelhoresMomentos")

    2. Verificar conta via SMS:
       - Acesse https://www.youtube.com/verify
       - Use um número de celular REAL (não VOIP — YouTube rejeita números virtuais)
       - Após verificação: recursos desbloqueados (uploads acima de 15 min, thumbnails customizadas)

    3. Configurar canal:
       - YouTube Studio → Customization → Basic info:
         - Nome do canal preenchido
         - Descrição (2-3 linhas sobre o canal de cortes de futebol)
       - YouTube Studio → Customization → Branding:
         - Banner (recomendado: 2560×1440px)
         - Foto de perfil (opcional neste momento)

    PARTE B — Google Cloud + OAuth (INFRA-04):

    4. Criar projeto no Google Cloud Console:
       - https://console.cloud.google.com/ → New Project
       - Nome sugerido: "canal-de-cortes"

    5. Ativar YouTube Data API v3:
       - APIs & Services → Library → pesquisar "YouTube Data API v3" → Enable

    6. Criar credenciais OAuth:
       - APIs & Services → Credentials → Create Credentials → OAuth client ID
       - Application type: Desktop app
       - Baixar o JSON → salvar como:
         /Users/alessandrobm1/develop/server/wordpress/canaldecortes/youtube/client_secret.json

    7. Mudar app para modo Production (CRÍTICO — evita token expirar em 7 dias):
       - APIs & Services → OAuth consent screen
       - "Publishing status" → clique em "Publish App"
       - Confirme. (Não precisa de verificação do Google para uso próprio/interno)

    8. Rodar o script de geração de token:
       ```bash
       cd /Users/alessandrobm1/develop/server/wordpress/canaldecortes/youtube
       pip install google-auth-oauthlib google-api-python-client
       python generate_token.py
       ```
       - Um browser abrirá para login — use a conta Google do canal criado
       - Autorize o acesso
       - Confirme que token.json foi criado com sucesso

    9. Reiniciar clip-processor com token real:
       ```bash
       cd /Users/alessandrobm1/develop/server/wordpress
       docker compose restart clip-processor
       ```
  </action>
  <resume-signal>
    Digite "concluído" quando:
    - Canal do YouTube existe com verificação SMS, nome e descrição preenchidos
    - token.json existe em canaldecortes/youtube/token.json
    - App OAuth está em modo Production
    Descreva qualquer passo que não conseguiu completar.
  </resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Phase 1 completa: infraestrutura Docker (INFRA-01), schema MySQL (INFRA-02), canal do YouTube (INFRA-03) e secrets/OAuth (INFRA-04).
  </what-built>
  <how-to-verify>
    1. Rodar o script de validação completo:
       ```bash
       bash /Users/alessandrobm1/develop/server/wordpress/canaldecortes/scripts/validate-infra.sh
       ```
       Esperado: todos os checks PASS (incluindo token.json com refresh_token)

    2. Verificar token.json válido:
       ```bash
       python3 -c "
       import json
       with open('/Users/alessandrobm1/develop/server/wordpress/canaldecortes/youtube/token.json') as f:
           d = json.load(f)
       print('Token válido:', bool(d.get('token_uri')))
       print('Tem refresh_token:', bool(d.get('refresh_token')))
       "
       ```

    3. Confirmar canal no YouTube Studio:
       - https://studio.youtube.com
       - Canal aparece na lista com nome e status "verified"
  </how-to-verify>
  <resume-signal>
    Digite "phase 1 completa" se validate-infra.sh retornou 0 FAILs e o canal do YouTube está configurado.
    Descreva qualquer problema se algo ainda está pendente.
  </resume-signal>
</task>

</tasks>

<verification>
```bash
# Validação completa Phase 1
bash /Users/alessandrobm1/develop/server/wordpress/canaldecortes/scripts/validate-infra.sh

# Token válido
python3 -c "
import json
with open('/Users/alessandrobm1/develop/server/wordpress/canaldecortes/youtube/token.json') as f:
    d = json.load(f)
assert d.get('refresh_token'), 'FAIL: sem refresh_token'
print('PASS: token.json válido com refresh_token')
"

# Script sem erros de sintaxe
python3 -m py_compile /Users/alessandrobm1/develop/server/wordpress/canaldecortes/youtube/generate_token.py
```
</verification>

<success_criteria>
- generate_token.py existe, passa syntax check, usa access_type=offline + prompt=consent
- token.json existe com refresh_token (não apenas `{}`)
- Canal do YouTube existe com verificação SMS, nome e descrição preenchidos
- validate-infra.sh retorna 0 FAILs em todos os checks (INFRA-01 + INFRA-02 + INFRA-04)
- Phase 1 completa: todos os 4 requisitos (INFRA-01 a INFRA-04) satisfeitos
</success_criteria>

<output>
Após completar (incluindo aprovação final), criar /Users/alessandrobm1/develop/server/wordpress/canaldecortes/.planning/phases/01-infraestrutura-base/01-04-SUMMARY.md
</output>
