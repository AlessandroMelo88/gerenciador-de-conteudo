---
phase: 07-schema-multi-canal-python-pipeline
plan: "05"
type: execute
wave: 3
depends_on:
  - "07-02"
files_modified:
  - clip-processor/src/video_processor.py
  - clip-processor/src/metadata_generator.py
autonomous: true
requirements:
  - COPY-01
  - COPY-02

must_haves:
  truths:
    - "overlay_watermark() chama FFmpeg com -filter_complex overlay=W-w-20:20 quando watermark existe"
    - "overlay_watermark() retorna input_path sem chamar FFmpeg quando watermark ausente (graceful degradation)"
    - "append_credits() adiciona linha de créditos ao final da descrição via credit_template"
    - "append_credits() com template ou handle vazio retorna descrição sem modificação"
    - "process_clip() ainda passa em todos os testes existentes (sem regressão)"
  artifacts:
    - path: "clip-processor/src/video_processor.py"
      provides: "overlay_watermark() exportada"
      contains: "overlay_watermark"
    - path: "clip-processor/src/metadata_generator.py"
      provides: "append_credits() exportada"
      contains: "append_credits"
  key_links:
    - from: "video_processor.overlay_watermark"
      to: "FFmpeg -filter_complex overlay=W-w-20:20"
      via: "subprocess.run com dois inputs"
      pattern: "filter_complex.*overlay=W-w-20:20"
    - from: "metadata_generator.append_credits"
      to: "credit_template.format(channel_handle=handle)"
      via: "str.format nativo"
      pattern: "format.*channel_handle"
---

<objective>
Adicionar `overlay_watermark()` em `video_processor.py` (watermark FFmpeg com PNG alpha no canto superior direito) e `append_credits()` em `metadata_generator.py` (append de créditos pós-Claude). Ambas são funções independentes que o publisher chamará na Wave 4.

Purpose: COPY-01 (todo clip tem watermark do canal) e COPY-02 (toda descrição tem créditos do canal original).

Output: 2 módulos atualizados com novas funções exportadas e testes GREEN.
</objective>

<execution_context>
@/Users/alessandrobm1/.claude/get-shit-done/workflows/execute-plan.md
@/Users/alessandrobm1/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-schema-multi-canal-python-pipeline/07-CONTEXT.md
@.planning/phases/07-schema-multi-canal-python-pipeline/07-RESEARCH.md
@.planning/phases/07-schema-multi-canal-python-pipeline/07-02-SUMMARY.md

<interfaces>
<!-- Interfaces FFmpeg existentes e novos contratos -->

burn_subtitles atual (video_processor.py — padrão a seguir):
```python
def burn_subtitles(input_clip_path: str, srt_path: str, output_path: str) -> str:
    subprocess.run(
        ['ffmpeg', '-i', input_clip_path, '-vf', subtitle_filter,
         '-c:v', 'libx264', '-preset', 'veryfast', '-crf', '23',
         '-c:a', 'copy', output_path, '-y'],
        check=True, capture_output=True,
    )
    return output_path
```

ATENÇÃO FFmpeg — dois inputs obriga -filter_complex (não -vf):
```python
# clip PRIMEIRO, watermark SEGUNDO — W/H referencia o clip (primeiro input)
def overlay_watermark(input_path: str, watermark_path: str, output_path: str) -> str:
    if not os.path.exists(watermark_path):
        _log(f'[WATERMARK] Arquivo não encontrado: {watermark_path} — pulo overlay')
        return input_path  # graceful: retorna input sem modificar

    os.makedirs(os.path.dirname(output_path), exist_ok=True)
    subprocess.run(
        ['ffmpeg',
         '-i', input_path,        # [0] = clip vídeo
         '-i', watermark_path,    # [1] = PNG watermark
         '-filter_complex', 'overlay=W-w-20:20',  # canto sup direito, margem 20px
         '-c:v', 'libx264', '-preset', 'veryfast', '-crf', '23',
         '-c:a', 'copy',
         output_path, '-y'],
        check=True, capture_output=True,
    )
    return output_path
```

append_credits contrato (metadata_generator.py — novo):
```python
def append_credits(description: str, credit_template: str, channel_handle: str) -> str:
    """Adiciona linha de créditos ao final da descrição.
    Nunca sobrescreve conteúdo existente. Template vazio ou handle vazio = sem créditos."""
    if not credit_template or not channel_handle:
        return description
    credits_line = credit_template.format(channel_handle=channel_handle)
    return f'{description}\n\n{credits_line}'
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Adicionar overlay_watermark() em video_processor.py (COPY-01)</name>
  <files>clip-processor/src/video_processor.py</files>
  <behavior>
    - `overlay_watermark(input, wm_path, out)` com wm_path existente (mock `os.path.exists=True`) chama `subprocess.run` com args contendo `-filter_complex` e `'overlay=W-w-20:20'` e `clip ANTES do watermark` nos `-i` args
    - `overlay_watermark(input, wm_path, out)` com wm_path ausente (mock `os.path.exists=False`) retorna `input_path` e NÃO chama `subprocess.run`
    - `overlay_watermark` aparece no docstring do módulo (linha com `- overlay_watermark(...)`)
    - Função é exportável: `from src.video_processor import overlay_watermark` não dá erro
  </behavior>
  <action>
Ler `clip-processor/src/video_processor.py` completo. Adicionar `overlay_watermark()` como nova função pública após `burn_subtitles`. Atualizar o docstring do módulo no topo do arquivo para incluir `- overlay_watermark(input_path, watermark_path, output_path) -> str`.

Implementar exatamente conforme o contrato da seção `<interfaces>`:
- `os.path.exists(watermark_path)` como guard
- `os.makedirs(os.path.dirname(output_path), exist_ok=True)`
- `subprocess.run(['ffmpeg', '-i', input_path, '-i', watermark_path, '-filter_complex', 'overlay=W-w-20:20', '-c:v', 'libx264', '-preset', 'veryfast', '-crf', '23', '-c:a', 'copy', output_path, '-y'], check=True, capture_output=True)`
- Retornar `output_path` em sucesso, `input_path` em watermark ausente

NÃO modificar `process_clip()` ainda — a integração do watermark no fluxo de corte acontece no Plan 07-06 quando o `destination_channel_slug` estará disponível.
  </action>
  <verify>
    <automated>docker exec clip-processor python -m pytest tests/test_video_processor.py -v --tb=short -k "watermark"</automated>
  </verify>
  <done>
Testes TestOverlayWatermark: PASS. Testes existentes de video_processor: PASS. Zero regressões.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Adicionar append_credits() em metadata_generator.py (COPY-02)</name>
  <files>clip-processor/src/metadata_generator.py</files>
  <behavior>
    - `append_credits('Descrição', 'Créditos: @{channel_handle}', 'sportv')` retorna `'Descrição\n\nCréditos: @sportv'`
    - `append_credits('Descrição', '', 'sportv')` retorna `'Descrição'` sem modificação
    - `append_credits('Descrição', 'Créditos: @{channel_handle}', '')` retorna `'Descrição'` sem modificação
    - `from src.metadata_generator import append_credits` funciona sem erro
  </behavior>
  <action>
Ler `clip-processor/src/metadata_generator.py` completo. Adicionar `append_credits()` como nova função pública após `update_clip_metadata`. Atualizar o docstring do módulo no topo do arquivo para incluir `- append_credits(description, credit_template, channel_handle) -> str`.

Implementar exatamente conforme o contrato da seção `<interfaces>`:
```python
def append_credits(description: str, credit_template: str, channel_handle: str) -> str:
    """Adiciona linha de créditos ao final da descrição.
    
    Nunca sobrescreve conteúdo existente.
    Retorna descrição sem modificação se template ou handle estiverem vazios.
    """
    if not credit_template or not channel_handle:
        return description
    credits_line = credit_template.format(channel_handle=channel_handle)
    return f'{description}\n\n{credits_line}'
```

NÃO modificar `generate_metadata()` ou `update_clip_metadata()` — a chamada de `append_credits` dentro do fluxo de publicação acontece no Plan 07-06 quando o `credit_template` e `channel_handle` estarão disponíveis.
  </action>
  <verify>
    <automated>docker exec clip-processor python -m pytest tests/test_metadata_generator.py -v --tb=short -k "credits"</automated>
  </verify>
  <done>
Testes TestAppendCredits: PASS. Testes existentes de metadata_generator: PASS. Zero regressões.
  </done>
</task>

</tasks>

<verification>
```bash
# Verificar os dois módulos e suíte total
docker exec clip-processor python -m pytest tests/test_video_processor.py tests/test_metadata_generator.py -v --tb=short
```
</verification>

<success_criteria>
- `test_video_processor.py`: todos GREEN (incluindo TestOverlayWatermark)
- `test_metadata_generator.py`: todos GREEN (incluindo TestAppendCredits)
- Suíte total: zero novas regressões
</success_criteria>

<output>
Após conclusão, criar `.planning/phases/07-schema-multi-canal-python-pipeline/07-05-SUMMARY.md`
</output>
