---
phase: 07-schema-multi-canal-python-pipeline
plan: "02"
type: execute
wave: 2
depends_on:
  - "07-01"
files_modified:
  - clip-processor/tests/test_quota_manager.py
  - clip-processor/tests/test_uploader.py
  - clip-processor/tests/test_publisher.py
  - clip-processor/tests/test_video_processor.py
  - clip-processor/tests/test_metadata_generator.py
  - clip-processor/tests/test_rss_poller.py
autonomous: true
requirements:
  - MCAN-01
  - MCAN-02
  - MCAN-03
  - MCAN-04
  - COPY-01
  - COPY-02
  - COPY-03

must_haves:
  truths:
    - "Todos os 6 arquivos de teste têm novos testes em RED state (falham com ImportError ou AttributeError antes da implementação)"
    - "Nenhum teste existente foi quebrado pelos novos testes"
    - "Cada requirement MCAN/COPY tem ao menos um teste correspondente"
  artifacts:
    - path: "clip-processor/tests/test_quota_manager.py"
      provides: "Testes RED para channel_id no QuotaManager"
      contains: "channel_id"
    - path: "clip-processor/tests/test_publisher.py"
      provides: "Testes RED para multi-canal e destination_channel_id"
      contains: "destination_channel"
    - path: "clip-processor/tests/test_video_processor.py"
      provides: "Testes RED para overlay_watermark"
      contains: "overlay_watermark"
    - path: "clip-processor/tests/test_metadata_generator.py"
      provides: "Testes RED para append_credits"
      contains: "append_credits"
    - path: "clip-processor/tests/test_rss_poller.py"
      provides: "Testes RED para blacklist guard"
      contains: "blacklist"
    - path: "clip-processor/tests/test_uploader.py"
      provides: "Testes RED para channel_slug token path"
      contains: "channel_slug"
  key_links:
    - from: "tests/test_publisher.py::SAMPLE_CLIP"
      to: "destination_channel_id"
      via: "campo adicionado ao dict SAMPLE_CLIP"
      pattern: "destination_channel_id"
---

<objective>
Estender os 6 arquivos de teste existentes com novos testes em RED state que cobrem todos os behaviors multi-canal a serem implementados nas Waves 3-4.

Purpose: TDD — os testes devem falhar (RED) antes de qualquer implementação. Cada teste descreve o contrato esperado. A suíte existente continua GREEN; apenas os novos testes estão RED.

Output: 6 arquivos de teste atualizados com ~15-20 novos testes no total, todos falhando até implementação.
</objective>

<execution_context>
@/Users/alessandrobm1/.claude/get-shit-done/workflows/execute-plan.md
@/Users/alessandrobm1/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-schema-multi-canal-python-pipeline/07-CONTEXT.md
@.planning/phases/07-schema-multi-canal-python-pipeline/07-RESEARCH.md
@.planning/phases/07-schema-multi-canal-python-pipeline/07-VALIDATION.md
@.planning/phases/07-schema-multi-canal-python-pipeline/07-01-SUMMARY.md

<interfaces>
<!-- Padrões de teste estabelecidos no projeto -->

De clip-processor/tests/test_quota_manager.py (padrão de mock Redis):
```python
def make_redis(count=None):
    r = MagicMock()
    r.get.return_value = str(count) if count is not None else None
    r.incr.return_value = (count or 0) + 1
    r.expire.return_value = True
    return r

def dt_sp(hour, minute=0):
    return datetime(2026, 6, 18, hour, minute, 0, tzinfo=TZ_SP)

# Instanciação atual (SEM channel_id):
qm = QuotaManager(r, max_uploads_per_day=2)
```

De clip-processor/tests/test_publisher.py (SAMPLE_CLIP atual — SEM destination_channel_id):
```python
SAMPLE_CLIP = {
    'id': 1,
    'source_video_id': 10,
    'clip_path': '/app/videos/clips/clip_001.mp4',
    'thumbnail_path': '/app/videos/thumbnails/clip_001.jpg',
    'title': 'Gol incrível do Vini Jr',
    'description': 'Descrição do clip',
    'tags': 'futebol,gol,vini',
}
```

De clip-processor/tests/test_video_processor.py (padrão mock subprocess):
```python
# Testes existentes mockam subprocess.run via mocker.patch
```

De clip-processor/tests/test_rss_poller.py (padrão mock HTTP + DB):
```python
# Testes existentes mockam requests.get e cursor.fetchall
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Estender test_quota_manager.py e test_uploader.py (MCAN-01, MCAN-03)</name>
  <files>clip-processor/tests/test_quota_manager.py, clip-processor/tests/test_uploader.py</files>
  <behavior>
    - test_quota_manager.py — novos testes RED:
      1. `QuotaManager(r, channel_id='UCabc123')._key(dt_sp(20))` retorna `'youtube_uploads:UCabc123:2026-06-18'` (MCAN-03)
      2. `QuotaManager(r)._key(dt_sp(20))` sem channel_id retorna `'youtube_uploads:2026-06-18'` (retrocompat fallback)
      3. `QuotaManager(r, channel_id='UCaaa').can_upload(dt_sp(20))` e `QuotaManager(r, channel_id='UCbbb').can_upload(dt_sp(20))` usam keys Redis diferentes — independência de quota (MCAN-04)
    - test_uploader.py — novos testes RED:
      1. `YouTubeUploader(channel_slug='futebol-em-cortes').token_file` é `'/app/youtube/token-futebol-em-cortes.json'` (MCAN-01)
      2. `YouTubeUploader()` sem channel_slug usa `DEFAULT_TOKEN_FILE` (retrocompat)
      3. `YouTubeUploader(token_file='/custom/path.json')` usa o path explícito (retrocompat)
  </behavior>
  <action>
Ler os arquivos atuais completos antes de modificar. Adicionar novos testes ao FINAL de cada arquivo (após as classes existentes), em nova classe separada: `class TestQuotaManagerMultiCanal` e `class TestYouTubeUploaderChannelSlug`.

Para test_quota_manager.py, importar `QuotaManager` (já importado). Os testes novos vão falhar com `TypeError` porque `QuotaManager.__init__` ainda não aceita `channel_id`.

Para test_uploader.py, importar `YouTubeUploader, DEFAULT_TOKEN_FILE` (já importado ou adicionar import). Os testes novos vão falhar com `TypeError` porque `YouTubeUploader.__init__` ainda não aceita `channel_slug`.

Não alterar nenhum teste existente — apenas ADICIONAR ao final.
  </action>
  <verify>
    <automated>docker exec clip-processor python -m pytest tests/test_quota_manager.py tests/test_uploader.py -v --tb=short 2>&1 | tail -30</automated>
  </verify>
  <done>
- Testes existentes: PASS (suíte não foi quebrada)
- Novos testes TestQuotaManagerMultiCanal e TestYouTubeUploaderChannelSlug: FAIL com TypeError (RED state correto)
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Estender test_publisher.py, test_video_processor.py, test_metadata_generator.py, test_rss_poller.py (MCAN-02/04, COPY-01/02/03)</name>
  <files>
    clip-processor/tests/test_publisher.py,
    clip-processor/tests/test_video_processor.py,
    clip-processor/tests/test_metadata_generator.py,
    clip-processor/tests/test_rss_poller.py
  </files>
  <behavior>
    - test_publisher.py:
      1. Adicionar `destination_channel_id: 1, channel_handle: '@sportv'` ao SAMPLE_CLIP existente (não quebra testes atuais que não verificam esses campos)
      2. Nova classe `TestPublisherMultiCanal`:
         - `_fetch_pending_clips_for_channel(conn, dest_id=1)` retorna clips com `destination_channel_id=1` e não retorna clips com `destination_channel_id=2`
         - Dois canais-destino ativos: atingir quota no canal 1 não bloqueia canal 2
    - test_video_processor.py:
      1. Nova classe `TestOverlayWatermark`:
         - `overlay_watermark(input_path, wm_path, out_path)` com wm_path existente (mock `os.path.exists=True`) chama `subprocess.run` com `-filter_complex` e `overlay=W-w-20:20`
         - `overlay_watermark(input_path, wm_path_missing, out_path)` com wm_path ausente (mock `os.path.exists=False`) retorna `input_path` sem chamar subprocess
    - test_metadata_generator.py:
      1. Nova classe `TestAppendCredits`:
         - `append_credits('Descrição do clip', 'Créditos: @{channel_handle}', 'sportv')` retorna `'Descrição do clip\n\nCréditos: @sportv'`
         - `append_credits('Descrição', '', 'sportv')` retorna `'Descrição'` sem modificação (template vazio)
         - `append_credits('Descrição', 'Créditos: @{channel_handle}', '')` retorna `'Descrição'` sem modificação (handle vazio)
    - test_rss_poller.py:
      1. Nova classe `TestBlacklistGuard`:
         - `poll_all_channels` com canal onde `blacklisted=TRUE` no mock do cursor não chama `insert_video`
         - `poll_all_channels` com canal onde `blacklisted=FALSE` chama `insert_video` normalmente

Para test_publisher.py: atualizar `SAMPLE_CLIP` adicionando os campos novos (verificar se algum teste existente quebraria — os testes existentes verificam comportamento via mock, não o dict em si, então é seguro).

Para test_rss_poller.py: o mock de `cursor.fetchall` precisa incluir `blacklisted` e `target_niche` no dict do canal.

Todos os novos testes falham com `AttributeError: module has no attribute 'overlay_watermark'` ou `TypeError` — RED state correto.
  </behavior>
  <action>
Ler cada arquivo atual completo antes de modificar. Adicionar novos testes ao FINAL de cada arquivo em nova classe separada. Não alterar testes existentes.

Para test_rss_poller.py, o mock do SELECT de canais deve retornar dicts com `{'id': 1, 'channel_name': 'Canal Bloqueado', 'rss_url': '...', 'blacklisted': True, 'target_niche': 'futebol'}`. O teste verifica que `insert_video` NÃO é chamado quando `blacklisted=True`.

Para test_video_processor.py, usar `mocker.patch('os.path.exists', return_value=True/False)` e `mocker.patch('subprocess.run')`.

Para test_metadata_generator.py, importar `append_credits` de `src.metadata_generator` — vai falhar com ImportError (RED state).
  </action>
  <verify>
    <automated>docker exec clip-processor python -m pytest tests/test_publisher.py tests/test_video_processor.py tests/test_metadata_generator.py tests/test_rss_poller.py -v --tb=short 2>&1 | tail -40</automated>
  </verify>
  <done>
- Testes existentes: PASS (suíte não foi quebrada)
- Novos testes em cada arquivo: FAIL com AttributeError, ImportError ou TypeError (RED state correto)
- Total da suíte completa: pytest -v mostra "X passed, Y failed" onde Y são apenas os novos testes
  </done>
</task>

</tasks>

<verification>
```bash
# Verificar estado completo da suíte após Wave 0
docker exec clip-processor python -m pytest -v --tb=short 2>&1 | grep -E "PASSED|FAILED|ERROR" | tail -50

# Confirmar que apenas os novos testes falham (os existentes continuam GREEN)
docker exec clip-processor python -m pytest tests/ -q --tb=line 2>&1 | tail -20
```
</verification>

<success_criteria>
- Suíte completa: todos os testes existentes PASS, apenas os novos testes falham
- Novos testes cobrem: MCAN-01 (channel_slug), MCAN-02 (destination_channel_id), MCAN-03 (channel_id key), MCAN-04 (quota independente), COPY-01 (overlay_watermark), COPY-02 (append_credits), COPY-03 (blacklist guard)
- Nenhum arquivo de teste novo foi criado — extensões apenas em arquivos existentes
</success_criteria>

<output>
Após conclusão, criar `.planning/phases/07-schema-multi-canal-python-pipeline/07-02-SUMMARY.md`
</output>
