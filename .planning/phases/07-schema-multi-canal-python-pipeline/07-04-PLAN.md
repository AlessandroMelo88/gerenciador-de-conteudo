---
phase: 07-schema-multi-canal-python-pipeline
plan: "04"
type: execute
wave: 3
depends_on:
  - "07-02"
files_modified:
  - clip-processor/src/rss_poller.py
autonomous: true
requirements:
  - MCAN-02
  - COPY-03

must_haves:
  truths:
    - "rss_poller não processa canais com blacklisted=TRUE — insert_video não é chamado para vídeos desses canais"
    - "SELECT de source_channels inclui target_niche e channel_handle nas colunas retornadas"
    - "Canais com blacklisted=FALSE continuam sendo processados normalmente (sem regressão)"
  artifacts:
    - path: "clip-processor/src/rss_poller.py"
      provides: "Blacklist guard + target_niche no SELECT"
      contains: "blacklisted"
  key_links:
    - from: "rss_poller.poll_all_channels"
      to: "source_channels WHERE active=TRUE AND blacklisted=FALSE"
      via: "SELECT estendido"
      pattern: "blacklisted = FALSE"
    - from: "channel['target_niche']"
      to: "roteamento no selector"
      via: "coluna retornada no SELECT"
      pattern: "target_niche"
---

<objective>
Estender `rss_poller.py` com dois comportamentos: (1) filtrar canais blacklistados no SELECT de canais ativos, e (2) incluir `target_niche` e `channel_handle` nas colunas retornadas para uso no roteamento.

Purpose: COPY-03 (canais blacklistados nunca têm vídeos baixados) e MCAN-02 (pipeline sabe o nicho do canal-fonte para roteamento).

Output: `rss_poller.py` atualizado com testes GREEN.
</objective>

<execution_context>
@/Users/alessandrobm1/.claude/get-shit-done/workflows/execute-plan.md
@/Users/alessandrobm1/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-schema-multi-canal-python-pipeline/07-CONTEXT.md
@.planning/phases/07-schema-multi-canal-python-pipeline/07-RESEARCH.md
@.planning/phases/07-schema-multi-canal-python-pipeline/07-02-SUMMARY.md

<interfaces>
<!-- SELECT atual em rss_poller.py que será modificado -->

SELECT atual (linha ~149 em rss_poller.py):
```python
cur.execute('SELECT id, channel_name, rss_url FROM source_channels WHERE active = TRUE')
```

SELECT Phase 7 (conforme Pattern 6 do RESEARCH.md):
```python
cur.execute(
    'SELECT id, channel_name, rss_url, target_niche, channel_handle '
    'FROM source_channels '
    'WHERE active = TRUE AND blacklisted = FALSE'
)
```

Nota: O filtro `blacklisted = FALSE` está no SELECT de canais, não por entrada individual do feed.
Um canal blacklisted não é processado de forma alguma — nem seus vídeos individuais são considerados.

Log a adicionar quando canal seria processado mas está blacklisted:
Não é necessário log aqui — o canal simplesmente não aparece no resultado do SELECT.
Para observability, o operador pode verificar via SQL: SELECT channel_name FROM source_channels WHERE blacklisted=TRUE.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Atualizar SELECT de canais com blacklist guard e target_niche (COPY-03, MCAN-02)</name>
  <files>clip-processor/src/rss_poller.py</files>
  <behavior>
    - `poll_all_channels` com cursor mockado retornando canal com `blacklisted=True` → `insert_video` NÃO é chamado
    - `poll_all_channels` com cursor mockado retornando canal com `blacklisted=False` → `insert_video` é chamado normalmente (sem regressão)
    - `channel` dict disponível dentro do loop tem `target_niche` e `channel_handle` (para uso futuro pelo selector)
  </behavior>
  <action>
Ler `clip-processor/src/rss_poller.py` completo. Fazer exatamente 1 modificação cirúrgica:

Localizar a linha:
```python
cur.execute('SELECT id, channel_name, rss_url FROM source_channels WHERE active = TRUE')
```

Substituir por:
```python
cur.execute(
    'SELECT id, channel_name, rss_url, target_niche, channel_handle '
    'FROM source_channels '
    'WHERE active = TRUE AND blacklisted = FALSE'
)
```

Essa é a única alteração necessária. O dict `channel` dentro do loop passa a ter os campos `target_niche` e `channel_handle` disponíveis. Canais blacklistados simplesmente não aparecem no resultado do SELECT — não há código extra necessário.

Nenhuma outra alteração. Não modificar `_process_ai_pipeline`, `insert_video`, loop de entries, ou qualquer outra lógica.

Atualizar o docstring do módulo (linha 6: `- poll_all_channels(db_conn=None, redis_client=None)`) para mencionar que canais blacklistados são filtrados.
  </action>
  <verify>
    <automated>docker exec clip-processor python -m pytest tests/test_rss_poller.py -v --tb=short</automated>
  </verify>
  <done>
Todos os testes de test_rss_poller.py passam (incluindo TestBlacklistGuard). Zero regressões.
  </done>
</task>

</tasks>

<verification>
```bash
# Verificar que a suíte completa não regrediu
docker exec clip-processor python -m pytest tests/test_rss_poller.py tests/test_publisher.py -v --tb=short
```
</verification>

<success_criteria>
- `test_rss_poller.py`: todos GREEN (incluindo TestBlacklistGuard com blacklisted=True bloqueado)
- Zero regressões na suíte total
</success_criteria>

<output>
Após conclusão, criar `.planning/phases/07-schema-multi-canal-python-pipeline/07-04-SUMMARY.md`
</output>
