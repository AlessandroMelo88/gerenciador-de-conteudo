---
phase: 07-schema-multi-canal-python-pipeline
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - mysql/init/06-multi-canal-migration.sql
autonomous: true
requirements:
  - MCAN-01
  - MCAN-02
  - MCAN-03
  - COPY-02
  - COPY-03

must_haves:
  truths:
    - "Tabela destination_channels existe com colunas id, slug, name, niche, youtube_channel_id, credit_template, active"
    - "source_channels tem colunas target_niche, channel_handle e blacklisted"
    - "generated_clips tem coluna destination_channel_id com FK para destination_channels"
    - "Seed inicial popula 2 canais destino (futebol, podcast) e marca canais blacklistados"
  artifacts:
    - path: "mysql/init/06-multi-canal-migration.sql"
      provides: "Migration idempotente para multi-canal"
      min_lines: 80
  key_links:
    - from: "generated_clips.destination_channel_id"
      to: "destination_channels.id"
      via: "FK INT com INDEX"
      pattern: "FOREIGN KEY.*destination_channels"
    - from: "source_channels.target_niche"
      to: "destination_channels.niche"
      via: "JOIN no publisher e selector"
      pattern: "target_niche"
---

<objective>
Criar a migration SQL idempotente que habilita o suporte multi-canal: nova tabela `destination_channels`, três novas colunas em `source_channels` (`target_niche`, `channel_handle`, `blacklisted`) e uma nova coluna em `generated_clips` (`destination_channel_id` FK).

Purpose: Todas as fases seguintes (QuotaManager, publisher, selector, rss_poller) dependem deste schema. A migration deve ser aplicada antes dos testes Wave 0.

Output: `mysql/init/06-multi-canal-migration.sql` — aplicado automaticamente pelo MySQL init ao criar o container, e manualmente via `docker exec mysql mysql -u root -p$MYSQL_ROOT_PASSWORD clips_automation < /docker-entrypoint-initdb.d/06-multi-canal-migration.sql` em ambientes já rodando.
</objective>

<execution_context>
@/Users/alessandrobm1/.claude/get-shit-done/workflows/execute-plan.md
@/Users/alessandrobm1/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/07-schema-multi-canal-python-pipeline/07-CONTEXT.md
@.planning/phases/07-schema-multi-canal-python-pipeline/07-RESEARCH.md

<interfaces>
<!-- Padrão idempotente estabelecido nas Fases 3-5 (INFORMATION_SCHEMA + prepared statements) -->

Padrão ADD COLUMN idempotente (Source: mysql/init/03-schema-migration.sql):
```sql
SET @has_col = (
  SELECT COUNT(*)
  FROM INFORMATION_SCHEMA.COLUMNS
  WHERE TABLE_SCHEMA = 'clips_automation'
    AND TABLE_NAME = 'source_channels'
    AND COLUMN_NAME = 'target_niche'
);
SET @sql = IF(
  @has_col = 0,
  'ALTER TABLE source_channels ADD COLUMN target_niche VARCHAR(50) NULL AFTER channel_name',
  'SELECT 1'
);
PREPARE stmt FROM @sql;
EXECUTE stmt;
DEALLOCATE PREPARE stmt;
```

Schema destination_channels decidido (Source: 07-CONTEXT.md):
```sql
CREATE TABLE IF NOT EXISTS destination_channels (
  id                 INT AUTO_INCREMENT PRIMARY KEY,
  slug               VARCHAR(50) UNIQUE NOT NULL,
  name               VARCHAR(120) NOT NULL,
  niche              VARCHAR(50) NOT NULL,
  youtube_channel_id VARCHAR(50) UNIQUE NOT NULL,
  credit_template    TEXT,
  active             BOOLEAN DEFAULT TRUE,
  created_at         TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
  updated_at         TIMESTAMP DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP
);
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Criar migration idempotente 06-multi-canal-migration.sql</name>
  <files>mysql/init/06-multi-canal-migration.sql</files>
  <action>
Criar `mysql/init/06-multi-canal-migration.sql` com as seguintes seções em ordem:

**Seção 1 — Criar tabela destination_channels (IF NOT EXISTS):**
```sql
CREATE TABLE IF NOT EXISTS destination_channels (
  id                 INT AUTO_INCREMENT PRIMARY KEY,
  slug               VARCHAR(50) UNIQUE NOT NULL,
  name               VARCHAR(120) NOT NULL,
  niche              VARCHAR(50) NOT NULL,
  youtube_channel_id VARCHAR(50) UNIQUE NOT NULL,
  credit_template    TEXT,
  active             BOOLEAN DEFAULT TRUE,
  created_at         TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
  updated_at         TIMESTAMP DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP
);
```

**Seção 2 — ALTER source_channels (3 colunas, cada uma com bloco INFORMATION_SCHEMA idempotente):**
- `target_niche VARCHAR(50) NULL` — qual nicho esse canal-fonte alimenta
- `channel_handle VARCHAR(100) NULL` — ex: `@sportv` para créditos na descrição
- `blacklisted BOOLEAN DEFAULT FALSE NOT NULL` — bloqueia download no rss_poller

Para cada coluna, usar o padrão INFORMATION_SCHEMA + prepared statement (ver Padrão ADD COLUMN acima). Para `blacklisted`, adicionar `INDEX idx_blacklisted (blacklisted)` via bloco idempotente separado.

**Seção 3 — ALTER generated_clips (1 coluna):**
- `destination_channel_id INT NULL` com FK `FOREIGN KEY (destination_channel_id) REFERENCES destination_channels(id)` — adicionar FK em bloco separado via INFORMATION_SCHEMA no `TABLE_CONSTRAINTS` para idempotência.

Usar o padrão:
```sql
SET @has_fk = (
  SELECT COUNT(*) FROM INFORMATION_SCHEMA.TABLE_CONSTRAINTS
  WHERE TABLE_SCHEMA = 'clips_automation'
    AND TABLE_NAME = 'generated_clips'
    AND CONSTRAINT_NAME = 'fk_generated_clips_destination_channel'
);
SET @sql = IF(
  @has_fk = 0,
  'ALTER TABLE generated_clips ADD CONSTRAINT fk_generated_clips_destination_channel FOREIGN KEY (destination_channel_id) REFERENCES destination_channels(id)',
  'SELECT 1'
);
PREPARE stmt FROM @sql;
EXECUTE stmt;
DEALLOCATE PREPARE stmt;
```

**Seção 4 — Seed destination_channels (INSERT IGNORE para idempotência):**
```sql
INSERT IGNORE INTO destination_channels (slug, name, niche, youtube_channel_id, credit_template, active)
VALUES
  ('futebol-em-cortes',  'Futebol em Cortes',  'futebol',  'UC_PLACEHOLDER_FUTEBOL',  'Créditos: @{channel_handle}',  TRUE),
  ('podcast-cortes',     'Podcast Cortes',      'podcast',  'UC_PLACEHOLDER_PODCAST',   'Créditos: @{channel_handle}',  TRUE);
```
Nota: `youtube_channel_id` usa placeholder `UC_PLACEHOLDER_*` — operador substitui antes de ativar o canal real.

**Seção 5 — UPDATE source_channels com blacklist inicial e target_niche padrão:**
Usar UPDATE com WHERE para não sobrescrever dados existentes:
```sql
-- Canais blacklistados (IDs placeholder — operador preenche com IDs reais)
-- UPDATE source_channels SET blacklisted = TRUE WHERE channel_name IN ('Globo Esporte','SBT','Band');

-- Todos os canais ativos existentes recebem target_niche='futebol' como padrão se ainda NULL
UPDATE source_channels SET target_niche = 'futebol' WHERE target_niche IS NULL AND active = TRUE;
```
O UPDATE de blacklist fica comentado — operador descomenta com IDs reais. Isso evita blacklistar acidentalmente canais em ambiente de dev.

O arquivo deve ter cabeçalho comentado:
```sql
-- Phase 7: Schema Multi-Canal Migration
-- Idempotente — pode ser re-executado sem erro
-- Padrão: INFORMATION_SCHEMA + prepared statements (ver fases 3-5)
```
  </action>
  <verify>
    <automated>
docker exec mysql mysql -u root -p"${MYSQL_ROOT_PASSWORD}" -e "
  SELECT TABLE_NAME FROM INFORMATION_SCHEMA.TABLES WHERE TABLE_SCHEMA='clips_automation' AND TABLE_NAME='destination_channels';
  SELECT COLUMN_NAME FROM INFORMATION_SCHEMA.COLUMNS WHERE TABLE_SCHEMA='clips_automation' AND TABLE_NAME='source_channels' AND COLUMN_NAME IN ('target_niche','channel_handle','blacklisted');
  SELECT COLUMN_NAME FROM INFORMATION_SCHEMA.COLUMNS WHERE TABLE_SCHEMA='clips_automation' AND TABLE_NAME='generated_clips' AND COLUMN_NAME='destination_channel_id';
" clips_automation 2>/dev/null || echo "APPLY MIGRATION FIRST: docker exec -i mysql mysql -u root -p\${MYSQL_ROOT_PASSWORD} clips_automation < mysql/init/06-multi-canal-migration.sql"
    </automated>
  </verify>
  <done>
- `mysql/init/06-multi-canal-migration.sql` criado e válido
- Ao aplicar: tabela `destination_channels` existe, `source_channels` tem as 3 novas colunas, `generated_clips` tem `destination_channel_id` com FK
- Seed inseriu 2 linhas em `destination_channels` (INSERT IGNORE = idempotente)
- Re-executar a migration não gera erro
  </done>
</task>

</tasks>

<verification>
```bash
# Aplicar migration no MySQL rodando
docker exec -i mysql mysql -u root -p${MYSQL_ROOT_PASSWORD} clips_automation < mysql/init/06-multi-canal-migration.sql

# Verificar schema resultante
docker exec mysql mysql -u root -p${MYSQL_ROOT_PASSWORD} -e "DESCRIBE destination_channels; DESCRIBE source_channels; DESCRIBE generated_clips;" clips_automation

# Verificar seed
docker exec mysql mysql -u root -p${MYSQL_ROOT_PASSWORD} -e "SELECT id, slug, niche FROM destination_channels;" clips_automation

# Idempotência: re-executar não deve dar erro
docker exec -i mysql mysql -u root -p${MYSQL_ROOT_PASSWORD} clips_automation < mysql/init/06-multi-canal-migration.sql && echo "IDEMPOTENT OK"
```
</verification>

<success_criteria>
- `destination_channels` tem todas as colunas do schema decidido no CONTEXT.md
- `source_channels` tem `target_niche`, `channel_handle`, `blacklisted`
- `generated_clips` tem `destination_channel_id INT NULL` com FK
- Migration re-executada: zero erros
- Seed: 2 canais destino com `active=TRUE`
</success_criteria>

<output>
Após conclusão, criar `.planning/phases/07-schema-multi-canal-python-pipeline/07-01-SUMMARY.md`
</output>
