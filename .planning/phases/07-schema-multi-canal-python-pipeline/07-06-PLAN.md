---
phase: 07-schema-multi-canal-python-pipeline
plan: "06"
type: execute
wave: 4
depends_on:
  - "07-03"
  - "07-04"
  - "07-05"
files_modified:
  - clip-processor/src/selector.py
  - clip-processor/src/video_processor.py
  - clip-processor/src/publisher.py
autonomous: true
requirements:
  - MCAN-01
  - MCAN-02
  - MCAN-03
  - MCAN-04
  - COPY-01
  - COPY-02

must_haves:
  truths:
    - "insert_selected_moments persiste destination_channel_id em generated_clips via JOIN com destination_channels"
    - "process_clip aplica overlay_watermark no clip após burn_subtitles usando slug do canal-destino"
    - "publish_pending_clips itera por canais-destino ativos, instanciando QuotaManager e YouTubeUploader por canal"
    - "créditos são anexados à descrição antes do upload via append_credits"
    - "quota atingida no canal A não bloqueia canal B (independência verificável via dois QuotaManagers separados)"
  artifacts:
    - path: "clip-processor/src/selector.py"
      provides: "insert_selected_moments com destination_channel_id"
      contains: "destination_channel_id"
    - path: "clip-processor/src/video_processor.py"
      provides: "process_clip integrado com overlay_watermark"
      contains: "overlay_watermark"
    - path: "clip-processor/src/publisher.py"
      provides: "publish_pending_clips multi-canal com loop por destination_channel"
      contains: "_fetch_destination_channels"
  key_links:
    - from: "selector.insert_selected_moments"
      to: "generated_clips.destination_channel_id"
      via: "JOIN source_channels → destination_channels WHERE niche = target_niche"
      pattern: "destination_channel_id"
    - from: "publisher._fetch_destination_channels"
      to: "destination_channels WHERE active=TRUE"
      via: "SELECT no publisher"
      pattern: "_fetch_destination_channels"
    - from: "publisher.publish_pending_clips"
      to: "QuotaManager(redis, channel_id=dest['youtube_channel_id'])"
      via: "instanciação por canal no loop"
      pattern: "channel_id=dest"
    - from: "video_processor.process_clip"
      to: "overlay_watermark(subtitled_path, watermark_path, final_path)"
      via: "destination_channel_slug via JOIN em _fetch_clip"
      pattern: "overlay_watermark"
---

<objective>
Integrar todos os módulos das Waves 1-3 na cadeia de publicação: (1) selector persiste `destination_channel_id` em `generated_clips`, (2) process_clip aplica watermark no clip final, (3) publisher itera por canais-destino com quota e uploader independentes por canal e appenda créditos na descrição.

Purpose: Este é o plano de integração — onde todas as peças se conectam para implementar MCAN-01..04 e COPY-01..02 end-to-end.

Output: 3 módulos atualizados (selector, video_processor, publisher) com suíte GREEN completa.
</objective>

<execution_context>
@/Users/alessandrobm1/.claude/get-shit-done/workflows/execute-plan.md
@/Users/alessandrobm1/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-schema-multi-canal-python-pipeline/07-CONTEXT.md
@.planning/phases/07-schema-multi-canal-python-pipeline/07-RESEARCH.md
@.planning/phases/07-schema-multi-canal-python-pipeline/07-03-SUMMARY.md
@.planning/phases/07-schema-multi-canal-python-pipeline/07-04-SUMMARY.md
@.planning/phases/07-schema-multi-canal-python-pipeline/07-05-SUMMARY.md

<interfaces>
<!-- Contratos atualizados de cada módulo -->

selector.py — insert_selected_moments atual:
```python
def insert_selected_moments(conn, source_video_id: int, video_id: str, moments: list[dict]) -> int:
    # INSERT INTO generated_clips (source_video_id, start_time, end_time, score, reason, status)
    # SEM destination_channel_id
```

selector.py — Phase 7 deve persistir destination_channel_id:
O `source_video_id` já é INT FK. Para obter `destination_channel_id`:
```sql
SELECT sc.id as channel_id, sc.target_niche
FROM source_videos sv
JOIN source_channels sc ON sc.id = sv.channel_id
WHERE sv.id = {source_video_id}
```
Depois:
```sql
SELECT id FROM destination_channels
WHERE niche = {target_niche} AND active = TRUE
LIMIT 1
```
Persistir no INSERT:
```sql
INSERT INTO generated_clips (source_video_id, start_time, end_time, score, reason, status, destination_channel_id)
VALUES (%s, %s, %s, %s, %s, 'pending_cut', %s)
```
Edge case: `target_niche` NULL ou sem `destination_channel` ativo → `destination_channel_id=NULL` (inserção continua).

video_processor.py — _fetch_clip atual (não tem destination_channel_slug):
```python
def _fetch_clip(conn, clip_id: int) -> dict | None:
    # SELECT gc.id, gc.source_video_id, gc.start_time, gc.end_time,
    #   gc.score, gc.transcript_path, sv.local_path AS source_local_path, ...
    # SEM destination_channel_slug
```

video_processor.py — Phase 7 estende _fetch_clip com JOIN:
```sql
SELECT gc.id, gc.source_video_id, gc.start_time, gc.end_time,
  gc.score, gc.transcript_path,
  sv.local_path AS source_local_path,
  dc.slug AS destination_channel_slug
FROM generated_clips gc
JOIN source_videos sv ON sv.id = gc.source_video_id
LEFT JOIN destination_channels dc ON dc.id = gc.destination_channel_id
WHERE gc.id = {clip_id}
```
Watermark path derivado: `/app/branding/watermark-{destination_channel_slug}.png` (None se slug NULL).
Ordem no process_clip: `burn_subtitles` → arquivo intermediário `{clip_id}_subtitled.mp4` → `overlay_watermark` → `final_clip_path`.
Se watermark ausente/NULL: `overlay_watermark` retorna `subtitled_path` → renomear para `final_clip_path`.
Limpar arquivo intermediário `_subtitled.mp4` em ambos os casos.

publisher.py — novo fluxo multi-canal (conforme Pattern 7 do RESEARCH.md):
```python
def publish_pending_clips(conn, redis_client, uploader=None, quota_manager=None, now=None) -> int:
    dest_channels = _fetch_destination_channels(conn)
    total = 0
    for dest in dest_channels:
        ch_uploader = uploader or YouTubeUploader(channel_slug=dest['slug'])
        ch_quota = quota_manager or QuotaManager(redis_client, channel_id=dest['youtube_channel_id'])
        clips = _fetch_pending_clips_for_channel(conn, dest['id'])
        for clip in clips:
            # append créditos antes do upload
            if dest.get('credit_template') and clip.get('channel_handle'):
                clip['description'] = append_credits(
                    clip['description'] or '',
                    dest['credit_template'],
                    clip['channel_handle']
                )
            total += _publish_one(conn, clip, ch_uploader, ch_quota, now)
    return total

def _fetch_destination_channels(conn) -> list[dict]:
    # SELECT id, slug, name, niche, youtube_channel_id, credit_template
    # FROM destination_channels WHERE active = TRUE

def _fetch_pending_clips_for_channel(conn, destination_channel_id: int) -> list[dict]:
    # SELECT gc.id, ..., sc.channel_handle
    # FROM generated_clips gc
    # JOIN source_videos sv ON sv.id = gc.source_video_id
    # JOIN source_channels sc ON sc.id = sv.channel_id
    # WHERE gc.status = {_publishable_status()} AND gc.destination_channel_id = {dest_id}
    # AND gc.clip_path IS NOT NULL AND gc.title IS NOT NULL
    # ORDER BY gc.created_at ASC
```

ATENÇÃO retrocompat: `publish_pending_clips` continua aceitando `uploader=None, quota_manager=None` — quando injetados (testes), usa os injetados PARA TODOS os canais (não instancia por canal).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: selector.py — persistir destination_channel_id + video_processor.py — integrar overlay_watermark (MCAN-02, COPY-01)</name>
  <files>clip-processor/src/selector.py, clip-processor/src/video_processor.py</files>
  <behavior>
    - selector: `insert_selected_moments(conn, source_video_id, video_id, moments)` → INSERT inclui `destination_channel_id` obtido via JOIN `source_videos → source_channels → destination_channels`
    - selector: quando `target_niche` é NULL ou sem canal-destino ativo, `destination_channel_id=NULL` (sem erro)
    - video_processor: `process_clip(conn, clip_id)` → `_fetch_clip` retorna `destination_channel_slug` via LEFT JOIN
    - video_processor: `process_clip` chama `overlay_watermark(subtitled_path, f'/app/branding/watermark-{slug}.png', final_path)` após `burn_subtitles`
    - video_processor: arquivo `_subtitled.mp4` intermediário é removido após uso (sem acúmulo em disco)
    - video_processor: slug NULL → watermark_path None → `overlay_watermark` com path None = graceful skip (log warning + continua)
  </behavior>
  <action>
**selector.py:**
Ler o arquivo completo. Modificar `insert_selected_moments`:
1. Antes do loop de `moments`, fazer lookup do `destination_channel_id`:
   - SELECT `sc.target_niche` FROM `source_videos sv JOIN source_channels sc ON sc.id = sv.channel_id WHERE sv.id = source_video_id`
   - Se `target_niche` não NULL: SELECT `id` FROM `destination_channels WHERE niche = {target_niche} AND active = TRUE LIMIT 1`
   - Caso contrário: `destination_channel_id = None`
2. No INSERT, adicionar `destination_channel_id` como campo (aceita NULL via `%s`):
   ```sql
   INSERT INTO generated_clips
   (source_video_id, start_time, end_time, score, reason, status, destination_channel_id)
   VALUES (%s, %s, %s, %s, %s, %s, %s)
   ```

**video_processor.py:**
Ler o arquivo completo.
1. Modificar `_fetch_clip()` para incluir LEFT JOIN com `destination_channels`:
   ```sql
   LEFT JOIN destination_channels dc ON dc.id = gc.destination_channel_id
   ```
   E adicionar `dc.slug AS destination_channel_slug` no SELECT.

2. Modificar `process_clip()`:
   - Após `burn_subtitles(raw_clip_path, srt_path, subtitled_path)` [usar `subtitled_path = f'/app/videos/clips/clip_{clip_id}_subtitled.mp4'` como path intermediário]
   - Derivar `watermark_path`:
     ```python
     slug = clip.get('destination_channel_slug')
     watermark_path = f'/app/branding/watermark-{slug}.png' if slug else None
     ```
   - Se `watermark_path` não None: `actual_final = overlay_watermark(subtitled_path, watermark_path, final_clip_path)`
   - Se `watermark_path` None: mover/renomear `subtitled_path` para `final_clip_path` via `os.rename`
   - Em ambos os casos: se `subtitled_path != actual_final` e `os.path.exists(subtitled_path)`: `os.remove(subtitled_path)`
   - Importar `overlay_watermark` no topo (já está no mesmo arquivo — chamada direta)
  </action>
  <verify>
    <automated>docker exec clip-processor python -m pytest tests/test_selector.py tests/test_video_processor.py -v --tb=short</automated>
  </verify>
  <done>
- test_selector.py: todos GREEN (testes existentes + novos de destination_channel_id)
- test_video_processor.py: todos GREEN (incluindo TestOverlayWatermark)
- Zero regressões
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: publisher.py — loop multi-canal com quota e uploader por canal (MCAN-01/03/04, COPY-02)</name>
  <files>clip-processor/src/publisher.py</files>
  <behavior>
    - `publish_pending_clips(conn, redis)` busca canais-destino ativos e itera por eles
    - Para cada canal: instancia `QuotaManager(redis, channel_id=dest['youtube_channel_id'])` e `YouTubeUploader(channel_slug=dest['slug'])` independentemente
    - `_fetch_pending_clips_for_channel(conn, dest_id)` filtra por `destination_channel_id = dest_id`
    - Antes do upload: `append_credits(clip['description'], dest['credit_template'], clip['channel_handle'])` se template e handle disponíveis
    - Dois canais-destino: atingir quota no canal A não bloqueia canal B (QuotaManagers separados com channel_ids diferentes)
    - Injeção `uploader=mock, quota_manager=mock` (testes): usa os mocks para TODOS os canais (retrocompat dos testes existentes)
    - `publish_pending_clips` continua retornando `int` (quantidade publicada)
  </behavior>
  <action>
Ler `clip-processor/src/publisher.py` completo. Este é o módulo mais complexo — reestruturar o fluxo principal:

1. Adicionar imports no topo: `from src.quota_manager import QuotaManager`, `from src.uploader import YouTubeUploader`, `from src.metadata_generator import append_credits` (verificar se já importados; adicionar apenas os faltantes).

2. Adicionar função `_fetch_destination_channels(conn)`:
   ```python
   def _fetch_destination_channels(conn) -> list[dict]:
       with conn.cursor() as cur:
           cur.execute(
               'SELECT id, slug, name, niche, youtube_channel_id, credit_template '
               'FROM destination_channels WHERE active = TRUE'
           )
           return cur.fetchall() or []
   ```

3. Adicionar função `_fetch_pending_clips_for_channel(conn, destination_channel_id)`:
   ```python
   def _fetch_pending_clips_for_channel(conn, destination_channel_id: int) -> list[dict]:
       with conn.cursor() as cur:
           cur.execute(
               'SELECT gc.id, gc.source_video_id, gc.clip_path, gc.thumbnail_path, '
               'gc.title, gc.description, gc.tags, '
               'sv.local_path AS source_local_path, '
               'sc.channel_handle '
               'FROM generated_clips gc '
               'JOIN source_videos sv ON sv.id = gc.source_video_id '
               'JOIN source_channels sc ON sc.id = sv.channel_id '
               'WHERE gc.status = %s '
               'AND gc.destination_channel_id = %s '
               'AND gc.clip_path IS NOT NULL '
               'AND gc.title IS NOT NULL '
               'ORDER BY gc.created_at ASC',
               (_publishable_status(), destination_channel_id),
           )
           return cur.fetchall() or []
   ```

4. Reescrever `publish_pending_clips()`:
   - Manter a assinatura: `publish_pending_clips(conn, redis_client, uploader=None, quota_manager=None, now=None) -> int`
   - Novo fluxo:
     ```python
     dest_channels = _fetch_destination_channels(conn)
     if not dest_channels:
         _log('[PUBLISHER] Nenhum canal-destino ativo encontrado — usando fluxo legado')
         # fallback: usar _fetch_pending_clips() original se destination_channels vazio
         # (garante compatibilidade com ambientes que ainda não aplicaram a migration)
         return _publish_channel(conn, redis_client, None, uploader, quota_manager, now)
     
     total = 0
     for dest in dest_channels:
         ch_uploader = uploader or YouTubeUploader(channel_slug=dest['slug'])
         ch_quota = quota_manager or QuotaManager(redis_client, channel_id=dest['youtube_channel_id'])
         clips = _fetch_pending_clips_for_channel(conn, dest['id'])
         for clip in clips:
             if dest.get('credit_template') and clip.get('channel_handle'):
                 clip = dict(clip)  # não mutar o original
                 clip['description'] = append_credits(
                     clip.get('description') or '',
                     dest['credit_template'],
                     clip['channel_handle'],
                 )
             total += _publish_one(conn, clip, ch_uploader, ch_quota, now)
     return total
     ```

5. Extrair `_publish_one(conn, clip, uploader, quota_manager, now) -> int` da lógica atual de publicação de um clip (sem mudança de comportamento — apenas extração para reuso).

ATENÇÃO: A função `_fetch_pending_clips()` original DEVE ser mantida para o fallback legado. Não remover.

ATENÇÃO retrocompat testes: o `SAMPLE_CLIP` existente em test_publisher.py agora tem `destination_channel_id` e `channel_handle` (adicionados no Plan 07-02). O mock de `_fetch_destination_channels` nos novos testes retorna uma lista com 1 ou 2 dicts de canais-destino.
  </action>
  <verify>
    <automated>docker exec clip-processor python -m pytest tests/test_publisher.py -v --tb=short</automated>
  </verify>
  <done>
- test_publisher.py: todos GREEN (existentes + TestPublisherMultiCanal)
- Zero regressões na suíte total
- `publish_pending_clips` com 2 canais-destino no mock: publica independentemente por canal
  </done>
</task>

</tasks>

<verification>
```bash
# Suíte completa após integração
docker exec clip-processor python -m pytest -v --tb=short 2>&1 | tail -50

# Confirmar que todos os 7 requirements têm testes GREEN
docker exec clip-processor python -m pytest tests/test_quota_manager.py tests/test_uploader.py tests/test_publisher.py tests/test_video_processor.py tests/test_metadata_generator.py tests/test_rss_poller.py tests/test_selector.py -v --tb=short
```
</verification>

<success_criteria>
- Suíte completa: todos os testes GREEN (zero falhas, zero erros)
- `generated_clips.destination_channel_id` é persistido pelo selector para clips novos
- publisher itera por canais-destino ativos com QuotaManagers independentes
- watermark é aplicado no clip via process_clip (para clips com destination_channel_slug)
- créditos são appended à descrição antes do upload
</success_criteria>

<output>
Após conclusão, criar `.planning/phases/07-schema-multi-canal-python-pipeline/07-06-SUMMARY.md`
</output>
