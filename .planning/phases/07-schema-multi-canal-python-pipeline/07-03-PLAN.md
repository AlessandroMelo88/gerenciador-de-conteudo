---
phase: 07-schema-multi-canal-python-pipeline
plan: "03"
type: execute
wave: 3
depends_on:
  - "07-02"
files_modified:
  - clip-processor/src/quota_manager.py
  - clip-processor/src/uploader.py
autonomous: true
requirements:
  - MCAN-01
  - MCAN-03
  - MCAN-04

must_haves:
  truths:
    - "QuotaManager aceita channel_id opcional; key Redis inclui channel_id quando presente"
    - "QuotaManager sem channel_id continua funcionando (retrocompat Phase 5)"
    - "YouTubeUploader aceita channel_slug; token_file derivado como /app/youtube/token-{slug}.json"
    - "YouTubeUploader sem channel_slug continua usando DEFAULT_TOKEN_FILE (retrocompat)"
  artifacts:
    - path: "clip-processor/src/quota_manager.py"
      provides: "QuotaManager com channel_id no construtor e _key()"
      contains: "channel_id"
    - path: "clip-processor/src/uploader.py"
      provides: "YouTubeUploader com channel_slug no construtor"
      contains: "channel_slug"
  key_links:
    - from: "QuotaManager._key(now)"
      to: "redis key youtube_uploads:{channel_id}:{date}"
      via: "channel_id no construtor"
      pattern: "youtube_uploads.*channel_id"
    - from: "YouTubeUploader.__init__"
      to: "/app/youtube/token-{slug}.json"
      via: "channel_slug → f-string"
      pattern: "token-.*slug.*json"
---

<objective>
Estender `QuotaManager` com `channel_id` opcional no construtor (key Redis independente por canal) e `YouTubeUploader` com `channel_slug` opcional (path do token por convenção). Retrocompatibilidade total com Phases 5-6.

Purpose: Habilita quota de uploads independente por canal-destino (MCAN-03/04) e autenticação OAuth por canal (MCAN-01).

Output: 2 módulos atualizados com novos testes passando (GREEN).
</objective>

<execution_context>
@/Users/alessandrobm1/.claude/get-shit-done/workflows/execute-plan.md
@/Users/alessandrobm1/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-schema-multi-canal-python-pipeline/07-CONTEXT.md
@.planning/phases/07-schema-multi-canal-python-pipeline/07-RESEARCH.md
@.planning/phases/07-schema-multi-canal-python-pipeline/07-02-SUMMARY.md

<interfaces>
<!-- Interfaces atuais a serem estendidas -->

clip-processor/src/quota_manager.py — interface atual:
```python
class QuotaManager:
    def __init__(self, redis_client, max_uploads_per_day: int | None = None):
        # channel_id NÃO existe ainda
        ...
    def _key(self, now: datetime) -> str:
        return f'youtube_uploads:{now.strftime("%Y-%m-%d")}'  # sem channel_id
```

clip-processor/src/uploader.py — interface atual:
```python
DEFAULT_TOKEN_FILE = '/app/token.json'

class YouTubeUploader:
    def __init__(
        self,
        token_file: str | None = None,
        # channel_slug NÃO existe ainda
        ...
    ):
        self.token_file = token_file or os.environ.get('YOUTUBE_TOKEN_FILE', DEFAULT_TOKEN_FILE)
```

Extensão esperada (Source: 07-RESEARCH.md Pattern 2 + 3):
```python
# QuotaManager
def __init__(self, redis_client, max_uploads_per_day=None, channel_id=None):
    self.channel_id = channel_id
    ...
def _key(self, now):
    date_str = now.strftime('%Y-%m-%d')
    if self.channel_id:
        return f'youtube_uploads:{self.channel_id}:{date_str}'
    return f'youtube_uploads:{date_str}'  # fallback retrocompat

# YouTubeUploader
def __init__(self, token_file=None, channel_slug=None, ...):
    if channel_slug:
        self.token_file = f'/app/youtube/token-{channel_slug}.json'
    else:
        self.token_file = token_file or os.environ.get('YOUTUBE_TOKEN_FILE', DEFAULT_TOKEN_FILE)
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Estender QuotaManager com channel_id (MCAN-03, MCAN-04)</name>
  <files>clip-processor/src/quota_manager.py</files>
  <behavior>
    - `QuotaManager(r, channel_id='UCabc123')._key(dt_sp(20))` retorna `'youtube_uploads:UCabc123:2026-06-18'`
    - `QuotaManager(r)._key(dt_sp(20))` sem channel_id retorna `'youtube_uploads:2026-06-18'` (fallback)
    - `QuotaManager(r, channel_id='UCaaa').can_upload(...)` usa key diferente de `QuotaManager(r, channel_id='UCbbb')` — verificável pelo `r.get.call_args`
  </behavior>
  <action>
Ler `clip-processor/src/quota_manager.py` completo. Fazer exatamente 2 modificações:

1. `__init__`: adicionar `channel_id: str | None = None` como parâmetro com default None, após `max_uploads_per_day`. Salvar em `self.channel_id = channel_id`.

2. `_key()`: substituir o corpo por:
```python
def _key(self, now: datetime) -> str:
    date_str = now.strftime('%Y-%m-%d')
    if self.channel_id:
        return f'youtube_uploads:{self.channel_id}:{date_str}'
    return f'youtube_uploads:{date_str}'
```

Nenhuma outra alteração. `can_upload`, `record_upload` e todos os outros métodos ficam intactos.
  </action>
  <verify>
    <automated>docker exec clip-processor python -m pytest tests/test_quota_manager.py -v --tb=short</automated>
  </verify>
  <done>
Todos os testes de test_quota_manager.py passam (incluindo os novos TestQuotaManagerMultiCanal). Zero regressões.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Estender YouTubeUploader com channel_slug (MCAN-01)</name>
  <files>clip-processor/src/uploader.py</files>
  <behavior>
    - `YouTubeUploader(channel_slug='futebol-em-cortes').token_file` é `'/app/youtube/token-futebol-em-cortes.json'`
    - `YouTubeUploader()` usa `DEFAULT_TOKEN_FILE` (retrocompat)
    - `YouTubeUploader(token_file='/custom/path.json')` usa o path explícito (retrocompat)
    - `channel_slug` tem precedência sobre `token_file` e `YOUTUBE_TOKEN_FILE` env var
  </behavior>
  <action>
Ler `clip-processor/src/uploader.py` completo. Fazer exatamente 1 modificação:

No método `__init__` da classe `YouTubeUploader`:
- Adicionar `channel_slug: str | None = None` como parâmetro (após `token_file`)
- Substituir a linha de resolução do `self.token_file`:
```python
if channel_slug:
    self.token_file = f'/app/youtube/token-{channel_slug}.json'
else:
    self.token_file = token_file or os.environ.get('YOUTUBE_TOKEN_FILE', DEFAULT_TOKEN_FILE)
```

Nenhuma outra alteração. `upload_clip`, `_load_credentials` e todos os outros métodos ficam intactos.
  </action>
  <verify>
    <automated>docker exec clip-processor python -m pytest tests/test_uploader.py -v --tb=short</automated>
  </verify>
  <done>
Todos os testes de test_uploader.py passam (incluindo os novos TestYouTubeUploaderChannelSlug). Zero regressões.
  </done>
</task>

</tasks>

<verification>
```bash
# Verificar suíte completa não regrediu
docker exec clip-processor python -m pytest tests/test_quota_manager.py tests/test_uploader.py tests/test_publisher.py -v --tb=short
```
</verification>

<success_criteria>
- `test_quota_manager.py`: todos GREEN (incluindo TestQuotaManagerMultiCanal)
- `test_uploader.py`: todos GREEN (incluindo TestYouTubeUploaderChannelSlug)
- Suíte total: zero novas regressões nos outros arquivos de teste
</success_criteria>

<output>
Após conclusão, criar `.planning/phases/07-schema-multi-canal-python-pipeline/07-03-SUMMARY.md`
</output>
