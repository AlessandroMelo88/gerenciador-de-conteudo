---
phase: 09-bot-telegram-no-laravel
plan: 03
type: execute
wave: 2
depends_on: [09-01]
files_modified:
  - /Users/alessandrobm1/develop/server/wordpress/canaldecortes/clip-processor/src/telegram_notifier.py
  - /Users/alessandrobm1/develop/server/wordpress/canaldecortes/clip-processor/src/ttl_worker.py
  - /Users/alessandrobm1/develop/server/wordpress/canaldecortes/clip-processor/src/internal_api.py
  - /Users/alessandrobm1/develop/server/wordpress/canaldecortes/painel/routes/console.php
  - /Users/alessandrobm1/develop/server/wordpress/docker/php/crontab
  - /Users/alessandrobm1/develop/server/wordpress/docker-compose.yml
autonomous: true
requirements: [BOT-03]
must_haves:
  truths:
    - "telegram_notifier.notify() faz POST para LARAVEL_NOTIFY_URL com header Host: canaldecortes.local"
    - "ttl_worker.py usa LARAVEL_NOTIFY_URL (variável de módulo) em vez de N8N_NOTIFY_URL"
    - "internal_api.py expõe POST /internal/process-url que chama processar_main(url) e retorna {'exit_code': N}"
    - "Artisan schedule dispara resumo diário às 18h BRT via Schedule::call → Telegram::sendMessage (skip se 0 pendentes)"
    - "docker/php/crontab tem entrada '* * * * *' chamando php artisan schedule:run no painel"
    - "clip-processor no docker-compose.yml tem LARAVEL_NOTIFY_URL e LARAVEL_HOST_HEADER"
  artifacts:
    - path: "clip-processor/src/telegram_notifier.py"
      provides: "notify() com LARAVEL_NOTIFY_URL + Host header + X-Internal-Token"
      contains: "LARAVEL_NOTIFY_URL"
    - path: "clip-processor/src/internal_api.py"
      provides: "Endpoint POST /internal/process-url chamando processar_main"
      contains: "/internal/process-url"
    - path: "painel/routes/console.php"
      provides: "Schedule::call dailyAt('18:00') timezone America/Sao_Paulo"
      contains: "dailyAt"
    - path: "docker/php/crontab"
      provides: "schedule:run a cada minuto para o painel"
      contains: "schedule:run"
  key_links:
    - from: "clip-processor/src/telegram_notifier.py"
      to: "http://nginx/internal/pipeline-event"
      via: "requests.post(LARAVEL_NOTIFY_URL, headers={'Host': LARAVEL_HOST_HEADER, 'X-Internal-Token': ...})"
      pattern: "LARAVEL_NOTIFY_URL"
    - from: "clip-processor/src/internal_api.py"
      to: "src.processar.main"
      via: "POST /internal/process-url → processar_main(url)"
      pattern: "processar_main"
    - from: "painel/routes/console.php"
      to: "Telegram::sendMessage"
      via: "Schedule::call(function() { ... })->dailyAt('18:00')->timezone('America/Sao_Paulo')"
      pattern: "dailyAt.*18:00"
---

<objective>
Migração Python e configuração do Artisan Schedule: o clip-processor passa a notificar o Laravel (não o n8n), internal_api.py ganha o endpoint /internal/process-url para o bot delegar /processar, e o container php passa a executar php artisan schedule:run a cada minuto para o resumo diário.

Purpose: Este plano cobre BOT-03 no lado Python — o "outro lado" do Plan 09-02. Pode rodar em paralelo com 09-02 pois não toca os mesmos arquivos. Elimina a dependência do n8n e do Cloudflare Tunnel para notificações.

Output: telegram_notifier.py e ttl_worker.py apontando para Laravel; internal_api.py com /internal/process-url; Artisan Schedule configurado; crontab atualizado; docker-compose.yml com novas env vars do clip-processor.
</objective>

<execution_context>
@/Users/alessandrobm1/.claude/get-shit-done/workflows/execute-plan.md
@/Users/alessandrobm1/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/alessandrobm1/develop/server/wordpress/canaldecortes/.planning/PROJECT.md
@/Users/alessandrobm1/develop/server/wordpress/canaldecortes/.planning/ROADMAP.md
@/Users/alessandrobm1/develop/server/wordpress/canaldecortes/.planning/phases/09-bot-telegram-no-laravel/09-RESEARCH.md
@/Users/alessandrobm1/develop/server/wordpress/canaldecortes/.planning/phases/09-bot-telegram-no-laravel/09-01-SUMMARY.md

<interfaces>
<!-- Contratos existentes relevantes para este plano. -->

From clip-processor/src/telegram_notifier.py (estado atual):
```python
N8N_NOTIFY_URL = os.getenv('N8N_NOTIFY_URL', 'http://n8n:5678/webhook/notify')

def notify(event_type: str, payload: dict, timeout: float = 5.0) -> bool:
    try:
        resp = requests.post(N8N_NOTIFY_URL, json={'event': event_type, 'payload': payload}, timeout=timeout)
        if resp.status_code >= 400:
            print(f'[NOTIFY] warn: n8n retornou HTTP {resp.status_code} para {event_type}')
            return False
        return True
    except requests.RequestException as exc:
        print(f'[NOTIFY] warn: falha ao notificar {event_type}: {exc}')
        return False
```

From clip-processor/src/ttl_worker.py (estado atual):
```python
N8N_NOTIFY_URL = os.getenv('N8N_NOTIFY_URL', 'http://n8n:5678/webhook/notify')
# usado diretamente em requests.post(N8N_NOTIFY_URL, ...) dentro de run_ttl_once()
```

From clip-processor/src/internal_api.py (estado atual):
```python
from src.rejeitar import rejeitar  # importação existente no topo
INTERNAL_TOKEN = os.environ.get('CLIP_PROCESSOR_INTERNAL_TOKEN')
app = Flask(__name__)
# Endpoints existentes: /internal/resolve-channel, /internal/reject-clip
# Adicionar: /internal/process-url
```

From clip-processor/src/processar.py (padrão Phase 6):
```python
def main(url: str) -> int:
    """Retorna exit code: 0=ok, 2=URL inválida, 3=metadata falhou."""
    # ... implementação existente
```

docker-compose.yml clip-processor environment atual:
```yaml
- CLIP_PROCESSOR_INTERNAL_TOKEN=${CLIP_PROCESSOR_INTERNAL_TOKEN}
# Adicionar:
- LARAVEL_NOTIFY_URL=${LARAVEL_NOTIFY_URL:-http://nginx/internal/pipeline-event}
- LARAVEL_HOST_HEADER=${LARAVEL_HOST_HEADER:-canaldecortes.local}
```

Decisão (Research Pattern 3/4): URL interna sempre http://nginx/... com Host header explícito.
Sem Host header, nginx serve o default_server (404). Pitfall 3 do RESEARCH.md.

Decisão (State.md): "Nunca chamar Telegram API direto do Python após Phase 9" —
todo envio passa pelo endpoint Laravel /internal/pipeline-event.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Migrar telegram_notifier.py + ttl_worker.py + adicionar /internal/process-url</name>
  <files>
    clip-processor/src/telegram_notifier.py,
    clip-processor/src/ttl_worker.py,
    clip-processor/src/internal_api.py
  </files>
  <behavior>
    - Test: notify() faz POST para LARAVEL_NOTIFY_URL (não N8N_NOTIFY_URL) — test_notifier_uses_laravel_url
    - Test: notify() inclui header 'Host' na chamada — test_notifier_sends_host_header
    - Test: POST /internal/process-url sem token → 401 — test_process_url_requires_auth
    - Test: POST /internal/process-url com token e url → chama processar_main(url), retorna {'exit_code': 0} — test_process_url_calls_processar_main
    - Test: POST /internal/process-url sem campo 'url' → 400 — test_process_url_missing_url_returns_400
  </behavior>
  <action>
SUBSTITUIR completamente `clip-processor/src/telegram_notifier.py`:

```python
"""
telegram_notifier.py — Cliente HTTP que publica eventos do pipeline para o Laravel.

Decisão (Phase 9): o clip-processor NÃO conhece TELEGRAM_BOT_TOKEN. Faz POST
para o endpoint interno do Laravel (rede docker) que centraliza credencial e
formatação das mensagens. O Laravel encaminha para o Telegram do operador.

O header Host é obrigatório para o nginx rotear corretamente para
canaldecortes.local (Pitfall 3 do Research: sem Host → nginx serve default_server).

Eventos suportados (event_type):
  - 'clip_ttl_warning'   — clipe entrou em janela de aviso (WARN_HOURS)
  - 'upload_published'   — upload concluído com sucesso
  - 'pipeline_failure'   — erro crítico em qualquer etapa do pipeline

Comportamento best-effort: nunca propaga exceções (não derruba o pipeline por
um problema de rede no notificador).

Exporta:
  - LARAVEL_NOTIFY_URL (constante module-level)
  - LARAVEL_HOST_HEADER (constante module-level)
  - notify(event_type, payload, timeout=5.0) -> bool
"""
import os

import requests


LARAVEL_NOTIFY_URL = os.getenv(
    'LARAVEL_NOTIFY_URL',
    'http://nginx/internal/pipeline-event',
)
LARAVEL_HOST_HEADER = os.getenv('LARAVEL_HOST_HEADER', 'canaldecortes.local')
INTERNAL_TOKEN = os.getenv('CLIP_PROCESSOR_INTERNAL_TOKEN', '')


def notify(event_type: str, payload: dict, timeout: float = 5.0) -> bool:
    """POST para LARAVEL_NOTIFY_URL com {event, payload}. Retorna True em 2xx, False em erro.

    Best-effort: erros de rede/HTTP são logados em stderr mas NUNCA propagados.
    """
    try:
        resp = requests.post(
            LARAVEL_NOTIFY_URL,
            json={'event': event_type, 'payload': payload},
            headers={
                'Host': LARAVEL_HOST_HEADER,
                'X-Internal-Token': INTERNAL_TOKEN,
            },
            timeout=timeout,
        )
        if resp.status_code >= 400:
            print(f'[NOTIFY] warn: Laravel retornou HTTP {resp.status_code} para {event_type}')
            return False
        return True
    except requests.RequestException as exc:
        print(f'[NOTIFY] warn: falha ao notificar {event_type}: {exc}')
        return False
```

Atualizar `clip-processor/src/ttl_worker.py` — somente a constante e a chamada requests.post:

1. Remover a linha: `N8N_NOTIFY_URL = os.getenv('N8N_NOTIFY_URL', 'http://n8n:5678/webhook/notify')`
2. Adicionar import do módulo notifier e usar `notify()` em vez de `requests.post()` direto.

Substituir o bloco dentro do `if redis_client.set(...)` em run_ttl_once():

ANTES:
```python
        warned = 0
        for clip in soon_to_expire:
            key = f'clip_warned:{clip["id"]}'
            if redis_client.set(key, '1', nx=True, ex=WARN_TTL_SECONDS):
                try:
                    requests.post(
                        N8N_NOTIFY_URL,
                        json={
                            'event': 'clip_ttl_warning',
                            'payload': {
                                'clip_id': clip['id'],
                                'title': clip.get('title'),
                                'expires_in_hours': TTL_HOURS - WARN_HOURS,
                            },
                        },
                        timeout=5,
                    )
                    warned += 1
                except requests.RequestException as exc:
                    print(f'[TTL] warn POST falhou para clip {clip["id"]}: {exc}')
```

DEPOIS (substituir as linhas acima — manter todo o resto intacto):
```python
        warned = 0
        for clip in soon_to_expire:
            key = f'clip_warned:{clip["id"]}'
            if redis_client.set(key, '1', nx=True, ex=WARN_TTL_SECONDS):
                sent = notify('clip_ttl_warning', {
                    'clip_id': clip['id'],
                    'title': clip.get('title'),
                    'expires_in_hours': TTL_HOURS - WARN_HOURS,
                })
                if sent:
                    warned += 1
                else:
                    print(f'[TTL] warn POST falhou para clip {clip["id"]}')
                    # NÃO incrementa warned; Redis já marcou — mesmo tradeoff da Phase 6.
```

Adicionar ao topo de ttl_worker.py (após os imports existentes, removendo N8N_NOTIFY_URL):
```python
from src.telegram_notifier import notify
```

Também remover a linha `import requests` se ela não for mais usada em outro lugar de ttl_worker.py (confirmar — se ainda usada, manter).

Adicionar ao `clip-processor/src/internal_api.py`:

1. Adicionar import no topo (após `from src.rejeitar import rejeitar`):
```python
from src.processar import main as processar_main
```

2. Adicionar endpoint antes do final do arquivo:
```python
@app.post('/internal/process-url')
def _route_process_url():
    if not _check_auth():
        return jsonify(error='unauthorized'), 401
    payload = request.get_json(silent=True) or {}
    url = payload.get('url')
    if not url:
        return jsonify(error='missing url'), 400
    try:
        exit_code = processar_main(url)
    except Exception as e:  # noqa: BLE001
        return jsonify(error=str(e)), 500
    return jsonify(exit_code=exit_code), 200
```

Confirmar GREEN state:

```bash
docker exec clip-processor python -m pytest tests/test_telegram_notifier.py tests/test_internal_api.py -v 2>&1 | tail -20
```
  </action>
  <verify>
    <automated>docker exec clip-processor python -m pytest tests/test_telegram_notifier.py tests/test_internal_api.py -x -v 2>&1 | tail -15</automated>
  </verify>
  <done>
    Todos os testes de test_telegram_notifier.py GREEN (incluindo test_notifier_uses_laravel_url e test_notifier_sends_host_header).
    Todos os testes de test_internal_api.py GREEN (incluindo os 3 novos test_process_url_*).
    clip-processor/src/telegram_notifier.py exporta LARAVEL_NOTIFY_URL (não N8N_NOTIFY_URL).
    clip-processor/src/internal_api.py tem endpoint POST /internal/process-url.
  </done>
</task>

<task type="auto">
  <name>Task 2: Artisan Schedule resumo diário + crontab + docker-compose env vars</name>
  <files>
    painel/routes/console.php,
    docker/php/crontab,
    docker-compose.yml
  </files>
  <action>
SUBSTITUIR completamente `painel/routes/console.php`:

```php
<?php

use App\Models\GeneratedClip;
use Illuminate\Support\Facades\Artisan;
use Illuminate\Support\Facades\Schedule;
use Telegram\Bot\Laravel\Facades\Telegram;

// Comando legado
Artisan::command('inspire', function () {
    $this->comment(\Illuminate\Foundation\Inspiring::quote());
})->purpose('Display an inspiring quote');

/**
 * Resumo diário às 18h BRT — substitui o workflow n8n 06-cron-resumo-diario.json.
 * Regra Phase 6: skip silencioso se 0 clips pendentes.
 */
Schedule::call(function () {
    $pending = GeneratedClip::where('status', 'pending')->count();
    if ($pending === 0) {
        return;  // skip se não há clips aguardando — idêntico ao comportamento do n8n
    }

    Telegram::sendMessage([
        'chat_id' => config('telegram.bots.mybot.chat_id_allowed'),
        'text'    => "Resumo diário: {$pending} clip(s) aguardando aprovação.",
    ]);
})->dailyAt('18:00')->timezone('America/Sao_Paulo');
```

Atualizar `docker/php/crontab` — ADICIONAR a linha do Artisan schedule:run (manter a linha existente do feeb):

Conteúdo final do arquivo:
```
*/5 * * * * cd /var/www/html/feeb && /usr/local/bin/php app/Cron/cron.php >> /var/log/cron.log 2>&1
* * * * * cd /var/www/html/painel && /usr/local/bin/php artisan schedule:run >> /dev/null 2>&1
```

Nota: a linha feeb já existe — NÃO removê-la. Apenas adicionar a linha do painel.

Após atualizar crontab, reconstruir e reiniciar o container php (fora do repo canaldecortes/, no diretório /Users/alessandrobm1/develop/server/wordpress/):

```bash
docker compose build php && docker compose up -d php
```

Verificar que o crontab está ativo no container reconstruído:

```bash
docker exec php crontab -l
```

Atualizar `docker-compose.yml` — adicionar env vars ao bloco `environment` do serviço `clip-processor` (após a linha CLIP_PROCESSOR_INTERNAL_TOKEN):

```yaml
      - LARAVEL_NOTIFY_URL=${LARAVEL_NOTIFY_URL:-http://nginx/internal/pipeline-event}
      - LARAVEL_HOST_HEADER=${LARAVEL_HOST_HEADER:-canaldecortes.local}
```

Após atualizar docker-compose.yml, reiniciar o clip-processor para pegar as novas envs:

```bash
docker compose up -d clip-processor
```

IMPORTANTE: O arquivo docker-compose.yml fica em /Users/alessandrobm1/develop/server/wordpress/docker-compose.yml (FORA do repo canaldecortes/) — seguir o mesmo padrão das Phases 6 e 8: modificar diretamente no FS, sem commit git (decisão State.md).

Verificar que o clip-processor tem as novas envs:

```bash
docker exec clip-processor env | grep LARAVEL
```
  </action>
  <verify>
    <automated>
docker exec php crontab -l 2>&1 | grep "schedule:run" && docker exec clip-processor env | grep LARAVEL_NOTIFY_URL
    </automated>
  </verify>
  <done>
    `docker exec php crontab -l` mostra a linha `schedule:run` do painel.
    `docker exec clip-processor env | grep LARAVEL_NOTIFY_URL` retorna a URL configurada.
    `painel/routes/console.php` tem Schedule::call dailyAt('18:00') timezone America/Sao_Paulo.
    Suite Python completa ainda GREEN: `docker exec clip-processor python -m pytest -v` sem novos FAIL.
  </done>
</task>

</tasks>

<verification>
```bash
# Python — suite completa deve estar GREEN
docker exec clip-processor python -m pytest -v 2>&1 | tail -10

# LARAVEL_NOTIFY_URL é a constante correta
docker exec clip-processor python -c "from src.telegram_notifier import LARAVEL_NOTIFY_URL; print(LARAVEL_NOTIFY_URL)"

# /internal/process-url existe
docker exec clip-processor python -c "from src.internal_api import app; print([r.rule for r in app.url_map.iter_rules()])"

# Crontab tem schedule:run
docker exec php crontab -l | grep schedule:run

# Artisan schedule registrado
docker exec php bash -c "cd /var/www/html/painel && php artisan schedule:list 2>&1" | grep -i "daily\|18:00"
```
</verification>

<success_criteria>
- telegram_notifier.py: exporta LARAVEL_NOTIFY_URL, notify() envia Host header
- ttl_worker.py: usa notify() do telegram_notifier (não requests.post direto para n8n)
- internal_api.py: POST /internal/process-url retorna {'exit_code': N}
- routes/console.php: Schedule dailyAt('18:00') timezone America/Sao_Paulo, skip se pending=0
- docker/php/crontab: linha schedule:run ativa no container php reconstruído
- docker-compose.yml clip-processor: LARAVEL_NOTIFY_URL e LARAVEL_HOST_HEADER presentes
- Suite Python completa GREEN (pytest -v — sem novos FAIL)
</success_criteria>

<output>
Após conclusão, criar `.planning/phases/09-bot-telegram-no-laravel/09-03-SUMMARY.md`
</output>
