---
phase: 09-bot-telegram-no-laravel
plan: 02
type: execute
wave: 2
depends_on: [09-01]
files_modified:
  - /Users/alessandrobm1/develop/server/wordpress/canaldecortes/painel/app/Http/Controllers/TelegramWebhookController.php
  - /Users/alessandrobm1/develop/server/wordpress/canaldecortes/painel/app/Telegram/Commands/StatusCommand.php
  - /Users/alessandrobm1/develop/server/wordpress/canaldecortes/painel/app/Telegram/Commands/ClipesCommand.php
  - /Users/alessandrobm1/develop/server/wordpress/canaldecortes/painel/app/Telegram/Commands/AprovarCommand.php
  - /Users/alessandrobm1/develop/server/wordpress/canaldecortes/painel/app/Telegram/Commands/RejeitarCommand.php
  - /Users/alessandrobm1/develop/server/wordpress/canaldecortes/painel/app/Telegram/Commands/ProcessarCommand.php
  - /Users/alessandrobm1/develop/server/wordpress/canaldecortes/painel/app/Telegram/Commands/AjudaCommand.php
  - /Users/alessandrobm1/develop/server/wordpress/canaldecortes/painel/app/Services/ClipProcessorClient.php
  - /Users/alessandrobm1/develop/server/wordpress/canaldecortes/painel/routes/web.php
  - /Users/alessandrobm1/develop/server/wordpress/canaldecortes/painel/config/telegram.php
autonomous: true
requirements: [BOT-01, BOT-02, BOT-03]
must_haves:
  truths:
    - "POST /telegramcanal com update_id válido e chat_id autorizado retorna HTTP 200 'ok'"
    - "POST /telegramcanal com chat_id não autorizado retorna HTTP 200 silenciosamente (sem enviar mensagem)"
    - "POST /telegramcanal com mesmo update_id enviado duas vezes executa o comando apenas uma vez"
    - "/aprovar <id> muda clip de pending → approved no GeneratedClip (Eloquent guard WHERE status='pending')"
    - "/rejeitar <id> chama ClipProcessorClient::rejectClip() com o clip_id"
    - "/processar <url> chama ClipProcessorClient::processUrl() com a URL"
    - "POST /internal/pipeline-event com token válido chama Telegram::sendMessage ao chat_id autorizado"
    - "POST /internal/pipeline-event sem token retorna HTTP 401"
  artifacts:
    - path: "painel/app/Http/Controllers/TelegramWebhookController.php"
      provides: "Webhook handler: allowlist + Redis SET NX dedup + commandsHandler + pipelineEvent"
      exports: ["handle", "pipelineEvent"]
    - path: "painel/app/Telegram/Commands/AprovarCommand.php"
      provides: "Comando /aprovar com Eloquent guard pending→approved"
    - path: "painel/app/Telegram/Commands/RejeitarCommand.php"
      provides: "Comando /rejeitar delegando a ClipProcessorClient::rejectClip"
    - path: "painel/app/Telegram/Commands/ProcessarCommand.php"
      provides: "Comando /processar delegando a ClipProcessorClient::processUrl"
    - path: "painel/app/Services/ClipProcessorClient.php"
      provides: "Método processUrl(string $url): int adicionado"
  key_links:
    - from: "painel/routes/web.php"
      to: "TelegramWebhookController::handle"
      via: "Route::post('/telegramcanal', [TelegramWebhookController::class, 'handle'])"
      pattern: "Route::post.*telegramcanal"
    - from: "TelegramWebhookController::handle"
      to: "Redis::connection('default')"
      via: "SET NX tg:dedup:{update_id} EX 300 — atômico, sem race condition"
      pattern: "tg:dedup"
    - from: "config/telegram.php"
      to: "6 Command classes"
      via: "bots.mybot.commands = [StatusCommand::class, ...]"
      pattern: "commands.*Command::class"
---

<objective>
Implementar o core do bot: controller webhook com allowlist e deduplicação Redis, 6 Command classes para os comandos v1, endpoint /internal/pipeline-event para notificações Python, e método processUrl no ClipProcessorClient.

Purpose: Este é o plano que transforma os testes RED do Plan 09-01 em GREEN para BOT-01 e BOT-02. O Plan 09-03 (paralelo) cobre BOT-03 no lado Python — este plano cobre BOT-03 no lado Laravel (o endpoint /internal/pipeline-event).

Output: TelegramWebhookController.php, 6 Command classes, processUrl() em ClipProcessorClient, rotas atualizadas, config/telegram.php com commands registrados.
</objective>

<execution_context>
@/Users/alessandrobm1/.claude/get-shit-done/workflows/execute-plan.md
@/Users/alessandrobm1/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/alessandrobm1/develop/server/wordpress/canaldecortes/.planning/PROJECT.md
@/Users/alessandrobm1/develop/server/wordpress/canaldecortes/.planning/ROADMAP.md
@/Users/alessandrobm1/develop/server/wordpress/canaldecortes/.planning/phases/09-bot-telegram-no-laravel/09-RESEARCH.md
@/Users/alessandrobm1/develop/server/wordpress/canaldecortes/.planning/phases/09-bot-telegram-no-laravel/09-01-SUMMARY.md

<interfaces>
<!-- Contratos existentes que o executor usa diretamente. -->

From painel/app/Services/ClipProcessorClient.php:
```php
class ClipProcessorClient
{
    public function __construct(protected ?string $baseUrl = null, protected ?string $token = null)
    {
        $this->baseUrl ??= config('services.clip_processor.url');
        $this->token   ??= config('services.clip_processor.token');
    }

    public function resolveChannel(string $url): array { ... }

    /** @return int exit code (0=ok, 1=clip não existe, 2=status inválido). */
    public function rejectClip(int $clipId): int
    {
        $response = Http::timeout(15)
            ->withHeader('X-Internal-Token', (string) $this->token)
            ->post($this->baseUrl.'/internal/reject-clip', ['clip_id' => $clipId]);
        if (! $response->successful()) {
            throw new RuntimeException('Erro ao rejeitar clip: HTTP '.$response->status());
        }
        return (int) $response->json('exit_code', 1);
    }
    // ← Adicionar processUrl() seguindo exatamente o mesmo padrão
}
```

From painel/app/Models/GeneratedClip.php (inferido do ClipApprovalActionTest):
```php
// GeneratedClip::factory()->create(['status' => 'pending']) funciona
// GeneratedClip::query()->where('id', $id)->where('status', 'pending')->update(['status' => 'approved'])
```

From painel/app/Models/SourceVideo.php (inferido do DashboardPollingTest):
```php
// SourceVideo::factory()->create(['status' => 'published']) funciona
// SourceVideo::query()->selectRaw('status, count(*) as n')->groupBy('status')->pluck('n', 'status')
```

From painel/routes/web.php (acrescentar abaixo das rotas existentes):
```php
// Rotas existentes ficam intactas (source-channels, destination-channels, clips).
// Adicionar FORA do middleware group auth (webhook Telegram e pipeline-event NÃO usam auth):
Route::post('/telegramcanal', [TelegramWebhookController::class, 'handle']);
Route::post('/internal/pipeline-event', [TelegramWebhookController::class, 'pipelineEvent']);
```

Decisão (Research Pattern 1): commandsHandler(true) lê o update do webhook input.
O controller chama getWebhookUpdate() PRIMEIRO para acessar o update (allowlist + dedup),
depois chama commandsHandler(true) que relê internamente. O SDK cacheia o update — seguro.

Decisão (State.md): Redis DB 1 é a conexão 'default' do Laravel (isolada do DB 0 Python).
Dedup key: 'tg:dedup:{update_id}', TTL 300 segundos, SET NX atômico.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: TelegramWebhookController + rotas + pipeline-event</name>
  <files>
    painel/app/Http/Controllers/TelegramWebhookController.php,
    painel/routes/web.php,
    painel/config/telegram.php
  </files>
  <behavior>
    - Test: POST /telegramcanal com chat_id autorizado + update_id único → 200 'ok' (TelegramWebhookTest.php)
    - Test: POST /telegramcanal com chat_id 9999999 → 200, Http::assertNothingSent() (unauthorized silent)
    - Test: POST /telegramcanal mesmo update_id twice → Redis key tg:dedup:{id} com TTL > 0, Http::assertSentCount(1)
    - Test: POST /internal/pipeline-event sem token → 401
    - Test: POST /internal/pipeline-event upload_published com token → 200 + Telegram::sendMessage com 'Gol do Brasil'
    - Test: POST /internal/pipeline-event pipeline_failure → 200 + mensagem com 'cutting'
    - Test: POST /internal/pipeline-event daily_summary → 200 + mensagem com 'Resumo'
  </behavior>
  <action>
Criar `painel/app/Http/Controllers/TelegramWebhookController.php`:

```php
<?php

namespace App\Http\Controllers;

use Illuminate\Http\Request;
use Illuminate\Http\Response;
use Illuminate\Support\Facades\Redis;
use Telegram\Bot\Laravel\Facades\Telegram;

class TelegramWebhookController extends Controller
{
    public function handle(): Response
    {
        $update = Telegram::getWebhookUpdate();

        // 1) Allowlist — silencioso se chat_id não autorizado
        $chatId  = optional($update->getMessage())?->getChat()?->getId();
        $allowed = (string) config('telegram.bots.mybot.chat_id_allowed');
        if ($chatId && (string) $chatId !== $allowed) {
            return response('ok');
        }

        // 2) Deduplicação — SET NX atômico; silencioso se update_id já visto
        $updateId = $update->updateId;
        if ($updateId) {
            $key   = "tg:dedup:{$updateId}";
            $isNew = Redis::connection('default')->set($key, '1', 'NX', 'EX', 300);
            if (! $isNew) {
                return response('ok');
            }
        }

        // 3) Despacha para Command classes registradas em config/telegram.php
        Telegram::commandsHandler(true);

        return response('ok');
    }

    public function pipelineEvent(Request $request): \Illuminate\Http\JsonResponse
    {
        // Auth: mesmo X-Internal-Token do ClipProcessorClient (Phase 8)
        $token = config('services.clip_processor.token');
        if ($request->header('X-Internal-Token') !== $token) {
            return response()->json(['error' => 'unauthorized'], 401);
        }

        $event   = $request->input('event');
        $payload = $request->input('payload', []);

        $text = match ($event) {
            'upload_published' => "Upload publicado: {$payload['title']} — {$payload['youtube_url']}",
            'pipeline_failure' => "Falha crítica [{$payload['stage']}]: {$payload['error_msg']}",
            'clip_ttl_warning' => "Clip #{$payload['clip_id']} expira em {$payload['expires_in_hours']}h: {$payload['title']}",
            'daily_summary'    => $payload['text'] ?? 'Resumo diário: sem clips pendentes.',
            default            => "Evento desconhecido: {$event}",
        };

        Telegram::sendMessage([
            'chat_id' => config('telegram.bots.mybot.chat_id_allowed'),
            'text'    => $text,
        ]);

        return response()->json(['ok' => true]);
    }
}
```

Adicionar ao `painel/routes/web.php` ANTES do closing PHP tag, FORA de qualquer middleware group (após as rotas auth existentes):

```php
// Phase 9 (BOT-01, BOT-03): rotas sem autenticação — chamadas por Telegram e pelo clip-processor.
// CSRF excluído para ambas em bootstrap/app.php (Plan 09-01).
use App\Http\Controllers\TelegramWebhookController;

Route::post('/telegramcanal', [TelegramWebhookController::class, 'handle']);
Route::post('/internal/pipeline-event', [TelegramWebhookController::class, 'pipelineEvent']);
```

Atualizar `painel/config/telegram.php` — adicionar lista de commands ao array mybot (commands ainda vazia neste momento, será preenchida na Task 2 após criar as classes):

NÃO alterar config/telegram.php neste task — as classes ainda não existem. O task 2 atualiza commands[].
  </action>
  <verify>
    <automated>docker exec php bash -c "cd /var/www/html/painel && ./vendor/bin/pest tests/Feature/TelegramWebhookTest.php tests/Feature/PipelineEventTest.php -x 2>&1" | tail -10</automated>
  </verify>
  <done>
    TelegramWebhookTest.php: 3 testes PASSED.
    PipelineEventTest.php: 4 testes PASSED.
    Nenhum teste de outras features quebrado.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 6 Command classes + processUrl em ClipProcessorClient</name>
  <files>
    painel/app/Telegram/Commands/StatusCommand.php,
    painel/app/Telegram/Commands/ClipesCommand.php,
    painel/app/Telegram/Commands/AprovarCommand.php,
    painel/app/Telegram/Commands/RejeitarCommand.php,
    painel/app/Telegram/Commands/ProcessarCommand.php,
    painel/app/Telegram/Commands/AjudaCommand.php,
    painel/app/Services/ClipProcessorClient.php,
    painel/config/telegram.php
  </files>
  <behavior>
    - Test: /status → mensagem com "status" no texto enviado ao Telegram (TelegramCommandsTest.php)
    - Test: /clipes → mensagem enviada ao Telegram
    - Test: /aprovar {id} pending clip → GeneratedClip status muda para 'approved'
    - Test: /aprovar {id} published clip → status permanece 'published' (guard WHERE status='pending')
    - Test: /rejeitar {id} → Http::assertSent chamou /internal/reject-clip com clip_id
    - Test: /processar url → Http::assertSent chamou /internal/process-url com url
    - Test: /ajuda → mensagem enviada ao Telegram
  </behavior>
  <action>
Criar diretório `painel/app/Telegram/Commands/` e criar as 6 classes.

`painel/app/Telegram/Commands/StatusCommand.php`:
```php
<?php

namespace App\Telegram\Commands;

use App\Models\GeneratedClip;
use App\Models\SourceVideo;
use Telegram\Bot\Commands\Command;

class StatusCommand extends Command
{
    protected string $name        = 'status';
    protected string $description = 'Mostra status do pipeline';

    public function handle(): void
    {
        $videos = SourceVideo::query()
            ->selectRaw('status, count(*) as n')
            ->groupBy('status')
            ->pluck('n', 'status');

        $clips = GeneratedClip::query()
            ->selectRaw('status, count(*) as n')
            ->groupBy('status')
            ->pluck('n', 'status');

        $lines = ['Pipeline status:'];
        foreach ($videos as $s => $n) {
            $lines[] = "  vídeos {$s}: {$n}";
        }
        foreach ($clips as $s => $n) {
            $lines[] = "  clips {$s}: {$n}";
        }

        $this->replyWithMessage(['text' => implode("\n", $lines)]);
    }
}
```

`painel/app/Telegram/Commands/ClipesCommand.php`:
```php
<?php

namespace App\Telegram\Commands;

use App\Models\GeneratedClip;
use Telegram\Bot\Commands\Command;

class ClipesCommand extends Command
{
    protected string $name        = 'clipes';
    protected string $description = 'Lista clips aguardando aprovação';

    public function handle(): void
    {
        $clips = GeneratedClip::query()
            ->whereIn('status', ['pending', 'approved'])
            ->orderBy('created_at', 'desc')
            ->limit(10)
            ->get(['id', 'title', 'status', 'created_at']);

        if ($clips->isEmpty()) {
            $this->replyWithMessage(['text' => 'Nenhum clip aguardando aprovação.']);
            return;
        }

        $lines = ['Clips pendentes:'];
        foreach ($clips as $clip) {
            $lines[] = "  #{$clip->id} [{$clip->status}] {$clip->title}";
        }

        $this->replyWithMessage(['text' => implode("\n", $lines)]);
    }
}
```

`painel/app/Telegram/Commands/AprovarCommand.php`:
```php
<?php

namespace App\Telegram\Commands;

use App\Models\GeneratedClip;
use Telegram\Bot\Commands\Command;

class AprovarCommand extends Command
{
    protected string $name        = 'aprovar';
    protected string $description = 'Aprova um clip para publicação';
    protected string $pattern     = '{clip_id}';

    public function handle(): void
    {
        $clipId = (int) $this->argument('clip_id');

        if (! $clipId) {
            $this->replyWithMessage(['text' => 'Uso: /aprovar <id>']);
            return;
        }

        $affected = GeneratedClip::query()
            ->where('id', $clipId)
            ->where('status', 'pending')
            ->update(['status' => 'approved']);

        $text = $affected
            ? "Clip #{$clipId} aprovado."
            : "Clip #{$clipId} não encontrado ou status inválido para aprovação.";

        $this->replyWithMessage(['text' => $text]);
    }
}
```

`painel/app/Telegram/Commands/RejeitarCommand.php`:
```php
<?php

namespace App\Telegram\Commands;

use App\Services\ClipProcessorClient;
use Telegram\Bot\Commands\Command;

class RejeitarCommand extends Command
{
    protected string $name        = 'rejeitar';
    protected string $description = 'Rejeita um clip e apaga o MP4';
    protected string $pattern     = '{clip_id}';

    public function handle(): void
    {
        $clipId = (int) $this->argument('clip_id');

        if (! $clipId) {
            $this->replyWithMessage(['text' => 'Uso: /rejeitar <id>']);
            return;
        }

        try {
            $exit = app(ClipProcessorClient::class)->rejectClip($clipId);
        } catch (\RuntimeException $e) {
            $this->replyWithMessage(['text' => "Erro ao rejeitar clip #{$clipId}: {$e->getMessage()}"]);
            return;
        }

        $text = match ($exit) {
            0       => "Clip #{$clipId} rejeitado e MP4 apagado.",
            1       => "Clip #{$clipId} não encontrado.",
            2       => "Clip #{$clipId} já está em status inválido para rejeição.",
            default => "Clip #{$clipId}: exit_code={$exit}.",
        };

        $this->replyWithMessage(['text' => $text]);
    }
}
```

`painel/app/Telegram/Commands/ProcessarCommand.php`:
```php
<?php

namespace App\Telegram\Commands;

use App\Services\ClipProcessorClient;
use Telegram\Bot\Commands\Command;

class ProcessarCommand extends Command
{
    protected string $name        = 'processar';
    protected string $description = 'Adiciona vídeo ao pipeline pelo URL do YouTube';
    protected string $pattern     = '{url}';

    public function handle(): void
    {
        $url = $this->argument('url');

        if (! $url) {
            $this->replyWithMessage(['text' => 'Uso: /processar <url_youtube>']);
            return;
        }

        try {
            $exit = app(ClipProcessorClient::class)->processUrl($url);
        } catch (\RuntimeException $e) {
            $this->replyWithMessage(['text' => "Erro ao processar URL: {$e->getMessage()}"]);
            return;
        }

        $text = match ($exit) {
            0       => "URL enfileirada com sucesso. Pipeline processará em breve.",
            2       => "URL inválida — verifique o link do YouTube.",
            3       => "Falha ao buscar metadados do vídeo (yt-dlp).",
            default => "exit_code={$exit}.",
        };

        $this->replyWithMessage(['text' => $text]);
    }
}
```

`painel/app/Telegram/Commands/AjudaCommand.php`:
```php
<?php

namespace App\Telegram\Commands;

use Telegram\Bot\Commands\Command;

class AjudaCommand extends Command
{
    protected string $name        = 'ajuda';
    protected string $description = 'Lista os comandos disponíveis';

    public function handle(): void
    {
        $text = implode("\n", [
            'Comandos disponíveis:',
            '/status — status do pipeline',
            '/clipes — clips aguardando aprovação',
            '/aprovar <id> — aprova clip para publicação',
            '/rejeitar <id> — rejeita clip e apaga MP4',
            '/processar <url> — adiciona vídeo ao pipeline',
            '/ajuda — exibe esta mensagem',
        ]);

        $this->replyWithMessage(['text' => $text]);
    }
}
```

Adicionar `processUrl()` ao final de `painel/app/Services/ClipProcessorClient.php`, antes do último `}`:

```php
    /**
     * Enfileira URL do YouTube para processamento pelo pipeline.
     *
     * @return int exit_code (0=ok, 2=URL inválida, 3=metadata yt-dlp falhou)
     *
     * @throws RuntimeException em erro HTTP 5xx ou timeout.
     */
    public function processUrl(string $url): int
    {
        $response = Http::timeout(30)
            ->withHeader('X-Internal-Token', (string) $this->token)
            ->post($this->baseUrl.'/internal/process-url', ['url' => $url]);

        if (! $response->successful()) {
            throw new RuntimeException('Erro ao processar URL: HTTP '.$response->status());
        }

        return (int) $response->json('exit_code', 3);
    }
```

Atualizar `painel/config/telegram.php` — preencher o array `commands` no bot mybot:

```php
'commands' => [
    \App\Telegram\Commands\StatusCommand::class,
    \App\Telegram\Commands\ClipesCommand::class,
    \App\Telegram\Commands\AprovarCommand::class,
    \App\Telegram\Commands\RejeitarCommand::class,
    \App\Telegram\Commands\ProcessarCommand::class,
    \App\Telegram\Commands\AjudaCommand::class,
],
```

IMPORTANTE: NÃO criar rotas adicionais, NÃO alterar Models existentes, NÃO usar `RefreshDatabase` nos testes.
  </action>
  <verify>
    <automated>docker exec php bash -c "cd /var/www/html/painel && ./vendor/bin/pest tests/Feature/TelegramCommandsTest.php -x 2>&1" | tail -10</automated>
  </verify>
  <done>
    TelegramCommandsTest.php: 7 testes PASSED.
    Suite completa Laravel (./vendor/bin/pest) ainda GREEN — nenhum teste anterior quebrado.
    6 classes Command existem em painel/app/Telegram/Commands/.
    processUrl() existe em ClipProcessorClient.php.
    config/telegram.php tem 6 classes no array commands.
  </done>
</task>

</tasks>

<verification>
```bash
# Todos os 3 arquivos de Feature Telegram devem estar GREEN
docker exec php bash -c "cd /var/www/html/painel && ./vendor/bin/pest tests/Feature/TelegramWebhookTest.php tests/Feature/TelegramCommandsTest.php tests/Feature/PipelineEventTest.php 2>&1" | tail -5

# Suite completa — verificar que testes anteriores não quebraram
docker exec php bash -c "cd /var/www/html/painel && ./vendor/bin/pest 2>&1" | tail -5

# Command classes existem
ls /Users/alessandrobm1/develop/server/wordpress/canaldecortes/painel/app/Telegram/Commands/

# processUrl em ClipProcessorClient
grep -n "processUrl" /Users/alessandrobm1/develop/server/wordpress/canaldecortes/painel/app/Services/ClipProcessorClient.php
```
</verification>

<success_criteria>
- POST /telegramcanal: 200 em qualquer cenário (authorized/unauthorized/dedup)
- Deduplicação: Redis key 'tg:dedup:{update_id}' com TTL 300s criada no primeiro request
- /aprovar: guard WHERE status='pending' impede aprovar clip que não está pendente
- /rejeitar: delega ao ClipProcessorClient::rejectClip, repassa exit_code ao operador
- /processar: delega ao ClipProcessorClient::processUrl com a URL extraída do comando
- /internal/pipeline-event: 401 sem token, 200 com sendMessage para os 4 event types
- Suite Laravel completa GREEN (incluindo testes das Phases 8 anteriores)
</success_criteria>

<output>
Após conclusão, criar `.planning/phases/09-bot-telegram-no-laravel/09-02-SUMMARY.md`
</output>
