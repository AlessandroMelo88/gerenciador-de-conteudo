---
phase: 09-bot-telegram-no-laravel
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - /Users/alessandrobm1/develop/server/wordpress/canaldecortes/painel/composer.json
  - /Users/alessandrobm1/develop/server/wordpress/canaldecortes/painel/composer.lock
  - /Users/alessandrobm1/develop/server/wordpress/canaldecortes/painel/config/telegram.php
  - /Users/alessandrobm1/develop/server/wordpress/canaldecortes/painel/bootstrap/app.php
  - /Users/alessandrobm1/develop/server/wordpress/canaldecortes/painel/.env
  - /Users/alessandrobm1/develop/server/wordpress/canaldecortes/painel/.env.example
  - /Users/alessandrobm1/develop/server/wordpress/canaldecortes/painel/tests/Feature/TelegramWebhookTest.php
  - /Users/alessandrobm1/develop/server/wordpress/canaldecortes/painel/tests/Feature/TelegramCommandsTest.php
  - /Users/alessandrobm1/develop/server/wordpress/canaldecortes/painel/tests/Feature/PipelineEventTest.php
  - /Users/alessandrobm1/develop/server/wordpress/canaldecortes/clip-processor/tests/test_telegram_notifier.py
  - /Users/alessandrobm1/develop/server/wordpress/canaldecortes/clip-processor/tests/test_internal_api.py
autonomous: true
requirements: [BOT-01, BOT-02, BOT-03]
must_haves:
  truths:
    - "composer show irazasyed/telegram-bot-sdk retorna versão ^3.16"
    - "painel/config/telegram.php existe com estrutura bots.mybot contendo token, chat_id_allowed e lista vazia de commands (a ser preenchida no Plan 09-02)"
    - "bootstrap/app.php exclui 'telegramcanal' e 'internal/pipeline-event' do middleware CSRF PreventRequestForgery"
    - "painel/.env tem TELEGRAM_BOT_TOKEN, TELEGRAM_CHAT_ID_ALLOWED e TELEGRAM_WEBHOOK_SECRET"
    - "TelegramWebhookTest.php existe com 3 testes RED (authorized OK, unauthorized silent, dedup)"
    - "TelegramCommandsTest.php existe com 7 testes RED (6 comandos + guard non-pending)"
    - "PipelineEventTest.php existe com 4 testes RED (upload, failure, summary, sem-token 401)"
    - "test_telegram_notifier.py tem testes RED para LARAVEL_NOTIFY_URL + Host header (imports dentro dos testes)"
    - "test_internal_api.py tem 3 testes RED para /internal/process-url"
  artifacts:
    - path: "painel/config/telegram.php"
      provides: "Configuração SDK com token, chat_id_allowed, default=mybot"
    - path: "painel/tests/Feature/TelegramWebhookTest.php"
      provides: "Tests RED para BOT-01 (webhook route + allowlist + dedup)"
    - path: "painel/tests/Feature/TelegramCommandsTest.php"
      provides: "Tests RED para BOT-02 (6 comandos via webhook)"
    - path: "painel/tests/Feature/PipelineEventTest.php"
      provides: "Tests RED para BOT-03 (/internal/pipeline-event)"
  key_links:
    - from: "painel/bootstrap/app.php"
      to: "POST /telegramcanal e POST /internal/pipeline-event"
      via: "preventRequestForgery(except: ['telegramcanal', 'internal/pipeline-event'])"
      pattern: "telegramcanal"
    - from: "painel/.env"
      to: "config/telegram.php"
      via: "env('TELEGRAM_BOT_TOKEN')"
      pattern: "TELEGRAM_BOT_TOKEN"
---

<objective>
Wave 0 da Phase 9: instalar o SDK, configurar o ambiente e criar todos os testes em RED state — sem implementar nenhuma lógica de negócio.

Purpose: O padrão estabelecido nas Phases 2/3/4/6/8 deste projeto exige que os testes existam e estejam vermelhos ANTES de qualquer implementação. Isso garante que o Plan 09-02 (controller + comandos) e o Plan 09-03 (Python migration) tenham destino claro e verificável.

Output: SDK instalado, config/telegram.php publicado, CSRF configurado, 3 arquivos PHP de teste RED, 2 arquivos Python de teste atualizados com comportamento RED.
</objective>

<execution_context>
@/Users/alessandrobm1/.claude/get-shit-done/workflows/execute-plan.md
@/Users/alessandrobm1/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/alessandrobm1/develop/server/wordpress/canaldecortes/.planning/PROJECT.md
@/Users/alessandrobm1/develop/server/wordpress/canaldecortes/.planning/ROADMAP.md
@/Users/alessandrobm1/develop/server/wordpress/canaldecortes/.planning/phases/09-bot-telegram-no-laravel/09-RESEARCH.md

<interfaces>
<!-- Padrões existentes do projeto que o executor precisa conhecer. -->

From painel/tests/Pest.php:
```php
pest()->extend(TestCase::class)->in('Feature');  // Feature tests usam TestCase
// RefreshDatabase está COMENTADO — usar DatabaseTransactions em vez disso
```

Padrão de Feature test existente (ClipApprovalActionTest.php):
```php
uses(DatabaseTransactions::class);

beforeEach(fn () => Http::fake([...]));

it('description', function () {
    $this->postJson('/route', [...])
        ->assertStatus(200);
});
```

From painel/app/Services/ClipProcessorClient.php:
```php
// config('services.clip_processor.token') é o CLIP_PROCESSOR_INTERNAL_TOKEN
// usado como X-Internal-Token — mesmo padrão para /internal/pipeline-event
```

From clip-processor/tests/test_internal_api.py:
```python
# Padrão de fixture com monkeypatch do INTERNAL_TOKEN:
@pytest.fixture
def client(monkeypatch):
    monkeypatch.setattr('src.internal_api.INTERNAL_TOKEN', 'test-token-123')
    app.config['TESTING'] = True
    with app.test_client() as c:
        yield c
```

Decisão Phase 7 (import de símbolo não existente):
"Import de símbolos não existentes feito localmente dentro dos métodos de teste —
evita ImportError no nível de módulo que quebraria coleta de testes existentes"
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Instalar SDK + publicar config + configurar CSRF + vars de ambiente</name>
  <files>
    painel/composer.json, painel/composer.lock, painel/config/telegram.php,
    painel/bootstrap/app.php, painel/.env, painel/.env.example
  </files>
  <action>
Executar no container php (cwd sempre /var/www/html/painel dentro do container):

```bash
docker exec php bash -c "cd /var/www/html/painel && composer require irazasyed/telegram-bot-sdk ^3.16 --no-interaction"
docker exec php bash -c "cd /var/www/html/painel && php artisan vendor:publish --tag='telegram-config' --no-interaction"
```

Após publicar, editar `painel/config/telegram.php` para ter exatamente esta estrutura (substituir o conteúdo gerado):

```php
<?php

return [
    'bots' => [
        'mybot' => [
            'token'           => env('TELEGRAM_BOT_TOKEN'),
            'webhook_url'     => env('TELEGRAM_WEBHOOK_URL', 'https://alessandromelo.com.br/telegramcanal'),
            'chat_id_allowed' => env('TELEGRAM_CHAT_ID_ALLOWED', '5760918317'),
            'commands'        => [],  // preenchido no Plan 09-02
        ],
    ],
    'default' => 'mybot',

    // Configurações globais do SDK (manter defaults do vendor)
    'async_requests'    => false,
    'http_client_handler' => null,
    'resolve_command_class' => true,
];
```

Em `painel/bootstrap/app.php`, adicionar exclusão de CSRF no bloco `->withMiddleware`:

```php
->withMiddleware(function (Middleware $middleware): void {
    $middleware->preventRequestForgery(except: [
        'telegramcanal',
        'internal/pipeline-event',
    ]);
})
```

Adicionar ao `painel/.env` (os valores do TELEGRAM_BOT_TOKEN já existem em wordpress/.env — usar os mesmos):

```
TELEGRAM_BOT_TOKEN=8820657949:AAHiwYUs7Aj_noVBZQeX-7lameswvOJiVpw
TELEGRAM_CHAT_ID_ALLOWED=5760918317
TELEGRAM_WEBHOOK_SECRET=
```

Nota: TELEGRAM_WEBHOOK_SECRET pode ficar vazio agora — será preenchido pelo operador antes do setWebhook em produção (Plan 09-04).

Adicionar ao `painel/.env.example`:

```
TELEGRAM_BOT_TOKEN=
TELEGRAM_CHAT_ID_ALLOWED=5760918317
TELEGRAM_WEBHOOK_SECRET=
```

NÃO criar config adicional, NÃO instalar outros pacotes, NÃO criar rotas neste task.
  </action>
  <verify>
    <automated>docker exec php bash -c "cd /var/www/html/painel && composer show irazasyed/telegram-bot-sdk" 2>&1 | grep -E "telegram-bot-sdk|3\."</automated>
  </verify>
  <done>
    `composer show irazasyed/telegram-bot-sdk` retorna versão ^3.16.
    `painel/config/telegram.php` existe com `bots.mybot.token`.
    `painel/bootstrap/app.php` contém `preventRequestForgery(except: ['telegramcanal', 'internal/pipeline-event'])`.
    `painel/.env` tem `TELEGRAM_BOT_TOKEN` e `TELEGRAM_CHAT_ID_ALLOWED`.
  </done>
</task>

<task type="auto">
  <name>Task 2: Criar Wave 0 RED tests (3 PHP Feature + 2 Python updates)</name>
  <files>
    painel/tests/Feature/TelegramWebhookTest.php,
    painel/tests/Feature/TelegramCommandsTest.php,
    painel/tests/Feature/PipelineEventTest.php,
    clip-processor/tests/test_telegram_notifier.py,
    clip-processor/tests/test_internal_api.py
  </files>
  <action>
Criar `painel/tests/Feature/TelegramWebhookTest.php` com este conteúdo exato:

```php
<?php

use Illuminate\Foundation\Testing\DatabaseTransactions;
use Illuminate\Support\Facades\Http;
use Illuminate\Support\Facades\Redis;

uses(DatabaseTransactions::class);

/**
 * Cria um payload de Update do Telegram com o chat_id autorizado por padrão.
 */
function fakeTgUpdate(int $updateId = 1, int|string $chatId = 5760918317, string $text = '/ajuda'): array
{
    return [
        'update_id' => $updateId,
        'message'   => [
            'message_id' => 100,
            'from'       => ['id' => (int) $chatId, 'first_name' => 'Test'],
            'chat'       => ['id' => (int) $chatId, 'type' => 'private'],
            'text'       => $text,
            'date'       => time(),
        ],
    ];
}

beforeEach(function () {
    // Fake a API do Telegram para não fazer chamadas HTTP reais durante testes.
    Http::fake([
        '*api.telegram.org*' => Http::response(['ok' => true, 'result' => ['message_id' => 1]], 200),
    ]);
});

it('returns 200 ok for valid update from authorized chat_id', function () {
    $uid = random_int(10000, 99999);
    Redis::connection('default')->del("tg:dedup:{$uid}");

    $this->postJson('/telegramcanal', fakeTgUpdate(updateId: $uid))
        ->assertStatus(200);
});

it('returns 200 silently for update from unauthorized chat_id', function () {
    $response = $this->postJson('/telegramcanal', fakeTgUpdate(chatId: 9999999));
    $response->assertStatus(200);
    // Não deve ter chamado a API do Telegram (nenhum replyWithMessage)
    Http::assertNothingSent();
});

it('deduplicates same update_id — second request returns 200 without re-executing command', function () {
    $uid = random_int(100000, 999999);
    Redis::connection('default')->del("tg:dedup:{$uid}");

    // Primeira request — deve processar e salvar Redis key
    $this->postJson('/telegramcanal', fakeTgUpdate(updateId: $uid))->assertStatus(200);

    // Segunda request com mesmo update_id — deve ser bloqueada silenciosamente
    $this->postJson('/telegramcanal', fakeTgUpdate(updateId: $uid))->assertStatus(200);

    // Redis key deve existir com TTL > 0 (SET NX EX 300)
    expect(Redis::connection('default')->exists("tg:dedup:{$uid}"))->toBe(1);
    expect(Redis::connection('default')->ttl("tg:dedup:{$uid}"))->toBeGreaterThan(0);

    // Telegram API deve ter sido chamada apenas UMA vez (não duas)
    Http::assertSentCount(1);
});
```

Criar `painel/tests/Feature/TelegramCommandsTest.php`:

```php
<?php

use App\Models\GeneratedClip;
use App\Models\SourceVideo;
use Illuminate\Foundation\Testing\DatabaseTransactions;
use Illuminate\Support\Facades\Http;

uses(DatabaseTransactions::class);

/**
 * Cria update com update_id único para evitar conflito de dedup entre testes.
 */
function tgCmd(string $text): array
{
    return [
        'update_id' => random_int(200000, 299999),
        'message'   => [
            'message_id' => 1,
            'from'       => ['id' => 5760918317, 'first_name' => 'Test'],
            'chat'       => ['id' => 5760918317, 'type' => 'private'],
            'text'       => $text,
            'date'       => time(),
        ],
    ];
}

beforeEach(function () {
    Http::fake([
        '*api.telegram.org*'       => Http::response(['ok' => true, 'result' => ['message_id' => 1]], 200),
        '*/internal/reject-clip*'  => Http::response(['exit_code' => 0], 200),
        '*/internal/process-url*'  => Http::response(['exit_code' => 0], 200),
    ]);
});

it('/status returns 200 and sends Telegram message with status data', function () {
    SourceVideo::factory()->create(['status' => 'published']);
    GeneratedClip::factory()->create(['status' => 'pending']);

    $this->postJson('/telegramcanal', tgCmd('/status'))->assertStatus(200);

    Http::assertSent(fn ($req) =>
        str_contains($req->url(), 'api.telegram.org') &&
        str_contains($req['text'] ?? '', 'status')
    );
});

it('/clipes returns 200 and sends Telegram message listing pending clips', function () {
    GeneratedClip::factory()->create(['status' => 'pending', 'title' => 'Clip Teste ABC']);

    $this->postJson('/telegramcanal', tgCmd('/clipes'))->assertStatus(200);

    Http::assertSent(fn ($req) => str_contains($req->url(), 'api.telegram.org'));
});

it('/aprovar <id> changes clip status pending → approved', function () {
    $clip = GeneratedClip::factory()->create(['status' => 'pending']);

    $this->postJson('/telegramcanal', tgCmd("/aprovar {$clip->id}"))->assertStatus(200);

    expect($clip->refresh()->status)->toBe('approved');
});

it('/aprovar <id> non-pending clip leaves status unchanged', function () {
    $clip = GeneratedClip::factory()->create(['status' => 'published']);

    $this->postJson('/telegramcanal', tgCmd("/aprovar {$clip->id}"))->assertStatus(200);

    expect($clip->refresh()->status)->toBe('published');
});

it('/rejeitar <id> calls ClipProcessorClient reject-clip endpoint', function () {
    $clip = GeneratedClip::factory()->create(['status' => 'pending']);

    $this->postJson('/telegramcanal', tgCmd("/rejeitar {$clip->id}"))->assertStatus(200);

    Http::assertSent(fn ($req) =>
        str_contains($req->url(), '/internal/reject-clip') &&
        $req['clip_id'] === $clip->id
    );
});

it('/processar <url> calls ClipProcessorClient process-url endpoint', function () {
    $this->postJson('/telegramcanal', tgCmd('/processar https://youtube.com/watch?v=abc123'))
        ->assertStatus(200);

    Http::assertSent(fn ($req) =>
        str_contains($req->url(), '/internal/process-url') &&
        str_contains($req['url'] ?? '', 'youtube.com')
    );
});

it('/ajuda returns 200 and sends help text via Telegram', function () {
    $this->postJson('/telegramcanal', tgCmd('/ajuda'))->assertStatus(200);

    Http::assertSent(fn ($req) => str_contains($req->url(), 'api.telegram.org'));
});
```

Criar `painel/tests/Feature/PipelineEventTest.php`:

```php
<?php

use Illuminate\Foundation\Testing\DatabaseTransactions;
use Illuminate\Support\Facades\Http;

uses(DatabaseTransactions::class);

beforeEach(function () {
    Http::fake([
        '*api.telegram.org*' => Http::response(['ok' => true, 'result' => ['message_id' => 1]], 200),
    ]);
});

it('POST /internal/pipeline-event without token returns 401', function () {
    $this->postJson('/internal/pipeline-event', [
        'event'   => 'upload_published',
        'payload' => [],
    ])->assertStatus(401);
});

it('POST /internal/pipeline-event upload_published sends Telegram message with title', function () {
    $token = config('services.clip_processor.token');

    $this->postJson('/internal/pipeline-event', [
        'event'   => 'upload_published',
        'payload' => [
            'title'       => 'Gol do Brasil',
            'youtube_url' => 'https://youtube.com/watch?v=abc',
        ],
    ], ['X-Internal-Token' => $token])
        ->assertStatus(200)
        ->assertJson(['ok' => true]);

    Http::assertSent(fn ($req) =>
        str_contains($req->url(), 'api.telegram.org') &&
        str_contains($req['text'] ?? '', 'Gol do Brasil')
    );
});

it('POST /internal/pipeline-event pipeline_failure sends message with stage', function () {
    $token = config('services.clip_processor.token');

    $this->postJson('/internal/pipeline-event', [
        'event'   => 'pipeline_failure',
        'payload' => ['stage' => 'cutting', 'error_msg' => 'ffmpeg crashed'],
    ], ['X-Internal-Token' => $token])
        ->assertStatus(200);

    Http::assertSent(fn ($req) =>
        str_contains($req->url(), 'api.telegram.org') &&
        str_contains($req['text'] ?? '', 'cutting')
    );
});

it('POST /internal/pipeline-event daily_summary sends the summary text', function () {
    $token = config('services.clip_processor.token');

    $this->postJson('/internal/pipeline-event', [
        'event'   => 'daily_summary',
        'payload' => ['text' => 'Resumo: 3 clips aguardando.'],
    ], ['X-Internal-Token' => $token])
        ->assertStatus(200);

    Http::assertSent(fn ($req) =>
        str_contains($req->url(), 'api.telegram.org') &&
        str_contains($req['text'] ?? '', 'Resumo')
    );
});
```

Atualizar `clip-processor/tests/test_telegram_notifier.py` — SUBSTITUIR completamente o conteúdo:

```python
"""
Testes para telegram_notifier.py — cliente HTTP que dispara eventos para o Laravel.

Decisão Phase 9: telegram_notifier.py passa a chamar LARAVEL_NOTIFY_URL
(http://nginx/internal/pipeline-event com Host: canaldecortes.local)
em vez de N8N_NOTIFY_URL.

Testes test_notifier_uses_laravel_url e test_notifier_sends_host_header estão
em RED até o Plan 09-03 renomear N8N_NOTIFY_URL → LARAVEL_NOTIFY_URL.
"""
import pytest
from unittest.mock import MagicMock, patch
import requests

from src.telegram_notifier import notify


def test_failure_event():
    """notify() captura RequestException e retorna False sem propagar."""
    payload = {'stage': 'cutting', 'error': 'ffmpeg crashed'}
    with patch('src.telegram_notifier.requests.post') as mock_post:
        mock_post.side_effect = requests.RequestException('connection refused')
        result = notify('pipeline_failure', payload)
    assert result is False


def test_http_4xx_returns_false():
    """notify() retorna False quando o endpoint retorna 4xx."""
    with patch('src.telegram_notifier.requests.post') as mock_post:
        mock_post.return_value = MagicMock(status_code=400)
        result = notify('upload_published', {})
    assert result is False


def test_notifier_uses_laravel_url():
    """notify() faz POST para LARAVEL_NOTIFY_URL — RED até Plan 09-03.

    Importa LARAVEL_NOTIFY_URL dentro do teste (padrão Phase 7) para não
    quebrar a coleta de testes se o símbolo ainda não existe no módulo.
    """
    from src.telegram_notifier import LARAVEL_NOTIFY_URL  # existe após Plan 09-03

    payload = {'clip_id': 42, 'youtube_video_id': 'abc123'}
    with patch('src.telegram_notifier.requests.post') as mock_post:
        mock_post.return_value = MagicMock(status_code=200)
        result = notify('upload_published', payload)

    assert result is True
    assert mock_post.call_count == 1
    url_used = (
        mock_post.call_args.args[0]
        if mock_post.call_args.args
        else mock_post.call_args.kwargs.get('url', '')
    )
    assert url_used == LARAVEL_NOTIFY_URL, (
        f'Expected LARAVEL_NOTIFY_URL={LARAVEL_NOTIFY_URL!r}, got {url_used!r}'
    )
    body = mock_post.call_args.kwargs.get('json') or {}
    assert body.get('event') == 'upload_published'
    assert body.get('payload') == payload


def test_notifier_sends_host_header():
    """notify() inclui header Host para nginx routing — RED até Plan 09-03."""
    with patch('src.telegram_notifier.requests.post') as mock_post:
        mock_post.return_value = MagicMock(status_code=200)
        notify('pipeline_failure', {'stage': 'download', 'error_msg': 'timeout'})

    headers = mock_post.call_args.kwargs.get('headers', {})
    assert 'Host' in headers, (
        'notify() deve enviar header Host para que nginx roteie para canaldecortes.local'
    )
```

Adicionar ao final de `clip-processor/tests/test_internal_api.py` (APPEND — não substituir o conteúdo existente):

```python


# ── Testes para /internal/process-url (Phase 9, BOT-03) ──────────────────────
# RED até Plan 09-03 adicionar o endpoint em internal_api.py.


def test_process_url_requires_auth(client):
    """POST /internal/process-url sem token retorna 401."""
    resp = client.post('/internal/process-url', json={'url': 'https://youtube.com/watch?v=abc'})
    assert resp.status_code == 401


def test_process_url_calls_processar_main_and_returns_exit_code(client):
    """POST /internal/process-url chama processar_main(url) e retorna {'exit_code': N}."""
    with patch('src.internal_api.processar_main') as mock_proc:
        mock_proc.return_value = 0
        resp = client.post(
            '/internal/process-url',
            json={'url': 'https://youtube.com/watch?v=abc'},
            headers={'X-Internal-Token': 'test-token-123'},
        )
    assert resp.status_code == 200
    assert resp.get_json()['exit_code'] == 0
    mock_proc.assert_called_once_with('https://youtube.com/watch?v=abc')


def test_process_url_missing_url_returns_400(client):
    """POST /internal/process-url sem campo 'url' retorna 400."""
    resp = client.post(
        '/internal/process-url',
        json={},
        headers={'X-Internal-Token': 'test-token-123'},
    )
    assert resp.status_code == 400
```

Confirmar RED state para os novos testes:

```bash
# PHP — deve retornar FAILED (404 nos novos endpoints)
docker exec php bash -c "cd /var/www/html/painel && ./vendor/bin/pest tests/Feature/TelegramWebhookTest.php 2>&1" | tail -5

# Python — teste_notifier_uses_laravel_url deve FALHAR (ImportError ou AssertionError)
docker exec clip-processor python -m pytest tests/test_telegram_notifier.py::test_notifier_uses_laravel_url -v 2>&1 | tail -5

# Python — test_process_url_requires_auth deve FALHAR (404)
docker exec clip-processor python -m pytest tests/test_internal_api.py::test_process_url_requires_auth -v 2>&1 | tail -5
```
  </action>
  <verify>
    <automated>
docker exec php bash -c "cd /var/www/html/painel && ./vendor/bin/pest tests/Feature/TelegramWebhookTest.php tests/Feature/TelegramCommandsTest.php tests/Feature/PipelineEventTest.php 2>&1" | grep -E "FAILED|Tests:|failed"
    </automated>
  </verify>
  <done>
    Os 3 arquivos PHP de teste existem e rodam (sem syntax error, sem erro de coleta).
    Todos os testes estão em FAILED (RED) — nenhum PASSED porque as rotas não existem ainda.
    Os 2 arquivos Python de teste existem, os novos testes estão em FAILED/ERROR (RED).
    Os testes ANTERIORES de test_internal_api.py (resolve-channel, reject-clip) continuam GREEN.
  </done>
</task>

</tasks>

<verification>
```bash
# SDK instalado
docker exec php bash -c "cd /var/www/html/painel && composer show irazasyed/telegram-bot-sdk" | grep telegram-bot-sdk

# CSRF excluído
grep -n "telegramcanal" /Users/alessandrobm1/develop/server/wordpress/canaldecortes/painel/bootstrap/app.php

# Config publicado com token env
grep -n "TELEGRAM_BOT_TOKEN" /Users/alessandrobm1/develop/server/wordpress/canaldecortes/painel/config/telegram.php

# Testes PHP em RED
docker exec php bash -c "cd /var/www/html/painel && ./vendor/bin/pest tests/Feature/TelegramWebhookTest.php 2>&1" | grep -E "FAILED|passed"

# Testes Python RED novos
docker exec clip-processor python -m pytest tests/test_telegram_notifier.py tests/test_internal_api.py -v 2>&1 | tail -15
```
</verification>

<success_criteria>
- `irazasyed/telegram-bot-sdk ^3.16` instalado e listado em composer.json
- `config/telegram.php` publicado com estrutura bots.mybot
- `bootstrap/app.php` exclui 'telegramcanal' e 'internal/pipeline-event' do CSRF
- `painel/.env` tem TELEGRAM_BOT_TOKEN e TELEGRAM_CHAT_ID_ALLOWED
- 3 arquivos PHP de teste existem e estão em RED (404 nos endpoints inexistentes)
- Testes test_notifier_uses_laravel_url e test_notifier_sends_host_header estão em RED
- Testes test_process_url_* estão em RED (404 no endpoint inexistente)
- Testes ANTERIORES de test_internal_api.py ainda em GREEN
</success_criteria>

<output>
Após conclusão, criar `.planning/phases/09-bot-telegram-no-laravel/09-01-SUMMARY.md`
</output>
