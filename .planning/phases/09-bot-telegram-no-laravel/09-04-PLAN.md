---
phase: 09-bot-telegram-no-laravel
plan: 04
type: execute
wave: 3
depends_on: [09-02, 09-03]
files_modified: []
autonomous: false
requirements: [BOT-01, BOT-02, BOT-03]
must_haves:
  truths:
    - "Suite Laravel (./vendor/bin/pest) 100% GREEN — incluindo os 14 novos testes Telegram"
    - "Suite Python (pytest -v) GREEN para test_telegram_notifier.py e test_internal_api.py"
    - "Webhook Telegram registrado via setWebhook apontando para https://alessandromelo.com.br/telegramcanal"
    - "getWebhookInfo retorna url correto e sem last_error_message"
    - "Bot responde a /status no Telegram com dados reais do pipeline"
    - "Workflow n8n 06-router.json (bot Telegram) desativado"
    - "Cloudflare Tunnel pode ser desativado sem quebrar o bot (webhook via nginx direto)"
  artifacts:
    - path: "painel/tests/Feature/TelegramWebhookTest.php"
      provides: "3 testes GREEN pós-implementação"
    - path: "painel/tests/Feature/TelegramCommandsTest.php"
      provides: "7 testes GREEN pós-implementação"
    - path: "painel/tests/Feature/PipelineEventTest.php"
      provides: "4 testes GREEN pós-implementação"
  key_links:
    - from: "Telegram (cloud)"
      to: "https://alessandromelo.com.br/telegramcanal"
      via: "setWebhook API call com secret_token"
      pattern: "setWebhook"
    - from: "https://alessandromelo.com.br/telegramcanal"
      to: "TelegramWebhookController::handle"
      via: "nginx → php-fpm → Laravel router"
      pattern: "telegramcanal"
---

<objective>
Checkpoint final da Phase 9: executar suites completas de teste, registrar o webhook no Telegram, verificar manualmente que o bot responde, e desativar os workflows n8n que foram substituídos.

Purpose: A entrega funcional requer que o bot esteja registrado no Telegram e respondendo em produção. Os testes automatizados garantem corretude interna; o checkpoint humano garante que o circuito externo (Telegram → nginx → Laravel) está fechado.

Output: Bot em produção respondendo via Laravel, n8n desativado para o bot, Cloudflare Tunnel opcional para o bot.
</objective>

<execution_context>
@/Users/alessandrobm1/.claude/get-shit-done/workflows/execute-plan.md
@/Users/alessandrobm1/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/alessandrobm1/develop/server/wordpress/canaldecortes/.planning/PROJECT.md
@/Users/alessandrobm1/develop/server/wordpress/canaldecortes/.planning/ROADMAP.md
@/Users/alessandrobm1/develop/server/wordpress/canaldecortes/.planning/phases/09-bot-telegram-no-laravel/09-02-SUMMARY.md
@/Users/alessandrobm1/develop/server/wordpress/canaldecortes/.planning/phases/09-bot-telegram-no-laravel/09-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verificação automática — suites completas GREEN</name>
  <files></files>
  <action>
Rodar suites completas e verificar que nenhum teste foi quebrado pelos Plans 09-02 e 09-03.

```bash
# Suite Laravel completa (inclui Phases 8 + novos Telegram)
docker exec php bash -c "cd /var/www/html/painel && ./vendor/bin/pest 2>&1"

# Suite Python completa (inclui Phases 2-8 + novos test_telegram_notifier + test_internal_api)
docker exec clip-processor python -m pytest -v 2>&1
```

Se algum teste falhar que NÃO é dos 3 arquivos novos de Phase 9:
1. Identificar qual teste falhou e em qual arquivo
2. Verificar se a falha é nova (regressão) ou pré-existente (documentada em STATE.md)
3. Falhas pré-existentes documentadas em STATE.md: test_quota_manager 3 falhas — ignorar
4. Para qualquer regressão nova: corrigir antes de prosseguir para o checkpoint humano

Verificar também que o Artisan schedule está registrado:
```bash
docker exec php bash -c "cd /var/www/html/painel && php artisan schedule:list 2>&1"
```

E que o /internal/process-url está ativo no container clip-processor:
```bash
docker exec clip-processor python -c "
from src.internal_api import app
rules = [r.rule for r in app.url_map.iter_rules()]
print('/internal/process-url in rules:', '/internal/process-url' in rules)
"
```
  </action>
  <verify>
    <automated>
docker exec php bash -c "cd /var/www/html/painel && ./vendor/bin/pest 2>&1" | grep -E "passed|failed" | tail -3
    </automated>
  </verify>
  <done>
    Suite Laravel: todos os testes PASSED (nenhuma regressão nas Phases 8 anteriores).
    Suite Python: test_telegram_notifier.py e test_internal_api.py todos GREEN.
    Artisan schedule list mostra a entrada do resumo diário às 18:00.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Checkpoint humano — nginx produção + setWebhook + verificação bot + desativação n8n</name>
  <files></files>
  <action>Verificação humana necessária. Seguir os passos em how-to-verify abaixo.</action>
  <what-built>
    Bot Telegram completo rodando no Laravel:
    - Webhook controller com allowlist (chat_id 5760918317) e deduplicação Redis NX
    - 6 comandos: /status, /clipes, /aprovar, /rejeitar, /processar, /ajuda
    - Endpoint POST /internal/pipeline-event para notificações do pipeline Python
    - Artisan schedule para resumo diário às 18h BRT
    - telegram_notifier.py e ttl_worker.py apontando para Laravel (não n8n)
  </what-built>
  <how-to-verify>
    **Pré-requisito — nginx em alessandromelo.com.br:**
    O nginx desse domínio precisa de um `location /telegramcanal` apontando para
    o PHP-FPM (porta 9000 no container php). Se ainda não existe, adicionar
    ao server block de alessandromelo.com.br (fora do repo canaldecortes/, direto no FS do Docker):

    ```nginx
    location /telegramcanal {
        fastcgi_pass php:9000;
        fastcgi_index index.php;
        include fastcgi_params;
        fastcgi_param SCRIPT_FILENAME /var/www/html/painel/public/index.php;
        fastcgi_param SERVER_NAME canaldecortes.local;
    }
    ```

    Após editar o server block, recarregar nginx: `docker exec nginx nginx -s reload`

    **Passo 1 — Registrar webhook no Telegram:**
    ```bash
    # Usar TELEGRAM_BOT_TOKEN do painel/.env
    TOKEN="$(grep TELEGRAM_BOT_TOKEN /Users/alessandrobm1/develop/server/wordpress/canaldecortes/painel/.env | cut -d= -f2)"
    curl -X POST "https://api.telegram.org/bot${TOKEN}/setWebhook" \
      -H "Content-Type: application/json" \
      -d '{"url": "https://alessandromelo.com.br/telegramcanal", "allowed_updates": ["message"]}'
    ```
    Esperado: `{"ok":true,"result":true,"description":"Webhook was set"}`

    **Passo 2 — Verificar webhook registrado:**
    ```bash
    curl "https://api.telegram.org/bot${TOKEN}/getWebhookInfo" | python3 -m json.tool
    ```
    Esperado: campo `url` = `https://alessandromelo.com.br/telegramcanal` e AUSÊNCIA de `last_error_message`.

    **Passo 3 — Testar bot no Telegram:**
    Abrir o bot no Telegram e enviar:
    1. `/ajuda` → deve responder com lista de 6 comandos
    2. `/status` → deve responder com contagem de vídeos e clips por status
    3. `/clipes` → deve responder com clips pendentes (ou "Nenhum clip aguardando")

    **Passo 4 — Desativar n8n:**
    No painel do n8n (http://canaldecortes.local:5678):
    1. Desativar workflow `06-router.json` (Telegram Trigger + 6 comandos)
    2. Desativar webhook `/webhook/notify` (receptor de notificações do Python)
    3. Desativar workflow `06-cron-resumo-diario.json` (substituído pelo Artisan Schedule)

    **Passo 5 — Confirmar independência do Cloudflare Tunnel:**
    O bot deve responder mesmo se o container cloudflared estiver parado.
    (O tunnel pode ser mantido para outros fins — apenas não é mais dependência do bot.)

    **Passo 6 (opcional) — Testar deduplicação em produção:**
    Enviar o mesmo update_id duas vezes via curl direto para a URL do webhook:
    ```bash
    PAYLOAD='{"update_id":99998,"message":{"message_id":1,"from":{"id":5760918317,"first_name":"Test"},"chat":{"id":5760918317,"type":"private"},"text":"/ajuda","date":1234567890}}'
    curl -s -X POST https://alessandromelo.com.br/telegramcanal -H "Content-Type: application/json" -d "$PAYLOAD"
    # bot responde com ajuda
    curl -s -X POST https://alessandromelo.com.br/telegramcanal -H "Content-Type: application/json" -d "$PAYLOAD"
    # bot NÃO deve responder novamente (dedup Redis)
    ```
  </how-to-verify>
  <verify>
    <automated>MISSING — verificação requer interação com Telegram API e bot real</automated>
  </verify>
  <done>Bot respondeu a /ajuda e /status no Telegram. getWebhookInfo confirma URL sem last_error_message. Workflows n8n do bot desativados.</done>
  <resume-signal>
    Digite "aprovado" se o bot respondeu a /ajuda e /status no Telegram E getWebhookInfo confirma webhook ativo.
    Ou descreva qualquer problema encontrado (ex: nginx 502, last_error_message no getWebhookInfo, bot não responde).
  </resume-signal>
</task>

</tasks>

<verification>
```bash
# Suite Laravel (final gate)
docker exec php bash -c "cd /var/www/html/painel && ./vendor/bin/pest 2>&1" | tail -3

# Suite Python (final gate — 3 falhas pré-existentes em test_quota_manager são aceitáveis)
docker exec clip-processor python -m pytest -v 2>&1 | tail -10

# Webhook registrado (requer TOKEN do .env)
TOKEN="$(grep TELEGRAM_BOT_TOKEN /Users/alessandrobm1/develop/server/wordpress/canaldecortes/painel/.env | cut -d= -f2)"
curl "https://api.telegram.org/bot${TOKEN}/getWebhookInfo" 2>/dev/null | python3 -m json.tool | grep -E "url|last_error"
```
</verification>

<success_criteria>
- Suite Laravel 100% GREEN (todos os testes, incluindo os 14 novos de Phase 9)
- Suite Python GREEN para test_telegram_notifier e test_internal_api (falhas pré-existentes em test_quota_manager não contam)
- Bot responde a /ajuda e /status no Telegram com respostas corretas
- getWebhookInfo sem last_error_message, url = https://alessandromelo.com.br/telegramcanal
- Workflow n8n do bot (06-router.json) desativado — n8n não está no fluxo do bot
- Cloudflare Tunnel não é dependência do bot (confirmado ou documentado)
- BOT-01, BOT-02, BOT-03 todos verificados end-to-end
</success_criteria>

<output>
Após conclusão, criar `.planning/phases/09-bot-telegram-no-laravel/09-04-SUMMARY.md`
</output>
