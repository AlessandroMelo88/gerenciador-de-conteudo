---
phase: 03-ia-transcri-o-e-sele-o
plan: 03
type: tdd
wave: 1
depends_on:
  - "03-01"
files_modified:
  - clip-processor/src/selector.py
autonomous: true
requirements:
  - AI-02
  - AI-03

must_haves:
  truths:
    - "select_moments() formata segmentos com timestamps [Ns-Ns] e envia ao Claude Haiku via output_config json_schema"
    - "select_moments() retorna lista de dicts com start_time, end_time, score, reason — máximo 3, sem overlap"
    - "insert_selected_moments() insere apenas momentos com score >= 7 em generated_clips com status pending_cut"
    - "Momentos sobrepostos: mantém o de maior score, descarta o outro"
    - "Máximo 3 momentos inseridos por vídeo independente de quantos forem passados"
  artifacts:
    - path: "clip-processor/src/selector.py"
      provides: "Implementação completa de AI-02 e AI-03"
      exports:
        - select_moments
        - insert_selected_moments
        - SYSTEM_PROMPT
        - MOMENT_OUTPUT_SCHEMA
  key_links:
    - from: "clip-processor/src/selector.py"
      to: "anthropic.Anthropic().messages.create()"
      via: "output_config=MOMENT_OUTPUT_SCHEMA — structured outputs, SEM prefill"
      pattern: "output_config"
    - from: "clip-processor/src/selector.py"
      to: "mysql generated_clips"
      via: "INSERT INTO generated_clips (source_video_id, start_time, end_time, score, reason, status) VALUES (%s, %s, %s, %s, %s, 'pending_cut')"
      pattern: "pending_cut"
---

<objective>
Implementar `selector.py` em ciclo TDD RED→GREEN para passar os 6 testes criados no Wave 0 (AI-02 e AI-03).

Purpose: Análise de transcrições via Claude Haiku com structured outputs para identificar momentos de alto impacto (5-10 min), filtrar por score e inserir em `generated_clips`.

Output: `selector.py` completo com implementação que passa todos os 6 testes unitários.
</objective>

<execution_context>
@/Users/alessandrobm1/.claude/get-shit-done/workflows/execute-plan.md
@/Users/alessandrobm1/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-ia-transcri-o-e-sele-o/03-CONTEXT.md
@.planning/phases/03-ia-transcri-o-e-sele-o/03-RESEARCH.md
@.planning/phases/03-ia-transcri-o-e-sele-o/03-01-SUMMARY.md

<interfaces>
<!-- Contratos definidos no Wave 0 — NÃO alterar assinaturas -->

From clip-processor/src/selector.py (skeleton Wave 0):
```python
def select_moments(transcript: dict, anthropic_client=None) -> list[dict]:
    """transcript = {'video_id', 'text', 'segments': [{'start', 'end', 'text'}]}
    Retorna lista de {'start_time', 'end_time', 'score', 'reason'}, máx 3, sem overlap."""

def insert_selected_moments(conn, source_video_id: int, video_id: str, moments: list[dict]) -> int:
    """source_video_id = FK INT para source_videos.id (não youtube_video_id string)
    Retorna número de momentos inseridos (score >= 7)."""
```

Pattern Claude Haiku structured outputs (de 03-RESEARCH.md):
```python
# CRÍTICO: usar output_config — prefill com '[' retorna HTTP 400 em modelos 4.x
MOMENT_OUTPUT_SCHEMA = {
    'format': {
        'type': 'json_schema',
        'schema': {
            'type': 'object',
            'properties': {
                'moments': {
                    'type': 'array',
                    'items': {
                        'type': 'object',
                        'properties': {
                            'start_time': {'type': 'number'},
                            'end_time': {'type': 'number'},
                            'score': {'type': 'number'},
                            'reason': {'type': 'string'},
                        },
                        'required': ['start_time', 'end_time', 'score', 'reason'],
                        'additionalProperties': False,
                    },
                }
            },
            'required': ['moments'],
            'additionalProperties': False,
        },
    }
}

response = anthropic_client.messages.create(
    model='claude-haiku-4-5',
    max_tokens=2048,
    system=SYSTEM_PROMPT,
    messages=[{'role': 'user', 'content': transcript_text}],
    output_config=MOMENT_OUTPUT_SCHEMA,
)
data = json.loads(response.content[0].text)
moments = data['moments']
```

Algoritmo de remoção de overlap (de 03-RESEARCH.md):
```python
def _remove_overlaps(moments: list[dict]) -> list[dict]:
    sorted_moments = sorted(moments, key=lambda m: m['score'], reverse=True)
    selected = []
    for candidate in sorted_moments:
        overlaps = any(
            not (candidate['end_time'] <= kept['start_time'] or
                 candidate['start_time'] >= kept['end_time'])
            for kept in selected
        )
        if not overlaps:
            selected.append(candidate)
        if len(selected) >= 3:
            break
    return selected
```

Schema gerado_clips (após migration do Wave 0):
```sql
INSERT INTO generated_clips
  (source_video_id, start_time, end_time, score, reason, status)
VALUES (%s, %s, %s, %s, %s, 'pending_cut')
-- NOTA: source_video_id é INT FK para source_videos.id, não o youtube_video_id string
```
</interfaces>
</context>

<feature>
  <name>selector.py — Seleção Claude Haiku + filtro de qualidade (AI-02 + AI-03)</name>
  <files>clip-processor/src/selector.py, clip-processor/tests/test_selector.py</files>
  <behavior>
Ciclo TDD RED → GREEN para os 6 testes de test_selector.py:

RED (já confirmado no Wave 0 — todos retornam NotImplementedError):
- TestSelectMoments::test_returns_moments_list
- TestSelectMoments::test_transcript_formatted_with_timestamps
- TestInsertMoments::test_score_7_inserted
- TestInsertMoments::test_score_6_discarded
- TestInsertMoments::test_overlap_keeps_higher_score
- TestInsertMoments::test_max_3_moments

GREEN (implementar):

1. `SYSTEM_PROMPT` — constante de módulo com instrução para o Haiku:
   "Você é um especialista em identificar momentos virais de vídeos de futebol e podcasts.
   Analise a transcrição fornecida e identifique os melhores segmentos para criar clips de 5 a 10 minutos.
   Para futebol: priorize análise tática, debate acalorado, reação a gol, revelação de bastidores.
   Para podcasts: priorize discussão intensa, revelação importante, momento de conflito ou humor.
   Retorne no máximo 3 momentos não-sobrepostos, ordenados por score decrescente (10 = viral garantido, 1 = sem valor).
   Considere apenas momentos onde o conteúdo é coeso e completo dentro do intervalo de 5-10 minutos."

2. `select_moments(transcript, anthropic_client=None) -> list[dict]`
   - Se anthropic_client é None → instanciar `import anthropic; anthropic_client = anthropic.Anthropic()`
   - Formatar transcript_text: para cada seg em transcript['segments'], montar `f'[{int(seg["start"])}s-{int(seg["end"])}s] {seg["text"]}'` e juntar com '\n'
   - Chamar messages.create() com output_config=MOMENT_OUTPUT_SCHEMA, model='claude-haiku-4-5', max_tokens=2048
   - Parse: `data = json.loads(response.content[0].text); moments = data.get('moments', [])`
   - Aplicar `_remove_overlaps(moments)` — retorna máx 3 sem overlap
   - Em qualquer Exception: logar e retornar []

3. `insert_selected_moments(conn, source_video_id, video_id, moments) -> int`
   - Para cada momento na lista:
     - Se score < 7: logar `[AI] Momento descartado (score {score}): {reason}` e continuar
     - Se score >= 7: INSERT INTO generated_clips (source_video_id, start_time, end_time, score, reason, status) VALUES (%s, %s, %s, %s, %s, 'pending_cut')
     - conn.commit() após cada insert
   - Respeitar limite de 3 momentos: parar após inserir 3 (momentos já vêm ordenados por score de select_moments)
   - Retornar count de inseridos

4. `_remove_overlaps(moments) -> list[dict]` — função interna (não exportada)
   - Ordenar por score decrescente
   - Para cada candidate: verificar se overlap com qualquer momento já selecionado
   - Overlap: candidate.start_time < kept.end_time AND candidate.end_time > kept.start_time
   - Se não overlaps: adicionar a selected
   - Parar quando len(selected) >= 3
   - Retornar selected
  </behavior>
  <implementation>
Implementar as funções em `clip-processor/src/selector.py` sobre o skeleton do Wave 0. Manter as assinaturas exatas.

Após implementar, rodar:
```bash
cd /Users/alessandrobm1/develop/server/wordpress/canaldecortes/clip-processor && python -m pytest tests/test_selector.py -v --tb=short
```

Todos os 6 testes devem passar (GREEN). Se algum falhar, corrigir antes de prosseguir.

Depois rodar a suite completa:
```bash
cd /Users/alessandrobm1/develop/server/wordpress/canaldecortes/clip-processor && python -m pytest tests/ -v --tb=short
```
  </implementation>
</feature>

<verification>
```bash
cd /Users/alessandrobm1/develop/server/wordpress/canaldecortes/clip-processor && python -m pytest tests/test_selector.py -v --tb=short
```

Esperado: 6 passed, 0 failed

Suite completa:
```bash
cd /Users/alessandrobm1/develop/server/wordpress/canaldecortes/clip-processor && python -m pytest tests/ -v --tb=short
```
</verification>

<success_criteria>
- `pytest tests/test_selector.py` → 6 passed, 0 failed
- `pytest tests/` → todos os testes passam (sem regressão)
- `selector.py` usa output_config (não prefill) — crítico para claude-haiku-4-5
- `insert_selected_moments` usa source_video_id INT (não youtube_video_id string) — FK correto
- Limite de 3 momentos e remoção de overlap implementados
</success_criteria>

<output>
Após conclusão, criar `.planning/phases/03-ia-transcri-o-e-sele-o/03-03-SUMMARY.md`
</output>
