---
phase: 03-ia-transcri-o-e-sele-o
plan: 04
type: execute
wave: 2
depends_on:
  - "03-02"
  - "03-03"
files_modified:
  - clip-processor/src/rss_poller.py
autonomous: false
requirements:
  - AI-01
  - AI-02
  - AI-03

must_haves:
  truths:
    - "poll_all_channels() chama transcrição e seleção para cada vídeo com status downloaded"
    - "Fluxo completo: downloaded → transcribing → selecting → (momentos em generated_clips com pending_cut)"
    - "Falha na transcrição marca o vídeo como failed sem abortar o poll dos demais canais"
    - "transcript_path é salvo no banco antes de chamar select_moments"
  artifacts:
    - path: "clip-processor/src/rss_poller.py"
      provides: "Integração do pipeline de IA no fluxo de polling"
      contains: "_process_ai_pipeline"
  key_links:
    - from: "clip-processor/src/rss_poller.py"
      to: "clip-processor/src/transcriber.py"
      via: "from src.transcriber import transcribe_video, save_transcript"
      pattern: "from src.transcriber import"
    - from: "clip-processor/src/rss_poller.py"
      to: "clip-processor/src/selector.py"
      via: "from src.selector import select_moments, insert_selected_moments"
      pattern: "from src.selector import"
    - from: "rss_poller._process_ai_pipeline"
      to: "source_videos.status"
      via: "update_status(conn, video_id, 'transcribing') → update_status(conn, video_id, 'selecting')"
      pattern: "transcribing.*selecting"
---

<objective>
Integrar `transcriber.py` e `selector.py` no fluxo existente do `rss_poller.py` e verificar o pipeline completo end-to-end com checkpoint humano.

Purpose: Fechar o ciclo da Phase 3 — vídeos com status `downloaded` agora transitam automaticamente por `transcribing` → `selecting` → clips em `generated_clips` com `pending_cut`, sem nenhuma intervenção manual.

Output:
- `rss_poller.py` atualizado com `_process_ai_pipeline()` integrado ao fluxo de polling
- Checkpoint humano confirmando que o fluxo end-to-end funciona
</objective>

<execution_context>
@/Users/alessandrobm1/.claude/get-shit-done/workflows/execute-plan.md
@/Users/alessandrobm1/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-ia-transcri-o-e-sele-o/03-CONTEXT.md
@.planning/phases/03-ia-transcri-o-e-sele-o/03-02-SUMMARY.md
@.planning/phases/03-ia-transcri-o-e-sele-o/03-03-SUMMARY.md

<interfaces>
<!-- Interfaces existentes em rss_poller.py que devem ser preservadas -->

From clip-processor/src/rss_poller.py (atual):
```python
def poll_all_channels(db_conn=None, redis_client=None) -> None:
    """Gerencia conexões próprias se None. Busca canais ativos, faz RSS poll, insere vídeos novos."""
    # ... fluxo atual: buscar canais → RSS → is_seen → insert_video
    # ADICIONAR: após insert_video (ou em chamada separada), processar vídeos downloaded
```

From clip-processor/src/db.py:
```python
def update_status(conn, video_id: str, status: str, local_path: str = None) -> None:
    """Atualiza source_videos.status. Não tem parâmetro transcript_path."""
```

From clip-processor/src/transcriber.py (Wave 1 — implementado):
```python
def transcribe_video(video_id: str, video_path: str, groq_client=None, db_conn=None) -> dict | None:
    """Retorna {'video_id', 'text', 'segments'} ou None em falha."""

def save_transcript(conn, video_id: str, transcript: dict) -> str:
    """Salva JSON em disco, atualiza transcript_path, retorna caminho."""

VIDEOS_DIR: str  # '/app/videos'
```

From clip-processor/src/selector.py (Wave 1 — implementado):
```python
def select_moments(transcript: dict, anthropic_client=None) -> list[dict]:
    """Retorna máx 3 momentos sem overlap com score >= threshold."""

def insert_selected_moments(conn, source_video_id: int, video_id: str, moments: list[dict]) -> int:
    """source_video_id = INT FK (não string). Insere score>=7. Retorna count inserido."""
```

NOTA CRÍTICA sobre source_video_id:
```python
# generated_clips.source_video_id é FK INT para source_videos.id
# rss_poller tem acesso ao db_conn e pode fazer lookup:
# SELECT id FROM source_videos WHERE youtube_video_id = %s
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Integrar pipeline de IA no rss_poller.py</name>
  <files>clip-processor/src/rss_poller.py</files>
  <behavior>
Comportamento esperado após a integração:

- poll_all_channels() mantém comportamento atual (RSS poll + insert de novos vídeos) E ADICIONALMENTE:
- Para cada vídeo com status `downloaded` no banco: executa _process_ai_pipeline()
- _process_ai_pipeline():
  - update_status(conn, video_id, 'transcribing')
  - transcript = transcribe_video(video_id, video_path, groq_client=groq_client)
  - Se transcript é None: update_status(conn, video_id, 'failed'); retornar
  - save_transcript(conn, video_id, transcript)
  - update_status(conn, video_id, 'selecting')
  - SELECT id FROM source_videos WHERE youtube_video_id = %s → source_video_id INT
  - moments = select_moments(transcript, anthropic_client=anthropic_client)
  - insert_selected_moments(conn, source_video_id, video_id, moments)
  - Não atualiza status final aqui — Phase 4 (cutting) fará a transição
- Falha em _process_ai_pipeline de um vídeo não aborta o processamento dos demais
  </behavior>
  <action>
Modificar `clip-processor/src/rss_poller.py` adicionando:

**1. Imports novos no topo:**
```python
from src.transcriber import transcribe_video, save_transcript, VIDEOS_DIR
from src.selector import select_moments, insert_selected_moments
```

**2. Função interna `_process_ai_pipeline`:**
```python
def _process_ai_pipeline(conn, video_id: str, local_path: str, groq_client=None, anthropic_client=None) -> None:
    """Executa transcrição + seleção para um vídeo com status downloaded.

    Args:
        conn: conexão pymysql ativa (quem chama fecha)
        video_id: youtube_video_id
        local_path: caminho do arquivo .mp4 em disco
        groq_client: cliente Groq (None = produção, injetado = testes)
        anthropic_client: cliente Anthropic (None = produção, injetado = testes)
    """
    from src.db import update_status

    try:
        # Transcrição
        update_status(conn, video_id, 'transcribing')
        transcript = transcribe_video(video_id, local_path, groq_client=groq_client)
        if transcript is None:
            _log(f'[AI] Transcrição falhou para {video_id} — marcando como failed')
            update_status(conn, video_id, 'failed')
            return

        save_transcript(conn, video_id, transcript)

        # Seleção
        update_status(conn, video_id, 'selecting')

        # Obter source_video_id INT para FK em generated_clips
        with conn.cursor() as cur:
            cur.execute('SELECT id FROM source_videos WHERE youtube_video_id = %s', (video_id,))
            row = cur.fetchone()
        if row is None:
            _log(f'[AI] AVISO: source_video_id não encontrado para {video_id}')
            return
        source_video_id = row['id']

        moments = select_moments(transcript, anthropic_client=anthropic_client)
        inserted = insert_selected_moments(conn, source_video_id, video_id, moments)
        _log(f'[AI] Pipeline concluído para {video_id}: {inserted} momento(s) inserido(s) em generated_clips')

    except Exception as exc:
        _log(f'[AI] ERRO no pipeline de IA para {video_id}: {exc}')
        try:
            update_status(conn, video_id, 'failed')
        except Exception:
            pass
```

**3. Adicionar chamada ao pipeline dentro de `poll_all_channels`**, após a seção de RSS polling, antes do `finally`:

```python
        # Processar vídeos que já estão downloaded (transcrição + seleção)
        try:
            with db_conn.cursor() as cur:
                cur.execute(
                    "SELECT youtube_video_id, local_path FROM source_videos "
                    "WHERE status = 'downloaded' AND local_path IS NOT NULL"
                )
                downloaded_videos = cur.fetchall()

            for video_row in downloaded_videos:
                vid_id = video_row['youtube_video_id']
                vid_path = video_row['local_path']
                _log(f'[AI] Iniciando pipeline de IA para {vid_id}')
                _process_ai_pipeline(db_conn, vid_id, vid_path)

        except Exception as exc:
            _log(f'[AI] ERRO ao buscar vídeos downloaded para processamento: {exc}')
```

**ATENÇÃO:** Preservar TODA a lógica existente de RSS polling em `poll_all_channels`. O bloco de IA vem DEPOIS do loop de canais existente, antes do `finally`.

Injeção de dependência `groq_client` e `anthropic_client` NÃO é necessária em `poll_all_channels` — os módulos transcriber e selector criam seus próprios clientes quando recebem None. Se futuramente for necessário injetar para testes de integração, pode ser adicionado como parâmetro opcional.
  </action>
  <verify>
    <automated>cd /Users/alessandrobm1/develop/server/wordpress/canaldecortes/clip-processor && python -m pytest tests/ -v --tb=short</automated>
  </verify>
  <done>
- `rss_poller.py` importa transcriber e selector
- `_process_ai_pipeline()` existe na função interna
- Chamada a `_process_ai_pipeline` existe dentro de `poll_all_channels` para vídeos `downloaded`
- Suite completa de testes passa sem regressão
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Checkpoint: Verificação end-to-end da Phase 3</name>
  <what-built>
Pipeline de IA completo integrado ao daemon:

1. **Schema MySQL migrado** — `generated_clips` aceita `pending_cut`, `reason`; `source_videos` tem `transcript_path`
2. **transcriber.py** — Groq Whisper API transcreve vídeos PT-BR, salva JSON, resiste a arquivos >25MB via ffmpeg
3. **selector.py** — Claude Haiku seleciona momentos 5-10min com score 1-10, filtra por score >=7, remove overlaps, máx 3 por vídeo
4. **rss_poller.py** — integrado: vídeos downloaded → transcribing → selecting → generated_clips(pending_cut)
5. **11 testes unitários** passando (5 AI-01 + 6 AI-02/AI-03)
  </what-built>
  <how-to-verify>
**Verificação 1 — Testes automatizados:**
```bash
cd /Users/alessandrobm1/develop/server/wordpress/canaldecortes/clip-processor
python -m pytest tests/ -v --tb=short
```
Esperado: todos os testes passam (incluindo os 11 novos + regressão dos anteriores).

**Verificação 2 — Schema MySQL aplicado (se container MySQL estiver rodando):**
```bash
cd /Users/alessandrobm1/develop/server/wordpress/canaldecortes
docker exec mysql mysql -uroot -prootpassword clips_automation \
  -e "SHOW COLUMNS FROM generated_clips;" \
  -e "SHOW COLUMNS FROM source_videos LIKE 'transcript_path';"
```
Esperado: coluna `reason`, `status` com `pending_cut` em generated_clips; `transcript_path` em source_videos.

**Verificação 3 — Aplicar migration se necessário:**
```bash
docker exec -i mysql mysql -uroot -prootpassword < /Users/alessandrobm1/develop/server/wordpress/canaldecortes/mysql/init/03-schema-migration.sql
```

**Verificação 4 — Inspeção de código:**
Confirmar visualmente que:
- `rss_poller.py` tem `_process_ai_pipeline()` e chamada para vídeos `downloaded`
- `selector.py` usa `output_config` (não prefill) para Claude Haiku
- `transcriber.py` tem verificação de tamanho de arquivo e fallback ffmpeg
  </how-to-verify>
  <resume-signal>Digite "aprovado" para prosseguir, ou descreva os problemas encontrados</resume-signal>
</task>

</tasks>

<verification>
Phase 3 completa quando:
1. `pytest tests/` → todos os testes passam (0 failures)
2. Schema MySQL tem `pending_cut` em `generated_clips.status`, `reason TEXT` e `transcript_path VARCHAR(500)`
3. `rss_poller.py` contém `_process_ai_pipeline` chamando transcriber e selector
4. Checkpoint humano aprovado
</verification>

<success_criteria>
- 11 novos testes unitários passando (5 AI-01 + 6 AI-02/AI-03)
- Suite completa sem regressão (testes Phase 1 e Phase 2 também passam)
- `rss_poller.py` integrado — vídeos `downloaded` avançam para `transcribing` → `selecting` → `generated_clips(pending_cut)`
- Checkpoint humano confirma pipeline funcional
</success_criteria>

<output>
Após conclusão, criar `.planning/phases/03-ia-transcri-o-e-sele-o/03-04-SUMMARY.md`
</output>
