---
phase: 06-controle-manual-n8n-telegram
plan: 02
type: execute
wave: 1
depends_on: [06-01]
files_modified:
  - clip-processor/src/publisher.py
autonomous: true
requirements: [CTRL-02]
validation_tasks: [6-02-01, 6-02-02, 6-02-03]

must_haves:
  truths:
    - "publisher.py seleciona apenas clipes com status='approved' (em vez de 'pending')"
    - "Lógica de quota (max 2/dia, configurável até 6) permanece intocada e bloqueia uploads quando excedida — clip continua approved"
    - "Janela horária 19h-22h America/Sao_Paulo permanece intocada — clip approved fora da janela não é publicado"
    - "Guard de status no UPDATE que move approved → publishing (defesa contra race com /rejeitar)"
    - "Testes TestPublishApprovedClips (criados em Plan 01) passam GREEN"
  artifacts:
    - path: "clip-processor/src/publisher.py"
      provides: "Publisher refatorado: pega approved + guard de status no UPDATE para publishing"
      contains: "status = 'approved'"
  key_links:
    - from: "clip-processor/src/publisher.py"
      to: "MySQL generated_clips"
      via: "SELECT ... WHERE status='approved' ORDER BY created_at ASC LIMIT 1"
      pattern: "status\\s*=\\s*'approved'"
    - from: "clip-processor/src/publisher.py"
      to: "MySQL generated_clips (transition)"
      via: "UPDATE ... SET status='publishing' WHERE id=%s AND status='approved'"
      pattern: "publishing.*status\\s*=\\s*'approved'"
---

<objective>
Substituir a query de seleção em `publisher.py` para pegar clipes `approved` em vez de `pending`, e endurecer o UPDATE para publishing com guard de status (defesa contra race com /rejeitar concorrente).

Purpose: CTRL-02 — o pipeline para de publicar automaticamente; passa a requerer aprovação humana. Mudança cirúrgica de 1 literal + 1 guard. Tudo o mais (quota, janela horária, cleanup de raw) permanece intocado.

Output: `publisher.py` atualizado, classe `TestPublishApprovedClips` (criada em Plan 01) em GREEN.
</objective>

<execution_context>
@/Users/alessandrobm1/.claude/get-shit-done/workflows/execute-plan.md
@/Users/alessandrobm1/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-controle-manual-n8n-telegram/06-CONTEXT.md
@.planning/phases/06-controle-manual-n8n-telegram/06-RESEARCH.md
@.planning/phases/06-controle-manual-n8n-telegram/06-VALIDATION.md
@.planning/phases/06-controle-manual-n8n-telegram/06-01-SUMMARY.md

# Implementação atual a modificar
@clip-processor/src/publisher.py

# Testes esperados a virar GREEN
@clip-processor/tests/test_publisher.py

# Dependências (permanecem intocadas, mas referência útil)
@clip-processor/src/quota_manager.py
@clip-processor/src/uploader.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: publisher.py — swap 'pending' → 'approved' + guard de status no UPDATE</name>
  <files>clip-processor/src/publisher.py</files>
  <behavior>
    - Test 1 (test_seleciona_apenas_approved): SELECT da próxima fila contém `status='approved'`. Mock cursor recebe execute() com query contendo essa string; o publisher chama o uploader normalmente.
    - Test 2 (test_quota_blocked_leaves_clip_as_approved): QuotaManager.can_upload_today() retorna False. Publisher NÃO chama uploader, NÃO faz UPDATE status='publishing'. Clip permanece approved.
    - Test 3 (test_fora_da_janela_horaria): QuotaManager.is_within_publishing_window() retorna False. Mesmo comportamento: nenhum upload, clip continua approved.
    - (Implícito) UPDATE para publishing usa guard `WHERE id=%s AND status='approved'`. Se 0 rows afetadas, pular o clip silenciosamente (defesa contra race com /rejeitar).
  </behavior>
  <action>
**Antes de tocar publisher.py:** rodar `pytest tests/test_publisher.py::TestPublishApprovedClips -x` e confirmar RED (testes existem e falham). Esta é a baseline.

**Mudança 1 — SELECT (linha-única):**
Localizar a query SELECT que escolhe o próximo clip a publicar. No padrão Phase 5, é algo como:
```python
"SELECT id, clip_path, thumbnail_path, title, description, tags, source_video_id "
"FROM generated_clips WHERE status='pending' ORDER BY created_at ASC LIMIT 1"
```
Trocar `status='pending'` por `status='approved'`. Manter ORDER BY (FIFO) e LIMIT 1.

**Mudança 2 — UPDATE de transição:**
O UPDATE que move o clip de pending → publishing (Phase 5) hoje provavelmente é:
```python
"UPDATE generated_clips SET status='publishing' WHERE id=%s"
```
Adicionar guard de status:
```python
"UPDATE generated_clips SET status='publishing' WHERE id=%s AND status='approved'"
```
**Verificar rowcount após o UPDATE.** Se `cur.rowcount == 0`, o clip foi rejeitado em paralelo. Logar `[PUB] Clip {id} pulado: status mudou durante seleção`, fazer COMMIT vazio e retornar do método sem invocar o uploader. Este é o cenário do RESEARCH Pitfall 2.

**Mudança 3 — não tocar nada além disso:**
- QuotaManager: NÃO modificar
- Janela horária: NÃO modificar
- Cleanup do raw video após published: NÃO modificar (PUB-04 continua válido — Phase 5)
- Logging de sucesso/falha: manter atual; Plan 06 vai adicionar a chamada ao telegram_notifier opcional
- Imports: nada novo precisa ser adicionado neste plan

**Logs/comentários:**
- Comentário no topo da função afetada: `# Phase 6: seleciona apenas 'approved' (mudança de pending). Bot Telegram aprova via /aprovar.`
- Log INFO: `[PUB] Próximo clip approved: id={id}` (substitui qualquer "pending" no log)

**Testes (verificar GREEN):**
Rodar `docker exec clip-processor pytest tests/test_publisher.py::TestPublishApprovedClips -x` e confirmar 3 GREEN. Se `test_seleciona_apenas_approved` ainda falhar, verificar a string da query no mock vs o publisher.

**Regressão:**
Rodar `docker exec clip-processor pytest tests/test_publisher.py -x` completo. Os testes legados de Phase 5 devem continuar passando — se algum quebrou, ajustar (provavelmente algum mock usa string 'pending' em assert; trocar para 'approved' OU manter o teste legado intocado se ele testa cenário ortogonal).
  </action>
  <verify>
    <automated>docker exec clip-processor pytest tests/test_publisher.py::TestPublishApprovedClips -x && docker exec clip-processor pytest tests/test_publisher.py -x && grep -E "status\s*=\s*'approved'" clip-processor/src/publisher.py</automated>
  </verify>
  <done>
    - publisher.py SELECT usa `status='approved'`
    - UPDATE para publishing tem guard `AND status='approved'` + rowcount check
    - TestPublishApprovedClips (3 testes) em GREEN
    - test_publisher.py completo em GREEN (sem regressão)
    - Comportamento de quota/janela/cleanup do raw permanece o mesmo (apenas o filtro de seleção mudou)
  </done>
</task>

</tasks>

<verification>
- `pytest tests/test_publisher.py -x` GREEN (todos os testes, incluindo TestPublishApprovedClips)
- `grep status.*approved` em publisher.py mostra ambos: SELECT e UPDATE
- Nenhuma referência a `status='pending'` resta em publisher.py (na lógica de publicação — outras menções a 'pending' podem permanecer se forem comentários explicativos)
</verification>

<success_criteria>
- 6-02-01: `pytest tests/test_publisher.py::TestPublishApprovedClips::test_seleciona_apenas_approved -x` GREEN
- 6-02-02: `pytest tests/test_publisher.py::TestPublishApprovedClips::test_quota_blocked_leaves_clip_as_approved -x` GREEN
- 6-02-03: `pytest tests/test_publisher.py::TestPublishApprovedClips::test_fora_da_janela_horaria -x` GREEN
</success_criteria>

<output>
Após completar, criar `.planning/phases/06-controle-manual-n8n-telegram/06-02-SUMMARY.md` com:
- Mudanças exatas (diff antes/depois das duas queries)
- Decisão de comportamento quando rowcount=0 no UPDATE (silent skip + log)
- Confirmação do que NÃO mudou (quota, janela, cleanup do raw)
- Backlog de clips em 'pending' no momento do deploy: pendência para checkpoint operacional (sql helper opcional em mysql/manual-workflow/approve-backlog.sql — referenciado, não criado neste plan)
</output>
