---
phase: 06-controle-manual-n8n-telegram
plan: 05
type: execute
wave: 2
depends_on: [06-01, 06-02]
files_modified:
  - clip-processor/src/ttl_worker.py
  - clip-processor/src/main.py
autonomous: true
requirements: [CTRL-05]
validation_tasks: [6-05-01, 6-05-02, 6-05-03]

must_haves:
  truths:
    - "run_ttl_once() faz UPDATE em massa: clipes pending com created_at < NOW() - INTERVAL 48 HOUR viram rejected"
    - "run_ttl_once() lista clipes na janela 24h-48h e envia warn para n8n via N8N_NOTIFY_URL (HTTP POST)"
    - "Warn é idempotente: Redis SET NX com TTL=24h impede duplo aviso pelo mesmo clip dentro da janela"
    - "Worker é executado periodicamente via APScheduler dentro do daemon main.py (IntervalTrigger hours=1)"
    - "Retorna dict {'expired': N, 'warned': M} para logging"
  artifacts:
    - path: "clip-processor/src/ttl_worker.py"
      provides: "run_ttl_once(conn=None, redis_client=None) — expire + warn com idempotência"
      contains: "def run_ttl_once"
    - path: "clip-processor/src/main.py"
      provides: "scheduler.add_job(run_ttl_once, 'interval', hours=1, id='clip_pending_ttl', ...)"
      contains: "clip_pending_ttl"
  key_links:
    - from: "clip-processor/src/ttl_worker.py"
      to: "MySQL generated_clips"
      via: "UPDATE ... SET status='rejected' WHERE status='pending' AND created_at < NOW() - INTERVAL %s HOUR"
      pattern: "INTERVAL.*HOUR"
    - from: "clip-processor/src/ttl_worker.py"
      to: "Redis"
      via: "redis.set(f'clip_warned:{id}', '1', nx=True, ex=24*3600) — idempotência do warn"
      pattern: "clip_warned"
    - from: "clip-processor/src/ttl_worker.py"
      to: "n8n (HTTP)"
      via: "requests.post(N8N_NOTIFY_URL, json={'event':'clip_ttl_warning', ...})"
      pattern: "clip_ttl_warning"
    - from: "clip-processor/src/main.py"
      to: "ttl_worker"
      via: "scheduler.add_job(run_ttl_once, 'interval', hours=1)"
      pattern: "add_job.*run_ttl_once"
---

<objective>
Implementar `clip-processor/src/ttl_worker.py` e integrar ao APScheduler em `main.py` (CTRL-05).

Purpose: clipes que ficam em `pending` por muito tempo (operador esqueceu de aprovar) devem ser auto-rejeitados — proteção contra acúmulo silencioso. Worker roda a cada 1h, expira clips com >48h, e avisa 24h antes via Telegram (1 vez por clip). Decisão registrada em CONTEXT/RESEARCH: usar APScheduler no clip-processor (consistente com Phase 2/5), não cron n8n.

Output: `ttl_worker.py` implementado, integrado em `main.py`, 3 testes em GREEN.
</objective>

<execution_context>
@/Users/alessandrobm1/.claude/get-shit-done/workflows/execute-plan.md
@/Users/alessandrobm1/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-controle-manual-n8n-telegram/06-CONTEXT.md
@.planning/phases/06-controle-manual-n8n-telegram/06-RESEARCH.md
@.planning/phases/06-controle-manual-n8n-telegram/06-VALIDATION.md
@.planning/phases/06-controle-manual-n8n-telegram/06-01-SUMMARY.md
@.planning/phases/06-controle-manual-n8n-telegram/06-02-SUMMARY.md

# Stub e testes
@clip-processor/src/ttl_worker.py
@clip-processor/tests/test_ttl_worker.py

# main.py existente (Phase 2/5 já estabeleceu APScheduler — adicionar 1 job)
@clip-processor/src/main.py

# Padrões
@clip-processor/src/db.py
@clip-processor/src/quota_manager.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implementar ttl_worker.run_ttl_once() — expire 48h + warn 24h idempotente</name>
  <files>clip-processor/src/ttl_worker.py</files>
  <behavior>
    - TestExpire::test_expire_marca_rejected_apos_48h: cursor.rowcount=5 no UPDATE. Resultado retornado contém 'expired': 5. Query usa INTERVAL TTL_HOURS HOUR.
    - TestWarn::test_warn_envia_para_n8n: cursor.fetchall() retorna 2 clipes. redis.set(nx=True) retorna True para ambos. requests.post chamado 2x com event='clip_ttl_warning'. Resultado: 'warned': 2.
    - TestWarn::test_no_warn_duplicado: cursor.fetchall() retorna 1 clipe. redis.set(nx=True) retorna False (já avisei). requests.post NÃO é chamado. Resultado: 'warned': 0.
  </behavior>
  <action>
**Antes:** `pytest tests/test_ttl_worker.py -x` → confirmar RED.

**Implementação** (segue Code Example 7 do RESEARCH):

```python
"""Worker de TTL para clipes pending (CTRL-05).

- Expira: clipes com status='pending' e created_at < NOW() - INTERVAL TTL_HOURS HOUR → 'rejected'.
- Avisa: clipes na janela [WARN_HOURS, TTL_HOURS) recebem 1 aviso via Telegram (idempotente via Redis SET NX).

Rodado periodicamente pelo APScheduler em main.py (IntervalTrigger hours=1).
"""
import os
from typing import Optional

import redis
import requests

from src.db import get_db_connection


TTL_HOURS = int(os.environ.get('CLIP_PENDING_TTL_HOURS', 48))
WARN_HOURS = int(os.environ.get('CLIP_PENDING_WARN_HOURS', 24))
N8N_NOTIFY_URL = os.environ.get('N8N_NOTIFY_URL', 'http://n8n:5678/webhook/notify')
WARN_TTL_SECONDS = 24 * 3600  # mesma duração da janela do warn


def run_ttl_once(conn=None, redis_client=None) -> dict:
    """Roda 1 iteração do TTL: expira clipes >48h, avisa clipes 24h-48h.

    Args:
        conn: conexão MySQL (DictCursor). Se None, cria nova e fecha ao final.
        redis_client: cliente Redis. Se None, cria a partir das envs REDIS_HOST/REDIS_PORT.

    Returns:
        {'expired': N, 'warned': M} — quantidade de clipes mudados.
    """
    own_db = conn is None
    own_redis = redis_client is None
    if own_db:
        conn = get_db_connection()
    if own_redis:
        redis_client = redis.Redis(
            host=os.environ.get('REDIS_HOST', 'redis'),
            port=int(os.environ.get('REDIS_PORT', 6379)),
            decode_responses=True,
        )

    try:
        # 1) Expirar clipes com mais de TTL_HOURS
        with conn.cursor() as cur:
            cur.execute(
                "UPDATE generated_clips SET status='rejected' "
                "WHERE status='pending' AND created_at < NOW() - INTERVAL %s HOUR",
                (TTL_HOURS,)
            )
            expired_count = cur.rowcount
        conn.commit()

        # 2) Listar clipes a ponto de expirar (entre WARN_HOURS e TTL_HOURS)
        with conn.cursor() as cur:
            cur.execute(
                "SELECT id, title FROM generated_clips "
                "WHERE status='pending' "
                "AND created_at < NOW() - INTERVAL %s HOUR "
                "AND created_at > NOW() - INTERVAL %s HOUR",
                (WARN_HOURS, TTL_HOURS)
            )
            soon_to_expire = cur.fetchall()

        # 3) Enviar warn idempotente (Redis SET NX com TTL = WARN_TTL_SECONDS)
        warned = 0
        for clip in soon_to_expire:
            key = f'clip_warned:{clip["id"]}'
            if redis_client.set(key, '1', nx=True, ex=WARN_TTL_SECONDS):
                try:
                    requests.post(
                        N8N_NOTIFY_URL,
                        json={
                            'event': 'clip_ttl_warning',
                            'payload': {
                                'clip_id': clip['id'],
                                'title': clip['title'],
                                'expires_in_hours': TTL_HOURS - WARN_HOURS,
                            },
                        },
                        timeout=5,
                    )
                    warned += 1
                except requests.RequestException as exc:
                    print(f'[TTL] warn POST falhou para clip {clip["id"]}: {exc}')
                    # NÃO incrementa warned; Redis já marcou — no próximo run, NX retorna False
                    # Tradeoff conhecido: 1 falha de rede pode "comer" 1 aviso. Aceitável v1.

        print(f'[TTL] expired={expired_count} warned={warned}')
        return {'expired': expired_count, 'warned': warned}
    finally:
        if own_db:
            conn.close()
```

**Pontos importantes:**

1. **Injeção de dependência** (conn, redis_client) — consistente com `poll_all_channels` da Phase 2 e `publish_one()` da Phase 5; testes injetam mocks via parâmetros.
2. **Redis SET NX com TTL=24h** — idempotência durável. Após o warn, mesmo se o worker rodar 24x na janela, NX bloqueia.
3. **Falha de POST não rola back o SET** — decisão pragmática: aceitar perda de no máximo 1 warn por race de rede. Alternativa (rollback do SET) introduziria atomicidade fictícia. Documentar como tradeoff.
4. **Order matters**: expirar PRIMEIRO, depois listar para warn. Caso contrário, um clipe a 47h59min poderia ser warned, depois expirado no mesmo run — confuso.
5. **NÃO chamar `telegram_notifier.notify()`** aqui — Plan 06 vai criar esse módulo, e o ttl_worker já tem seu próprio `requests.post` direto. Manter assim por isolamento (worker não depende de telegram_notifier para rodar).

**Verificar GREEN:**
`docker exec clip-processor pytest tests/test_ttl_worker.py -x` → 3 testes GREEN.
  </action>
  <verify>
    <automated>docker exec clip-processor pytest tests/test_ttl_worker.py -x && docker exec clip-processor python -c "from src.ttl_worker import run_ttl_once; assert callable(run_ttl_once)"</automated>
  </verify>
  <done>
    - ttl_worker.py implementado
    - 3 testes em GREEN (TestExpire + TestWarn 2 testes)
    - run_ttl_once retorna dict {'expired', 'warned'}
    - Redis SET NX garante idempotência do warn
  </done>
</task>

<task type="auto">
  <name>Task 2: Integrar ttl_worker ao APScheduler em main.py</name>
  <files>clip-processor/src/main.py</files>
  <action>
**Antes:** ler `clip-processor/src/main.py` atual. Localizar a seção onde outros jobs são adicionados ao scheduler (Phase 2 estabeleceu o BlockingScheduler com `recover_stuck_downloads` + `poll_all_channels` + `publisher`).

**Adicionar:**
1. Import no topo: `from src.ttl_worker import run_ttl_once`
2. Após os jobs existentes (mas antes de `scheduler.start()`), adicionar:
   ```python
   scheduler.add_job(
       run_ttl_once,
       'interval',
       hours=1,
       id='clip_pending_ttl',
       coalesce=True,
       max_instances=1,
       next_run_time=None,  # roda 1h após o boot, não imediatamente
   )
   ```
   - `coalesce=True` + `max_instances=1`: padrão estabelecido em Phase 2 (não duplicar execuções se o anterior atrasou).
   - `id='clip_pending_ttl'`: identifica o job nos logs e em `scheduler.get_jobs()`.
   - `next_run_time=None` (ou apenas omitir): APScheduler agenda a 1ª execução em `now + interval` = 1h depois do boot. Evita rodar TTL antes do banco estar quente.
3. Log informativo logo após o `add_job`:
   ```python
   print(f"[BOOT] TTL worker agendado: a cada 1h (TTL={ttl_worker.TTL_HOURS}h, WARN={ttl_worker.WARN_HOURS}h)")
   ```
   (Ou similar — o estilo de log do main.py já está estabelecido; manter consistência com os logs `[BOOT]` ou equivalente do Phase 2/5.)

**Não modificar nada mais** — quota_manager, publisher_job, recover_stuck_downloads, poll_all_channels permanecem como estão.

**Smoke check sem subir o daemon:**
```bash
docker exec clip-processor python -c "import src.main" 2>&1 | tail
```
Deve carregar sem ImportError. (Não dispara o `BlockingScheduler.start()` porque está dentro do `if __name__ == '__main__'`.)

**Não há teste unit para o `add_job`** — é configuração estática. O teste end-to-end (Plan 07 checkpoint manual) cobre.
  </action>
  <verify>
    <automated>docker exec clip-processor python -c "import src.main" && grep -E "run_ttl_once|clip_pending_ttl" clip-processor/src/main.py</automated>
  </verify>
  <done>
    - main.py importa `run_ttl_once`
    - `scheduler.add_job(run_ttl_once, 'interval', hours=1, id='clip_pending_ttl', coalesce=True, max_instances=1)` presente
    - `import src.main` sem erro
    - Nenhum job existente removido ou alterado
  </done>
</task>

</tasks>

<verification>
- `pytest tests/test_ttl_worker.py -x` GREEN (3 testes)
- `pytest tests/ -v --tb=short` GREEN (full suite — sem regressão nos testes do Phase 2/5)
- `grep run_ttl_once clip-processor/src/main.py` mostra import + add_job
</verification>

<success_criteria>
- 6-05-01: `pytest tests/test_ttl_worker.py::TestExpire -x` GREEN
- 6-05-02: `pytest tests/test_ttl_worker.py::TestWarn -x` GREEN
- 6-05-03: `pytest tests/test_ttl_worker.py::TestWarn::test_no_warn_duplicado -x` GREEN
</success_criteria>

<output>
Criar `.planning/phases/06-controle-manual-n8n-telegram/06-05-SUMMARY.md` com:
- Decisão: APScheduler dentro do clip-processor (vs n8n cron) — registrar no STATE.md como decisão de fase
- Decisão: ordem expire-then-warn (não warn-then-expire) para evitar clip avisado e expirado no mesmo run
- Tradeoff: falha de POST do warn marca Redis e perde aquela mensagem — aceitável no v1
- Confirmação: ttl_worker não depende de telegram_notifier (isolamento)
</output>
