# Continue Here — Phase 6 Plan 06-07

## Current State

Plan 06-07 Task 1 is complete and committed in `38902cc`.

Validated locally:
- `telegram-n8n/workflows/06-router.json` parses as JSON.
- `telegram-n8n/workflows/06-cron-resumo-diario.json` parses as JSON.
- Router contains Telegram Trigger, Switch, ExecuteCommand, MySQL, Webhook `/notify`, Telegram, Code and IF nodes.
- Critical commands are present for `/rejeitar` and `/processar`.
- `/aprovar` UPDATE guards `status='pending'` and writes `status='approved'`.
- Cron expression is `0 18 * * *` with timezone `America/Sao_Paulo`.
- `mysql/manual-workflow/approve-backlog.sql` exists.
- `telegram-n8n/SETUP.md` contains Cloudflare Tunnel and Telegram `setWebhook` sections.
- `python3 scripts/validate-phase6-n8n.py` covers the structural checks above without calling external services.

## Blocking Manual Checkpoint

Task 2 is pending and requires operator access to external dashboards/credentials:

1. Create Telegram bot in `@BotFather`.
2. Fill `/Users/alessandrobm1/develop/server/wordpress/.env`:
   - `TELEGRAM_BOT_TOKEN`
   - `TELEGRAM_WEBHOOK_SECRET`
   - confirm `TELEGRAM_CHAT_ID_ALLOWED=5760918317`
3. Create Cloudflare Tunnel in Zero Trust and fill:
   - `CLOUDFLARE_TUNNEL_TOKEN`
   - `N8N_WEBHOOK_URL=https://n8n.<domain>`
   - `N8N_HOST=n8n.<domain>`
4. From `/Users/alessandrobm1/develop/server/wordpress`, run:
   - `docker compose up -d cloudflared`
   - `docker compose restart n8n`
5. Configure Telegram `setWebhook` to `$N8N_WEBHOOK_URL/webhook/telegram-canaldecortes` with `secret_token`.
6. Import and activate:
   - `telegram-n8n/workflows/06-router.json`
   - `telegram-n8n/workflows/06-cron-resumo-diario.json`
7. Smoke test from allowed chat:
   - `/ajuda`
   - `/status`
   - `/clipes`
8. Smoke test allowlist from a non-allowed chat:
   - `/ajuda` should receive no response for 30 seconds.

## Resume Signal

When operator returns:
- `approved` means Task 2 passed and Task 3 smoke end-to-end can run.
- `failed: <description>` means investigate the failing setup step.
- `skip` means defer Phase 6 completion.

After Task 3 passes, create `06-07-SUMMARY.md`, mark Phase 6 complete, and run final verification.
