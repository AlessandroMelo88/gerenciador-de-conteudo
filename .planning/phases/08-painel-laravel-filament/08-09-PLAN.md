---
phase: 08-painel-laravel-filament
plan: 09
type: execute
wave: 5
depends_on: ["08-04", "08-08"]
files_modified:
  - /Users/alessandrobm1/develop/server/wordpress/canaldecortes/painel/README.md
autonomous: false
requirements: [PANEL-01, PANEL-02, PANEL-03, PANEL-04, PANEL-05]
must_haves:
  truths:
    - "Setup fresh do painel (composer install + create-user + up serviços) leva o operador de zero até dashboard funcional"
    - "Login com credenciais criadas via `painel:create-user` funciona no browser real"
    - "Adicionar canal-fonte via UI usa yt-dlp real (sidecar) e persiste em `source_channels`"
    - "Adicionar canal-destino via UI persiste em `destination_channels`; badge OAuth reflete arquivo `token-{slug}.json` de verdade"
    - "Aprovar/Rejeitar via UI produz mesmo efeito de `/aprovar` e `/rejeitar` do Telegram (approved status, MP4 removido)"
    - "Dashboard atualiza em ≤5s sem full-page reload (network tab só mostra XHR Livewire)"
    - "Suite completa (Python + Laravel) 100% GREEN"
  artifacts:
    - path: "/Users/alessandrobm1/develop/server/wordpress/canaldecortes/painel/README.md"
      provides: "Instruções finais atualizadas com setup completo end-to-end"
  key_links:
    - from: "browser em canaldecortes.local"
      to: "Filament AdminPanel"
      via: "nginx → php:9000 → Laravel"
      pattern: "canaldecortes.local"
---

<objective>
Verificação end-to-end humana + gate de suite verde. Encerra Phase 8 com prova de que os 5 requisitos PANEL-XX estão observáveis no ambiente rodando, não só nos testes.

Purpose: os testes cobrem comportamento; o checkpoint aqui cobre integração real (Filament renderiza no navegador, Livewire polling ocorre de fato, sidecar HTTP funciona com yt-dlp real, ação de rejeitar remove MP4 no disco).
Output: 1 relatório de verificação humana + gate de PR-ready.
</objective>

<execution_context>
@/Users/alessandrobm1/.claude/get-shit-done/workflows/execute-plan.md
@/Users/alessandrobm1/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/alessandrobm1/develop/server/wordpress/canaldecortes/.planning/phases/08-painel-laravel-filament/08-01-SUMMARY.md
@/Users/alessandrobm1/develop/server/wordpress/canaldecortes/.planning/phases/08-painel-laravel-filament/08-02-SUMMARY.md
@/Users/alessandrobm1/develop/server/wordpress/canaldecortes/.planning/phases/08-painel-laravel-filament/08-03-SUMMARY.md
@/Users/alessandrobm1/develop/server/wordpress/canaldecortes/.planning/phases/08-painel-laravel-filament/08-04-SUMMARY.md
@/Users/alessandrobm1/develop/server/wordpress/canaldecortes/.planning/phases/08-painel-laravel-filament/08-05-SUMMARY.md
@/Users/alessandrobm1/develop/server/wordpress/canaldecortes/.planning/phases/08-painel-laravel-filament/08-06-SUMMARY.md
@/Users/alessandrobm1/develop/server/wordpress/canaldecortes/.planning/phases/08-painel-laravel-filament/08-07-SUMMARY.md
@/Users/alessandrobm1/develop/server/wordpress/canaldecortes/.planning/phases/08-painel-laravel-filament/08-08-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rodar full suite (Python + Laravel) e atualizar README com fluxo de setup consolidado</name>
  <files>
    - /Users/alessandrobm1/develop/server/wordpress/canaldecortes/painel/README.md
  </files>
  <action>
    1. Rodar suíte Python completa:
    ```
    docker exec -T clip-processor pytest tests/ -v 2>&1 | tail -30
    ```
    Contar total, passed, failed. Copiar para o SUMMARY.

    2. Rodar suíte Laravel Pest completa:
    ```
    docker exec -T php bash -c "cd /var/www/html/painel && php artisan test --no-interaction 2>&1 | tail -30"
    ```
    Contar total, passed, failed. Copiar para o SUMMARY.

    3. Atualizar `painel/README.md` (criado no Plan 08-01, provavelmente enxuto) com **fluxo consolidado** de setup fresh — passo a passo:
    ```markdown
    # Painel Canal de Cortes

    Interface web (Laravel 13 + Filament 5) para o operador do pipeline Canal de Cortes:
    - Gerenciar canais-fonte (RSS) e canais-destino (YouTube).
    - Ver dashboard em tempo real do pipeline (uploads, cota, falhas).
    - Aprovar/rejeitar clips gerados pela IA — mesmo efeito de `/aprovar` e `/rejeitar` no Telegram.

    ## Setup local (uma vez)

    1. Garantir que `wordpress/` docker-compose está rodando:
       ```
       docker compose -f wordpress/docker-compose.yml up -d nginx php mysql redis clip-processor
       ```

    2. Adicionar host local:
       ```
       echo "127.0.0.1 canaldecortes.local" | sudo tee -a /etc/hosts
       ```

    3. Instalar dependências PHP:
       ```
       docker exec -it php bash -c "cd /var/www/html/painel && composer install"
       ```

    4. Gerar token do sidecar HTTP (compartilhado entre Laravel e clip-processor):
       ```
       openssl rand -hex 32
       ```
       Colar o hex em DOIS lugares:
       - `canaldecortes/.env` → `CLIP_PROCESSOR_INTERNAL_TOKEN=<hex>`
       - `canaldecortes/painel/.env` → `CLIP_PROCESSOR_INTERNAL_TOKEN=<hex>`

    5. Preencher `canaldecortes/painel/.env` com `DB_PASSWORD` (mesmo valor de `CLIPS_DB_PASSWORD` do `canaldecortes/.env`).

    6. Aplicar a migration de OAuth flag (idempotente):
       ```
       docker exec -i mysql mysql -uroot -p${MYSQL_ROOT_PASSWORD} clips_automation \
         < canaldecortes/mysql/init/07-panel-oauth-flag-migration.sql
       ```

    7. Rodar migrations do Laravel (só cria users/sessions/cache/jobs em `clips_automation`):
       ```
       docker exec -it php bash -c "cd /var/www/html/painel && php artisan migrate --no-interaction"
       ```

    8. Criar o operador (interativo — senha nunca ecoa):
       ```
       docker exec -it php php artisan --working-dir=/var/www/html/painel painel:create-user
       ```
       Email registrado: `alessandrobm1988@gmail.com`. Senha ≥10 chars.

    9. Recriar container `clip-processor` para carregar `CLIP_PROCESSOR_INTERNAL_TOKEN`:
       ```
       docker compose -f wordpress/docker-compose.yml up -d --force-recreate clip-processor
       ```

    10. Acessar: http://canaldecortes.local → login → dashboard.

    ## Reset de senha

    ```
    docker exec -it php php artisan --working-dir=/var/www/html/painel painel:reset-password alessandrobm1988@gmail.com
    ```

    ## Preview MP4 dos clips

    Este painel NÃO embute player HTML5 (por decisão explícita — reduz superfície de ataque).
    Os arquivos MP4 ficam em `canaldecortes/videos/clips/` no filesystem local:
    ```
    open canaldecortes/videos/clips/{clip_id}.mp4   # macOS
    ```

    ## Autorizar novo canal-destino (OAuth)

    Após criar o canal-destino no painel, o painel mostra este comando copy-paste:
    ```
    docker exec -it clip-processor python -m src.youtube_oauth --channel {slug}
    ```
    Rodar no host, seguir o fluxo do Google. Volta ao painel: badge OAuth vira "authorized".

    ## Testes

    - Suíte Laravel: `docker exec -it php bash -c "cd /var/www/html/painel && php artisan test"`
    - Suíte Python: `docker exec -it clip-processor pytest tests/ -v`
    ```

    4. Commit final. NÃO executar `git push` — apenas commit local (o operador decide push).
  </action>
  <verify>
    <automated>docker exec -T clip-processor pytest tests/ -q 2>&1 | tail -3 | grep -qE "(0 failed|passed)" && docker exec -T php bash -c "cd /var/www/html/painel && php artisan test --no-interaction 2>&1 | tail -3" | grep -qE "(0 failed|Tests:|passed)" && echo BOTH_SUITES_GREEN</automated>
  </verify>
  <done>
    - README atualizado com fluxo completo de setup fresh (10 passos).
    - Suíte Python 100% GREEN (contador copiado para SUMMARY).
    - Suíte Laravel 100% GREEN (contador copiado para SUMMARY).
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Checkpoint humano — validar 5 fluxos end-to-end no browser real</name>
  <files>N/A (checkpoint humano — leitura de dashboard no browser)</files>
  <action>Pausar execução automatizada e apresentar ao operador o bloco <how-to-verify> abaixo (7 passos: setup fresh, PANEL-05, PANEL-01, PANEL-02, PANEL-03, PANEL-04, gate de suite). Aguardar operador responder com "approved" ou lista de defeitos. NÃO prosseguir sem confirmação humana.</action>
  <what-built>
    Painel Laravel/Filament completo:
    - Bootstrap Docker + nginx + Laravel 13 + Filament 5 + Pest 4 (Plans 08-01 a 08-03).
    - Autenticação PANEL-05 (Plan 08-04).
    - CRUD canais-fonte com sidecar HTTP para yt-dlp (Plan 08-05 + 08-07).
    - CRUD canais-destino com badge OAuth e captura de RefreshError no uploader (Plan 08-06).
    - Dashboard com 4 widgets + Aprovar/Rejeitar inline (Plan 08-08).
  </what-built>
  <how-to-verify>
    Operador roda no host (mac):

    1. **Setup fresh via README**
       - Seguir os 10 passos do `canaldecortes/painel/README.md` numa sessão de terminal limpa.
       - Confirmar que `curl http://canaldecortes.local` retorna 302 para `/admin/login` (sem sessão).

    2. **PANEL-05 (Auth)**
       - Abrir `http://canaldecortes.local` no navegador.
       - Confirmar redirect para `/admin/login`.
       - Digitar senha errada → mensagem de erro.
       - Digitar senha correta (criada com `painel:create-user`) → dashboard `/admin`.
       - Acessar `/register` → 404.

    3. **PANEL-01 (canal-fonte)**
       - Menu esquerdo → "Canais-fonte" → "Criar novo".
       - Colar `https://www.youtube.com/@Nicola` (ou outro canal público real).
       - Selecionar niche "Futebol". Salvar.
       - Confirmar linha nova em `source_channels`:
         ```
         docker exec -T mysql mysql -uclips_user -p${CLIPS_DB_PASSWORD} clips_automation -e "SELECT id, youtube_channel_id, channel_name, channel_handle, target_niche FROM source_channels ORDER BY id DESC LIMIT 1;"
         ```
       - Clicar no toggle "Blacklisted" da linha → confirmar UPDATE:
         ```
         docker exec -T mysql mysql -uclips_user -p${CLIPS_DB_PASSWORD} clips_automation -e "SELECT id, blacklisted FROM source_channels ORDER BY id DESC LIMIT 1;"
         ```

    4. **PANEL-02 (canal-destino + OAuth)**
       - Menu esquerdo → "Canais-destino" → "Criar novo".
       - Preencher slug=`teste-oauth`, name, niche=Podcast, youtube_channel_id fictício, credit_template default.
       - Confirmar linha em `destination_channels`.
       - Voltar à listagem: badge OAuth = "missing" (arquivo `token-teste-oauth.json` não existe).
       - Copiar comando do bloco Placeholder e rodar num terminal separado; confirmar que criou `youtube/token-teste-oauth.json`.
       - Recarregar listagem → badge vira "authorized".
       - (Opcional avançado) simular expiração: revogar acesso via https://myaccount.google.com/permissions → forçar um upload → confirmar badge fica "expired" no próximo poll.

    5. **PANEL-03 (dashboard tempo-real)**
       - Abrir `/admin` e devtools do browser → aba Network.
       - Aguardar 10-15s. Confirmar que aparecem XHR Livewire a cada 5s (nome `livewire.update` ou similar) — SEM full-page reload.
       - Inserir manualmente uma linha em `source_videos` com `status='failed'`:
         ```
         docker exec -T mysql mysql -uclips_user -p${CLIPS_DB_PASSWORD} clips_automation \
           -e "INSERT INTO source_videos (youtube_video_id, channel_id, title, status) VALUES ('TEST_FAIL_1', 1, 'Test failure', 'failed');"
         ```
       - Confirmar que o widget "Últimas falhas" mostra a nova linha em ≤5s.

    6. **PANEL-04 (Aprovar/Rejeitar)**
       - Semear um clip pending para teste:
         ```
         docker exec -T mysql mysql -uclips_user -p${CLIPS_DB_PASSWORD} clips_automation \
           -e "INSERT INTO generated_clips (source_video_id, title, description, tags, score, start_time, end_time, status, clip_path) VALUES (1,'Teste Approve','Desc','tag1', 8, 10.0, 40.0, 'pending', '/app/videos/clips/teste_approve.mp4');"
         touch /Users/alessandrobm1/develop/server/wordpress/canaldecortes/videos/clips/teste_approve.mp4
         ```
       - Dashboard: clicar em "Aprovar" na linha → confirmar status:
         ```
         docker exec -T mysql mysql -uclips_user -p${CLIPS_DB_PASSWORD} clips_automation \
           -e "SELECT id, status FROM generated_clips ORDER BY id DESC LIMIT 1;"
         ```
         Esperado: `approved`.
       - Repetir com outro clip pending e "Rejeitar" → confirmar `status=rejected` E `videos/clips/teste_reject.mp4` foi deletado do disco.

    7. **Sanity final: gate de suite**
       - Rodar `docker exec -T php bash -c "cd /var/www/html/painel && php artisan test"` → 0 failed.
       - Rodar `docker exec -T clip-processor pytest tests/ -q` → 0 failed.
  </how-to-verify>
  <resume-signal>
    Digite "approved" para fechar Phase 8, OU descreva os defeitos observados por passo (1..7). Se algum defeito, o próximo passo é `/gsd:verify-work` para gerar plano de gap closure.
  </resume-signal>
  <verify>
    <automated>MISSING — verificação humana no browser (5 passos PANEL-XX + gate de suite). Executor NÃO deve simular esta task.</automated>
  </verify>
  <done>
    - Operador respondeu "approved" após executar os 7 passos do <how-to-verify>.
    - Nenhum defeito bloqueante reportado; issues não-bloqueantes documentadas no SUMMARY para follow-up.
  </done>
</task>

</tasks>

<verification>
Após o checkpoint humano APROVAR:
1. `docker exec -T php bash -c "cd /var/www/html/painel && php artisan test --no-interaction"` → 0 failed.
2. `docker exec -T clip-processor pytest tests/ -q` → 0 failed.
3. Contador de linhas em `source_channels`, `destination_channels`, `generated_clips` mostra pelo menos 1 linha CRIADA VIA UI.
4. Verify das 5 must_haves.truths acima checked manualmente.
</verification>

<success_criteria>
- 5 requisitos PANEL-XX observáveis no browser real.
- Suite completa GREEN.
- Operador consegue fazer setup fresh a partir do README sem consultar documentação externa.
- Aprovação humana explícita registrada.
</success_criteria>

<output>
Após conclusão, criar `.planning/phases/08-painel-laravel-filament/08-09-SUMMARY.md` com:
- Resultado detalhado do checkpoint humano (7 passos: OK/defeito).
- Contadores finais de Python + Laravel test suites.
- Confirmação explícita das 5 must_haves.truths.
- Nota se houver alguma issue conhecida deferida para futura correção.

E também `.planning/phases/08-painel-laravel-filament/PHASE-8-COMPLETE.md` (opcional — nice-to-have) com um resumo pra STATE.md quando Phase 9 começar.
</output>
