---
phase: 08-painel-laravel-filament
plan: 09
type: execute
wave: 5
depends_on: ["08-04", "08-08"]
files_modified:
  - /Users/alessandrobm1/develop/server/wordpress/canaldecortes/clip-processor/src/pipeline_runner.py
  - /Users/alessandrobm1/develop/server/wordpress/canaldecortes/clip-processor/src/quota_manager.py
  - /Users/alessandrobm1/develop/server/wordpress/canaldecortes/clip-processor/src/rss_poller.py
  - /Users/alessandrobm1/develop/server/wordpress/canaldecortes/clip-processor/src/selector.py
  - /Users/alessandrobm1/develop/server/wordpress/canaldecortes/clip-processor/src/transcriber.py
  - /Users/alessandrobm1/develop/server/wordpress/canaldecortes/clip-processor/src/youtube_oauth.py
  - /Users/alessandrobm1/develop/server/wordpress/canaldecortes/.planning/ROADMAP.md
  - /Users/alessandrobm1/develop/server/wordpress/canaldecortes/.planning/STATE.md
  - /Users/alessandrobm1/develop/server/wordpress/canaldecortes/.planning/phases/08-painel-laravel-filament/08-09-SUMMARY.md
autonomous: false
requirements: [PANEL-01, PANEL-02, PANEL-03, PANEL-04, PANEL-05]

must_haves:
  truths:
    - "Acesso sem sessão a qualquer rota do painel redireciona para /admin/login — nenhuma rota do painel e publica (PANEL-05)"
    - "Operador adiciona canal-fonte via formulario e linha aparece em source_channels sem SQL manual (PANEL-01)"
    - "Operador adiciona canal-destino via formulario; badge OAuth mostra 'missing' ate token-{slug}.json existir, depois 'authorized' (PANEL-02)"
    - "Dashboard atualiza status do pipeline em menor-ou-igual-a-5s sem full-page reload — Network tab mostra XHR Livewire, nao navigation request (PANEL-03)"
    - "Clicar Aprovar muda status=approved; Rejeitar muda status=rejected e remove MP4 do disco — identico ao bot Telegram (PANEL-04)"
    - "Suite Python maior-ou-igual-a-134/137 GREEN; suite Laravel 25/25 GREEN"
  artifacts:
    - path: "/Users/alessandrobm1/develop/server/wordpress/canaldecortes/.planning/ROADMAP.md"
      provides: "Planos 08-04/06/08/09 marcados [x], Phase 8 com status Complete no Progress table"
      contains: "[x] 08-09-PLAN.md"
    - path: "/Users/alessandrobm1/develop/server/wordpress/canaldecortes/.planning/STATE.md"
      provides: "completed_phases: 8, completed_plans: 45, status Phase 8 complete"
    - path: "/Users/alessandrobm1/develop/server/wordpress/canaldecortes/.planning/phases/08-painel-laravel-filament/08-09-SUMMARY.md"
      provides: "Resultado final do checkpoint humano (7 passos OK/defeito) + encerramento da Phase 8"
  key_links:
    - from: "browser em canaldecortes.local"
      to: "Filament AdminPanel"
      via: "nginx -> php:9000 -> Laravel"
      pattern: "canaldecortes.local"
    - from: "DestinationChannelResource (OAuth badge)"
      to: "youtube/token-{slug}.json"
      via: "bind mount ./canaldecortes/youtube:/var/www/html/youtube:ro no php service"
      pattern: "oauth_status.*authorized"
    - from: "PendingApprovalWidget (acao Rejeitar)"
      to: "src.rejeitar via ClipProcessorClient"
      via: "docker exec clip-processor python -m src.rejeitar {id}"
      pattern: "rejectClip"
---

<objective>
Fechar Phase 8 com verificacao end-to-end no browser real.

Context: Task 1 do plano original (README consolidado + suites GREEN) foi executada e commitada (commits 65dcb8d e e5d1728). Faltam 3 passos:
1. Commit dos arquivos Python pendentes no working tree + adicionar bind mount youtube:ro ao servico php (necessario para badge OAuth funcionar com tokens reais).
2. Checkpoint humano: operador valida os 5 requisitos PANEL-XX no browser.
3. Encerramento: atualizar STATE.md/ROADMAP e commitar fechamento da Phase 8.

Purpose: garantir que os 5 requisitos PANEL-XX sao observaveis no ambiente rodando — nao apenas nos testes automatizados.
Output: Phase 8 fechada, STATE.md/ROADMAP atualizados, SUMMARY completo com resultado humano.
</objective>

<execution_context>
@/Users/alessandrobm1/.claude/get-shit-done/workflows/execute-plan.md
@/Users/alessandrobm1/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/alessandrobm1/develop/server/wordpress/canaldecortes/.planning/phases/08-painel-laravel-filament/08-09-SUMMARY.md
@/Users/alessandrobm1/develop/server/wordpress/canaldecortes/.planning/phases/08-painel-laravel-filament/08-CONTEXT.md
@/Users/alessandrobm1/develop/server/wordpress/canaldecortes/.planning/ROADMAP.md
@/Users/alessandrobm1/develop/server/wordpress/canaldecortes/.planning/STATE.md

<interfaces>
<!-- Arquivos Python pendentes no working tree (verificado via git status/diff): -->

1. clip-processor/src/pipeline_runner.py (STAGED):
   Remove argumento `uploader=YouTubeUploader()` da chamada publish_pending_clips
   — alinha com assinatura atualizada da funcao.

2. clip-processor/src/quota_manager.py (UNSTAGED):
   Adiciona suporte ao env var UPLOAD_WINDOW_BYPASS na _is_upload_window():
   ```python
   if os.environ.get('UPLOAD_WINDOW_BYPASS', 'false').lower() == 'true':
       return True
   ```

3. clip-processor/src/rss_poller.py (UNSTAGED):
   Adiciona filtro de titulos de apostas/gambling (_TITLE_BLOCK_KEYWORDS) antes
   de inserir video em source_videos — bloqueia titulos como 'bet ', 'tigrinho', etc.

4. clip-processor/src/selector.py (UNSTAGED):
   Refatoracao: remove MOMENT_OUTPUT_SCHEMA (structured output JSON schema);
   adiciona Groq LLaMA 3.3-70b como fallback automatico quando ANTHROPIC_API_KEY
   ausente ou indisponivel. Producao: tenta Anthropic primeiro, cai para Groq.

5. clip-processor/src/transcriber.py (UNSTAGED):
   Helper _g(seg, key) para duck-type segmentos como dict OU objeto — compativel
   com diferentes versoes do Groq API que retornam formatos distintos.

6. clip-processor/src/youtube_oauth.py (UNSTAGED):
   Muda OAuth flow de run_local_server() (abre browser automaticamente, incompativel
   com Docker headless) para fluxo manual: imprime URL, usuario cola no browser,
   copia a redirect URL e cola no terminal. Muito mais compativel com ambiente Docker.

<!-- Bind mount necessario para badge OAuth no painel: -->
<!-- servico php em /Users/alessandrobm1/develop/server/wordpress/docker-compose.yml -->
<!-- nao tem ./canaldecortes/youtube montado. Laravel precisa ler token-{slug}.json -->
<!-- via CLIP_PROCESSOR_TOKEN_DIR default = /var/www/html/painel/../youtube -->
<!-- = /var/www/html/youtube. Montar: ./canaldecortes/youtube:/var/www/html/youtube:ro -->

<!-- Checkboxes do ROADMAP a corrigir (planos com SUMMARY mas marcados [ ]): -->
<!-- 08-04-PLAN.md: [ ] -> [x] -->
<!-- 08-06-PLAN.md: [ ] -> [x] -->
<!-- 08-08-PLAN.md: [ ] -> [x] -->
<!-- "5/9 plans executed" -> "9/9 plans" -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Commit Python pending fixes, adicionar bind mount youtube:ro, corrigir checkboxes ROADMAP</name>
  <files>
    - /Users/alessandrobm1/develop/server/wordpress/canaldecortes/clip-processor/src/pipeline_runner.py
    - /Users/alessandrobm1/develop/server/wordpress/canaldecortes/clip-processor/src/quota_manager.py
    - /Users/alessandrobm1/develop/server/wordpress/canaldecortes/clip-processor/src/rss_poller.py
    - /Users/alessandrobm1/develop/server/wordpress/canaldecortes/clip-processor/src/selector.py
    - /Users/alessandrobm1/develop/server/wordpress/canaldecortes/clip-processor/src/transcriber.py
    - /Users/alessandrobm1/develop/server/wordpress/canaldecortes/clip-processor/src/youtube_oauth.py
    - /Users/alessandrobm1/develop/server/wordpress/canaldecortes/.planning/ROADMAP.md
    - /Users/alessandrobm1/develop/server/wordpress/docker-compose.yml (fora do repo git)
  </files>
  <action>
    **Passo 1: Confirmar estado atual dos arquivos Python**
    ```bash
    cd /Users/alessandrobm1/develop/server/wordpress/canaldecortes
    git status clip-processor/src/
    ```
    Esperado: pipeline_runner.py STAGED (M na primeira coluna); os outros 5 UNSTAGED (M na segunda coluna).

    **Passo 2: Stage dos arquivos Python pendentes e commit unico**
    ```bash
    cd /Users/alessandrobm1/develop/server/wordpress/canaldecortes
    git add clip-processor/src/quota_manager.py \
             clip-processor/src/rss_poller.py \
             clip-processor/src/selector.py \
             clip-processor/src/transcriber.py \
             clip-processor/src/youtube_oauth.py
    git commit -m "$(cat <<'EOF'
    fix(pipeline): melhorias de robustez pre-E2E Phase 8

    - quota_manager: suporte a UPLOAD_WINDOW_BYPASS env var em _is_upload_window
    - rss_poller: filtro de titulos de apostas/gambling antes do INSERT
    - selector: fallback automatico Groq LLaMA 3.3-70b quando Anthropic indisponivel;
      remove structured output JSON schema (parse manual do JSON)
    - transcriber: helper duck-type _g(seg, key) para dict OU objeto de segmento
    - youtube_oauth: fluxo manual copy-paste URL para compatibilidade Docker headless
    - pipeline_runner: remove kwarg uploader= legado de publish_pending_clips

    Co-Authored-By: Claude Sonnet 4.6 <noreply@anthropic.com>
    EOF
    )"
    ```

    **Passo 3: Verificar que suite Python ainda esta GREEN**
    ```bash
    docker exec -T clip-processor pytest tests/ -q 2>&1 | tail -5
    ```
    Esperado: >=134 passed, <=3 failed (as mesmas 3 falhas pre-existentes de
    test_quota_manager.py::TestCanUpload::test_before/after/outside_window — causadas
    por UPLOAD_WINDOW_BYPASS=true em wordpress/.env, fora de escopo).

    **Passo 4: Adicionar bind mount youtube:ro ao servico php**
    Editar `/Users/alessandrobm1/develop/server/wordpress/docker-compose.yml`.
    No servico `php:` (linha ~26), dentro do bloco `volumes:`, adicionar apos a linha
    do painel (`./canaldecortes/painel:/var/www/html/painel:cached`):

    ```yaml
          - ./canaldecortes/youtube:/var/www/html/youtube:ro
    ```

    Isto expoe os token-{slug}.json dentro do container php em /var/www/html/youtube/,
    que e o caminho resolvido de CLIP_PROCESSOR_TOKEN_DIR default
    (/var/www/html/painel/../youtube). O badge OAuth do painel passa a ler o token real.

    Verificar sintaxe do compose antes de restartar:
    ```bash
    docker compose -f /Users/alessandrobm1/develop/server/wordpress/docker-compose.yml \
      config -q && echo CONFIG_VALID
    ```

    Restartar SOMENTE o servico php (zero downtime para outros servicos):
    ```bash
    docker compose -f /Users/alessandrobm1/develop/server/wordpress/docker-compose.yml \
      up -d --no-deps php
    ```

    Confirmar mount ativo:
    ```bash
    docker exec php ls /var/www/html/youtube/ 2>/dev/null | head -5 || \
      echo "(diretorio vazio — normal se ainda nao ha tokens OAuth)"
    ```
    Deve listar arquivos (client_secret.json, possivelmente token-*.json) OU estar vazio
    sem erro de "No such file". Um erro "No such file or directory" = mount falhou.

    **Passo 5: Corrigir checkboxes do ROADMAP para planos com SUMMARY mas marcados [ ]**
    Editar `/Users/alessandrobm1/develop/server/wordpress/canaldecortes/.planning/ROADMAP.md`.
    Na secao Phase 8, fazer as seguintes substituicoes (EXATAS, nao alterar texto alem dos colchetes):

    a. `- [ ] 08-04-PLAN.md` → `- [x] 08-04-PLAN.md`
    b. `- [ ] 08-06-PLAN.md` → `- [x] 08-06-PLAN.md`
    c. `- [ ] 08-08-PLAN.md` → `- [x] 08-08-PLAN.md`
    d. `**Plans**: 5/9 plans executed` → `**Plans**: 8/9 plans executed`
       (08-09 ficara [x] apenas na Task 3, apos o checkpoint humano)

    Verificar a edicao:
    ```bash
    grep -A 12 "08-04-PLAN\|08-06-PLAN\|08-08-PLAN\|Plans.*9 plans" \
      /Users/alessandrobm1/develop/server/wordpress/canaldecortes/.planning/ROADMAP.md | head -20
    ```
    Deve mostrar [x] nos 3 planos e "8/9 plans executed".

    Commitar a correcao:
    ```bash
    cd /Users/alessandrobm1/develop/server/wordpress/canaldecortes
    git add .planning/ROADMAP.md
    git commit -m "$(cat <<'EOF'
    docs(08): corrigir checkboxes ROADMAP para planos 08-04, 08-06, 08-08

    Os 3 planos tinham SUMMARY commitado mas continuavam com [ ] no ROADMAP.
    Correcao antes do checkpoint final da Phase 8.

    Co-Authored-By: Claude Sonnet 4.6 <noreply@anthropic.com>
    EOF
    )"
    ```

    **Passo 6: Verificar suite Laravel ainda GREEN**
    ```bash
    docker exec -T php bash -c "cd /var/www/html/painel && php artisan test --no-interaction 2>&1 | tail -5"
    ```
    Esperado: 25 passed, 0 failed.

    **NÃO** rodar `git push` — apenas commits locais. Operador decide push.
  </action>
  <verify>
    <automated>
      cd /Users/alessandrobm1/develop/server/wordpress/canaldecortes && \
      git log --oneline -3 | grep -qE "(pipeline|ROADMAP|08-04)" && \
      docker exec -T clip-processor pytest tests/ -q 2>&1 | tail -3 | grep -qE "passed" && \
      docker exec -T php bash -c "cd /var/www/html/painel && php artisan test --no-interaction 2>&1 | tail -3" | grep -qE "passed" && \
      docker exec php ls /var/www/html/youtube/ > /dev/null 2>&1 && \
      echo PRE_E2E_READY
    </automated>
  </verify>
  <done>
    - 6 arquivos Python commitados (pipeline_runner staged + 5 unstaged) em 1 commit.
    - Bind mount ./canaldecortes/youtube:/var/www/html/youtube:ro ativo no servico php
      (docker exec php ls /var/www/html/youtube/ sem erro).
    - ROADMAP.md: 08-04, 08-06, 08-08 marcados [x]; contagem "8/9 plans executed".
    - Suite Python >=134/137 GREEN.
    - Suite Laravel 25/25 GREEN.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Checkpoint humano — validar 5 fluxos PANEL-XX end-to-end no browser real</name>
  <files>N/A (checkpoint humano — verificacao no browser)</files>
  <action>
    Pausar execucao automatizada. Apresentar os 7 passos abaixo ao operador.
    Aguardar resposta "approved" OU lista de defeitos por passo (1..7).
    NAO prosseguir para Task 3 sem confirmacao humana explicita.
  </action>
  <what-built>
    Painel Laravel/Filament completo (Plans 08-01 a 08-09 Task 1):
    - Bootstrap Docker + nginx + Laravel 13 + Filament 5 + Pest 4 (Plans 08-01/03).
    - Autenticacao PANEL-05: painel:create-user, redirect para login, /register=404 (Plan 08-04).
    - CRUD canais-fonte com sidecar HTTP para yt-dlp (Plans 08-05 + 08-07).
    - CRUD canais-destino com badge OAuth authorized/expired/missing (Plan 08-06).
    - Dashboard 4 widgets + polling 5s + acoes Aprovar/Rejeitar (Plan 08-08).
    - README consolidado com fluxo de setup fresh 10 passos (Plan 08-09 Task 1).
    - Bugs de ambiente corrigidos: nginx bind mount do painel, redirect da raiz para /admin.
    - Bind mount youtube:ro no php service (Task 1 deste plano).
    - Arquivos Python melhorados e commitados (Task 1 deste plano).
  </what-built>
  <how-to-verify>
    Operador roda no host (Mac). Todos os passos pressupõem containers rodando
    (nginx, php, mysql, redis, clip-processor).

    **Passo 1: Setup fresh via README**
    - Seguir os 10 passos do `canaldecortes/painel/README.md` em terminal limpo.
      Especialmente: gerar CLIP_PROCESSOR_INTERNAL_TOKEN, preencher painel/.env,
      rodar `php artisan migrate`, criar usuario via `painel:create-user`.
    - Confirmar cadeia de redirect:
      ```
      curl -sIL http://canaldecortes.local | grep -E "HTTP|Location"
      ```
      Esperado: `302 Found` + `Location: /admin` → `302 Found` + `Location: /admin/login` → `200 OK`.

    **Passo 2: PANEL-05 (Autenticacao)**
    - Abrir `http://canaldecortes.local` no browser.
    - Confirmar redirect para `/admin/login`.
    - Digitar senha ERRADA → mensagem de erro do Filament.
    - Digitar senha CORRETA (criada em painel:create-user) → acessa `/admin` com dashboard.
    - Acessar `http://canaldecortes.local/register` → 404.

    **Passo 3: PANEL-01 (Canal-fonte)**
    - Menu esquerdo → "Canais-fonte" → "Criar novo".
    - Colar `https://www.youtube.com/@Nicola` (ou outro canal publico real). Salvar.
    - Confirmar linha nova inserida sem SQL:
      ```
      docker exec -T mysql mysql -uclips_user -p${CLIPS_DB_PASSWORD} clips_automation \
        -e "SELECT id, youtube_channel_id, name, channel_handle, target_niche FROM source_channels ORDER BY id DESC LIMIT 1;"
      ```
    - Na listagem, clicar no toggle "Blacklisted" da linha recém-criada.
    - Confirmar UPDATE no banco:
      ```
      docker exec -T mysql mysql -uclips_user -p${CLIPS_DB_PASSWORD} clips_automation \
        -e "SELECT id, blacklisted FROM source_channels ORDER BY id DESC LIMIT 1;"
      ```
      Esperado: `blacklisted = 1`.

    **Passo 4: PANEL-02 (Canal-destino + Badge OAuth)**
    - Menu esquerdo → "Canais-destino" → "Criar novo".
    - Preencher: slug=`teste-oauth`, name=`Teste OAuth`, niche=Podcast,
      youtube_channel_id=`UCfake123`, credit_template default. Salvar.
    - Confirmar linha em `destination_channels`.
    - Voltar a listagem: badge OAuth = "missing" (cinza) — arquivo
      `youtube/token-teste-oauth.json` nao existe ainda.
    - Copiar o comando OAuth do bloco no form de edicao e rodar num terminal separado:
      ```
      docker exec -it clip-processor python -m src.youtube_oauth --channel teste-oauth
      ```
      (Nao precisa completar o OAuth real — apenas confirmar que o comando executa
      e imprime uma URL do Google sem abrir browser automaticamente.)
    - Para simular "authorized" sem OAuth real, criar um JSON fake:
      ```
      echo '{"token":"fake","refresh_token":"fake"}' \
        > canaldecortes/youtube/token-teste-oauth.json
      ```
    - Recarregar listagem de canais-destino → badge vira "authorized" (verde).
    - (Opcional avancado) setar `oauth_expired_flag=1` manualmente no banco e
      confirmar que badge vira "expired" (vermelho).

    **Passo 5: PANEL-03 (Dashboard tempo-real)**
    - Abrir `/admin` e devtools → aba Network.
    - Aguardar 10-15s. Confirmar XHR requests Livewire a cada ~5s
      (nome do request contem "livewire" — NAO e navegacao completa da pagina).
    - Inserir uma falha manual para testar widget "Ultimas falhas":
      ```
      docker exec -T mysql mysql -uclips_user -p${CLIPS_DB_PASSWORD} clips_automation \
        -e "INSERT INTO source_videos (youtube_video_id, channel_id, title, status) \
            VALUES ('TEST_FAIL_E2E', 1, 'Teste de falha E2E', 'failed');"
      ```
    - Confirmar que widget "Ultimas falhas" exibe a nova linha em <=5s SEM recarregar
      a pagina manualmente.

    **Passo 6: PANEL-04 (Aprovar e Rejeitar)**
    - Criar clip pending para testar Aprovar:
      ```
      docker exec -T mysql mysql -uclips_user -p${CLIPS_DB_PASSWORD} clips_automation \
        -e "INSERT INTO generated_clips \
            (source_video_id, title, description, tags, score, start_time, end_time, \
             status, clip_path) \
            VALUES (1,'Teste Aprovar','Desc','tag1',8,10.0,40.0,'pending', \
                    '/app/videos/clips/teste_aprovar_e2e.mp4');"
      ```
    - Dashboard → fila de aprovacao → clicar "Aprovar" na linha.
    - Confirmar status no banco:
      ```
      docker exec -T mysql mysql -uclips_user -p${CLIPS_DB_PASSWORD} clips_automation \
        -e "SELECT id, status FROM generated_clips ORDER BY id DESC LIMIT 1;"
      ```
      Esperado: `approved`.
    - Criar clip pending para testar Rejeitar + criar MP4 fake no disco:
      ```
      docker exec -T mysql mysql -uclips_user -p${CLIPS_DB_PASSWORD} clips_automation \
        -e "INSERT INTO generated_clips \
            (source_video_id, title, description, tags, score, start_time, end_time, \
             status, clip_path) \
            VALUES (1,'Teste Rejeitar','Desc','tag1',8,10.0,40.0,'pending', \
                    '/app/videos/clips/teste_rejeitar_e2e.mp4');"
      touch canaldecortes/videos/clips/teste_rejeitar_e2e.mp4
      ```
    - Dashboard → clicar "Rejeitar" na linha.
    - Confirmar status=rejected E MP4 removido:
      ```
      docker exec -T mysql mysql -uclips_user -p${CLIPS_DB_PASSWORD} clips_automation \
        -e "SELECT id, status FROM generated_clips ORDER BY id DESC LIMIT 2;"
      ls canaldecortes/videos/clips/teste_rejeitar_e2e.mp4 2>/dev/null \
        && echo "ERRO: MP4 ainda existe" || echo "OK: MP4 removido"
      ```
      Esperado: `status=rejected` E "OK: MP4 removido".

    **Passo 7: Sanity final — gate de suite**
    ```bash
    docker exec -T php bash -c "cd /var/www/html/painel && php artisan test --no-interaction"
    docker exec -T clip-processor pytest tests/ -q 2>&1 | tail -5
    ```
    Esperado: Laravel 25/25 GREEN; Python >=134/137 GREEN (3 falhas pre-existentes
    test_quota_manager nao sao bloqueantes — documentadas).
  </how-to-verify>
  <resume-signal>
    Digite "approved" para fechar a Phase 8 e seguir para Task 3 (encerramento).
    OU descreva defeitos por passo (1..7). Se defeitos bloqueantes, usar
    `/gsd:verify-work` para gerar plano de gap closure antes de continuar.
  </resume-signal>
  <verify>
    <automated>MISSING — verificacao humana no browser (5 fluxos PANEL-XX + gate de suite). Executor NAO deve simular esta task.</automated>
  </verify>
  <done>
    - Operador respondeu "approved" apos executar os 7 passos.
    - Nenhum defeito bloqueante reportado.
    - Issues nao-bloqueantes (se houver) documentadas no SUMMARY para follow-up.
  </done>
</task>

<task type="auto">
  <name>Task 3: Encerrar Phase 8 — atualizar STATE.md, ROADMAP e SUMMARY final</name>
  <files>
    - /Users/alessandrobm1/develop/server/wordpress/canaldecortes/.planning/STATE.md
    - /Users/alessandrobm1/develop/server/wordpress/canaldecortes/.planning/ROADMAP.md
    - /Users/alessandrobm1/develop/server/wordpress/canaldecortes/.planning/phases/08-painel-laravel-filament/08-09-SUMMARY.md
  </files>
  <action>
    Esta task executa SOMENTE apos o operador responder "approved" na Task 2.
    Capturar o resultado da verificacao humana (passos OK/defeito) e fechar a fase.

    **Passo 1: Atualizar ROADMAP.md**
    Editar `/Users/alessandrobm1/develop/server/wordpress/canaldecortes/.planning/ROADMAP.md`:

    a. Na secao Phase 8, mudar:
       `- [ ] 08-09-PLAN.md` → `- [x] 08-09-PLAN.md`

    b. Mudar contagem:
       `**Plans**: 8/9 plans executed` → `**Plans**: 9/9 plans`

    c. Na tabela Progress (final do arquivo), mudar a linha de Phase 8:
       `| 8. Painel Laravel/Filament | 8/9 | In Progress|  |`
       →
       `| 8. Painel Laravel/Filament | 9/9 | Complete | 2026-07-02 |`

    d. Na lista de fases (topo do arquivo), mudar:
       `- [ ] **Phase 8: Painel Laravel/Filament**`
       →
       `- [x] **Phase 8: Painel Laravel/Filament** (completed 2026-07-02)`

    **Passo 2: Atualizar STATE.md**
    Editar `/Users/alessandrobm1/develop/server/wordpress/canaldecortes/.planning/STATE.md`.
    Substituir o bloco frontmatter e Current Position com:

    ```yaml
    ---
    gsd_state_version: 1.0
    milestone: v1.0
    milestone_name: milestone
    status: "Phase 8 completa — Painel Laravel/Filament verificado end-to-end. Pronto para Phase 9 (Bot Telegram no Laravel)."
    stopped_at: "N/A — Phase 8 encerrada. Proxima: Phase 9."
    last_updated: "2026-07-02T12:00:00.000Z"
    last_activity: "2026-07-02 — Phase 8 concluida. Checkpoint humano aprovado: 5 requisitos PANEL-XX verificados no browser. Suite Laravel 25/25 GREEN, Python 134/137 GREEN (3 falhas pre-existentes documentadas)."
    progress:
      total_phases: 9
      completed_phases: 8
      total_plans: 45
      completed_plans: 45
      percent: 100
    ---
    ```

    Atualizar tambem:
    - `Current Position` → Phase 9 como proxima
    - `Progress:` → 100% (45/45 planos — Phases 1-8 completas; Phase 9 pendente)
    - `Last activity:` → resumo do checkpoint aprovado

    **Passo 3: Atualizar 08-09-SUMMARY.md com resultado do checkpoint**
    Editar `.planning/phases/08-painel-laravel-filament/08-09-SUMMARY.md`.
    Adicionar secao `## Resultado do Checkpoint Humano` com:
    - Data/hora da aprovacao
    - Resultado de cada passo (1..7): OK ou defeito
    - Confirmacao das 5 must_haves.truths verificadas

    Mudar `status: PAUSED — Stopped at checkpoint` para `status: COMPLETE — Aprovado pelo operador`.

    **Passo 4: Commit de encerramento**
    ```bash
    cd /Users/alessandrobm1/develop/server/wordpress/canaldecortes
    git add .planning/ROADMAP.md .planning/STATE.md \
             .planning/phases/08-painel-laravel-filament/08-09-SUMMARY.md
    git commit -m "$(cat <<'EOF'
    docs(08): encerrar Phase 8 — checkpoint humano aprovado

    Painel Laravel/Filament verificado end-to-end:
    - PANEL-01 a PANEL-05 observados no browser real.
    - Suite Laravel 25/25 GREEN, Python 134/137 GREEN.
    - STATE.md: completed_phases=8, completed_plans=45, 100%.
    - ROADMAP.md: Phase 8 marcada como Complete.

    Co-Authored-By: Claude Sonnet 4.6 <noreply@anthropic.com>
    EOF
    )"
    ```

    **NÃO** rodar `git push`. Operador decide.
  </action>
  <verify>
    <automated>
      cd /Users/alessandrobm1/develop/server/wordpress/canaldecortes && \
      grep -q "\[x\] 08-09-PLAN.md" .planning/ROADMAP.md && \
      grep -q "completed_plans: 45" .planning/STATE.md && \
      grep -q "COMPLETE" .planning/phases/08-painel-laravel-filament/08-09-SUMMARY.md && \
      echo PHASE8_CLOSED
    </automated>
  </verify>
  <done>
    - ROADMAP.md: 08-09 marcado [x], Phase 8 = Complete no Progress table, [x] na lista de fases.
    - STATE.md: completed_phases=8, completed_plans=45, percent=100, status=Phase 8 completa.
    - 08-09-SUMMARY.md: resultado humano documentado, status=COMPLETE.
    - Commit de encerramento criado.
  </done>
</task>

</tasks>

<verification>
Apos Task 3 concluida:
1. `grep "\[x\].*08-09" .planning/ROADMAP.md` → existe.
2. `grep "completed_plans: 45" .planning/STATE.md` → existe.
3. `grep "completed_phases: 8" .planning/STATE.md` → existe.
4. `grep "Complete" .planning/ROADMAP.md | grep "Phase 8"` → existe na tabela Progress.
5. `grep "COMPLETE" .planning/phases/08-painel-laravel-filament/08-09-SUMMARY.md` → existe.
6. `docker exec -T php bash -c "cd /var/www/html/painel && php artisan test --no-interaction 2>&1 | tail -3"` → 25 passed.
7. `docker exec -T clip-processor pytest tests/ -q 2>&1 | tail -3` → >=134 passed.
</verification>

<success_criteria>
- 5 requisitos PANEL-XX observaveis no browser real por humano (aprovado na Task 2).
- Suite completa GREEN (Laravel 25/25, Python >=134/137).
- STATE.md mostra Phase 8 completa (completed_phases=8, completed_plans=45).
- ROADMAP.md: todos os 9 planos da Phase 8 marcados [x], Phase 8 = Complete.
- Bind mount youtube:ro ativo no container php (badge OAuth funcional).
- Arquivos Python melhorados commitados no git.
</success_criteria>

<output>
Apos conclusao, o SUMMARY em `.planning/phases/08-painel-laravel-filament/08-09-SUMMARY.md`
deve ser atualizado (Task 3) com:
- Resultado do checkpoint humano (7 passos: OK/defeito).
- Confirmacao das 5 must_haves.truths.
- Contadores finais de suite (Python + Laravel).
- Status: COMPLETE.
- Proxima fase: Phase 9 — Bot Telegram no Laravel.
</output>
