---
phase: 08-painel-laravel-filament
plan: 06
type: execute
wave: 3
depends_on: ["08-02", "08-03"]
files_modified:
  - /Users/alessandrobm1/develop/server/wordpress/canaldecortes/clip-processor/src/uploader.py
  - /Users/alessandrobm1/develop/server/wordpress/canaldecortes/painel/app/Filament/Resources/DestinationChannelResource.php
  - /Users/alessandrobm1/develop/server/wordpress/canaldecortes/painel/app/Filament/Resources/DestinationChannelResource/Pages/CreateDestinationChannel.php
  - /Users/alessandrobm1/develop/server/wordpress/canaldecortes/painel/app/Filament/Resources/DestinationChannelResource/Pages/ListDestinationChannels.php
  - /Users/alessandrobm1/develop/server/wordpress/canaldecortes/painel/app/Filament/Resources/DestinationChannelResource/Pages/EditDestinationChannel.php
autonomous: true
requirements: [PANEL-02]
must_haves:
  truths:
    - "Operador logado cria canal-destino via formulário com Select explícito de `niche` — sem SQL"
    - "Tabela de canais-destino mostra badge OAuth (authorized/expired/missing) com cores (verde/vermelho/cinza)"
    - "`uploader.py._load_credentials` captura `google.auth.exceptions.RefreshError`, seta `oauth_expired_flag=TRUE` em `destination_channels` para o `channel_slug` atual, e re-lança RefreshError"
    - "Upload bem-sucedido reseta `oauth_expired_flag=FALSE` (self-healing)"
    - "Página de edição mostra bloco copy-paste `docker exec -it clip-processor python -m src.youtube_oauth --channel {slug}`"
    - "2 testes de `DestinationChannelResourceTest` passam + 3 testes de `DestinationChannelOauthStatusTest` (unit) passam + `test_uploader_expired.py` GREEN"
  artifacts:
    - path: "/Users/alessandrobm1/develop/server/wordpress/canaldecortes/clip-processor/src/uploader.py"
      provides: "RefreshError capture + oauth_expired_flag write/reset"
    - path: "/Users/alessandrobm1/develop/server/wordpress/canaldecortes/painel/app/Filament/Resources/DestinationChannelResource.php"
      provides: "CRUD destination_channels + OAuth badge + copy-paste OAuth block"
  key_links:
    - from: "uploader.py._load_credentials"
      to: "destination_channels.oauth_expired_flag"
      via: "UPDATE via db_connect() cursor com WHERE slug = ..."
      pattern: "oauth_expired_flag"
    - from: "uploader.py.upload_clip (successful path)"
      to: "destination_channels.oauth_expired_flag = FALSE"
      via: "self-healing UPDATE após return video_id"
      pattern: "oauth_expired_flag.*FALSE"
    - from: "DestinationChannelResource::table"
      to: "DestinationChannel::getOauthStatusAttribute"
      via: "TextColumn::make('oauth_status')->badge()->color(...)"
      pattern: "oauth_status"
---

<objective>
Implementar PANEL-02 completo: CRUD `destination_channels` + badge OAuth authorized/expired/missing + captura de `RefreshError` em `uploader.py` para tornar o estado `expired` de fato observável. GREEN os 2 testes de `DestinationChannelResourceTest.php`, os 3 testes de `DestinationChannelOauthStatusTest.php` e o teste RED de `test_uploader_expired.py`.

Purpose: sem tocar em `uploader.py`, o badge `expired` nunca aparece — o requisito PANEL-02 fica falsamente atendido (badge só authorized/missing). Este plano fecha o ciclo produtor-consumidor.
Output: 4 arquivos Laravel + 1 mudança cirúrgica no `uploader.py`.
</objective>

<execution_context>
@/Users/alessandrobm1/.claude/get-shit-done/workflows/execute-plan.md
@/Users/alessandrobm1/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/alessandrobm1/develop/server/wordpress/canaldecortes/.planning/phases/08-painel-laravel-filament/08-CONTEXT.md
@/Users/alessandrobm1/develop/server/wordpress/canaldecortes/.planning/phases/08-painel-laravel-filament/08-RESEARCH.md
@/Users/alessandrobm1/develop/server/wordpress/canaldecortes/.planning/phases/08-painel-laravel-filament/08-02-SUMMARY.md
@/Users/alessandrobm1/develop/server/wordpress/canaldecortes/.planning/phases/08-painel-laravel-filament/08-03-SUMMARY.md
@/Users/alessandrobm1/develop/server/wordpress/canaldecortes/clip-processor/src/uploader.py
@/Users/alessandrobm1/develop/server/wordpress/canaldecortes/clip-processor/src/db.py
@/Users/alessandrobm1/develop/server/wordpress/canaldecortes/clip-processor/tests/test_uploader_expired.py
@/Users/alessandrobm1/develop/server/wordpress/canaldecortes/painel/tests/Feature/DestinationChannelResourceTest.php
@/Users/alessandrobm1/develop/server/wordpress/canaldecortes/painel/tests/Unit/DestinationChannelOauthStatusTest.php

<interfaces>
## Estado atual de `_load_credentials` (uploader.py)
```python
def _load_credentials(self):
    token_path = Path(self.token_file)
    if not token_path.exists():
        raise FileNotFoundError(f'Token OAuth não encontrado: {self.token_file}')
    creds = Credentials.from_authorized_user_file(str(token_path), YOUTUBE_UPLOAD_SCOPES)
    if getattr(creds, 'expired', False) and getattr(creds, 'refresh_token', None):
        from google.auth.transport.requests import Request
        creds.refresh(Request())   # ← RefreshError NÃO é capturado aqui hoje
    return creds
```

## Estado desejado (contrato do teste `test_uploader_expired.py`)
- `RefreshError` → UPDATE `destination_channels SET oauth_expired_flag=TRUE WHERE slug={channel_slug}` → re-lança RefreshError.
- Se `self.channel_slug` for None (compatibilidade legada com token único `/app/token.json`), skip do UPDATE e só re-lança.
- Successful `upload_clip` (fim do método): UPDATE `destination_channels SET oauth_expired_flag=FALSE WHERE slug={channel_slug}` (self-healing).

## Filament TextColumn badge com cores
```php
use Filament\Tables\Columns\TextColumn;

TextColumn::make('oauth_status')
    ->badge()
    ->color(fn (string $state): string => match ($state) {
        'authorized' => 'success',  // verde
        'expired' => 'danger',      // vermelho
        'missing' => 'gray',        // cinza
        default => 'primary',
    })
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: uploader.py — capturar RefreshError + persistir oauth_expired_flag + self-healing</name>
  <files>
    - /Users/alessandrobm1/develop/server/wordpress/canaldecortes/clip-processor/src/uploader.py
  </files>
  <behavior>
    - `_load_credentials` roda hoje → agora envolve `creds.refresh(Request())` em try/except `RefreshError` que abre conexão MySQL via `db_connect()`, faz UPDATE de `oauth_expired_flag=TRUE` para o `channel_slug` atual (se houver), e re-raise.
    - `upload_clip` no caminho de sucesso (após ganhar `video_id`) reseta o flag para FALSE (self-healing) — só se `channel_slug` existir.
    - Testes existentes (Phase 5/7) continuam GREEN.
  </behavior>
  <action>
    Edit cirúrgico em `clip-processor/src/uploader.py`:

    1. No topo do arquivo, adicionar import (dentro do `try/except ModuleNotFoundError` já usado no arquivo, para não quebrar env sem dependências):
    ```python
    try:
        from google.auth.exceptions import RefreshError
    except ModuleNotFoundError:
        class RefreshError(Exception):  # pragma: no cover
            pass
    ```
    Adicionar import de db (usado hoje pelo `publisher.py`):
    ```python
    from src.db import get_db_connection as db_connect  # alias — consistência com Phase 6
    ```

    2. Preservar o membro `channel_slug` no `__init__`:
    ```python
    self.channel_slug = channel_slug  # armazenado para uso em _flag_expired / _clear_expired
    ```
    (Basta guardar; o resto do __init__ mantém a lógica existente.)

    3. Reescrever `_load_credentials`:
    ```python
    def _load_credentials(self):
        token_path = Path(self.token_file)
        if not token_path.exists():
            raise FileNotFoundError(f'Token OAuth não encontrado: {self.token_file}')
        creds = Credentials.from_authorized_user_file(str(token_path), YOUTUBE_UPLOAD_SCOPES)
        if getattr(creds, 'expired', False) and getattr(creds, 'refresh_token', None):
            from google.auth.transport.requests import Request
            try:
                creds.refresh(Request())
            except RefreshError:
                self._flag_expired()  # persiste no MySQL antes de re-raise
                raise
        return creds

    def _flag_expired(self) -> None:
        """Marca destination_channels.oauth_expired_flag=TRUE para o slug atual.

        Best-effort: se channel_slug for None (uso legado), silenciosamente skip.
        Se a conexão MySQL falhar, log e continua (não substitui a exceção RefreshError).
        """
        if not getattr(self, 'channel_slug', None):
            return
        try:
            conn = db_connect()
            try:
                with conn.cursor() as cur:
                    cur.execute(
                        'UPDATE destination_channels SET oauth_expired_flag = TRUE WHERE slug = %s',
                        (self.channel_slug,),
                    )
                    conn.commit()
            finally:
                conn.close()
        except Exception as exc:  # pragma: no cover — defensive
            import logging
            logging.getLogger(__name__).warning('Falha ao marcar oauth_expired_flag: %s', exc)

    def _clear_expired(self) -> None:
        """Self-healing: reseta oauth_expired_flag=FALSE após upload bem-sucedido."""
        if not getattr(self, 'channel_slug', None):
            return
        try:
            conn = db_connect()
            try:
                with conn.cursor() as cur:
                    cur.execute(
                        'UPDATE destination_channels SET oauth_expired_flag = FALSE WHERE slug = %s',
                        (self.channel_slug,),
                    )
                    conn.commit()
            finally:
                conn.close()
        except Exception as exc:  # pragma: no cover
            import logging
            logging.getLogger(__name__).warning('Falha ao limpar oauth_expired_flag: %s', exc)
    ```

    4. No `upload_clip`, imediatamente após `if not video_id: raise ...` (portanto video_id está garantido), adicionar chamada:
    ```python
        # Self-healing OAuth flag
        self._clear_expired()
    ```
    (Antes do bloco de thumbnail, ou no final; o importante é rodar apenas quando `video_id` é válido.)

    5. Rodar os testes:
    ```
    docker exec -T clip-processor pytest tests/test_uploader_expired.py tests/test_uploader.py -v --no-header 2>&1 | tail -20
    ```
    Esperado: `test_uploader_expired.py` GREEN; nenhum teste da Phase 5/7 quebrou.

    **Zero regressão obrigatória:** rodar suite completa:
    ```
    docker exec -T clip-processor pytest tests/ --ignore=tests/test_internal_api.py -q 2>&1 | tail -5
    ```
    Esperado: 0 failed em testes preexistentes.
  </action>
  <verify>
    <automated>docker exec -T clip-processor pytest tests/test_uploader_expired.py tests/test_uploader.py -q --no-header 2>&1 | tail -5 | grep -qE "(0 failed|passed)" && echo UPLOADER_OK</automated>
  </verify>
  <done>
    - `_load_credentials` envolve `creds.refresh()` em try/except RefreshError.
    - `_flag_expired` e `_clear_expired` UPDATE `destination_channels` via db_connect.
    - `upload_clip` chama `_clear_expired` no caminho de sucesso.
    - `test_uploader_expired.py` GREEN.
    - Testes de `test_uploader.py` (Phase 5) e demais → 0 falhas.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Criar DestinationChannelResource + 3 Pages com Select `niche` explícito, TextColumn badge OAuth e bloco copy-paste OAuth</name>
  <files>
    - /Users/alessandrobm1/develop/server/wordpress/canaldecortes/painel/app/Filament/Resources/DestinationChannelResource.php
    - /Users/alessandrobm1/develop/server/wordpress/canaldecortes/painel/app/Filament/Resources/DestinationChannelResource/Pages/CreateDestinationChannel.php
    - /Users/alessandrobm1/develop/server/wordpress/canaldecortes/painel/app/Filament/Resources/DestinationChannelResource/Pages/ListDestinationChannels.php
    - /Users/alessandrobm1/develop/server/wordpress/canaldecortes/painel/app/Filament/Resources/DestinationChannelResource/Pages/EditDestinationChannel.php
  </files>
  <behavior>
    Deve GREEN:
    - 2 testes de `DestinationChannelResourceTest.php` (create + render badge).
    - 3 testes de `DestinationChannelOauthStatusTest.php` (accessor).
  </behavior>
  <action>
    1. Rodar generator SEM `--generate`:
    ```
    docker exec -T php bash -c "cd /var/www/html/painel && php artisan make:filament-resource DestinationChannel --no-interaction"
    ```

    2. Sobrescrever `app/Filament/Resources/DestinationChannelResource.php`:
    ```php
    <?php
    namespace App\Filament\Resources;

    use App\Filament\Resources\DestinationChannelResource\Pages;
    use App\Models\DestinationChannel;
    use Filament\Forms\Components\Placeholder;
    use Filament\Forms\Components\Select;
    use Filament\Forms\Components\TextInput;
    use Filament\Forms\Components\Toggle;
    use Filament\Forms\Components\Textarea;
    use Filament\Resources\Resource;
    use Filament\Schemas\Schema;
    use Filament\Tables\Columns\TextColumn;
    use Filament\Tables\Columns\ToggleColumn;
    use Filament\Tables\Table;
    use Illuminate\Support\HtmlString;

    class DestinationChannelResource extends Resource
    {
        protected static ?string $model = DestinationChannel::class;
        protected static ?string $navigationIcon = 'heroicon-o-tv';
        protected static ?string $modelLabel = 'Canal-destino';
        protected static ?string $pluralModelLabel = 'Canais-destino';

        public static function form(Schema $schema): Schema
        {
            return $schema->schema([
                TextInput::make('slug')
                    ->required()
                    ->helperText('Identificador único (ex: futebol-em-cortes)'),

                TextInput::make('name')->required(),

                // EXPLÍCITO — nunca --generate em ENUM
                Select::make('niche')
                    ->label('Nicho')
                    ->options([
                        'futebol' => 'Futebol',
                        'podcast' => 'Podcast',
                    ])
                    ->required(),

                TextInput::make('youtube_channel_id')
                    ->required()
                    ->label('YouTube Channel ID (UC...)'),

                Textarea::make('credit_template')
                    ->label('Template de créditos')
                    ->helperText('Placeholder disponível: {channel_handle}')
                    ->default('Créditos: @{channel_handle}'),

                Toggle::make('active')->default(true),

                Placeholder::make('oauth_instructions')
                    ->label('OAuth')
                    ->content(fn (?DestinationChannel $record) => new HtmlString(
                        $record
                            ? '<div class="text-sm">'
                                . 'Rode no host para autorizar este canal: '
                                . '<pre class="mt-1 p-2 bg-gray-100 rounded">docker exec -it clip-processor python -m src.youtube_oauth --channel ' . e($record->slug) . '</pre>'
                              . '</div>'
                            : '<em>Salve o canal primeiro para ver o comando OAuth.</em>'
                    ))
                    ->columnSpanFull(),
            ]);
        }

        public static function table(Table $table): Table
        {
            return $table
                ->columns([
                    TextColumn::make('slug')->searchable(),
                    TextColumn::make('name'),
                    TextColumn::make('niche')->badge(),
                    TextColumn::make('oauth_status')
                        ->label('OAuth')
                        ->badge()
                        ->color(fn (string $state): string => match ($state) {
                            'authorized' => 'success',
                            'expired' => 'danger',
                            'missing' => 'gray',
                            default => 'primary',
                        }),
                    ToggleColumn::make('active'),
                    TextColumn::make('youtube_channel_id')->label('YT Channel ID')->copyable(),
                ])
                ->defaultSort('created_at', 'desc');
        }

        public static function getPages(): array
        {
            return [
                'index'  => Pages\ListDestinationChannels::route('/'),
                'create' => Pages\CreateDestinationChannel::route('/create'),
                'edit'   => Pages\EditDestinationChannel::route('/{record}/edit'),
            ];
        }
    }
    ```

    3. Pages padrão (herdam sem sobrescrita) — verificar que os arquivos existem em `Pages/`.

    4. Rodar testes:
    ```
    docker exec -T php bash -c "cd /var/www/html/painel && php artisan test --filter='DestinationChannel' --no-interaction"
    ```
    Esperado: 2 do Feature + 3 do Unit = 5/5 GREEN.

    5. Confirmar zero uso de `--generate` no comando artisan que criou o resource (visual inspection do arquivo gerado — se algum autor deixou `->options()` inferido a partir do schema, substituir por lista explícita).

    **Sanity check:** rodar `grep -r "Filament\\\\Tables\\\\Actions" painel/app` → deve retornar vazio (Filament 5 namespace apenas).
  </action>
  <verify>
    <automated>docker exec -T php bash -c "cd /var/www/html/painel && php artisan test --filter='DestinationChannel' --no-interaction 2>&1 | tail -6" | grep -qE "(5 passed|Tests:  5|Passed: 5)" && echo DC_TESTS_GREEN</automated>
  </verify>
  <done>
    - `DestinationChannelResource` existe com Select explícito para `niche`.
    - Tabela mostra badge OAuth com 3 cores (success/danger/gray).
    - Bloco copy-paste OAuth aparece no form de edit (`docker exec -it clip-processor python -m src.youtube_oauth --channel {slug}`).
    - `DestinationChannelResourceTest` — 2/2 GREEN.
    - `DestinationChannelOauthStatusTest` (unit) — 3/3 GREEN.
  </done>
</task>

</tasks>

<verification>
1. `docker exec -T clip-processor pytest tests/test_uploader_expired.py -q` — 0 failed.
2. `docker exec -T clip-processor pytest tests/ --ignore=tests/test_internal_api.py -q 2>&1 | tail -5` — zero regressão.
3. `docker exec -T php bash -c "cd /var/www/html/painel && php artisan test --filter='DestinationChannel'"` — 5/5 GREEN.
4. `grep -c 'Select::make(.niche.)' /Users/alessandrobm1/develop/server/wordpress/canaldecortes/painel/app/Filament/Resources/DestinationChannelResource.php` — ≥1 (Select explícito confirmado).
5. `grep -c "options(\[" /Users/alessandrobm1/develop/server/wordpress/canaldecortes/painel/app/Filament/Resources/DestinationChannelResource.php` — ≥1 (options explícitas).
</verification>

<success_criteria>
- PANEL-02 completo: CRUD + badge OAuth com 3 estados observáveis (authorized/expired/missing).
- Produtor do estado `expired` (uploader.py) implementado e testado.
- Zero regressão Python.
- Zero `--generate` no fluxo do Resource.
</success_criteria>

<output>
Após conclusão, criar `.planning/phases/08-painel-laravel-filament/08-06-SUMMARY.md` com:
- Diff resumido do `uploader.py` (linhas adicionadas: RefreshError catch, _flag_expired, _clear_expired, chamada de _clear_expired).
- 5 novos testes GREEN documentados (2 Feature + 3 Unit).
- Contador de regressão Python antes/depois.
- Confirmação de Select explícito com print grep.
</output>
