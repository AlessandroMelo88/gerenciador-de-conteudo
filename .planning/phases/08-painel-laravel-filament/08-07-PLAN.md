---
phase: 08-painel-laravel-filament
plan: 07
type: execute
wave: 3
depends_on: ["08-02"]
files_modified:
  - /Users/alessandrobm1/develop/server/wordpress/canaldecortes/clip-processor/src/internal_api.py
  - /Users/alessandrobm1/develop/server/wordpress/canaldecortes/clip-processor/src/main.py
  - /Users/alessandrobm1/develop/server/wordpress/docker-compose.yml
autonomous: true
requirements: [PANEL-01, PANEL-04]
must_haves:
  truths:
    - "`resolve_channel(url)` chama `yt-dlp --flat-playlist --skip-download --dump-single-json {url}` via subprocess e retorna `{channel_id, channel_name, channel_handle}`"
    - "`reject_clip(clip_id)` chama `src.rejeitar.rejeitar(clip_id)` diretamente e retorna o exit code (0/1/2)"
    - "Ambos os endpoints exigem header `X-Internal-Token` bater com `CLIP_PROCESSOR_INTERNAL_TOKEN` — 401 se inválido/ausente"
    - "Endpoint responde em `http://clip-processor:8090/internal/...` na rede docker `internal`, SEM publicação de porta no host"
    - "Servidor Flask roda em thread daemon iniciada por `main.py` (não bloqueia o BlockingScheduler existente)"
    - "`docker-compose.yml` passa `CLIP_PROCESSOR_INTERNAL_TOKEN` para o container clip-processor via env do .env"
    - "6 testes em `test_internal_api.py` passam"
  artifacts:
    - path: "/Users/alessandrobm1/develop/server/wordpress/canaldecortes/clip-processor/src/internal_api.py"
      provides: "GREEN impl do sidecar HTTP"
    - path: "/Users/alessandrobm1/develop/server/wordpress/canaldecortes/clip-processor/src/main.py"
      provides: "Thread daemon Flask iniciada antes de scheduler.start()"
  key_links:
    - from: "internal_api.py.resolve_channel"
      to: "yt-dlp --flat-playlist --skip-download --dump-single-json"
      via: "subprocess.run"
      pattern: "yt-dlp"
    - from: "internal_api.py.reject_clip"
      to: "src.rejeitar.rejeitar"
      via: "chamada direta (sem subprocess)"
      pattern: "rejeitar\\("
    - from: "main.py"
      to: "internal_api.app"
      via: "threading.Thread daemon=True"
      pattern: "threading.Thread.*internal_api"
    - from: "docker-compose.yml (clip-processor)"
      to: "CLIP_PROCESSOR_INTERNAL_TOKEN"
      via: "environment env var"
      pattern: "CLIP_PROCESSOR_INTERNAL_TOKEN"
---

<objective>
GREEN os testes RED do Plan 08-02: implementar `resolve_channel` e `reject_clip`, wireup Flask thread em `main.py` e passar a env var `CLIP_PROCESSOR_INTERNAL_TOKEN` para o container clip-processor no docker-compose. Substitui a "ponte docker exec" por HTTP interno — ver Pattern 3 do RESEARCH.md.

Purpose: sem esta ponte, o Plan 08-05 (SourceChannel) e o Plan 08-08 (Approval/Rejection action) não têm caminho real de execução Python-side. Este plano é o "GREEN" que fecha o loop Python.
Output: implementação Python + wiring docker-compose.
</objective>

<execution_context>
@/Users/alessandrobm1/.claude/get-shit-done/workflows/execute-plan.md
@/Users/alessandrobm1/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/alessandrobm1/develop/server/wordpress/canaldecortes/.planning/phases/08-painel-laravel-filament/08-CONTEXT.md
@/Users/alessandrobm1/develop/server/wordpress/canaldecortes/.planning/phases/08-painel-laravel-filament/08-RESEARCH.md
@/Users/alessandrobm1/develop/server/wordpress/canaldecortes/.planning/phases/08-painel-laravel-filament/08-02-SUMMARY.md
@/Users/alessandrobm1/develop/server/wordpress/canaldecortes/clip-processor/src/internal_api.py
@/Users/alessandrobm1/develop/server/wordpress/canaldecortes/clip-processor/src/main.py
@/Users/alessandrobm1/develop/server/wordpress/canaldecortes/clip-processor/src/rejeitar.py
@/Users/alessandrobm1/develop/server/wordpress/canaldecortes/clip-processor/tests/test_internal_api.py

<interfaces>
## Contrato (do Plan 08-02, replicado como fonte de verdade)

### POST /internal/resolve-channel
Body: `{"url": string}` → 200 `{"channel_id","channel_name","channel_handle"}` OR 422 `{"error","stderr"}` OR 401.
Mapping (yt-dlp `--dump-single-json` output → response):
  - `id` → `channel_id`
  - `channel` → `channel_name` (fallback: `channel_id` se ausente)
  - `uploader_id` OU `channel_id` (com prefixo `@`) → `channel_handle`

### POST /internal/reject-clip
Body: `{"clip_id": int}` → 200 `{"exit_code": 0|1|2}` OR 401.
Chama `rejeitar(clip_id)` diretamente (import de `src.rejeitar`).

### Auth
Header: `X-Internal-Token: {os.environ['CLIP_PROCESSOR_INTERNAL_TOKEN']}`.
Ausência OU mismatch OU env var não setada → 401.

### Bind
`0.0.0.0:8090` dentro do container. Rede docker `internal` (compartilhada com `php`). SEM publicação de porta no host (não expõe pra fora).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implementar `resolve_channel` e `reject_clip` em `internal_api.py`</name>
  <files>
    - /Users/alessandrobm1/develop/server/wordpress/canaldecortes/clip-processor/src/internal_api.py
  </files>
  <behavior>
    Depois desta task, `pytest tests/test_internal_api.py -v` deve mostrar 6/6 GREEN.
  </behavior>
  <action>
    Substituir o skeleton `NotImplementedError` pela implementação real. O arquivo final deve importar `subprocess`, `json` e a função `rejeitar` (já importado no skeleton):

    ```python
    """
    internal_api.py — Sidecar HTTP interno consumido pelo painel Laravel.

    Endpoints:
      - POST /internal/resolve-channel  {url}      -> {channel_id, channel_name, channel_handle}
      - POST /internal/reject-clip      {clip_id}  -> {exit_code}

    Auth: header X-Internal-Token verificado contra CLIP_PROCESSOR_INTERNAL_TOKEN.
    Rede: bind 0.0.0.0:8090 dentro do container `clip-processor`, rede docker `internal`
          — sem publicação de porta no host.
    """
    import json
    import os
    import subprocess

    from flask import Flask, jsonify, request

    from src.rejeitar import rejeitar

    INTERNAL_TOKEN = os.environ.get('CLIP_PROCESSOR_INTERNAL_TOKEN')

    app = Flask(__name__)


    def _check_auth() -> bool:
        return bool(INTERNAL_TOKEN) and request.headers.get('X-Internal-Token') == INTERNAL_TOKEN


    def resolve_channel(url: str) -> dict:
        """Roda yt-dlp em modo metadata-only e extrai id/name/handle.

        Padrão yt-dlp: --flat-playlist --skip-download --dump-single-json {url}
        (Zero cota YouTube Data API — mesma tática do /processar da Phase 6.)

        Raises:
            RuntimeError: yt-dlp retornou exit code != 0.
        """
        result = subprocess.run(
            ['yt-dlp', '--flat-playlist', '--skip-download', '--dump-single-json', url],
            capture_output=True, text=True, timeout=30,
        )
        if result.returncode != 0:
            raise RuntimeError(result.stderr.strip() or 'yt-dlp failed')

        payload = json.loads(result.stdout)
        channel_id = payload.get('id') or payload.get('channel_id') or ''
        channel_name = payload.get('channel') or payload.get('title') or channel_id
        channel_handle = payload.get('uploader_id') or payload.get('channel_id') or ''
        if channel_handle and not channel_handle.startswith('@'):
            channel_handle = '@' + channel_handle.lstrip('@')

        return {
            'channel_id': channel_id,
            'channel_name': channel_name,
            'channel_handle': channel_handle,
        }


    def reject_clip(clip_id: int) -> int:
        """Chama src.rejeitar.rejeitar(clip_id) diretamente. Preserva exit codes 0/1/2."""
        return int(rejeitar(int(clip_id)))


    @app.post('/internal/resolve-channel')
    def _route_resolve_channel():
        if not _check_auth():
            return jsonify(error='unauthorized'), 401
        payload = request.get_json(silent=True) or {}
        url = payload.get('url')
        if not url:
            return jsonify(error='missing url'), 400
        try:
            data = resolve_channel(url)
        except RuntimeError as e:
            return jsonify(error='yt-dlp failed', stderr=str(e)), 422
        return jsonify(data), 200


    @app.post('/internal/reject-clip')
    def _route_reject_clip():
        if not _check_auth():
            return jsonify(error='unauthorized'), 401
        payload = request.get_json(silent=True) or {}
        clip_id = payload.get('clip_id')
        if clip_id is None:
            return jsonify(error='missing clip_id'), 400
        try:
            exit_code = reject_clip(int(clip_id))
        except Exception as e:
            return jsonify(error=str(e)), 500
        return jsonify(exit_code=exit_code), 200
    ```

    Rodar:
    ```
    docker exec -T clip-processor pytest tests/test_internal_api.py -v --no-header 2>&1 | tail -15
    ```
    Esperado: `6 passed`.

    Se rodar RED em algum caso "channel_handle" (yt-dlp real vs contrato do teste), ajustar somente a extração (não os testes).

    **Regressão check:**
    ```
    docker exec -T clip-processor pytest tests/ -q 2>&1 | tail -5
    ```
    Zero falhas em testes preexistentes.
  </action>
  <verify>
    <automated>docker exec -T clip-processor pytest tests/test_internal_api.py -q --no-header 2>&1 | tail -3 | grep -qE "6 passed|Tests: 6" && echo INTERNAL_API_GREEN</automated>
  </verify>
  <done>
    - `test_internal_api.py` 6/6 GREEN.
    - Zero regressão nos testes Python preexistentes.
    - `resolve_channel` mapeia yt-dlp JSON para `{channel_id, channel_name, channel_handle}` com prefixo `@` em handle.
    - `reject_clip` chama `rejeitar` diretamente (não subprocess) — preserva side-effects.
  </done>
</task>

<task type="auto">
  <name>Task 2: Wire `internal_api.app` em thread daemon no `main.py` e passar env `CLIP_PROCESSOR_INTERNAL_TOKEN` no docker-compose</name>
  <files>
    - /Users/alessandrobm1/develop/server/wordpress/canaldecortes/clip-processor/src/main.py
    - /Users/alessandrobm1/develop/server/wordpress/docker-compose.yml
  </files>
  <action>
    1. Editar `clip-processor/src/main.py` — ANTES da chamada `scheduler.start()` (ou antes de `main()` bloquear), adicionar:
    ```python
    import threading
    from src.internal_api import app as _internal_app


    def _start_internal_api():
        _internal_app.run(host='0.0.0.0', port=8090, use_reloader=False, debug=False)


    # Sidecar HTTP interno consumido pelo painel Laravel (Phase 8).
    # Thread daemon → morre com o processo principal.
    threading.Thread(target=_start_internal_api, daemon=True, name='internal-api').start()
    ```

    Posicionamento: DEPOIS dos imports do topo e imports de `pipeline_runner`/`ttl_worker`, mas ANTES de `scheduler.start()` / `main()`. Preservar o guard `if __name__ == '__main__':` já existente (padrão Phase 2 Plan 02-04).

    2. Editar `wordpress/docker-compose.yml`, no serviço `clip-processor`, ADICIONAR uma env var no bloco `environment:`:
    ```yaml
      - CLIP_PROCESSOR_INTERNAL_TOKEN=${CLIP_PROCESSOR_INTERNAL_TOKEN}
    ```
    NÃO adicionar `ports:` (não publicar no host) — a rede `internal` já expõe entre os containers.

    3. Documentar no `canaldecortes/.env.example` (criar se não existir) OU no `canaldecortes/painel/README.md` (do Plan 08-01):
    - Instrução para o operador gerar o token uma vez:
      ```
      openssl rand -hex 32
      ```
    - Colar em `canaldecortes/.env` (o de baixo, não do painel) como `CLIP_PROCESSOR_INTERNAL_TOKEN=<hex>` — ficará disponível para o docker-compose ambos os lados.
    - Colar o MESMO valor em `canaldecortes/painel/.env` como `CLIP_PROCESSOR_INTERNAL_TOKEN=<hex>` para o Laravel enviar no header.

    4. Recriar container clip-processor e validar:
    ```
    docker compose -f /Users/alessandrobm1/develop/server/wordpress/docker-compose.yml up -d --force-recreate clip-processor
    sleep 2
    docker exec -T clip-processor curl -s -o /dev/null -w "%{http_code}" -X POST http://localhost:8090/internal/resolve-channel -H "Content-Type: application/json" -d '{"url":"https://youtube.com/@teste"}'
    ```
    Esperado: `401` (sem header X-Internal-Token) — prova que endpoint está no ar E auth está ligada.

    5. Health-check via php container (simula o Laravel):
    ```
    docker exec -T php curl -s -o /dev/null -w "%{http_code}" http://clip-processor:8090/internal/reject-clip -X POST -H "Content-Type: application/json" -d '{"clip_id":1}'
    ```
    Esperado: `401`.

    **Perigo:** NÃO adicionar `ports: - "8090:8090"` — isso publicaria no host e expõe o sidecar a qualquer processo local. A comunicação deve ficar interna à rede `internal`.
  </action>
  <verify>
    <automated>docker compose -f /Users/alessandrobm1/develop/server/wordpress/docker-compose.yml up -d --force-recreate clip-processor >/dev/null 2>&1 && sleep 3 && test "$(docker exec -T php curl -s -o /dev/null -w '%{http_code}' -X POST http://clip-processor:8090/internal/reject-clip -H 'Content-Type: application/json' -d '{"clip_id":1}')" = "401" && echo SIDECAR_UP_AND_AUTH_OK</automated>
  </verify>
  <done>
    - `main.py` inicia thread daemon Flask antes do scheduler.
    - `docker-compose.yml` passa `CLIP_PROCESSOR_INTERNAL_TOKEN` ao container.
    - Sidecar responde na rede docker interna (from php container reaches http://clip-processor:8090).
    - Auth valida — request sem token retorna 401.
    - Nenhuma porta 8090 publicada no host (`docker port clip-processor` não mostra 8090).
    - `env.example` documenta como gerar `CLIP_PROCESSOR_INTERNAL_TOKEN`.
  </done>
</task>

</tasks>

<verification>
1. `docker exec -T clip-processor pytest tests/test_internal_api.py -q` — 6/6 GREEN.
2. `docker exec -T clip-processor pytest tests/ -q 2>&1 | tail -5` — zero regressão.
3. `docker exec -T php curl -s -o /dev/null -w "%{http_code}" -X POST http://clip-processor:8090/internal/resolve-channel -H "Content-Type: application/json" -d '{}'` → `401`.
4. `docker port clip-processor 2>&1 | grep -c 8090` → `0` (porta não publicada no host — CORRETO).
5. `grep -c CLIP_PROCESSOR_INTERNAL_TOKEN /Users/alessandrobm1/develop/server/wordpress/docker-compose.yml` → ≥1.
</verification>

<success_criteria>
- Sidecar HTTP interno de fato responde a requests do container `php` na rede `internal`.
- Ambos os endpoints protegidos por `X-Internal-Token`.
- Zero exposição no host (nenhum port publish).
- 6 testes GREEN em `test_internal_api.py`.
- Zero regressão Python.
</success_criteria>

<output>
Após conclusão, criar `.planning/phases/08-painel-laravel-filament/08-07-SUMMARY.md` com:
- Confirmação do 401 retornado pelo teste de auth via `docker exec php curl`.
- Confirmação de que 8090 NÃO está publicada no host.
- Instruções copiáveis (openssl rand) para o operador gerar o token no setup.
- 6 testes RED do 08-02 agora GREEN.
</output>
