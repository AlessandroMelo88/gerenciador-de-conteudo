---
phase: 08-painel-laravel-filament
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - /Users/alessandrobm1/develop/server/wordpress/docker-compose.yml
  - /Users/alessandrobm1/develop/server/wordpress/canaldecortes/docker/nginx/canaldecortes.conf
  - /Users/alessandrobm1/develop/server/wordpress/canaldecortes/painel/composer.json
  - /Users/alessandrobm1/develop/server/wordpress/canaldecortes/painel/.env
  - /Users/alessandrobm1/develop/server/wordpress/canaldecortes/painel/.env.example
  - /Users/alessandrobm1/develop/server/wordpress/canaldecortes/painel/config/database.php
  - /Users/alessandrobm1/develop/server/wordpress/canaldecortes/painel/config/services.php
  - /Users/alessandrobm1/develop/server/wordpress/canaldecortes/painel/README.md
autonomous: true
requirements: [PANEL-05]
must_haves:
  truths:
    - "Container `php` monta `canaldecortes/painel/` em `/var/www/html/painel:cached`"
    - "Container `nginx` serve `canaldecortes.local` via bind-mount de `canaldecortes/docker/nginx/canaldecortes.conf`"
    - "Laravel 13 + Filament 5.4 + Pest 4 estão instalados dentro do container `php` no diretório `canaldecortes/painel/`"
    - "`php artisan --version` roda dentro do container `php` sem erro"
    - "`php artisan test` executa (mesmo que sem testes escritos ainda) sem erro de bootstrap"
    - ".env do painel aponta para DB_HOST=mysql, DB_DATABASE=clips_automation e REDIS_DB=1"
    - "Nenhum arquivo fora de `canaldecortes/`, `wordpress/docker-compose.yml` e `wordpress/docker/nginx_conf/` foi tocado"
  artifacts:
    - path: "/Users/alessandrobm1/develop/server/wordpress/canaldecortes/painel/artisan"
      provides: "Laravel entrypoint — confirma projeto criado"
    - path: "/Users/alessandrobm1/develop/server/wordpress/canaldecortes/painel/vendor/filament/filament"
      provides: "Filament 5.4 instalado"
    - path: "/Users/alessandrobm1/develop/server/wordpress/canaldecortes/painel/tests/Pest.php"
      provides: "Pest bootstrap configurado"
    - path: "/Users/alessandrobm1/develop/server/wordpress/canaldecortes/docker/nginx/canaldecortes.conf"
      provides: "vhost nginx para canaldecortes.local → PHP-FPM em php:9000"
    - path: "/Users/alessandrobm1/develop/server/wordpress/canaldecortes/painel/config/database.php"
      provides: "Duas conexões Redis nomeadas: `default` (DB 1) e `pipeline` (DB 0)"
    - path: "/Users/alessandrobm1/develop/server/wordpress/canaldecortes/painel/config/services.php"
      provides: "`services.clip_processor.url` e `services.clip_processor.token` disponíveis"
  key_links:
    - from: "wordpress/docker-compose.yml (service `php`)"
      to: "canaldecortes/painel/"
      via: "bind mount cached"
      pattern: "./canaldecortes/painel:/var/www/html/painel:cached"
    - from: "wordpress/docker-compose.yml (service `nginx`)"
      to: "canaldecortes/docker/nginx/canaldecortes.conf"
      via: "bind mount cached em /etc/nginx/conf.d/canaldecortes.conf"
      pattern: "./canaldecortes/docker/nginx/canaldecortes.conf:/etc/nginx/conf.d/canaldecortes.conf:cached"
    - from: "canaldecortes/painel/.env"
      to: "MySQL clips_automation + Redis DB 1"
      via: "DB_HOST=mysql, REDIS_HOST=redis, REDIS_DB=1"
      pattern: "DB_DATABASE=clips_automation"
---

<objective>
Bootstrap do painel Laravel 13 + Filament 5.4 + Pest 4 dentro do container `php` já existente do stack `wordpress/`, com wiring Docker (bind mounts do painel/ no `php` e do vhost nginx no `nginx`) e configuração base (`.env`, `config/database.php` com conexão Redis `pipeline` separada, `config/services.php` com `clip_processor`).

Purpose: sem esta base, nenhum plano de Wave 2+ (test scaffolds, resources, widgets) pode existir. Este plano só resolve infraestrutura; nenhuma feature de PANEL-XX é implementada aqui — mas ele habilita todas.
Output: projeto Laravel funcional em `canaldecortes/painel/`, servido em `http://canaldecortes.local`, conectado à base `clips_automation` e à instância Redis compartilhada.
</objective>

<execution_context>
@/Users/alessandrobm1/.claude/get-shit-done/workflows/execute-plan.md
@/Users/alessandrobm1/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/alessandrobm1/develop/server/wordpress/canaldecortes/.planning/PROJECT.md
@/Users/alessandrobm1/develop/server/wordpress/canaldecortes/.planning/ROADMAP.md
@/Users/alessandrobm1/develop/server/wordpress/canaldecortes/.planning/STATE.md
@/Users/alessandrobm1/develop/server/wordpress/canaldecortes/.planning/phases/08-painel-laravel-filament/08-CONTEXT.md
@/Users/alessandrobm1/develop/server/wordpress/canaldecortes/.planning/phases/08-painel-laravel-filament/08-RESEARCH.md
@/Users/alessandrobm1/develop/server/wordpress/canaldecortes/.planning/phases/08-painel-laravel-filament/08-VALIDATION.md
@/Users/alessandrobm1/develop/server/wordpress/docker-compose.yml
@/Users/alessandrobm1/develop/server/wordpress/docker/nginx_conf/kelnab.conf

<interfaces>
<!-- Referência do vhost Kelnab (mesmo padrão a replicar, adaptado para painel/) -->
```nginx
# wordpress/docker/nginx_conf/kelnab.conf (REFERÊNCIA — NÃO EDITAR)
server {
    listen       80;
    server_name  kelnab.local;
    root   /var/www/html/kelnab/public;
    index  index.php;
    client_max_body_size 128m;
    location / { try_files $uri $uri/ /index.php?$query_string; }
    location ~ \.php$ {
        try_files $uri =404;
        fastcgi_pass php:9000;
        fastcgi_index index.php;
        fastcgi_param SCRIPT_FILENAME $document_root$fastcgi_script_name;
        include fastcgi_params;
        fastcgi_intercept_errors on;
        fastcgi_read_timeout 900;
    }
}
```

<!-- Serviços relevantes do wordpress/docker-compose.yml -->
- `nginx`: image `alessandrobm88/nginx:latest`, mount `./docker/nginx_conf:/etc/nginx/conf.d:cached`.
- `php`: build custom `Dockerfile`, monta múltiplos projetos Laravel em `/var/www/html/{project}`. **NÃO tocar/editar mounts de outros projetos** (kelnab, feeb, riodelux, gringo, placebeats).
- `mysql`: container `mysql`, hostname `mysql`, database `clips_automation` já criada.
- `redis`: container `redis`, hostname `redis`. Pipeline Python usa `db=0` implícito.
- `clip-processor`: container `clip-processor` na rede `internal`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Adicionar bind mounts no docker-compose.yml raiz e criar vhost nginx</name>
  <files>
    - /Users/alessandrobm1/develop/server/wordpress/docker-compose.yml
    - /Users/alessandrobm1/develop/server/wordpress/canaldecortes/docker/nginx/canaldecortes.conf
    - /Users/alessandrobm1/develop/server/wordpress/canaldecortes/painel/README.md
  </files>
  <action>
    1. Criar `canaldecortes/docker/nginx/canaldecortes.conf` copiando o padrão de `wordpress/docker/nginx_conf/kelnab.conf` mas com:
       - `server_name canaldecortes.local;`
       - `root /var/www/html/painel/public;`
       - Manter `fastcgi_pass php:9000;`, `client_max_body_size 128m`, gzip on, headers padrão.
       - **Comentário no topo:** "Vhost do painel Laravel/Filament do Canal de Cortes. Bind-mount único deste arquivo é feito pelo wordpress/docker-compose.yml no serviço nginx. Não incluir referência a nenhum outro projeto."

    2. Editar `wordpress/docker-compose.yml` fazendo APENAS estas 2 mudanças (NÃO tocar em nenhum mount de kelnab/feeb/riodelux/gringo/placebeats):
       - No serviço `nginx`, adicionar UMA linha nos volumes:
         `- ./canaldecortes/docker/nginx/canaldecortes.conf:/etc/nginx/conf.d/canaldecortes.conf:cached`
       - No serviço `php`, adicionar UMA linha nos volumes:
         `- ./canaldecortes/painel:/var/www/html/painel:cached`

    3. Rodar `docker compose -f /Users/alessandrobm1/develop/server/wordpress/docker-compose.yml config > /dev/null` para validar YAML. Deve terminar sem erro (exit 0).

    4. Criar `canaldecortes/painel/README.md` mínimo (~30 linhas) com:
       - Título "Painel Canal de Cortes"
       - Seção "Setup local" com:
         - `echo "127.0.0.1 canaldecortes.local" | sudo tee -a /etc/hosts`
         - `docker compose -f wordpress/docker-compose.yml up -d nginx php mysql redis`
         - `docker exec -it php bash -c "cd /var/www/html/painel && composer install"`
         - `docker exec -it php bash -c "cd /var/www/html/painel && php artisan painel:create-user"` (comando será criado no Plan 08-04)
       - Seção "URL" apontando para `http://canaldecortes.local`
       - Seção "Preview MP4" instruindo abrir arquivos de `canaldecortes/videos/clips/` direto no Finder/VLC (sem player HTML5 no painel).

    **Por que não instalar Docker CLI / montar `docker.sock` no `php`:** o container `php` é compartilhado por ~8 projetos Laravel independentes. Montar o socket dá controle root do host para todos eles. Comunicação PHP→Python é resolvida via HTTP interno no Plan 08-07.
  </action>
  <verify>
    <automated>docker compose -f /Users/alessandrobm1/develop/server/wordpress/docker-compose.yml config > /dev/null && echo CONFIG_VALID</automated>
  </verify>
  <done>
    - Novo arquivo `canaldecortes/docker/nginx/canaldecortes.conf` existe com `server_name canaldecortes.local;` e `root /var/www/html/painel/public;`.
    - `wordpress/docker-compose.yml` tem exatamente 2 linhas novas nos volumes (nginx + php); diff não mostra edição em nenhum mount pré-existente de outros projetos.
    - `docker compose config` valida sem erro.
    - `canaldecortes/painel/README.md` existe com seções Setup, URL e Preview MP4.
  </done>
</task>

<task type="auto">
  <name>Task 2: Bootstrap Laravel 13 + Filament 5.4 + Pest 4 dentro do container `php` e configurar `.env`, database.php, services.php</name>
  <files>
    - /Users/alessandrobm1/develop/server/wordpress/canaldecortes/painel/ (tree inteiro criado pelo composer)
    - /Users/alessandrobm1/develop/server/wordpress/canaldecortes/painel/.env
    - /Users/alessandrobm1/develop/server/wordpress/canaldecortes/painel/.env.example
    - /Users/alessandrobm1/develop/server/wordpress/canaldecortes/painel/config/database.php
    - /Users/alessandrobm1/develop/server/wordpress/canaldecortes/painel/config/services.php
  </files>
  <action>
    Executar todos os comandos abaixo via `docker exec` no container `php` (que já existe rodando). Se o container estiver parado, rodar `docker compose -f /Users/alessandrobm1/develop/server/wordpress/docker-compose.yml up -d php` antes.

    1. **Criar projeto Laravel 13** (dentro do container, aproveitando o bind mount recém-adicionado):
       ```
       docker exec -T php bash -c "mkdir -p /var/www/html/painel && cd /var/www/html/painel && composer create-project laravel/laravel . '^13.0' --no-interaction"
       ```
       Isso preenche `canaldecortes/painel/` com o esqueleto padrão Laravel 13.

    2. **Instalar Filament 5.4:**
       ```
       docker exec -T php bash -c "cd /var/www/html/painel && composer require filament/filament:'^5.4' --no-interaction"
       docker exec -T php bash -c "cd /var/www/html/painel && php artisan filament:install --panels --no-interaction"
       ```
       Isso cria `app/Providers/Filament/AdminPanelProvider.php` (guard default `web`, path `/admin`) — configuração de auth/login será feita no Plan 08-04.

    3. **Instalar Pest 4:**
       ```
       docker exec -T php bash -c "cd /var/www/html/painel && composer require pestphp/pest pestphp/pest-plugin-laravel --dev --no-interaction --with-all-dependencies"
       docker exec -T php bash -c "cd /var/www/html/painel && php artisan pest:install --no-interaction"
       ```

    4. **Configurar `.env` do painel:**
       - Editar `canaldecortes/painel/.env` gerado pelo `create-project` ajustando:
         - `APP_NAME="Canal de Cortes"`
         - `APP_URL=http://canaldecortes.local`
         - `DB_CONNECTION=mysql`
         - `DB_HOST=mysql`
         - `DB_PORT=3306`
         - `DB_DATABASE=clips_automation`
         - `DB_USERNAME=clips_user`
         - `DB_PASSWORD=` (operador preenche com `CLIPS_DB_PASSWORD` real do `canaldecortes/.env` no setup — deixar vazio no arquivo comitado)
         - `REDIS_HOST=redis`
         - `REDIS_PORT=6379`
         - `REDIS_DB=1` (Laravel session/cache)
         - `REDIS_CLIENT=phpredis` (phpredis já compilado no Dockerfile do `php`)
         - `CLIP_PROCESSOR_INTERNAL_URL=http://clip-processor:8090`
         - `CLIP_PROCESSOR_INTERNAL_TOKEN=` (operador preenche no setup; será usado pelo Plan 08-07)
       - Criar/atualizar `.env.example` com os mesmos campos mas todos valores vazios ou de exemplo (comitável).
       - **NUNCA gravar senha nem token em arquivo versionado.** `.env` deve estar no `.gitignore` (Laravel já faz isso por default).

    5. **`config/database.php` — adicionar segunda conexão Redis nomeada `pipeline` (DB 0):**
       - Localizar o array `'redis' => [...]` e ADICIONAR a chave `'pipeline'` **DEPOIS** de `'default'` e `'cache'`:
       ```php
       'pipeline' => [
           'url' => env('REDIS_URL'),
           'host' => env('REDIS_HOST', '127.0.0.1'),
           'password' => env('REDIS_PASSWORD'),
           'port' => env('REDIS_PORT', '6379'),
           'database' => 0, // MESMO DB do pipeline Python (rss_poller/quota_manager)
       ],
       ```
       Comentário Portu: `// Conexão SÓ para leitura de chaves geradas pelo pipeline Python (youtube_uploads:{channel_id}:{date}). NÃO usar para session/cache do Laravel.`

    6. **`config/services.php` — adicionar bloco `clip_processor`:**
       ```php
       'clip_processor' => [
           'url' => env('CLIP_PROCESSOR_INTERNAL_URL', 'http://clip-processor:8090'),
           'token' => env('CLIP_PROCESSOR_INTERNAL_TOKEN'),
       ],
       ```

    7. **Verificação final:**
       ```
       docker exec -T php bash -c "cd /var/www/html/painel && php artisan --version"
       docker exec -T php bash -c "cd /var/www/html/painel && php artisan test --no-interaction"
       ```
       `--version` deve imprimir "Laravel Framework 13.x.x"; `test` deve rodar (mesmo com 0 testes ou só o ExampleTest default gerado por Pest).

    **Anti-patterns a evitar:**
    - NÃO usar `php artisan make:migration create_source_channels_table` — a tabela já existe no schema Python (`mysql/init/*.sql`). Models virão no Plan 08-03 apontando para tabelas existentes.
    - NÃO rodar `filament:install --generate` em nenhum momento — regra registrada no Roadmap v2.0.
    - NÃO usar `Redis::get(...)` (conexão default) para ler chaves do pipeline — sempre `Redis::connection('pipeline')->get(...)` (Pitfall 3 do RESEARCH.md).
  </action>
  <verify>
    <automated>docker exec -T php bash -c "cd /var/www/html/painel && php artisan --version" && docker exec -T php bash -c "cd /var/www/html/painel && php artisan test --no-interaction"</automated>
  </verify>
  <done>
    - `canaldecortes/painel/artisan` existe e `php artisan --version` imprime `Laravel Framework 13.*`.
    - `canaldecortes/painel/vendor/filament/filament/composer.json` existe e é v5.4.x.
    - `canaldecortes/painel/tests/Pest.php` existe.
    - `.env` do painel tem `DB_HOST=mysql`, `DB_DATABASE=clips_automation`, `REDIS_DB=1` e placeholders para senha/token.
    - `.env.example` comitado com os mesmos campos vazios.
    - `config/database.php` tem conexão Redis nomeada `pipeline` com `database => 0` além da default `1`.
    - `config/services.php` tem chave `clip_processor` com `url` e `token`.
    - `php artisan test` roda sem erro (mesmo com 0 testes reais).
  </done>
</task>

</tasks>

<verification>
Após ambas as tasks:
1. `docker compose -f /Users/alessandrobm1/develop/server/wordpress/docker-compose.yml config > /dev/null` → exit 0.
2. `docker exec -T php test -f /var/www/html/painel/artisan && echo LARAVEL_OK` → imprime LARAVEL_OK.
3. `docker exec -T php bash -c "cd /var/www/html/painel && php artisan test --no-interaction"` → passa (0 falhas).
4. `test -f /Users/alessandrobm1/develop/server/wordpress/canaldecortes/docker/nginx/canaldecortes.conf` → true.
5. `grep -c "canaldecortes/painel" /Users/alessandrobm1/develop/server/wordpress/docker-compose.yml` → 1.
6. `grep -c "canaldecortes/docker/nginx/canaldecortes.conf" /Users/alessandrobm1/develop/server/wordpress/docker-compose.yml` → 1.
</verification>

<success_criteria>
- Painel Laravel 13 + Filament 5.4 + Pest 4 instalado em `canaldecortes/painel/`.
- Container `php` monta o painel; container `nginx` serve `canaldecortes.local`.
- `.env` conecta a `clips_automation` + Redis DB 1; conexão nomeada `pipeline` disponível para DB 0.
- Config `services.clip_processor` disponível para o Plan 08-05/08-08.
- Zero mount de outros projetos (kelnab, feeb, etc.) foi editado ou lido.
</success_criteria>

<output>
Após conclusão, criar `.planning/phases/08-painel-laravel-filament/08-01-SUMMARY.md` seguindo o template padrão com:
- Task-by-task decisions (nomes das versões instaladas, quaisquer ajustes feitos no Dockerfile — não deveria haver).
- Comando(s) exatos que o operador deve rodar no setup fresco.
- Confirmação de que 0 arquivos fora do escopo foram tocados.
</output>
