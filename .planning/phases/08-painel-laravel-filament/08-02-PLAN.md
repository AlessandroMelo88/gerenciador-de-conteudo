---
phase: 08-painel-laravel-filament
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - /Users/alessandrobm1/develop/server/wordpress/canaldecortes/mysql/init/07-panel-oauth-flag-migration.sql
  - /Users/alessandrobm1/develop/server/wordpress/canaldecortes/clip-processor/requirements.txt
  - /Users/alessandrobm1/develop/server/wordpress/canaldecortes/clip-processor/src/internal_api.py
  - /Users/alessandrobm1/develop/server/wordpress/canaldecortes/clip-processor/tests/test_internal_api.py
  - /Users/alessandrobm1/develop/server/wordpress/canaldecortes/clip-processor/tests/test_uploader_expired.py
autonomous: true
requirements: [PANEL-01, PANEL-02, PANEL-04]
must_haves:
  truths:
    - "SQL migration idempotente adiciona coluna `oauth_expired_flag BOOLEAN DEFAULT FALSE` em `destination_channels`"
    - "Módulo Python `clip-processor/src/internal_api.py` existe como skeleton exportando `app` (Flask), `resolve_channel(url)`, `reject_clip(clip_id)` — todos levantando NotImplementedError"
    - "Arquivo `clip-processor/tests/test_internal_api.py` importa `resolve_channel` e `reject_clip` do módulo skeleton (import no topo) e possui testes que falham em RED contra os endpoints"
    - "Arquivo `clip-processor/tests/test_uploader_expired.py` possui teste RED que assume `RefreshError` deve ser capturada e coluna `oauth_expired_flag` deve virar TRUE em `destination_channels`"
    - "`flask` está listado em `clip-processor/requirements.txt`"
    - "`pytest clip-processor/tests/test_internal_api.py` retorna RED (ao menos 1 test FAIL, 0 errors de coleta)"
  artifacts:
    - path: "/Users/alessandrobm1/develop/server/wordpress/canaldecortes/mysql/init/07-panel-oauth-flag-migration.sql"
      provides: "Migration idempotente com INFORMATION_SCHEMA guard para `oauth_expired_flag`"
    - path: "/Users/alessandrobm1/develop/server/wordpress/canaldecortes/clip-processor/src/internal_api.py"
      provides: "Skeleton Flask app + resolve_channel + reject_clip"
      exports: ["app", "resolve_channel", "reject_clip", "INTERNAL_TOKEN"]
    - path: "/Users/alessandrobm1/develop/server/wordpress/canaldecortes/clip-processor/tests/test_internal_api.py"
      provides: "Testes RED que cobrem POST /internal/resolve-channel e POST /internal/reject-clip"
    - path: "/Users/alessandrobm1/develop/server/wordpress/canaldecortes/clip-processor/tests/test_uploader_expired.py"
      provides: "Teste RED que espera captura de RefreshError e escrita de oauth_expired_flag"
  key_links:
    - from: "clip-processor/src/internal_api.py"
      to: "clip-processor/src/rejeitar.py"
      via: "from src.rejeitar import rejeitar"
      pattern: "import.*rejeitar"
    - from: "mysql/init/07-panel-oauth-flag-migration.sql"
      to: "destination_channels"
      via: "ALTER TABLE ADD COLUMN oauth_expired_flag"
      pattern: "oauth_expired_flag"
---

<objective>
Wave 0 do lado Python: (1) criar SQL migration idempotente que adiciona `oauth_expired_flag` em `destination_channels`; (2) criar skeleton de `clip-processor/src/internal_api.py` com Flask app e funções `resolve_channel`/`reject_clip` levantando `NotImplementedError`; (3) escrever testes RED para o sidecar HTTP e para a captura de `RefreshError` no `uploader.py`.

Este plano NÃO implementa a lógica — só habilita: implementação GREEN vem no Plan 08-07 (internal_api) e Plan 08-06 (uploader.py RefreshError). Segue exatamente o pattern RED-state estabelecido nas Phases 2/3/4/6 deste projeto (skeletons com imports+constants module-level, corpo levantando NotImplementedError, testes com imports no topo).

Purpose: cria o substrato Python que o painel Laravel vai consumir (resolver canal-fonte via yt-dlp, rejeitar clip com side-effect) sem exigir Docker socket compartilhado — decisão de arquitetura do Pattern 3 do RESEARCH.md.
Output: 1 SQL, 1 módulo Python skeleton, 2 arquivos de teste RED, 1 nova dependência em `requirements.txt`.
</objective>

<execution_context>
@/Users/alessandrobm1/.claude/get-shit-done/workflows/execute-plan.md
@/Users/alessandrobm1/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/alessandrobm1/develop/server/wordpress/canaldecortes/.planning/PROJECT.md
@/Users/alessandrobm1/develop/server/wordpress/canaldecortes/.planning/ROADMAP.md
@/Users/alessandrobm1/develop/server/wordpress/canaldecortes/.planning/STATE.md
@/Users/alessandrobm1/develop/server/wordpress/canaldecortes/.planning/phases/08-painel-laravel-filament/08-CONTEXT.md
@/Users/alessandrobm1/develop/server/wordpress/canaldecortes/.planning/phases/08-painel-laravel-filament/08-RESEARCH.md
@/Users/alessandrobm1/develop/server/wordpress/canaldecortes/.planning/phases/08-painel-laravel-filament/08-VALIDATION.md
@/Users/alessandrobm1/develop/server/wordpress/canaldecortes/mysql/init/06-multi-canal-migration.sql
@/Users/alessandrobm1/develop/server/wordpress/canaldecortes/clip-processor/src/uploader.py
@/Users/alessandrobm1/develop/server/wordpress/canaldecortes/clip-processor/src/rejeitar.py
@/Users/alessandrobm1/develop/server/wordpress/canaldecortes/clip-processor/requirements.txt

<interfaces>
<!-- Contratos que o sidecar Python vai expor ao Laravel (implementação no Plan 08-07). -->

## Endpoints HTTP internos (rede docker `internal`, sem publicar no host)

### POST /internal/resolve-channel
- Header: `X-Internal-Token: {CLIP_PROCESSOR_INTERNAL_TOKEN}` (obrigatório)
- Body JSON: `{"url": "https://youtube.com/@handle"}`
- Response 200: `{"channel_id": "UCxxx", "channel_name": "Nome", "channel_handle": "@handle"}`
- Response 422: `{"error": "yt-dlp failed", "stderr": "..."}` (URL inválida ou canal inexistente)
- Response 401: `{"error": "unauthorized"}` (token inválido/ausente)

### POST /internal/reject-clip
- Header: `X-Internal-Token: {CLIP_PROCESSOR_INTERNAL_TOKEN}` (obrigatório)
- Body JSON: `{"clip_id": 42}`
- Response 200: `{"exit_code": 0}` (sucesso, MP4 removido, status='rejected') OU `{"exit_code": 1}` (clip não existe) OU `{"exit_code": 2}` (status inválido/argv inválido)
- Response 401: `{"error": "unauthorized"}`

## Funções Python expostas (para testabilidade unitária)
```python
# clip-processor/src/internal_api.py
INTERNAL_TOKEN: str | None  # lido de env CLIP_PROCESSOR_INTERNAL_TOKEN
app: flask.Flask            # exportado para main.py rodar em thread daemon

def resolve_channel(url: str) -> dict:
    """Chama yt-dlp --flat-playlist --skip-download --dump-single-json {url}.
    Returns: {'channel_id', 'channel_name', 'channel_handle'}
    Raises: RuntimeError com stderr se yt-dlp falhar."""

def reject_clip(clip_id: int) -> int:
    """Chama src.rejeitar.rejeitar(clip_id) diretamente (sem subprocess).
    Returns: exit code (0=ok, 1=clip não existe, 2=status inválido)."""
```

## Coluna nova em `destination_channels`
```sql
oauth_expired_flag BOOLEAN NOT NULL DEFAULT FALSE
-- Producer: uploader.py._load_credentials() ao capturar google.auth.exceptions.RefreshError
-- Consumer: Laravel DestinationChannel model (accessor oauth_status)
-- Self-healing: uploader.py reseta para FALSE em upload bem-sucedido
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Criar SQL migration idempotente `07-panel-oauth-flag-migration.sql`</name>
  <files>
    - /Users/alessandrobm1/develop/server/wordpress/canaldecortes/mysql/init/07-panel-oauth-flag-migration.sql
  </files>
  <action>
    Criar `mysql/init/07-panel-oauth-flag-migration.sql` seguindo EXATAMENTE o padrão INFORMATION_SCHEMA + prepared statements da Phase 7 (`06-multi-canal-migration.sql`):

    ```sql
    -- Phase 8: Painel — Migration para OAuth expired flag
    -- Idempotente — pode ser re-executado sem erro
    -- Padrão: INFORMATION_SCHEMA + prepared statements (ver Phases 3-5, 7)
    -- Executar com:
    --   docker exec -i mysql mysql -u root -p${MYSQL_ROOT_PASSWORD} clips_automation < mysql/init/07-panel-oauth-flag-migration.sql

    USE clips_automation;

    -- ============================================================================
    -- oauth_expired_flag: sinalizado por uploader.py ao capturar RefreshError
    -- Painel lê este campo para decidir se badge OAuth vira "expired" (vermelho)
    -- Self-healing: uploader reseta para FALSE após upload bem-sucedido
    -- ============================================================================

    SET @has_col = (
      SELECT COUNT(*)
      FROM INFORMATION_SCHEMA.COLUMNS
      WHERE TABLE_SCHEMA = 'clips_automation'
        AND TABLE_NAME = 'destination_channels'
        AND COLUMN_NAME = 'oauth_expired_flag'
    );
    SET @sql = IF(
      @has_col = 0,
      'ALTER TABLE destination_channels ADD COLUMN oauth_expired_flag BOOLEAN NOT NULL DEFAULT FALSE AFTER active',
      'SELECT 1'
    );
    PREPARE stmt FROM @sql;
    EXECUTE stmt;
    DEALLOCATE PREPARE stmt;
    ```

    Aplicar a migration no MySQL rodando localmente:
    ```
    docker exec -i mysql mysql -uroot -p${MYSQL_ROOT_PASSWORD:-rootpassword} clips_automation < /Users/alessandrobm1/develop/server/wordpress/canaldecortes/mysql/init/07-panel-oauth-flag-migration.sql
    ```
    (Se o operador não tiver `MYSQL_ROOT_PASSWORD` na sessão, deixar comentário indicando que a migration é auto-aplicada no próximo boot do MySQL — arquivos em `mysql/init/` rodam automaticamente pelo entrypoint do container mysql, mas apenas em first-boot; para banco já criado, rodar manualmente.)

    Verificar:
    ```
    docker exec -i mysql mysql -uroot -p${MYSQL_ROOT_PASSWORD:-rootpassword} clips_automation -e "DESCRIBE destination_channels" | grep oauth_expired_flag
    ```

    **NÃO** rodar `--generate` do Filament nem criar migration Laravel para `destination_channels` — schema fica 100% no Python-owned SQL.
  </action>
  <verify>
    <automated>docker exec -i mysql mysql -uroot -p${MYSQL_ROOT_PASSWORD:-rootpassword} clips_automation -e "SHOW COLUMNS FROM destination_channels LIKE 'oauth_expired_flag'" | grep -q oauth_expired_flag && echo COL_EXISTS</automated>
  </verify>
  <done>
    - `mysql/init/07-panel-oauth-flag-migration.sql` existe com bloco idempotente INFORMATION_SCHEMA.
    - Coluna `oauth_expired_flag` presente em `destination_channels` (BOOLEAN, NOT NULL, DEFAULT FALSE).
    - Re-executar a migration não gera erro (idempotência confirmada).
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Criar `internal_api.py` skeleton + adicionar flask a requirements.txt</name>
  <files>
    - /Users/alessandrobm1/develop/server/wordpress/canaldecortes/clip-processor/src/internal_api.py
    - /Users/alessandrobm1/develop/server/wordpress/canaldecortes/clip-processor/requirements.txt
  </files>
  <behavior>
    O módulo deve permitir que `test_internal_api.py` (Task 3) colete testes sem `ModuleNotFoundError` mas falhe em RED ao chamar `resolve_channel`/`reject_clip` ou os endpoints Flask (NotImplementedError → HTTP 500).
  </behavior>
  <action>
    1. Adicionar `flask` (sem pin agressivo — última linha do arquivo) ao final de `clip-processor/requirements.txt`. Confirmar que a linha `flask` fica isolada (uma dependência por linha, sem espaços trailing).

    2. Criar `clip-processor/src/internal_api.py` com este conteúdo EXATO (skeleton RED):
    ```python
    """
    internal_api.py — Sidecar HTTP interno consumido pelo painel Laravel.

    Endpoints (implementação GREEN no Plan 08-07):
      - POST /internal/resolve-channel  {url}      -> {channel_id, channel_name, channel_handle}
      - POST /internal/reject-clip      {clip_id}  -> {exit_code}

    Auth: header X-Internal-Token verificado contra CLIP_PROCESSOR_INTERNAL_TOKEN.
    Rede: bind 0.0.0.0:8090 dentro do container `clip-processor`, rede docker `internal`
          — sem publicação de porta no host.
    """
    import os
    from flask import Flask, jsonify, request

    # Imports que a implementação GREEN vai usar (mantidos aqui para expor à collect
    # dos testes; NotImplementedError vem do corpo das funções, não do import).
    from src.rejeitar import rejeitar  # noqa: F401  # usado por reject_clip()

    INTERNAL_TOKEN = os.environ.get('CLIP_PROCESSOR_INTERNAL_TOKEN')

    app = Flask(__name__)


    def _check_auth() -> bool:
        return bool(INTERNAL_TOKEN) and request.headers.get('X-Internal-Token') == INTERNAL_TOKEN


    def resolve_channel(url: str) -> dict:
        """RED skeleton — implementação em Plan 08-07."""
        raise NotImplementedError('resolve_channel — Plan 08-07 GREEN pendente')


    def reject_clip(clip_id: int) -> int:
        """RED skeleton — implementação em Plan 08-07."""
        raise NotImplementedError('reject_clip — Plan 08-07 GREEN pendente')


    @app.post('/internal/resolve-channel')
    def _route_resolve_channel():
        if not _check_auth():
            return jsonify(error='unauthorized'), 401
        payload = request.get_json(silent=True) or {}
        url = payload.get('url')
        if not url:
            return jsonify(error='missing url'), 400
        try:
            data = resolve_channel(url)
            return jsonify(data), 200
        except NotImplementedError as e:
            return jsonify(error=str(e)), 501
        except RuntimeError as e:
            return jsonify(error='yt-dlp failed', stderr=str(e)), 422


    @app.post('/internal/reject-clip')
    def _route_reject_clip():
        if not _check_auth():
            return jsonify(error='unauthorized'), 401
        payload = request.get_json(silent=True) or {}
        clip_id = payload.get('clip_id')
        if clip_id is None:
            return jsonify(error='missing clip_id'), 400
        try:
            exit_code = reject_clip(int(clip_id))
            return jsonify(exit_code=exit_code), 200
        except NotImplementedError as e:
            return jsonify(error=str(e)), 501
    ```

    **Padrão RED estabelecido:** imports no topo (yt-dlp import fica para Plan 08-07 para evitar dependência circular; o RED é levantado dentro do corpo das funções puras, não no módulo). Ao colecionar `test_internal_api.py`, os imports devem funcionar.

    3. Verificar que o módulo importa sem erro (a coleta dos testes deve funcionar):
       ```
       docker exec -T clip-processor python -c "from src import internal_api; print('OK', hasattr(internal_api, 'app'))"
       ```
       Deve imprimir `OK True`. Se der `ModuleNotFoundError: flask`, rebuild da imagem:
       ```
       docker compose -f /Users/alessandrobm1/develop/server/wordpress/docker-compose.yml build clip-processor
       docker compose -f /Users/alessandrobm1/develop/server/wordpress/docker-compose.yml up -d clip-processor
       ```
  </action>
  <verify>
    <automated>docker exec -T clip-processor python -c "from src import internal_api; assert hasattr(internal_api, 'app') and hasattr(internal_api, 'resolve_channel') and hasattr(internal_api, 'reject_clip'); print('SKELETON_OK')"</automated>
  </verify>
  <done>
    - `flask` presente em `requirements.txt`.
    - `clip-processor/src/internal_api.py` existe com `app`, `resolve_channel`, `reject_clip`, `INTERNAL_TOKEN` no top-level.
    - Import do módulo funciona dentro do container.
    - `resolve_channel(...)` e `reject_clip(...)` levantam `NotImplementedError`.
    - Endpoints registrados no `app.url_map` (ambos POST).
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Escrever testes RED — test_internal_api.py + test_uploader_expired.py</name>
  <files>
    - /Users/alessandrobm1/develop/server/wordpress/canaldecortes/clip-processor/tests/test_internal_api.py
    - /Users/alessandrobm1/develop/server/wordpress/canaldecortes/clip-processor/tests/test_uploader_expired.py
  </files>
  <behavior>
    - `pytest tests/test_internal_api.py` deve rodar coleta sem erro e ter ao menos 4 testes falhando (RED): auth ausente, resolve com URL válida, reject com clip_id válido, auth com token inválido.
    - `pytest tests/test_uploader_expired.py` deve rodar coleta sem erro e ter ao menos 1 teste falhando (RED): RefreshError capturado + UPDATE em destination_channels SET oauth_expired_flag=TRUE.
  </behavior>
  <action>
    1. Criar `clip-processor/tests/test_internal_api.py`:
    ```python
    """RED tests for internal_api sidecar (implementação GREEN no Plan 08-07)."""
    import os
    import pytest
    from unittest.mock import patch, MagicMock

    from src.internal_api import app, resolve_channel, reject_clip


    @pytest.fixture
    def client(monkeypatch):
        monkeypatch.setattr('src.internal_api.INTERNAL_TOKEN', 'test-token-123')
        app.config['TESTING'] = True
        with app.test_client() as c:
            yield c


    def test_resolve_channel_requires_auth(client):
        resp = client.post('/internal/resolve-channel', json={'url': 'https://youtube.com/@x'})
        assert resp.status_code == 401


    def test_resolve_channel_returns_channel_data(client):
        # This test drives Plan 08-07 GREEN: resolve_channel must return a dict with
        # channel_id, channel_name, channel_handle when yt-dlp succeeds.
        fake_json = '{"id":"UCxxx","channel":"Nome","uploader_id":"@handle"}'
        with patch('src.internal_api.subprocess.run') as mock_run:
            mock_run.return_value = MagicMock(returncode=0, stdout=fake_json, stderr='')
            resp = client.post(
                '/internal/resolve-channel',
                json={'url': 'https://youtube.com/@handle'},
                headers={'X-Internal-Token': 'test-token-123'},
            )
        assert resp.status_code == 200, resp.get_json()
        body = resp.get_json()
        assert body['channel_id'] == 'UCxxx'
        assert body['channel_name'] == 'Nome'
        assert body['channel_handle'] == '@handle'


    def test_resolve_channel_returns_422_on_ytdlp_failure(client):
        with patch('src.internal_api.subprocess.run') as mock_run:
            mock_run.return_value = MagicMock(returncode=1, stdout='', stderr='not a channel')
            resp = client.post(
                '/internal/resolve-channel',
                json={'url': 'https://youtube.com/@nope'},
                headers={'X-Internal-Token': 'test-token-123'},
            )
        assert resp.status_code == 422
        assert resp.get_json()['error'] == 'yt-dlp failed'


    def test_reject_clip_requires_auth(client):
        resp = client.post('/internal/reject-clip', json={'clip_id': 42})
        assert resp.status_code == 401


    def test_reject_clip_calls_rejeitar_and_returns_exit_code(client):
        with patch('src.internal_api.rejeitar') as mock_rejeitar:
            mock_rejeitar.return_value = 0
            resp = client.post(
                '/internal/reject-clip',
                json={'clip_id': 42},
                headers={'X-Internal-Token': 'test-token-123'},
            )
        assert resp.status_code == 200
        assert resp.get_json()['exit_code'] == 0
        mock_rejeitar.assert_called_once_with(42)


    def test_reject_clip_propagates_exit_code_1(client):
        with patch('src.internal_api.rejeitar') as mock_rejeitar:
            mock_rejeitar.return_value = 1  # clip não existe
            resp = client.post(
                '/internal/reject-clip',
                json={'clip_id': 999},
                headers={'X-Internal-Token': 'test-token-123'},
            )
        assert resp.status_code == 200
        assert resp.get_json()['exit_code'] == 1
    ```
    Notas:
    - Imports no topo (padrão RED-state deste projeto). O Plan 08-07 vai adicionar `import subprocess` ao `internal_api.py`, então o teste patcha `src.internal_api.subprocess.run` (o patch falha no RED porque o import ainda não existe — isso é aceitável e é o RED esperado).
    - `test_resolve_channel_returns_channel_data` estabelece o CONTRATO de saída de `resolve_channel`: campos `channel_id`, `channel_name`, `channel_handle` extraídos de `id`, `channel`, `uploader_id` do JSON do yt-dlp.
    - `test_reject_clip_calls_rejeitar_and_returns_exit_code` verifica que o endpoint chama `rejeitar(clip_id)` diretamente (não via subprocess).

    2. Criar `clip-processor/tests/test_uploader_expired.py`:
    ```python
    """RED test para captura de RefreshError e persistência de oauth_expired_flag
    (implementação GREEN no Plan 08-06)."""
    import pytest
    from unittest.mock import patch, MagicMock

    from src.uploader import YouTubeUploader


    def test_load_credentials_catches_refresh_error_and_flags_channel(tmp_path):
        """RefreshError em creds.refresh() deve setar oauth_expired_flag=TRUE em
        destination_channels para o channel_slug atual."""
        try:
            from google.auth.exceptions import RefreshError
        except ModuleNotFoundError:
            RefreshError = type('RefreshError', (Exception,), {})

        token_file = tmp_path / 'token-futebol-em-cortes.json'
        token_file.write_text('{}')

        uploader = YouTubeUploader(channel_slug='futebol-em-cortes')
        # Force the token_file to the tmp path
        uploader.token_file = str(token_file)

        fake_creds = MagicMock()
        fake_creds.expired = True
        fake_creds.refresh_token = 'stale'
        fake_creds.refresh.side_effect = RefreshError('invalid_grant: Token has been expired or revoked')

        fake_conn = MagicMock()
        fake_cursor = MagicMock()
        fake_conn.cursor.return_value.__enter__.return_value = fake_cursor

        with patch('src.uploader.Credentials.from_authorized_user_file', return_value=fake_creds), \
             patch('src.uploader.db_connect', return_value=fake_conn):
            with pytest.raises(RefreshError):
                uploader._load_credentials()

        # Assert: uploader marcou a coluna oauth_expired_flag=TRUE para este slug.
        calls = [c.args[0] for c in fake_cursor.execute.call_args_list]
        assert any('oauth_expired_flag' in sql and 'destination_channels' in sql for sql in calls), (
            f'Nenhum UPDATE em destination_channels.oauth_expired_flag detectado. SQLs vistas: {calls}'
        )
    ```

    3. Rodar coleta:
    ```
    docker exec -T clip-processor pytest tests/test_internal_api.py tests/test_uploader_expired.py --collect-only 2>&1 | tail -30
    ```
    Não deve ter `ImportError` / `ModuleNotFoundError`. Se houver, o Task 2 tem bug — corrigir.

    4. Rodar os testes — devem falhar em RED:
    ```
    docker exec -T clip-processor pytest tests/test_internal_api.py tests/test_uploader_expired.py -v 2>&1 | tail -40
    ```
    Esperado: ao menos 4 falhas em `test_internal_api.py` (auth passa em 2, os endpoints "GREEN cases" falham porque `resolve_channel`/`reject_clip` levantam `NotImplementedError`) e 1 falha em `test_uploader_expired.py` (RefreshError não é capturada hoje — propaga sem tocar em `destination_channels`).

    **Commit RED explícito:** o SUMMARY deve declarar quais testes estão RED e por quê — mesmo padrão de Phases 3/4.
  </action>
  <verify>
    <automated>docker exec -T clip-processor pytest tests/test_internal_api.py tests/test_uploader_expired.py --collect-only 2>&1 | grep -qE "collected [0-9]+ item" && echo COLLECT_OK</automated>
  </verify>
  <done>
    - `test_internal_api.py` coleta ≥6 testes; `test_uploader_expired.py` coleta ≥1 teste.
    - Nenhum `ImportError`/`ModuleNotFoundError` durante coleta.
    - Rodar `pytest -v` mostra testes falhando (RED state confirmado).
    - Testes existentes de Phases 2-7 continuam GREEN (regressão zero): `pytest tests/ --ignore=tests/test_internal_api.py --ignore=tests/test_uploader_expired.py` → 0 falhas.
  </done>
</task>

</tasks>

<verification>
1. `docker exec -i mysql mysql -uroot -p${MYSQL_ROOT_PASSWORD:-rootpassword} clips_automation -e "SHOW COLUMNS FROM destination_channels LIKE 'oauth_expired_flag'"` → 1 linha retornada.
2. `docker exec -T clip-processor python -c "from src import internal_api"` → sem erro.
3. `docker exec -T clip-processor pytest tests/test_internal_api.py tests/test_uploader_expired.py --collect-only` → coleta sem erro.
4. `docker exec -T clip-processor pytest tests/ -q --ignore=tests/test_internal_api.py --ignore=tests/test_uploader_expired.py` → todos passam (regressão zero).
5. Rodar a migration SQL duas vezes — segunda execução não gera erro (idempotência).
</verification>

<success_criteria>
- Coluna `oauth_expired_flag` existe em `destination_channels`.
- Skeleton `internal_api.py` importa dentro do container e expõe `app`, `resolve_channel`, `reject_clip`.
- 2 arquivos de teste RED existem e cobrem: auth, resolve success, resolve fail, reject success + exit codes, uploader RefreshError.
- Zero regressão nos testes existentes.
- Migration é idempotente (compatível com re-execução).
</success_criteria>

<output>
Após conclusão, criar `.planning/phases/08-painel-laravel-filament/08-02-SUMMARY.md` com:
- Task-by-task decisions.
- Lista explícita dos testes RED e o que cada um cobre.
- Confirmação de zero regressão (contador `pytest -q` antes/depois).
- Contrato JSON dos endpoints copiado do bloco `<interfaces>` (para o Plan 08-07/08-05 consumirem).
</output>
