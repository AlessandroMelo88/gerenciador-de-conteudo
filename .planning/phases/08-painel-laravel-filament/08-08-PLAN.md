---
phase: 08-painel-laravel-filament
plan: 08
type: execute
wave: 4
depends_on: ["08-05", "08-06", "08-07"]
files_modified:
  - /Users/alessandrobm1/develop/server/wordpress/canaldecortes/painel/app/Filament/Widgets/QuotaTodayWidget.php
  - /Users/alessandrobm1/develop/server/wordpress/canaldecortes/painel/app/Filament/Widgets/PendingApprovalWidget.php
  - /Users/alessandrobm1/develop/server/wordpress/canaldecortes/painel/app/Filament/Widgets/RecentUploadsWidget.php
  - /Users/alessandrobm1/develop/server/wordpress/canaldecortes/painel/app/Filament/Widgets/RecentFailuresWidget.php
  - /Users/alessandrobm1/develop/server/wordpress/canaldecortes/painel/app/Providers/Filament/AdminPanelProvider.php
autonomous: true
requirements: [PANEL-03, PANEL-04]
must_haves:
  truths:
    - "Dashboard `/admin` mostra 4 widgets: Cota, Fila de aprovação, Últimos uploads, Últimas falhas"
    - "Cada widget expõe polling nativo `->poll('5s')` (Table) OU `$pollingInterval='5s'` (Stats)"
    - "QuotaTodayWidget lê `youtube_uploads:{destination.youtube_channel_id}:{data America/Sao_Paulo}` via `Redis::connection('pipeline')` — não a conexão default"
    - "PendingApprovalWidget mostra clips com `status='pending'` e expõe recordActions Aprovar/Rejeitar"
    - "Ação `Aprovar` executa `UPDATE generated_clips SET status='approved' WHERE id=? AND status='pending'` (guard)"
    - "Ação `Rejeitar` chama `ClipProcessorClient::rejectClip($clipId)` e reage aos exit codes 0/1/2 com notification adequada"
    - "4 testes de `ClipApprovalActionTest` e 2 de `DashboardPollingTest` passam"
  artifacts:
    - path: "/Users/alessandrobm1/develop/server/wordpress/canaldecortes/painel/app/Filament/Widgets/QuotaTodayWidget.php"
      provides: "Stats widget de cota (Redis pipeline DB 0)"
    - path: "/Users/alessandrobm1/develop/server/wordpress/canaldecortes/painel/app/Filament/Widgets/PendingApprovalWidget.php"
      provides: "TableWidget com pending clips + recordActions Aprovar/Rejeitar"
    - path: "/Users/alessandrobm1/develop/server/wordpress/canaldecortes/painel/app/Filament/Widgets/RecentUploadsWidget.php"
      provides: "TableWidget últimos 10 uploads publicados"
    - path: "/Users/alessandrobm1/develop/server/wordpress/canaldecortes/painel/app/Filament/Widgets/RecentFailuresWidget.php"
      provides: "TableWidget falhas source_videos + generated_clips"
  key_links:
    - from: "QuotaTodayWidget::getStats"
      to: "Redis DB 0 (pipeline)"
      via: "Redis::connection('pipeline')->get('youtube_uploads:{yt_id}:{date}')"
      pattern: "connection..pipeline"
    - from: "PendingApprovalWidget::recordActions"
      to: "GeneratedClip UPDATE guard status=pending"
      via: "UPDATE...WHERE status='pending'"
      pattern: "where.*pending"
    - from: "PendingApprovalWidget (Rejeitar action)"
      to: "ClipProcessorClient::rejectClip"
      via: "app(ClipProcessorClient::class)->rejectClip($record->id)"
      pattern: "rejectClip"
---

<objective>
Implementar PANEL-03 + PANEL-04: dashboard com 4 widgets, polling 5s nativo, ações Aprovar/Rejeitar inline na fila. GREEN 4 testes de `ClipApprovalActionTest` + 2 de `DashboardPollingTest`.

Purpose: entrega o dashboard de trabalho do operador. Fecha o loop UI → banco (Aprovar) e UI → Python sidecar → banco + filesystem (Rejeitar).
Output: 4 widgets Filament + registro no AdminPanelProvider.
</objective>

<execution_context>
@/Users/alessandrobm1/.claude/get-shit-done/workflows/execute-plan.md
@/Users/alessandrobm1/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/alessandrobm1/develop/server/wordpress/canaldecortes/.planning/phases/08-painel-laravel-filament/08-CONTEXT.md
@/Users/alessandrobm1/develop/server/wordpress/canaldecortes/.planning/phases/08-painel-laravel-filament/08-RESEARCH.md
@/Users/alessandrobm1/develop/server/wordpress/canaldecortes/.planning/phases/08-painel-laravel-filament/08-05-SUMMARY.md
@/Users/alessandrobm1/develop/server/wordpress/canaldecortes/.planning/phases/08-painel-laravel-filament/08-06-SUMMARY.md
@/Users/alessandrobm1/develop/server/wordpress/canaldecortes/.planning/phases/08-painel-laravel-filament/08-07-SUMMARY.md
@/Users/alessandrobm1/develop/server/wordpress/canaldecortes/painel/tests/Feature/ClipApprovalActionTest.php
@/Users/alessandrobm1/develop/server/wordpress/canaldecortes/painel/tests/Feature/DashboardPollingTest.php
@/Users/alessandrobm1/develop/server/wordpress/canaldecortes/painel/app/Services/ClipProcessorClient.php
@/Users/alessandrobm1/develop/server/wordpress/canaldecortes/painel/app/Models/GeneratedClip.php

<interfaces>
## Filament 5.x widget APIs

### Stats widget (cota)
```php
use Filament\Widgets\StatsOverviewWidget as BaseWidget;
use Filament\Widgets\StatsOverviewWidget\Stat;

class QuotaTodayWidget extends BaseWidget
{
    protected static ?string $pollingInterval = '5s';  // 5s nativo
    protected function getStats(): array { /* return [Stat::make(...)]; */ }
}
```

### Table widget (fila, uploads, falhas)
```php
use Filament\Widgets\TableWidget as BaseWidget;
use Filament\Actions\Action;              // Filament 5 (não Filament\Tables\Actions)
use Filament\Tables\Table;
use Filament\Tables\Columns\TextColumn;

class PendingApprovalWidget extends BaseWidget
{
    public function table(Table $table): Table
    {
        return $table
            ->query(...)
            ->poll('5s')                   // polling nativo
            ->recordActions([              // Filament 5 (não ->actions([...]))
                Action::make('aprovar')...,
                Action::make('rejeitar')...,
            ]);
    }
}
```

## Chave Redis de cota (Phase 7 - publisher.py)
```
youtube_uploads:{destination_channels.youtube_channel_id}:{YYYY-MM-DD em America/Sao_Paulo}
```
Notas críticas do Pitfall 3 do RESEARCH:
- `youtube_channel_id`, NÃO `destination_channels.id` interno.
- `America/Sao_Paulo`, NÃO UTC.
- Redis DB 0 (pipeline), NÃO DB 1 (Laravel).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Criar 4 widgets Filament (Quota, PendingApproval, RecentUploads, RecentFailures)</name>
  <files>
    - /Users/alessandrobm1/develop/server/wordpress/canaldecortes/painel/app/Filament/Widgets/QuotaTodayWidget.php
    - /Users/alessandrobm1/develop/server/wordpress/canaldecortes/painel/app/Filament/Widgets/PendingApprovalWidget.php
    - /Users/alessandrobm1/develop/server/wordpress/canaldecortes/painel/app/Filament/Widgets/RecentUploadsWidget.php
    - /Users/alessandrobm1/develop/server/wordpress/canaldecortes/painel/app/Filament/Widgets/RecentFailuresWidget.php
  </files>
  <behavior>
    - Cada widget expõe `wire:poll.5s` (Livewire polling) no HTML renderizado.
    - `PendingApprovalWidget` inclui `Action::make('aprovar')` (UPDATE guard `status='pending'`) e `Action::make('rejeitar')` (chama `ClipProcessorClient::rejectClip`).
    - `QuotaTodayWidget` lê chave real do Redis pipeline (DB 0) com `youtube_channel_id` + timezone São Paulo.
  </behavior>
  <action>
    1. `app/Filament/Widgets/QuotaTodayWidget.php`:
    ```php
    <?php
    namespace App\Filament\Widgets;

    use App\Models\DestinationChannel;
    use Filament\Widgets\StatsOverviewWidget as BaseWidget;
    use Filament\Widgets\StatsOverviewWidget\Stat;
    use Illuminate\Support\Carbon;
    use Illuminate\Support\Facades\Redis;

    class QuotaTodayWidget extends BaseWidget
    {
        protected static ?string $pollingInterval = '5s';  // Livewire polling nativo Filament 5
        protected static ?int $sort = 1;

        protected function getStats(): array
        {
            $date = Carbon::now('America/Sao_Paulo')->format('Y-m-d');
            $stats = [];

            foreach (DestinationChannel::query()->where('active', true)->get() as $channel) {
                $key = "youtube_uploads:{$channel->youtube_channel_id}:{$date}";
                try {
                    $count = (int) (Redis::connection('pipeline')->get($key) ?? 0);
                } catch (\Throwable $e) {
                    $count = 0;
                }
                $limit = 3;  // MCAN-03: 3 uploads/dia por canal
                $stats[] = Stat::make(
                    label: 'Cota — ' . $channel->name,
                    value: "{$count}/{$limit}",
                )->description($channel->slug)
                 ->color($count >= $limit ? 'danger' : ($count > 0 ? 'warning' : 'success'));
            }
            if (empty($stats)) {
                $stats[] = Stat::make('Cota', '—')->description('Nenhum canal-destino ativo');
            }
            return $stats;
        }
    }
    ```
    Ponto crítico: `Redis::connection('pipeline')` — NÃO `Redis::get(...)` (default) — Pitfall 3.

    2. `app/Filament/Widgets/PendingApprovalWidget.php`:
    ```php
    <?php
    namespace App\Filament\Widgets;

    use App\Models\GeneratedClip;
    use App\Services\ClipProcessorClient;
    use Filament\Actions\Action;
    use Filament\Notifications\Notification;
    use Filament\Tables\Columns\TextColumn;
    use Filament\Tables\Table;
    use Filament\Widgets\TableWidget as BaseWidget;
    use RuntimeException;

    class PendingApprovalWidget extends BaseWidget
    {
        protected static ?int $sort = 2;
        protected int|string|array $columnSpan = 'full';
        protected static ?string $heading = 'Fila de aprovação';

        public function table(Table $table): Table
        {
            return $table
                ->query(GeneratedClip::query()->where('status', 'pending')->latest())
                ->poll('5s')
                ->columns([
                    TextColumn::make('id')->label('ID')->sortable(),
                    TextColumn::make('title')->label('Título')->limit(60),
                    TextColumn::make('sourceVideo.title')->label('Vídeo-fonte')->limit(40)->toggleable(),
                    TextColumn::make('destinationChannel.name')->label('Destino'),
                    TextColumn::make('score')->label('Score')->badge(),
                    TextColumn::make('created_at')->label('Criado')->since(),
                ])
                ->recordActions([
                    Action::make('aprovar')
                        ->color('success')
                        ->icon('heroicon-o-check-badge')
                        ->requiresConfirmation()
                        ->action(function (GeneratedClip $record) {
                            $affected = GeneratedClip::query()
                                ->where('id', $record->id)
                                ->where('status', 'pending')
                                ->update(['status' => 'approved']);
                            if ($affected === 0) {
                                Notification::make()->title('Clip não estava mais pending')->warning()->send();
                                return;
                            }
                            Notification::make()->title("Clip #{$record->id} aprovado")->success()->send();
                        }),
                    Action::make('rejeitar')
                        ->color('danger')
                        ->icon('heroicon-o-x-circle')
                        ->requiresConfirmation()
                        ->action(function (GeneratedClip $record) {
                            $client = app(ClipProcessorClient::class);
                            try {
                                $exit = $client->rejectClip($record->id);
                            } catch (RuntimeException $e) {
                                Notification::make()->title('Falha ao rejeitar')->body($e->getMessage())->danger()->send();
                                session()->flash('error', $e->getMessage());
                                return;
                            }
                            match ($exit) {
                                0 => Notification::make()->title("Clip #{$record->id} rejeitado (MP4 removido)")->success()->send(),
                                1 => tap(session(), fn ($s) => $s->flash('error', 'Clip não encontrado'))
                                        && Notification::make()->title('Clip não existe')->danger()->send(),
                                2 => tap(session(), fn ($s) => $s->flash('error', 'Status inválido para rejeitar'))
                                        && Notification::make()->title('Status inválido')->danger()->send(),
                                default => tap(session(), fn ($s) => $s->flash('error', "exit_code={$exit}"))
                                        && Notification::make()->title("Exit code inesperado: {$exit}")->danger()->send(),
                            };
                        }),
                ]);
        }
    }
    ```
    Nota sobre `session()->flash('error', ...)`: garante que `assertSessionHas('error')` do teste `ClipApprovalActionTest` funcione quando `exit_code != 0`.

    3. `app/Filament/Widgets/RecentUploadsWidget.php`:
    ```php
    <?php
    namespace App\Filament\Widgets;

    use App\Models\GeneratedClip;
    use Filament\Tables\Columns\TextColumn;
    use Filament\Tables\Table;
    use Filament\Widgets\TableWidget as BaseWidget;

    class RecentUploadsWidget extends BaseWidget
    {
        protected static ?int $sort = 3;
        protected int|string|array $columnSpan = 'full';
        protected static ?string $heading = 'Últimos uploads';

        public function table(Table $table): Table
        {
            return $table
                ->query(GeneratedClip::query()->where('status', 'published')->latest()->limit(10))
                ->poll('5s')
                ->columns([
                    TextColumn::make('id')->sortable(),
                    TextColumn::make('title')->limit(60),
                    TextColumn::make('destinationChannel.name')->label('Canal'),
                    TextColumn::make('youtube_video_id')
                        ->label('YouTube')
                        ->formatStateUsing(fn (?string $s) => $s ? "https://youtu.be/{$s}" : '')
                        ->url(fn ($state) => $state ?: null, shouldOpenInNewTab: true),
                    TextColumn::make('updated_at')->label('Publicado')->since(),
                ]);
        }
    }
    ```

    4. `app/Filament/Widgets/RecentFailuresWidget.php`:
    ```php
    <?php
    namespace App\Filament\Widgets;

    use App\Models\GeneratedClip;
    use App\Models\SourceVideo;
    use Filament\Tables\Columns\TextColumn;
    use Filament\Tables\Table;
    use Filament\Widgets\TableWidget as BaseWidget;
    use Illuminate\Database\Query\Builder;
    use Illuminate\Support\Facades\DB;

    class RecentFailuresWidget extends BaseWidget
    {
        protected static ?int $sort = 4;
        protected int|string|array $columnSpan = 'full';
        protected static ?string $heading = 'Últimas falhas';

        public function table(Table $table): Table
        {
            // UNION virtual (2 origens de falha): source_videos.failed + generated_clips.failed
            $failedVideos = DB::table('source_videos')
                ->select(DB::raw("'source_video' as origin"), 'id', 'title', 'updated_at')
                ->where('status', 'failed');
            $failedClips = DB::table('generated_clips')
                ->select(DB::raw("'generated_clip' as origin"), 'id', 'title', 'updated_at')
                ->where('status', 'failed');

            return $table
                ->query(fn () => $failedVideos->unionAll($failedClips)->orderByDesc('updated_at')->limit(20))
                ->poll('5s')
                ->columns([
                    TextColumn::make('origin')->label('Origem')->badge(),
                    TextColumn::make('id'),
                    TextColumn::make('title')->limit(60),
                    TextColumn::make('updated_at')->label('Quando')->since(),
                ]);
        }
    }
    ```
    Se `->query(fn () => ...)` não aceitar builder de union (limitação Filament), fallback: exibir só `generated_clips` failed nesta versão inicial + adicionar um Stat separado com contador de `source_videos.failed`. Documentar decisão no SUMMARY.

    5. Verificar sintaxe:
    ```
    docker exec -T php bash -c "cd /var/www/html/painel && php -l app/Filament/Widgets/QuotaTodayWidget.php app/Filament/Widgets/PendingApprovalWidget.php app/Filament/Widgets/RecentUploadsWidget.php app/Filament/Widgets/RecentFailuresWidget.php"
    ```
    Todos devem imprimir "No syntax errors detected".
  </action>
  <verify>
    <automated>docker exec -T php bash -c "cd /var/www/html/painel && php -l app/Filament/Widgets/QuotaTodayWidget.php app/Filament/Widgets/PendingApprovalWidget.php app/Filament/Widgets/RecentUploadsWidget.php app/Filament/Widgets/RecentFailuresWidget.php" | grep -c "No syntax errors" | tr -d ' ' | grep -qE '^4$' && echo WIDGETS_SYNTAX_OK</automated>
  </verify>
  <done>
    - 4 arquivos de widget existem e passam em lint php.
    - Cada widget usa `->poll('5s')` OU `$pollingInterval = '5s'`.
    - `QuotaTodayWidget` usa `Redis::connection('pipeline')` (não default).
    - `PendingApprovalWidget` usa `Filament\Actions\Action` (namespace v5) e `->recordActions(...)` (não `->actions([...])`).
    - Ação Aprovar tem guard `->where('status','pending')` no UPDATE.
    - Ação Rejeitar chama `ClipProcessorClient::rejectClip` + `session()->flash('error', ...)` em exit code != 0.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Registrar widgets no AdminPanelProvider + rodar suite completa</name>
  <files>
    - /Users/alessandrobm1/develop/server/wordpress/canaldecortes/painel/app/Providers/Filament/AdminPanelProvider.php
  </files>
  <behavior>
    Após registro, `GET /admin` renderiza os 4 widgets e `wire:poll.5s` está presente no HTML — GREEN os testes de `DashboardPollingTest` e `ClipApprovalActionTest`.
  </behavior>
  <action>
    1. Editar `AdminPanelProvider::panel()` para incluir os 4 widgets:
    ```php
    ->widgets([
        \App\Filament\Widgets\QuotaTodayWidget::class,
        \App\Filament\Widgets\PendingApprovalWidget::class,
        \App\Filament\Widgets\RecentUploadsWidget::class,
        \App\Filament\Widgets\RecentFailuresWidget::class,
    ])
    ```
    Alternativa idiomática: manter `discoverWidgets(in: app_path('Filament/Widgets'), for: 'App\\Filament\\Widgets')` (já introduzido no Plan 08-04) — nesse caso, os widgets acima são descobertos automaticamente. Se `discoverWidgets` já cobre, remover o array `->widgets([...])` para evitar duplicação.

    2. Ajustar o teste `DashboardPollingTest` se necessário:
       - O assertion `assertSee('wire:poll.5s', false)` procura no HTML renderizado. Filament 5 pode renderizar `wire:poll.5000ms` ou `wire:poll` sem intervalo explícito — inspecionar o HTML gerado por `docker exec -T php bash -c "cd /var/www/html/painel && php artisan tinker --execute='...'` OU pelo output do teste.
       - Se o formato for diferente, atualizar o TESTE (não trocar o comportamento do widget) para casar com o formato real de Filament 5. Documentar a decisão no SUMMARY.

    3. Rodar toda a suite:
    ```
    docker exec -T php bash -c "cd /var/www/html/painel && php artisan test --no-interaction 2>&1 | tail -20"
    ```
    Esperado: TODOS os testes de Phase 8 verdes.

    4. Se `DashboardPollingTest` continuar RED por causa de `assertSee('Cota',false)` — o widget de Cota só renderiza stats quando há `DestinationChannel::active=true` — semear via factory:
       - Ajustar o teste (não o widget) para criar um `DestinationChannel::factory()->create(['active' => true])` antes de `->get('/admin')`.

    5. Verificação final:
    ```
    docker exec -T clip-processor pytest tests/ -q 2>&1 | tail -5
    docker exec -T php bash -c "cd /var/www/html/painel && php artisan test --no-interaction 2>&1 | tail -20"
    ```
    Zero regressão em Python + Phase 8 Laravel 100% GREEN.
  </action>
  <verify>
    <automated>docker exec -T php bash -c "cd /var/www/html/painel && php artisan test --no-interaction 2>&1 | tail -5" | grep -qE "(0 failed|Tests:|passed)" && echo SUITE_GREEN</automated>
  </verify>
  <done>
    - `AdminPanelProvider` registra (ou discovery cobre) os 4 widgets.
    - `ClipApprovalActionTest` 4/4 GREEN.
    - `DashboardPollingTest` 2/2 GREEN.
    - Suite completa Phase 8 Laravel: 0 failed.
    - Suite completa Python: 0 failed.
  </done>
</task>

</tasks>

<verification>
1. `docker exec -T php bash -c "cd /var/www/html/painel && php artisan test --no-interaction"` — 0 failed.
2. `docker exec -T clip-processor pytest tests/ -q` — 0 failed.
3. Manualmente confirmar via `docker exec -T php bash -c "cd /var/www/html/painel && php artisan route:list | grep Widgets"` que os widgets estão discoverable/registered.
4. `grep -q "connection('pipeline')" /Users/alessandrobm1/develop/server/wordpress/canaldecortes/painel/app/Filament/Widgets/QuotaTodayWidget.php` — Redis conexão nomeada usada.
5. `grep -q "Filament\\\\Actions\\\\Action" /Users/alessandrobm1/develop/server/wordpress/canaldecortes/painel/app/Filament/Widgets/PendingApprovalWidget.php` — namespace v5.
</verification>

<success_criteria>
- Dashboard renderiza 4 widgets com polling 5s.
- Aprovar/Rejeitar funcionam com guard de status e via sidecar Python.
- PANEL-03 + PANEL-04 satisfeitos.
- Full test suite (Python + Laravel) GREEN.
</success_criteria>

<output>
Após conclusão, criar `.planning/phases/08-painel-laravel-filament/08-08-SUMMARY.md` com:
- Screenshot conceitual do dashboard (via descrição — não precisa de imagem real).
- Contador final de testes Phase 8: total, GREEN, RED (esperado 0 RED).
- Confirmação de zero regressão Python.
- Nota sobre qualquer ajuste feito nos testes de `DashboardPollingTest` para casar com Filament 5 output real.
- Confirmação `Redis::connection('pipeline')` usado no Quota widget.
</output>
