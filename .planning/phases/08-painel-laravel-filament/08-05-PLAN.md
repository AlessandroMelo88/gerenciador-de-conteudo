---
phase: 08-painel-laravel-filament
plan: 05
type: execute
wave: 3
depends_on: ["08-03"]
files_modified:
  - /Users/alessandrobm1/develop/server/wordpress/canaldecortes/painel/app/Services/ClipProcessorClient.php
  - /Users/alessandrobm1/develop/server/wordpress/canaldecortes/painel/app/Filament/Resources/SourceChannelResource.php
  - /Users/alessandrobm1/develop/server/wordpress/canaldecortes/painel/app/Filament/Resources/SourceChannelResource/Pages/CreateSourceChannel.php
  - /Users/alessandrobm1/develop/server/wordpress/canaldecortes/painel/app/Filament/Resources/SourceChannelResource/Pages/ListSourceChannels.php
  - /Users/alessandrobm1/develop/server/wordpress/canaldecortes/painel/app/Filament/Resources/SourceChannelResource/Pages/EditSourceChannel.php
autonomous: true
requirements: [PANEL-01]
must_haves:
  truths:
    - "Operador logado consegue adicionar canal-fonte pela URL do YouTube e a linha aparece em `source_channels`"
    - "yt-dlp falha (URL inválida) → notificação de erro Filament exibida, NENHUMA linha inserida"
    - "Toggle blacklist inline funciona (UPDATE `blacklisted`) e mostra tooltip 'Afeta apenas novos vídeos'"
    - "Filament Select de `target_niche` é explícito (`Select::make(...)->options([...])`) — nunca --generate"
    - "3 testes de `SourceChannelResourceTest` passam"
  artifacts:
    - path: "/Users/alessandrobm1/develop/server/wordpress/canaldecortes/painel/app/Services/ClipProcessorClient.php"
      provides: "resolveChannel(url), rejectClip(clipId) — wrappers Http do sidecar"
      exports: ["resolveChannel", "rejectClip"]
    - path: "/Users/alessandrobm1/develop/server/wordpress/canaldecortes/painel/app/Filament/Resources/SourceChannelResource.php"
      provides: "CRUD source_channels + toggle blacklist inline + Select niche explícito"
  key_links:
    - from: "SourceChannelResource::form()"
      to: "ClipProcessorClient::resolveChannel()"
      via: "custom create action que chama o client e preenche o Model"
      pattern: "resolveChannel"
    - from: "ClipProcessorClient::resolveChannel"
      to: "clip-processor sidecar /internal/resolve-channel"
      via: "Illuminate\\Support\\Facades\\Http::withHeader('X-Internal-Token')->post(...)"
      pattern: "internal/resolve-channel"
---

<objective>
Implementar PANEL-01: Filament Resource para `source_channels` com CRUD via URL do YouTube (resolvido pelo sidecar HTTP do clip-processor), toggle blacklist inline e Select de `target_niche` explícito. Espelhar 3 testes RED de `SourceChannelResourceTest`.

Purpose: primeiro artefato de UI real do painel. Também introduz o `ClipProcessorClient` que o Plan 08-08 vai reusar para ação de rejeitar clip.
Output: 1 Service class, 1 Filament Resource + 3 Pages (List, Create, Edit).
</objective>

<execution_context>
@/Users/alessandrobm1/.claude/get-shit-done/workflows/execute-plan.md
@/Users/alessandrobm1/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/alessandrobm1/develop/server/wordpress/canaldecortes/.planning/phases/08-painel-laravel-filament/08-CONTEXT.md
@/Users/alessandrobm1/develop/server/wordpress/canaldecortes/.planning/phases/08-painel-laravel-filament/08-RESEARCH.md
@/Users/alessandrobm1/develop/server/wordpress/canaldecortes/.planning/phases/08-painel-laravel-filament/08-03-SUMMARY.md
@/Users/alessandrobm1/develop/server/wordpress/canaldecortes/painel/tests/Feature/SourceChannelResourceTest.php
@/Users/alessandrobm1/develop/server/wordpress/canaldecortes/painel/app/Models/SourceChannel.php

<interfaces>
## Contrato do sidecar (do Plan 08-02 - `<interfaces>`)
```
POST http://clip-processor:8090/internal/resolve-channel
Header: X-Internal-Token: {config('services.clip_processor.token')}
Body:   {"url": "..."}
200:    {"channel_id": "UCxxx", "channel_name": "Nome", "channel_handle": "@handle"}
422:    {"error": "yt-dlp failed", "stderr": "..."}
```

## Filament 5.x
```php
// Namespaces IMPORTANTES (Filament 4/5, não 3):
use Filament\Actions\Action;                     // não Filament\Tables\Actions
use Filament\Forms\Components\TextInput;
use Filament\Forms\Components\Select;
use Filament\Tables\Table;
use Filament\Tables\Columns\TextColumn;
use Filament\Tables\Columns\ToggleColumn;

// Tables Actions API:
$table->recordActions([Action::make('foo')...]);  // não ->actions([...])
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Criar `ClipProcessorClient` service (resolve + reject wrappers)</name>
  <files>
    - /Users/alessandrobm1/develop/server/wordpress/canaldecortes/painel/app/Services/ClipProcessorClient.php
  </files>
  <behavior>
    - `resolveChannel(string $url): array` chama `POST /internal/resolve-channel` com token; retorna `[channel_id, channel_name, channel_handle]` em sucesso; lança `RuntimeException` com mensagem amigável em 4xx/5xx.
    - `rejectClip(int $clipId): int` chama `POST /internal/reject-clip`; retorna `exit_code` (0/1/2); lança em 401/timeout.
    - Ambos os métodos são fakeáveis via `Http::fake([...])` (Laravel HTTP Client) — necessário para `SourceChannelResourceTest` e `ClipApprovalActionTest`.
  </behavior>
  <action>
    Criar `app/Services/ClipProcessorClient.php`:
    ```php
    <?php
    namespace App\Services;

    use Illuminate\Support\Facades\Http;
    use RuntimeException;

    class ClipProcessorClient
    {
        public function __construct(
            protected ?string $baseUrl = null,
            protected ?string $token = null,
        ) {
            $this->baseUrl ??= config('services.clip_processor.url');
            $this->token   ??= config('services.clip_processor.token');
        }

        /**
         * @return array{channel_id: string, channel_name: string, channel_handle: string}
         * @throws RuntimeException  ao falhar (yt-dlp ou HTTP 4xx/5xx).
         */
        public function resolveChannel(string $url): array
        {
            $response = Http::timeout(30)
                ->withHeader('X-Internal-Token', (string) $this->token)
                ->post($this->baseUrl . '/internal/resolve-channel', ['url' => $url]);

            if ($response->status() === 422) {
                throw new RuntimeException('Não consegui resolver esse canal. Verifique a URL.');
            }
            if (!$response->successful()) {
                throw new RuntimeException('Erro interno ao chamar o clip-processor: HTTP ' . $response->status());
            }
            $data = $response->json();
            return [
                'channel_id' => (string) ($data['channel_id'] ?? ''),
                'channel_name' => (string) ($data['channel_name'] ?? ''),
                'channel_handle' => (string) ($data['channel_handle'] ?? ''),
            ];
        }

        /** @return int  exit code (0=ok, 1=clip não existe, 2=status inválido). */
        public function rejectClip(int $clipId): int
        {
            $response = Http::timeout(15)
                ->withHeader('X-Internal-Token', (string) $this->token)
                ->post($this->baseUrl . '/internal/reject-clip', ['clip_id' => $clipId]);

            if (!$response->successful()) {
                throw new RuntimeException('Erro ao rejeitar clip: HTTP ' . $response->status());
            }
            return (int) $response->json('exit_code', 1);
        }
    }
    ```

    Verificação isolada (via tinker):
    ```
    docker exec -T php bash -c "cd /var/www/html/painel && php artisan tinker --execute='dump(class_exists(App\\Services\\ClipProcessorClient::class));'"
    ```
    Deve imprimir `bool(true)`.
  </action>
  <verify>
    <automated>docker exec -T php bash -c "cd /var/www/html/painel && php artisan tinker --execute='dump(class_exists(App\\Services\\ClipProcessorClient::class));'" 2>&1 | grep -q "bool(true)" && echo CLIENT_OK</automated>
  </verify>
  <done>
    - `ClipProcessorClient::class` existe e resolve via Laravel autoload.
    - Ambos métodos usam `Http::withHeader('X-Internal-Token', ...)`.
    - `resolveChannel` traduz 422 para mensagem PT-BR amigável.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Criar SourceChannelResource + 3 Pages, com Select niche explícito, ToggleColumn e Custom Create action</name>
  <files>
    - /Users/alessandrobm1/develop/server/wordpress/canaldecortes/painel/app/Filament/Resources/SourceChannelResource.php
    - /Users/alessandrobm1/develop/server/wordpress/canaldecortes/painel/app/Filament/Resources/SourceChannelResource/Pages/CreateSourceChannel.php
    - /Users/alessandrobm1/develop/server/wordpress/canaldecortes/painel/app/Filament/Resources/SourceChannelResource/Pages/ListSourceChannels.php
    - /Users/alessandrobm1/develop/server/wordpress/canaldecortes/painel/app/Filament/Resources/SourceChannelResource/Pages/EditSourceChannel.php
  </files>
  <behavior>
    Deve GREEN os 3 testes de `SourceChannelResourceTest.php`:
    - `it creates a source_channel via internal API resolve` (Http fake retornando 200 → SourceChannel::query()->where('youtube_channel_id','UC_TEST_123')->exists() true)
    - `it shows an error when yt-dlp resolution fails` (Http fake 422 → sessionHasErrors, nenhuma linha)
    - `it toggles blacklisted` (PATCH `/admin/source-channels/{id}` com `blacklisted=true` → row atualiza)
  </behavior>
  <action>
    1. **NÃO usar `php artisan make:filament-resource --generate SourceChannel`** — regra registrada (evita corrupção de Selects com valores fixos).
       Rodar `php artisan make:filament-resource SourceChannel --view` (sem --generate) para criar o esqueleto:
       ```
       docker exec -T php bash -c "cd /var/www/html/painel && php artisan make:filament-resource SourceChannel --no-interaction"
       ```
       Isso cria os arquivos base.

    2. Sobrescrever `app/Filament/Resources/SourceChannelResource.php`:
    ```php
    <?php
    namespace App\Filament\Resources;

    use App\Filament\Resources\SourceChannelResource\Pages;
    use App\Models\SourceChannel;
    use Filament\Forms\Components\Select;
    use Filament\Forms\Components\TextInput;
    use Filament\Resources\Resource;
    use Filament\Schemas\Schema;
    use Filament\Tables\Columns\TextColumn;
    use Filament\Tables\Columns\ToggleColumn;
    use Filament\Tables\Table;

    class SourceChannelResource extends Resource
    {
        protected static ?string $model = SourceChannel::class;
        protected static ?string $navigationIcon = 'heroicon-o-rss';
        protected static ?string $modelLabel = 'Canal-fonte';
        protected static ?string $pluralModelLabel = 'Canais-fonte';

        public static function form(Schema $schema): Schema
        {
            return $schema->schema([
                TextInput::make('url')
                    ->label('URL do canal YouTube (ou channel_id)')
                    ->helperText('Exemplo: https://youtube.com/@sportv OU https://youtube.com/channel/UC...')
                    ->required()
                    ->live()
                    ->dehydrated(false),  // não persiste em source_channels; usado só para resolver via yt-dlp

                Select::make('target_niche')
                    ->label('Nicho de destino')
                    ->options([
                        'futebol' => 'Futebol',
                        'podcast' => 'Podcast',
                    ])
                    ->required()
                    ->default('futebol'),
            ]);
        }

        public static function table(Table $table): Table
        {
            return $table
                ->columns([
                    TextColumn::make('channel_name')->label('Nome')->searchable(),
                    TextColumn::make('channel_handle')->label('Handle'),
                    TextColumn::make('target_niche')->label('Nicho')->badge(),
                    ToggleColumn::make('active')->label('Ativo'),
                    ToggleColumn::make('blacklisted')
                        ->label('Blacklisted')
                        ->tooltip('Afeta apenas novos vídeos. Para purgar a fila use SQL manual.'),
                    TextColumn::make('created_at')->label('Criado')->dateTime()->since(),
                ])
                ->defaultSort('created_at', 'desc');
        }

        public static function getPages(): array
        {
            return [
                'index'  => Pages\ListSourceChannels::route('/'),
                'create' => Pages\CreateSourceChannel::route('/create'),
                'edit'   => Pages\EditSourceChannel::route('/{record}/edit'),
            ];
        }
    }
    ```

    3. Sobrescrever `Pages/CreateSourceChannel.php` — custom `handleRecordCreation` que chama `ClipProcessorClient::resolveChannel`:
    ```php
    <?php
    namespace App\Filament\Resources\SourceChannelResource\Pages;

    use App\Filament\Resources\SourceChannelResource;
    use App\Models\SourceChannel;
    use App\Services\ClipProcessorClient;
    use Filament\Notifications\Notification;
    use Filament\Resources\Pages\CreateRecord;
    use RuntimeException;

    class CreateSourceChannel extends CreateRecord
    {
        protected static string $resource = SourceChannelResource::class;

        protected function handleRecordCreation(array $data): SourceChannel
        {
            $client = app(ClipProcessorClient::class);
            try {
                $resolved = $client->resolveChannel((string) $data['url']);
            } catch (RuntimeException $e) {
                Notification::make()
                    ->title('Erro ao resolver canal')
                    ->body($e->getMessage())
                    ->danger()
                    ->send();
                $this->halt();  // impede persistência
            }

            $ytId = $resolved['channel_id'];

            return SourceChannel::create([
                'youtube_channel_id' => $ytId,
                'channel_name' => $resolved['channel_name'],
                'channel_handle' => $resolved['channel_handle'],
                'rss_url' => "https://www.youtube.com/feeds/videos.xml?channel_id={$ytId}",
                'active' => true,
                'blacklisted' => false,
                'target_niche' => $data['target_niche'] ?? 'futebol',
            ]);
        }
    }
    ```

    4. `Pages/ListSourceChannels.php` — padrão Filament (herda `ListRecords`) sem sobrescrita.
       `Pages/EditSourceChannel.php` — padrão Filament (herda `EditRecord`).

    5. Habilitar o discovery no `AdminPanelProvider` (o Plan 08-04 já colocou `->discoverResources(...)` — confirmar que está lá).

    6. Ajustar as URLs esperadas nos testes RED — os testes usam `POST /admin/source-channels` e `PATCH /admin/source-channels/{id}`. Filament expõe essas rotas por default quando o Resource é registrado.

    7. Rodar os testes:
    ```
    docker exec -T php bash -c "cd /var/www/html/painel && php artisan test --filter=SourceChannelResourceTest --no-interaction"
    ```
    Esperado: 3/3 GREEN.

    Se algum teste falhar por causa de URL/rota diferente do que Filament expõe, a solução é NÃO alterar o teste — em vez disso, ajustar o resource para expor a rota esperada (ou o teste original assumiu Filament exact-route pattern; conferir `php artisan route:list --path=admin/source-channels`).

    **Anti-patterns proibidos:**
    - `Select::make('target_niche')` sem `->options([...])` → corrompe ENUM Roadmap v2.0.
    - `--generate` no `make:filament-resource` → idem.
    - Namespace `Filament\Tables\Actions` (Filament 3) — usar `Filament\Actions` (v5).
  </action>
  <verify>
    <automated>docker exec -T php bash -c "cd /var/www/html/painel && php artisan test --filter=SourceChannelResourceTest --no-interaction 2>&1 | tail -5" | grep -qE "(3 passed|Passed: 3|Tests:  3)" && echo SC_TESTS_GREEN</automated>
  </verify>
  <done>
    - `SourceChannelResource` existe com Select explícito para `target_niche`.
    - `CreateSourceChannel::handleRecordCreation` chama `ClipProcessorClient::resolveChannel` e insere row com dados resolvidos.
    - `SourceChannelResourceTest` — 3/3 GREEN.
    - Notification::danger é acionada quando yt-dlp falha (422).
    - `ToggleColumn::make('blacklisted')` tem tooltip PT-BR "Afeta apenas novos vídeos...".
  </done>
</task>

</tasks>

<verification>
1. `docker exec -T php bash -c "cd /var/www/html/painel && php artisan test --filter=SourceChannelResourceTest --no-interaction"` — 3/3 GREEN.
2. `docker exec -T php bash -c "cd /var/www/html/painel && php artisan route:list --path=admin/source-channels"` — mostra rotas CRUD.
3. Não existe nenhuma referência a `Filament\Tables\Actions` no código (Filament v3 namespace) — `grep -r "Filament\\\\Tables\\\\Actions" /Users/alessandrobm1/develop/server/wordpress/canaldecortes/painel/app` retorna vazio.
</verification>

<success_criteria>
- PANEL-01 atendido: operador adiciona/remove/blacklista canal-fonte pela URL do YouTube sem SQL.
- Nenhuma chamada direta ao `docker exec` do container clip-processor a partir do PHP (comunicação via `ClipProcessorClient::resolveChannel`).
- Select `target_niche` explícito.
- 3 testes GREEN em SourceChannelResourceTest.
</success_criteria>

<output>
Após conclusão, criar `.planning/phases/08-painel-laravel-filament/08-05-SUMMARY.md` com:
- Contador antes/depois de testes GREEN (3 novos).
- Rotas expostas pelo Resource (route:list output).
- Confirmação de zero `Filament\Tables\Actions` e zero `--generate`.
- Nota sobre `ClipProcessorClient` estar pronto para o Plan 08-08 reusar em `rejectClip`.
</output>
